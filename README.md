# 销售标准管理 · CRM 项目章程（路线 B：TS + NestJS 全栈）

> 本文档是 AI 协作开发的"交通规则"。每次让 AI 改代码前，先读它。

## 一、技术栈定案（2026-09-05 决策）

- **后端**：NestJS 12（Node + TypeScript）+ MySQL 8 + Redis
- **前端**：Vue 3（TypeScript）+ Vite + Element Plus
- **全栈统一语言/类型**：前端与后端共用一套 TypeScript 类型，来源是后端 OpenAPI（Swagger），由 `openapi-typescript` 生成到 `frontend/src/api/types.ts`。**禁止手写对接、禁止前后端各维护一份接口类型**（双真相源是 AI 出错重灾区）。
- **数据库为唯一真相源**：DB 约束（唯一索引、CHECK、生成列）兜底业务不变量；应用层只做"友好提示"，不替代约束。
- **ORM 待定**：TypeORM 或 Prisma 二选一（见第七节待确认项）。骨架阶段用 Repository 接口 + 内存 Fake 验证业务逻辑，不锁定具体 ORM。

## 二、抑制 AI 错误的四道闸（按杠杆排序）

1. **数据库约束兜底**：唯一索引（撞单/激活冲突）、CHECK（审批人≠申请人）、生成列（active_key）。→ 挡"撞单、重复激活、自批"。
2. **类型安全贯穿前后端**：后端 DTO（class-validator 运行期校验）+ 前端 TS + OpenAPI 生成共享类型。→ 挡"字段拼错、枚举乱传、接口错位"。
3. **自动化测试当护栏**：每条业务铁律写单测/集成测，CI 必跑（当前本地 `npm test`）。→ 挡"公海规则、掉海判定、状态机错乱"。
4. **人工 SME 复审闸**：关键模块（业绩/提成/掉公海/脱敏）上线前由你走查关键流程。→ 挡"逻辑语义错"（类型与测试都挡不住这类）。

> 第 4 点是 solo + 不读代码者的"第二道防线"：你读 TS（JS 超集，Vue 即用 TS）可扫结构；但业务正确性最终靠测试 + 你给的"验收检查点"。

## 三、Git 使用约定（AI 本地开发：提交即还原点）

- **本地单线**：AI 直接在 `main` 上提交，不切 feat/dev 分支——保持简单，你不用管分支。
- **阶段性提交**：AI 每完成一个独立阶段（一个模块 / 一次修复 / 一次文档调整），做一次提交，提交信息写清"改了什么、为什么"。
- **还原保障（核心）**：每个提交都是可回退的节点。万一提交错了：
  - 回到上一个节点：`git reset --hard <提交号>`；
  - 或只撤销某一次错误提交：`git revert <提交号>`；
  - `git log` 查看全部提交与说明 = 开发过程汇总。
- 里程碑 / 大版本可打 tag（如 `v0.1-mvp-org`）。
- 将来接远程仓库 / CI 再严格走分支（feat/dev/main），到时再说。

## 四、spec-driven 开发流程（AI 每次按此顺序）

1. 锁需求：**《销售CRM业务需求文档》V1.1**（业务规则唯一真相源）→ 锁表结构：**《销售CRM数据架构文档》V1.2**（Schema 唯一真相源）
   - 两份文档由原《数据库设计文档-助理版》V1.0.7 于 **2026-09-09 拆分**而来（助理版同步归档至 `归档/助理版合并文档（已拆分）/`，停止维护）
   - V1.0「档案库版」四件套（销售CRM开发需求 V2.1 / 前端页面 V2.6 / 接口 API V1.4 / 数据库设计 V1.2）已于 **2026-09-09 整体归档**至 `归档/V1.0档案库版本需求框架/`；其中 §三 核心业务流程、§六 功能模块详细需求 ~490 行**已在拆分时捞回**业务需求文档
2. ⚠ **当前缺口**：**接口 API 文档、前端页面与交互文档尚未创建**（四件套中的后两份，V1 后置）。素材见 `归档/V1.0档案库版本需求框架/归档说明.md` §三 + 业务需求文档 §13 全局交互规范，**切勿从零重写**
3. 后端按模块实现 + 写铁律单测（当前阶段，Repository 用 Fake 验证逻辑）
4. 从 OpenAPI 自动生成前端类型（`npm run gen:types`）
5. 前端按类型开发
6. 你用浏览器走查 + 勾验收检查点
7. 通过后合并 `dev`

## 五、当前骨架结构

```
backend/   NestJS 12 后端（可编译 + 铁律单测通过）
frontend/  Vue3 + Vite + TS 桩（待接类型生成）
```

## 六、MVP 边界

严格按《销售CRM业务需求文档》§14 V1 范围（做/不做清单）；V2 的表单采集、企微对接、介绍费分账 **一期不做**（高危，你暂难验收）。另：客户出口、报销审批、客户地图/顺路推荐 **一期不做**（见需求文档 §14.2）。

## 七、待确认项（需你拍板）

- ORM：TypeORM vs Prisma
- 验证环境：轻量应用服务器（你只开浏览器，¥38~45/月）vs 本机 Node 跑（需装 Node）
- Redis/MySQL 8 的 provision 方式（轻量一键镜像 vs 本机 Docker）

---

## 八、需求变更全文档对齐规则（铁律）

**触发**：任何需求变更（新增/修改/删除功能、字段、流程、规则），经七叔确认后生效。

**对齐动作（强制）**：变更确认后，必须同步审视并修订**所有相关文档**，不允许"只改一处、别处漂移"。

> ### ★ L0 变更联动铁律（2026-09-09 七叔立，最高优先级）
> **业务需求一改，下游三份必须同批跟着改；四份没同步，这次变更就算没做完。**
> 1. 一次变更 = 一张变更单：写清改了哪条规则 + 影响哪些表/字段、接口、页面
> 2. **先改需求文档，再改下游**；下游不得私自定义业务规则，冲突一律回改需求文档
> 3. 受影响文档**同批升版**，修改记录写同一句说明（便于对账）
> 4. 需求改了但下游没跟上的，记为"进行中"，**不得标记已落地**
> 5. 被推翻的规则标"已废止+日期+替代规则"，保留原文一行，**停用不删**

> ### ★ L1 单一事实源铁律
> **同一事实只在四份文档中的一处定义一次；其余只写"见《业务需求文档》§X"，绝不复制一遍。**
> 业务规则→只在需求文档；表字段→只在数据架构；枚举业务含义→需求文档，物理编码→数据架构字典。

1. **四份文档与级联优先级**（冲突时以上游为准，下游只写指针不复制）：
   - ① 业务规则 → **《销售CRM业务需求文档》V1.1**（需求规格/）
   - ② 表结构 → **《销售CRM数据架构文档》V1.2**（需求规格/）
   - ③ 接口端点 → **《销售CRM接口API文档》待建**（V1 后置）
   - ④ 页面与字段/交互 → **《销售CRM前端页面与交互文档》待建**（V1 后置）
   - 对账工具：数据架构文档 **§十四 需求追溯索引**（按表反查需求章节）
   - 关联文档：《技术栈方案对比》《销售CRM项目摘要-给老板看》同步更新摘要与结论
2. **不变量**：① 技术栈以本文档（路线 B：NestJS 12）为准；② 工作流以 6 步状态机为准；③ 台账扩展字段以 `field_template` + `ledger.extra_fields` JSON 为准；④ 意向跟进体系以两字段正交（进度≠紧迫：`stage_id`/`urgency`）+ 两轴（客户等级≠开发价值：`customer_level`/`value_tier`）为准（需求文档 §8）；⑤ 跟单可见性分档 + 协同=共同跟进（`collaborator`）+ 公司详情简版抽屉（需求文档 §4.3 / §13.2）；⑥ **实体可点铁律**：公司/联系人/员工在任何位置出现都可点开查看，不许回头搜索（需求文档 §13.1）。
3. **留痕**：每次对齐在涉及文档顶部"修改记录"补一行（版本/日期/改了什么）；重大变更打 `git tag`。
4. **校验**：变更落地后，重新跑一次"需求一致性核查"（见《需求一致性核查报告》），确认无新漂移再合并 `dev`。

> 目的：让"同一事实在任意文档只有一种说法"，避免开发按过期描述实现。

---

## 九、目录架构与文件归类规则（2026-09-08 定）

**目录结构（根目录不再散放文档）**：

```
销售标准管理/
├── README.md          # 项目章程 + 对齐铁律（根，唯一不归入子目录的文档）
├── _smoke.js          # 原型冒烟测试脚本（根，工具类）
├── 需求规格/          # 真相源文档（四件套：需求 / 数据架构 / 接口 / 前端交互）
│   ├── 销售CRM业务需求文档.md      # ★ 业务规则唯一真相源（做什么、为什么）
│   └── 销售CRM数据架构文档.md      # ★ Schema 唯一真相源（表 / 字段 / 索引 / 字典）
├── 技术决策/          # 选型对比、架构裁决、路线拍板
│   └── 技术栈方案对比.md
├── 项目介绍/          # 给老板 / 外部的摘要、介绍、汇报
│   └── 销售CRM项目摘要-给老板看.md
├── 过程产出/          # 核查报告、构想图等过程产物
│   ├── 需求一致性核查报告.md
│   └── 项目全景构想图.html
├── backend/           # NestJS 12 后端代码
├── frontend/          # Vue3 前端代码
└── 原型设计/          # 高保真 HTML 原型 + _smoke.js 配套资源
```

**归类映射（新增文件先归类再落盘）**：

| 文件特征 | 归入目录 |
| --- | --- |
| 需求 / 规格 / 接口 / 库表设计 | 需求规格/ |
| 技术选型对比、架构裁决、路线拍板 | 技术决策/ |
| 给老板 / 外部看的摘要、介绍、汇报 | 项目介绍/ |
| 核查报告、构想图、评审纪要等过程产物 | 过程产出/ |
| 项目章程、对齐铁律（唯一例外，留根） | README.md（根） |
| 后端 / 前端 / 原型代码 | backend/ frontend/ 原型设计/ |
| 测试 / 工具脚本 | 根目录（或 原型设计/） |

**铁律**：
1. 任何新文档落地前，先判断它属于哪一类，放进对应目录；**禁止散落在根目录**。
2. 文档间一律以**标题 + 版本号**互相引用（如《数据库设计文档》V1.1），不依赖相对路径——日后移动文件不会破坏引用（见第八节不变量④同理）。
3. 归类有歧义时，按"用途"维度判断；仍不确定先放 `过程产出/`，并 @ 七叔确认，不要擅自建目录。

---

## 十、文档时态与状态规则（2026-09-08 定）

**核心原则**：用"文档位置"作为唯一时态信号——**现行有效文档留在用途目录，过去时 / 被取代文档移入根目录 `归档/`**。七叔翻阅文件夹时，四个用途目录里只该看到进行时文档；历史文档沉到 `归档/`，不干扰日常。

> 本规则能成立，依赖 §九 铁律②：文档间以"标题 + 版本号"互引、不依赖路径。因此把文档从用途目录挪到 `归档/` 不会断任何引用——两条规则配套。

**四态定义**：

| 状态 | 位置 | 含义 |
| --- | --- | --- |
| 草稿 | 用途目录（标题注明「草稿」） | 未确认，可能大改 |
| 现行有效 | 用途目录 | 当前生效，被引用为真相 / 依据 |
| 已归档·历史 | `归档/<用途>/` | 功能作用已完结，备查不常用 |
| 已废弃·被取代 | `归档/<用途>/` | 被新版取代，地图标注取代者 |

**归档触发（铁律）**：

1. **不自动归档，绝不因"放着久了"归档。**
2. 当某文档的"功能作用"已完成（如：方案对比已出决策、一致性核查的整改已落地），**AI 主动提问七叔"是否归档"，由七叔拍板**；七叔确认后才挪入 `归档/`。
3. 出现新版本取代旧版时，同理先问七叔，确认后旧版入 `归档/` 并在文档地图标注"被《新文档 Vx》取代"。
4. 归档 = 移动，**绝不删除**；移动后引用按 §九 铁律② 自动成立，无需改链接。

**README 文档地图（总索引，仅在看全貌 / 取代关系时查）**：

| 文档 | 状态 | 版本 | 位置 | 备注 |
| --- | --- | --- | --- | --- |
| **销售CRM业务需求文档** | **现行有效** | **V1.1** | 需求规格/ | **业务规则唯一真相源**：做什么 / 为什么 / 规则（含 §13 全局交互规范） |
| **销售CRM数据架构文档** | **现行有效** | **V1.2** | 需求规格/ | **Schema 唯一真相源**：表 / 字段 / 索引 / 字典 / 权限（§十四 需求追溯索引供 L0 对账） |
| 销售CRM接口API文档 | **待建·V1 后置** | — | 需求规格/ | 等业务需求稳定后创建 |
| 销售CRM前端页面与交互文档 | **待建·V1 后置** | — | 需求规格/ | 等业务需求稳定后创建 |
| 数据库设计文档-助理版 | 已归档·被拆分 | V1.0.7 | 归档/助理版合并文档（已拆分）/ | 2026-09-09 拆为上述两份，**禁止照此实现** |
| 销售CRM开发需求 | 已归档·被取代 | V2.1 | 归档/V1.0档案库版本需求框架/ | **§三 流程 / §六 功能模块 ~490 行已于 09-09 捞回业务需求文档** |
| 数据库设计文档 | 已归档·被取代 | V1.2 | 归档/V1.0档案库版本需求框架/ | 纸面设计从未开发，已被取代 |
| 接口API文档 | 已归档·历史 | V1.4 | 归档/V1.0档案库版本需求框架/ | 表结构已换；新建 API 文档时可作格式参考 |
| 前端页面开发需求文档 | 已归档·历史 | V2.6 | 归档/V1.0档案库版本需求框架/ | **§4.3 一层抽屉/悬浮卡已并入需求文档 §13**；页面清单可参考 |
| **助理版拆分·归档说明** | 已归档 | — | 归档/助理版合并文档（已拆分）/ | 拆到哪去了、后来人须知 |
| 销售CRM项目摘要-给老板看 | 现行有效 | — | 项目介绍/ | 给老板 / 外部 |
| 项目全景构想图 | 现行有效 | — | 过程产出/ | 启动大会底图 |
| 技术栈方案对比 | 已归档·历史 | V1 | 归档/技术决策/ | 决策已定，理由备查 |
| 需求一致性核查报告 | 已归档·历史 | 2026-09-08 | 归档/过程产出/ | 9 项已落地，留痕备查 |
| **V1.0 档案库版需求框架·归档说明** | 已归档 | — | 归档/V1.0档案库版本需求框架/ | 四件套为何归档（注：其 §三/§六 已于 09-09 捞回业务需求文档） |
