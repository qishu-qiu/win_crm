# 交接说明（M1~M3 已闭环 · M4-01~M4-08 已落地 · 下个窗口必读）

> **本文件是「下个窗口开工前要交代的事」的唯一落点**，只记 **状态 / 缺口 / 待拍板**。
> **读法**：本文件 → 《技术决策/AI协作铁律与踩坑复盘》**V1.8**（AI 行为铁律 ＋ 踩坑）→ 《过程产出/开发计划-V1》（任务定义与判据）。
> ⚠ **业务口径一律以五份现行规格为准**（需求 V1.26 / 数据架构 V1.31 / 接口 **V1.17** / 前端 V1.16 / 设计规范 V1.0），本文件不复述。
> 📌 2026-09-15 本窗口：**A 已裁定＝阶段用数字 `1~7`**（接口升 V1.17 / 废止表 #37）；**M4-01~M4-08 已三绿＋起服验证**（未提交前先看 §四）。
> 版本沿革查 git。**环境踩坑已上收至铁律文档坑 25~29，本文不再复述。**

## 一、当前进度

| 里程碑 | 状态 | 判据 |
| --- | --- | --- |
| **M1** A 域（登录 / 刷新 / me ＋ 四个组织只读接口 ＋ 登录页） | ✅ 已落地并提交（`edf46a2`） | 登录＝手机号 / 账号名双通道；数据范围四档；`/org/employees` 按 G7 收敛；成功响应一律 **200** |
| **M2** B 域（建档 / 查重 ＋ 前端建档页） | ✅ 闭环（`07bbb0e`） | M2-01~M2-17 全绿 |
| **M3** C 域（业务关系 / 私海 / 公海 ＋ 前端列表页） | ✅ 闭环 | M3-01~M3-14 全绿 ＋ **真库实测**：激活 / 撞单（409·20401）/ 越权 403 / 私海三种范围 / 公海按部门收敛且 `owner=null` / 非灰度未标价值 422·20403 / 第二 owner 409 / @求助跨部门 403（同部门默认 7 天）/ 掉海释放活跃位后同键可再激活 |
| **2026-09-15 口径补齐批** | ✅ 已提交 | ① `product_line.color_key` 补列（migration `0004` ＋ 数据架构 A7 → **V1.31**）；② 业务码 **`20407` 跨部门 @求助**（→ 接口 **V1.16**）；③「非灰度必标开发价值」改为「**非空即算**」（`pending` 也算，**回退了原先更严的自造口径**）—— 三处均真库验证 |

| **2026-09-15 M4 上半段** | 🟡 **代码已写、待提交** | **M4-01~M4-08**：D 域 `modules/engine/` 四层 ＋ 三条纯规则（有效沟通 / 回写 / 幂等键）＋ 事件写入与**内容幂等** ＋ 时间线取数。**无 controller**（M4-12 才挂接口，故本段只有单测 ＋ 起服验证，**没有 curl 真库验证**）。三绿 ＋ `/docs`=200 |

**下一步 ＝ M4 下半段：M4-09~M4-17**（承诺三型 / 每日工作台 / 事件与承诺的 controller / 首页参与关系时间线）。

## 二、★ 必须先拍板（阻塞 M4）

| # | 问题 | 现状与影响 |
| --- | --- | --- |
| **A** | ✅ **已裁定＝数字 `1~7`**（2026-09-15 七叔拍板） | 接口文档升 **V1.17**：§2.6 / §4.14.3 的英文码举例**作废**，登记《废止口径登记表》**#37**；DB 三处数值列（`business_relation.stage` / `relation_stage_log.from_stage|to_stage` / `commitment.stage_forward`）**不动**，中文名仍出字典 `workflow_stage.label`。⚠ **残账**：代码注释里约 50 处 `V1.16` 指针未扫（混有「前端 V1.16」，不可批量替换）→ 下一次**专项窗口**处理 |
| **B** | **AI 起草的 6 个字典码是否照用？** | 规格只给中文、未给码：`company_identity_tag` / `company_policy_tag` / `decision_chain` / `contact_trait` / `relation_risk_label` / `review_win_reason` —— 现由 AI 按语义起草（全站要求英文码）。**业务确认后可改**（改前先 grep 引用）。见 `服务端/prisma/README.md` §五 |
| **C** | **7 条正式产品线名称清单** | 现为 3 条占位线（过渡色键 `website=blue` / `miniapp=green` / `operation=orange`），正式清单（法务 / 财税 / 招聘 / 房产 / 网站建设 / 短视频 / GEO推广）**待拍板**，见 `服务端/prisma/seed/001_dev_seed.sql` 注释 |

## 三、待确认 / 欠账（非阻塞，逐项「状态 / 何时做」）

| # | 项 | 状态 | 何时做 |
| --- | --- | --- | --- |
| 1 | **数据范围注入（统一机制）** | ⛔ 未开工 | **M5**（B/C 域此前无列表可注入；`serving` 还需读 E 域合同服务期） |
| 2 | **脱敏**（详情全可见 / 列表 `phone_masked` / 联系人锁 / 跨部门跟单与金额） | ⛔ 未开工 | **M5**（需先有真实业务 DTO——先写必错，且错向＝把经理该看的真金额打码） |
| 3 | **管理员「只读 ＋ 每次查看留痕 ＋ 不解除金额脱敏」** | ⛔ 未开工 | **M5**（管理员同时要能写组织 / 字典 / 规则，不能一刀切拦写） |
| 4 | **`permission_matrix` 服务端消费方** | ⛔ 未开工 | **M5**（先在 `/account/me` 出参供前端用） |
| 5 | **跨域领域事件**（`kernel/event-bus` 目前是空壳） | 🟡 已知 | **M4** 起才真用 |
| 6 | **列表未分页**（`GET /companies` / `GET /contacts` 暂「按 id 倒序取前 100」） | 🟡 已知 | **M6**（统一 `page / page_size`，接口 §2.7） |
| 7 | **`GET /companies/:id`（公司详情）未做** | 🟡 已知 | **M6**（建档页「用现有公司」目前只带 `id ＋ 全称`，**不假装有字段**） |
| 8 | **`/org/product-lines` 未实现** | 🟡 已知 | **M6**（组织架构管理页；出参须带 `color_key`） |
| 9 | **`department` 没有色值列**（需求 §13.3 要求部门可配颜色） | 🟡 已知 | **M6**（与产品线侧是同一个洞的两个面，产品线侧已由 `0004` 补） |
| 10 | **跨域派生字段**（`relation_count` / `old_customer`） | 🟡 已知 | `relation_count` **M3 起已可由 C 域提供**（`RelationService` 加计数方法），**M6** 公司列表页接入时补 |
| 11 | **交付·客服的私海列表比规格更窄** | 🟡 已知 | 规格＝「仅**服务中**（在合同服务期内）」；M3 先收敛到「我参与的关系（owner ∪ 有效协同）」＝**比规格窄、不会越权**。**M4 / M5** 接上合同后补「服务期」条件 |
| 12 | **关系出参未返回的字段（一律不填假值）** | 🟡 已知 | 故意不返回：`drop_in_x_days`（掉海 M7）/ `overdue`（D 域 M4）/ `amount` `amount_masked` `old_customer` `is_weekly`（E 域）/ `round_no` `prev_round`（`sea_record` 未落库）/ `competitors` `labels` `stage_logs` `try_count_30d`（C3/C4/D） |
| 13 | **`Idempotency-Key`（接口 §2.5）横切能力未实现** | 🟡 已知 | M2 起同一现状，随横切批次 |
| 14 | **写权限口径靠三处合成**（规格无「谁能写业务关系」的表） | 🟡 待确认 | 可写＝`sale` / `dept_manager` / `gm`；**管理员与交付·客服只读**；叠加角色（`admin ＋ sale`）**仍可写**。待规格补齐 |
| 15 | **「同部门」的解释** | 🟡 待规格细化 | 现＝「我所属 ∪ 我管辖」的部门交集（依据接口 §4.2） |
| 16 | **前端未引 vue-router / pinia** | 🟡 已知 | 方案 A 最小页够用；**§五 29 个路由开工时必须拍板引依赖** |
| 17 | **前端 bundle 1.5 MB（gzip 466 KB）** | 🟡 已知 | 页面少、内部系统可接受（**先测量后优化**）；**M6** 按需引入 / 代码分割 |
| 18 | **分区滚动（运维）** | 🟡 低风险 | `pmax` 兜底**不会插失败**；真实风险是 **2028-01 起新行全落单一 `pmax`、老分区无法 `DROP PARTITION` 清理** → **M7**（Worker）起需每月续期机制，2028-01 前必须有。口径见《数据架构文档》§十二 ＋ `prisma/README.md` §十.9 |
| 19 | **规格缺口：多角色取更宽 / refresh 出参不带 user** | ⏸ 已实现、未入规格 | 一句话「写」即可 |

## 四、开工动作（下个窗口）

1. 读《AI协作铁律与踩坑复盘》V1.8 → 本文件 → 开发计划 M4 判据 → 五份规格相关章节。
2. **先拿 §二 的三个答案**（A 阻塞 M4；B / C 可并行推进）。
3. 每完成一个里程碑：**三绿**（`tsc --noEmit` / `lint` / `test`）＋ **真起进程验证** ＋ **逐文件 `git add` 提交**（**严禁 `git add -A`** → 铁律 §一 E）。
4. 当前基线：**38 suites · 536 tests 全绿**（改代码后以此为准，不要照抄旧快照里的 `18 / 23 / 25 suites` —— 那是过期快照）。
5. **M4 上半段多动了两处别人域的文件（已在本窗口登记，勿惊）**：① **C 域** `RelationService.requireWritableRelation` / `touchLastEventAt` ＋ `RelationRepository.updateLastEventAt`（理由：`last_event_at` 在 **C 域的表**，跨域禁止直连与跨域事务，只能走对方 exports 的 service）；② **B 域** `CompanyService.getContactRefs`（时间线要给联系人名，`contact` 是 B 域的表）。两者都只是**加出口方法**，未动既有逻辑。
