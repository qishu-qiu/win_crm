# AI 协作铁律与踩坑复盘 · **案例库**（V1.13 · 按需查 · 已不是每轮必读）

> **⚠ 本文已降级为「案例库」（2026-09-16 七叔定）**
> · **每轮必读**的只有《**AI执行清单**》（`技术决策/AI执行清单.md`，**≤3 KB** —— 只写「触发时机 → 一个动作」）；
> · **本文**保留**坑的完整证据、为什么、通例、新坑归档**，**按需查**（遇到具体场景时回来翻）。
>
> **为什么拆**：本文 21 KB 作为"每轮重放层"过长 —— 2026-09-16 复盘实测：**规则被读了，动作发生时却想不起来**（换窗判据漏 1 次、命令输出读两份漏 10 次、四要素漏 1 次、动手前未先发"准备做"数处）。七叔原话：**"我宁可少都执行，也不要啥都有你今天漏这个明天漏那个。"**
>
> **★ 维护（2026-09-16 改）**：新坑 / 新元规则 → **写进本文（案例库）**；**只要能变成一条可判定动作，同批往《AI执行清单》加一行** —— **两处都要动，不许只写一处**。
> **容量**：本文**不再设上限**（它不重放，长一点只影响"翻查速度"）；《AI执行清单》**≤3 KB 硬上限**（它每轮重放）——**到顶先删而不是先加**。
>
> **业务口径不进来**：业务规则 / 表字段 / 接口 / 页面 / 状态一律以五份现行规格为准（进度 / 缺口另见交接说明与《欠账登记表》），本文只留指针。
> **与 README 的分界**：`README.md` §三 / §八 / §九 / §十 管「**文档治理**」（Git 约定、L0 / L1 联动、归档规则、权威落点总表）；本文管「**AI 行为与经验**」。两者重叠处**只写指针、不复述**。

---

## 一、AI 行为硬约束 → **正文已迁至《AI执行清单》**

> 本节原 A~G（明文执行令 / 短回复不外推 / 动手前先发「准备做」/ 回退机制 / 提交前逐文件认领 / L0 联动边界 / 待拍板四要素）的**完整表述，已搬进 `技术决策/AI执行清单.md`**（对应其第二、五、六、八节），此处**只留指针** —— 内容重复＝双真相源（改一处漏一处）。
> **迁移动因（2026-09-16）**：A~G 原来是"原则"，而 AI 真正需要的是**动作发生时能对上的检查点**。重构后形态＝「触发时机 → 一个动作」，且**每轮重放层从 21 KB 降到 ≤3 KB**。
> **本节不再保留原文**：属**结构性搬迁**（内容未推翻），权威落点已改为《AI执行清单》。

---

## 二、踩坑复盘（本项目已发生，勿重演）

| # | 坑 | 后果 | 已立的防线 |
| --- | --- | --- | --- |
| 1 | **AI 按关键词命中**：旧口径散落正文，命中哪处就照哪处写 | 同一文档自相矛盾（`1062` vs `P2002`）、15 处正文没跟上拍板 | 正文为准 ＋「索引·非规范」标注 ＋《废止口径登记表》 |
| 2 | **索引被当规范**（速查表 / 接口总目录 / 追溯索引 / 文档地图） | 漏掉正文里的完整条件 | 索引类必须显式标「索引·非规范」 |
| 3 | **归档目录检索污染**（旧文档近乎同名） | grep 到作废口径照做 | `.ignore` 机械隔离 `归档/`；归档件加"⚠已作废"头标 |
| 4 | **负面约束只收一半** | AI 以为"表里没有＝允许"，自行补全被封路功能 | 需求 §十六 收全为**完整清单**，"表里没有"才代表允许 |
| 5 | **机械扫描只扫 `.md`** | 漏掉 `schema.prisma`、`prisma/README.md`、过程 HTML 的旧版本引用 | 扫描覆盖全部文本类型（`.md / .html / .prisma / .sql / .json`） |
| 6 | **版本沿革写进正文** | 正文漂着旧版本号，改一处漏一处 | 沿革只留 `git`，文档内删「修改记录」 |
| 7 | **派生对外文档没同步** | 给老板的摘要说出已推翻口径 | L0 联动要求**同批、同次提交** |
| 8 | **AI「缺一块就自行补全」** | 实现被封路的功能 | 动手前先查需求 §十六；"表里没有"才代表允许 |
| 9 | **同事实多处落点＝双真相源** | 改一处漏两处 | L1 单一事实源；非权威位置**只留指针** |
| 10 | **`git add -A` 扫进别人未完成的改动**（多窗口并行：工作树 ≠ 本轮改动范围） | 另一窗口改到一半的服务端文件被挂进 docs 提交 `ffd899e` | 提交前 `git status --short` 逐文件认领；**只 add 本轮明确路径**；他人未完成改动原地报告等指令 |
| 11 | **`@/` 路径别名只活在编译期**：`tsconfig.paths` ＋ jest `moduleNameMapper` 能骗过 `tsc --noEmit` 与单测，但 **`tsc` 不会重写产物里的路径** | 源码里写 `@/kernel` 的进程，本地三绿全过、CI 也过，却**到 `node dist/main.js` 才报 Cannot find module** | **src 内一律相对路径**（如 `../../kernel`），`@/` 只留给 spec；口径已写死在 `服务端/src/kernel/index.ts` 顶部注释 |
| 12 | **`nest build` 默认读 `tsconfig.json`**，不排除测试文件 | `dist/` 里混进 `*.spec.js`（发行产物不卫生） | 已有 `服务端/tsconfig.build.json`（`exclude` 全部 `*.spec.ts` ＋ 独立 `tsBuildInfoFile`）；**验收＝dist 里 spec 数 0**。⚠ **2026-09-20 改判据**：`nest-cli.json` 已设 `deleteOutDir:false`（本工作区的删除守卫会拦「清 dist」→《欠账登记表》**D-58**）⇒ dist **允许留旧文件**，判据改为「**本次构建不新增 spec ＋ 无孤儿 `.js`**」：`Get-ChildItem dist -Recurse -Filter *.spec.js`（比新产物还新＝构建把 spec 编进去了）＋ 逐个 `dist/**/*.js` 反查 `src` 有无同名 `.ts`，孤儿出现＝该 `npm run clean` 了 |
| 13 | **Jest 载不动 NestJS 12**（`@nestjs/common` 是 ESM-only） | 含 `@Injectable` 的用例一律 `Must use import to load ES Module` —— 守卫 / 拦截器 / 服务**全都写不了测试** | `test` 脚本已改用 `node --experimental-vm-modules …/jest.js`（**运行时不受影响**：Node 24 原生 `require(ESM)`） |
| 14 | **`@nestjs/testing` 没装**（只装了 Jest） | 用例 import 不到 → TS2307；顺手一装＝**未拿执行令装依赖** | 单测要 DI 容器时用 `@nestjs/core` 的 `NestFactory.createApplicationContext`（**零新依赖**）；真要装 `@nestjs/testing` 须先拿执行令 |
| 15 | **拿 Proxy 做「库访问绊线」放进 DI 容器**（想证「没查库」） | Nest 的 `callModuleInitHook` 会对容器里**每个 provider** 读 `onModuleInit` 等生命周期钩子 → 绊线被框架自省误伤而抛，用例**假红**，跟业务有没有查库毫无关系 | 「不碰库」改用两条硬证：① 容器里**根本不放**库客户端也能装配成功（若注入 Prisma，Nest 此刻报依赖解析失败）；② 断言类上**构造依赖个数**（多挂一个依赖立刻红） |
| 16 | **测试假件比真实形状少字段** | 用例假红：守卫读元数据时 `executionContext.getHandler is not a function`，看着像守卫有 bug，实际是假件不是真形状 | 假件按**真实上下文形状**造：`getType` / `getHandler` / `getClass` / `switchToHttp` 一个不少；**假红先修假件，再怀疑被测代码** |
| 17 | **计划书判据与规格冲突**（M0-35 写"坏 DTO → 422"，接口 §2.4 定为 **400 / 20001**） | 照计划写会把"前端传错类型"错记成业务校验失败，前端整条分支接错 | **以规格为准**（`CONSTRAINTS` 首行）；冲突**写进报告 ＋ 实现处注释**，不静默按计划做（→ §三 同名通例） |
| 18 | **按 bash 语法写命令，但本机 shell 是 PowerShell**（例：`npm test 2>&1 \| tail -n 25`） | `tail` 不存在 → 该段整条失败；而它挂在 `&&` 链**最末**，前面 `tsc` / `lint` 已打印 OK → 极易误读成"三绿全过"，实际 **`npm test` 根本没跑** | 取尾用 PowerShell 原生的 `... \| Select-Object -Last N`；**命令语法按当日 shell 写**（本机＝PowerShell，非 bash）；验收必须看到**该命令自己的结果输出**（如 jest 的 `Tests: N passed`），不能只看"没报错" |
| 19 | **用 PowerShell `-replace`（正则）批量改中文文档** | 实测 **37 个文件被写坏**（`《销售` → `销销售`、`**` 被吃掉），而 `tsc / lint / test` **全绿**（坏的是 md 与注释）—— 差点直接提交 | ① 中文**只用字面 `.Replace()`**（或编辑器替换），**禁可选 / 贪婪模式**；② **先单文件试跑** ＋ `git diff --numstat` 确认只变 1~3 行再全量；③ 全量后跑**三校验**（损坏签名 grep / 行数上限 / 剩余旧值 grep）；④ 怀疑写坏就 `git checkout -- `<paths>`` **整体回退**，别在坏文件上二次修补 |
| 20 / 27 | **DTO 漏 `@ApiProperty`（入参）／可空字段没写 `type` ＋ `nullable`（出参）**（同源、两个方向） | OpenAPI 退化成**空对象** `Record<string, never>` → 前端类型不可用 → 被迫手写对接（**本项目一号坑**）。⚠ 后端三绿**全过**（它不读 OpenAPI），只在**前端真用该字段**时才炸 | ① **入参每个字段都要 `@ApiProperty`**；② 可空字段一律写全 `{ type: X, nullable: true }`；③ **改完 DTO 必重跑 `gen:types`** 并 grep `Record<string, never>`（除 `webhooks` / `$defs` 样板外应为 **0**），diff 留档 |
| 21 | **单测假对象比真实阶段"更完整"**（如事务内 `RETURNING` 发生在成员写入**之前**，`members` 必然为空，假对象却给了带成员的整行） | 拿**入库前**那份行装配 → 真库出参 `owner` 为空，而**单测全绿**（假对象"好心"把 bug 盖住）；假对象越"完整"，越测不出真链路 | 假对象必须**复刻真实阶段**（哪次 SELECT 在哪一步之后）；**改完必须真库跑一遍出参**。★ 单测只验你自己写的逻辑，验不了**框架的返回时机** |
| 22 | **用 `npx jest `<pattern>` 跑单测**（例：`npx jest relation`） | 绕过 `npm test` 里的 `NODE_OPTIONS=--experimental-vm-modules` → 套件直接 **"Must use import to load ES Module: @nestjs/common"** 加载失败。极易误读成"**我刚写的代码坏了**"，然后去改根本没问题的源码 | 跑单测**只用 `npm test`**（或原样带上项目脚本里的环境变量）；看到 ESM 加载类报错先怀疑**调用方式**，再怀疑代码 |
| 23 | **把模糊规格「解读得比原意更严」**（本次：数据架构 C1 只说「非灰度必须**已标** `value_tier`」，实现却加码成「必须落在已定档三值内，`pending` 不算」） | 被七叔拍板纠正：「**只要标了就行**」。**过度解读与漏做同样是偏离规格**，而且它更隐蔽——单测还会把这条自造的严格规则一起钉死；实际后果是逼出一堆随手点的**假档位**（数据质量反而更差） | 规格用词模糊时，**按最直接的字面读法实现**（「已标」＝有值），把「要不要更严」当作**待确认项写进报告**，而不是先按更严的做。★ 判据：**你实现的那条"额外限制"，规格原文里有没有一句话能逐字支持？** 没有就报，不要做 |
| 24 | **上游规格自身自相矛盾**（本次：`stage` 英文码 vs 数字 1~7；`product_line.color_key` 需求要返回而数据架构 A7 无此列） | 静默挑一个＝**替七叔做决定**，下个窗口会照你挑的那套继续盖楼；两套并存则前端筛选 / 展示 / 字典种子各认一套（→ 废止口径 #36） | ① 按「多处一致 ＋ 业务真相源（需求）＋ DB 一致性」取一个实现，但**必须书面登记冲突**（交接说明 ＋ 报告点名）；② 判定是"文档漏落"就**补齐文档与库并升版**（`color_key` 即如此收口）；③ 涉及**契约词汇**（如 stage 码）**只报不改**，等拍板 |
| 25 | **vite dev server 只监听 `::1`（IPv6）** | 用 `http://127.0.0.1:5173` 探针一律 **HTTP 000**，极易误判成"前端没起来 / 接口不通" | **已定解（2026-09-22）**：`前端/vite.config.ts` 写死 `server.host: true`（绑 `0.0.0.0`，实测 `Get-NetTCPConnection` 显示监听 `::` 双栈）⇒ `127.0.0.1` 与 `localhost` **均可**；后端默认 **3000**、前端 dev **5173**，抢端口时后端用 `PORT=3100` |
| 26 | **`gen:types` 依赖后端在线**（脚本写死 `localhost:3000/docs-json`） | 后端没起就生成 → `types.ts` **缺接口**，前端照残缺类型开发，直到联调才炸 | 跑 `gen:types` **前先起后端**；生成后按坑 20 的 grep 校验，并把 diff 留作接口变更证据 |
| 28 | **PowerShell 给外部命令送含中文的 JSON**（管道 / `-Body` / 内联 `-e` SQL 都会吞中文）→ 本次（09-22）复现清库又踩两次（探针联系人名变 `??`、内联 SQL 丢引号） | 汉字变 `?`、字段空串 / SQL 丢引号语法错 —— **看着像业务 bug 或库坏了，其实是探针的锅**（差点去改没问题的源码） | ★ **UTF-8 字节体一行法**：`Invoke-RestMethod ... -Body ([Text.Encoding]::UTF8.GetBytes($json))`；或写 ASCII 临时 `.js` 用 `node fetch` 发请求、SQL 写文件再 `source`。**先证实探针没错，再怀疑被测代码** |
| 29 | **`mysql -e "…"` 内层双引号被 PowerShell 吞掉** | SQL 语法错 → 误判成"库 / 表有问题" | SQL 写进 **ASCII 路径**临时文件再 `-e "source `<path>"`（`source` **打不开中文路径**） |
| 30 | **`nest start` 起的真进程是 `node dist/main`**（父进程才叫 `nest start`）：按命令行含 `nest` 杀，只杀得掉父进程 | 改完代码"重启"后**旧进程仍占 3000**，"探针 200"照样过 —— 实测因此**白验两轮**（改了 Swagger 定义，`gen:types` 一直是旧形状） | 重启＝**按 `dist/main` / `dist/worker` 特征杀**（`Get-CimInstance Win32_Process -Filter "Name='node.exe'"` 看命令行）；★ 判据＝**必须看到「只有新代码才有」的行为**（如直接比对 `/docs-json` 那一段），**不能只看端口 200** |
| 31 | **DI 注入的类用 `import type` 导入** | `tsc` / `lint` / `test` **全绿**，起服才报 Nest「依赖解析失败」：`design:paramtypes` 里那一项退化成 `Function`（类型导入被编译期擦除） | **凡是「构造函数参数类型＝注入令牌」的类，一律值导入**（`import { EventBus }`）；判据＝元数据里必须是那个**类**。凡"三绿全过、起服挂掉"，先查这类被擦除的导入（属坑 11 家族） |
| 32 | **2026-09-16 窗口实测三连**（都发生在"AI 明明读过规则"的前提下）：① **命令输出读两份** —— PowerShell 把原生命令的 stderr 包成 ErrorRecord，`2>&1` 之后 stdout / stderr **各回显一遍**，本窗口 10 次命令全部白读一倍；② **git 输出中文变八进制**（`M "\345\211\215\347\253\257/..."`）—— 体积 ×3；③ **8 个提交挤一个窗口** —— 每轮都要重放「全部历史 ＋ 常驻规则」，成本随轮数**平方**涨（本窗口 60+ 工具调用、20+ 轮，第 20 轮输入约为第 3 轮的 5~8 倍） | 单窗口成本数倍于必要值；**但最伤的不是钱，是"规则读过了却没执行"**（换窗判据漏 1 次、四要素漏 1 次、动手前未先发"准备做"数处）—— 说明**21 KB 的重放层已经不起作用：读过 ≠ 用得上** | ① 命令用 `2>$null` **只读一路**；② git 加 `-c core.quotepath=false`；③ **本轮完成 ≥2 次提交，AI 必须主动提示换窗**。★ **根本对策**＝把"规则清单"重构为"**可判定检查点**"，重放层从 21 KB 压到 ≤3 KB（→ `技术决策/AI执行清单.md`，七叔 2026-09-16 原话：「我宁可少都执行，也不要啥都有你今天漏这个明天漏那个」） |
| 33 | **AntD Vue 的 `<a-form @finish>` 在表单没有 `:model` / 字段 `name` 时根本不触发**：`<form>` 在、按钮 `type=submit` 在、`submit` 事件也被拦截（`defaultPrevented=true`），但 `onFinish` **一次都不调** | 2026-09-18 浏览器走查实测：**录入页第 1 步「下一步」是死按钮**（点 / 回车都没反应、**无任何报错**），而**后端三绿 ＋ 前端 lint / build 全绿**（→ 与坑 11 / 31 同族：**静态检查照不出交互**）。登录页因为写了 `:model` ＋ `name` 所以一直正常 ⇒ 这个坑从 M6-09 起藏到现在 | ① 提交**不靠 `@finish`**：按钮 `@click` ＋ 输入框 `@press-enter`（本项目「写跟单 / 建承诺」一向这么写，故那些页没中招）；② **凡新增"能点的入口"，必须真点一次**（浏览器走查 / 人点），"类型过 ＋ 构建过"**不算验** |
| 34 | **给 AntD 组件传 slot 时，槽里第 0 个节点是注释**（2026-09-20：`前端/src/App.vue` 的 `<a-dropdown>`，`#overlay` 槽那段注释排在 `<a-menu>` 前面） | AntD 取 `slots.overlay()[0]` 当菜单（`ant-design-vue/es/dropdown/dropdown.js` 的 `renderOverlay()`），而 **dev 下 Vue 保留模板注释** ⇒ 注释被**当成菜单**拿去 clone，**弹层里只剩一个注释节点**（实测 `innerHTML` ＝ `<!----><!---->`、`height:0`、`.ant-dropdown-menu-item` 计数 **0**）——表现是「**点也"没反应"、悬停也"不显示"**」，而弹层**其实已展开**（触发器带着 `ant-dropdown-open`）。⚠ **lint / build 全绿**：生产构建会剥掉注释、反而正常（实测对照：`dist` 里菜单文案命中 2 处、注释片段命中 **0** 处）⇒ **只有真跑 dev 页面才暴露**（→ 坑 33 同族："静态检查照不出交互"）。后果：「外观设置 / 退出登录」两个入口等于不存在，人只能问"主题切换去哪了" | ① **组件标签内的注释一律写在槽外** —— 槽里第 0 个节点必须是**真组件**；② 判据：弹层"没反应"先量 **`getBoundingClientRect().height` ＋ `innerHTML`**，再看事件有没有绑上（"状态已展开"骗过过一轮）；③ 顺带：`a-dropdown` 默认 `trigger: 'hover'`（点击 / 触屏 / 键盘全不可用）—— **要点的菜单必须显式写 `:trigger="['click']"`** |
| 35 | **给 Worker 新挂一个业务域时，那个域「构造函数要的东西」远不止它自己**（2026-09-20：`worker.module.ts` 挂 `SeaModule` 落 M7-03 掉海预警） | 三绿（`tsc` / `lint` / `test` 748→765 **全绿**）之后**真起 `node dist/worker` 连炸三轮**：① `Nest can't resolve dependencies of the SeaService (SeaRepository, RelationService, ?)`（`EventBus` 缺席）；② 补上后 `…of the OrgService (OrgRepository, JwtService, ?, PrismaService)`（`AuditService` 缺席）；③ 再补后 `…of the SeaWarningJob (?)`（`SeaModule` 没 `exports` `SeaService`）。根因：**那两个是 kernel 的 `@Global()` 装配点，而 `@Global()` 只在"被根模块 import 过"之后才生效** —— Web 那边靠 `app.module.ts` 组装 kernel，Worker 的根模块是 `worker.module.ts`，**没人替它装**（两个 `.module.ts` 头注都写了「装配发生在根模块」，漏读即中招）⇒ **`@Global()` ≠ 不用管** | ① 凡给 Worker / 新根模块挂域，**必须真起一次进程**（→《AI执行清单》#15 就是为这类事存在的：**静态检查照不出装配**，与坑 31 / 33 / 34 同族）；② 挂域时**顺着域的构造函数清点 kernel 全局件**：把 `app.module.ts` 那一层列的（`ContextModule` / `EventBusModule` / `PrismaModule` / `AuditModule`）过一遍，**只装用得到的、并写明为什么不用其余**；③ 跨模块注入的 service **必须出现在被依赖模块的 `exports`** —— 「不预先导出没人用的东西」这条纪律在**出现新调用方时就要当场改**（不算破例） |
| 36 | **整篇重写一份"状态类文档"，会把同窗口里别人刚写进去的结论静默吞掉**（2026-09-20：交接说明；本窗口并行发生了 `b33c49b`） | 收尾时我为了压 5 KB 上限，**按开工前读到的那一版内容整篇重写**了交接说明 —— 而该文件在本窗口中途已被 `b33c49b` 追加过「**F-01 片 2 浏览器走查通过 ⇒ 片 3 解锁**」的结论 ⇒ 重写把这条结论**抹掉了**（后人按"待人点验"办：重复验一遍、甚至误判进度；已用 `da1e209` 补回）。⚠ 破坏是**静默**的：`git status` 只显示 `M`，"丢了一段"完全看不出来；**压容量时尤其危险**（注意力在 byte 上，把"重写"当成了"改格式"） | ① **整篇重写（覆盖式写入）任何被 git 跟踪的文件之前，先 `git log -1 -- `<file>`**，确认工作树版本＝你手上这一版（→《AI执行清单》#11）；② 重写类提交**提交后必须 diff 一遍**（`git diff `<上一版>` -- `<file>`）逐条认领"少的 / 变的"，别只看 `git status`；③ **状态类文档（交接说明 / 台账）优先定点增删，不整篇重写** —— 要压容量就先删旧段，别再重新生成全文 |
| 37 | **文档版本号有「两处落点」（文件标题行 `# … V1.xx` ＋ 头部 `> 状态：**V1.xx**`），只改一处就自相矛盾**（2026-09-20：接口文档） | 我给接口文档升 V1.27 时**只改了状态行**，标题行留在 `V1.26`（→ 实测 `Select-String "^# "` 仍是旧值）；同日**另一窗口**升 V1.28 同样只改状态行 ⇒ 同一份文件里两个版本号，且 README §现行文档表那行也还写着 V1.26（**三处落点两处旧值**）。⚠ 不是"本来就没人维护标题行"：`git log -L 1,1:需求规格/销售CRM接口API文档.md` 证明标题行**每版都改过**（V1.18 / V1.20 / V1.21 / V1.23 / V1.26 都动过它） | ① 改版本号＝**先 `git log -L 1,1:`<file>` 查"这个值历史上改过哪几处"**，再**逐处**改；② 连带 `README.md §现行文档表` 那一行（文档版本 ＋ 案例库版本都是这个形状）；③ 判据＝改完 `grep -n "V1\.2[0-9]"` 全库只剩**当前版本**。★ 这是**一号坑（双真相源）的文档版**：值有多个落点 ⇒ 必有一处先腐。**已定解（2026-09-21）：版本号只留「状态行」一处 —— 标题不带版本号、互引一律不写版本号（→《AI执行清单》#13）** |
| 38 | **并行窗口会把你"已 `git add` 但还没 `commit`"的东西一起提交走**（2026-09-20：多窗口同时开在本工作区） | 我 `git mv 过程产出/F-01 施工单.md 归档/过程产出/` 之后去跑三绿（约 90 s），期间**另一窗口执行了一次 `git commit`** —— 对方提交的 `--stat` 里出现 `{过程产出 => 归档/过程产出}/F-01 施工单.md \| 0`（**与它自己的主题无关**）：我的 rename **挂在了别人的提交名下**。⚠ `git status --short` **完全看不出异常**（rename 已不在我的暂存区，只剩归档后那几行改动）—— 是事后 `git log --oneline -3` 发现多出一笔不属于我的提交才查出来 | ① **多窗口并行时，`git add` 与 `git commit` 之间不插长动作**（跑测试 / 长编辑 / 跨文件改动）；② 收尾用 `git show --stat `<hash>` **逐条认领自己那笔提交里有什么** —— `git status` 只说"我暂存了什么"，不说"提交带走了什么"（→《AI执行清单》#18–20 的补充判据）；③ 提交带**显式路径**（`git commit -- `<path>`）可把误带面压到最小 |
| 39 | **给方案 / 选型只图"当前最快"，默认给技术债方案**（2026-09-21：公司详情跨域聚合，我直接推荐"破例让 B 调 C"的桥①，未先评估长期价值；被七叔拍板改选聚合层桥③） | 每加一块跨域信息就再破一次例，白名单越加越长、模块边界腐烂，后期返工数倍于当初 | 给方案 / 选型**必附长期价值评估**（多方案排长期序 ＋ 标"技术债成本"），不唯省事；**七叔铁律：「推荐时把本次改动的长期价值给到我，别只图省事给我一大堆技术债务，后期更麻烦」**（→ 通例） |

> **三条最核心**：① 先读《废止口径登记表》＋需求 §十六，再动手；② 改文档＝必须 git 提交，四份同批同次；③ 不要自行补全"缺的一块"，先查是否被封路。

---

## 三、通例（由上述坑提炼，适用于所有文档与协作）

- **"防 AI 误读"的自检，必须出现在 AI 真会读到的那一页**（表头 / 章节首行），**不能只写在章程** —— 因为 AI 读表时不会回头翻章程（2026-09-12 定）。
- **规则本体放"读者会直接打开的那份文件"里**，上级文档只留**指针与动作项**。
- **同一事实只允许一个权威落点**；非权威位置必须显式标注定位（如「索引·非规范」），否则会被 AI 当成规范。
- **"三绿"不等于"能跑"**：`tsc` / `lint` / `test` 全过只证明**编译期与测试期**成立。凡涉及**模块解析、依赖加载格式、打包产物**的改动，必须**真起一次进程**（`node dist/*.js`）才算验完 —— 坑 11 / 12 / 13 都是"三绿全过但跑不起来或产物不干净"（2026-09-14 定）。
- **否定式约束（"不许查库 / 不许越权 / 不许依赖某层"）要用「无法表达」来证，不要用「跑一遍没发生」来证**：前者是结构事实（依赖没注入 → 不可能调用），后者只覆盖一次执行、还容易被框架自身行为干扰（坑 15）（2026-09-14 定）。
- **判据与规格冲突时**：先比规格（上游优先），按规格做；**冲突本身必须写进报告**并在实现处注明依据，**不许静默按计划/顺手口径做**（坑 17）（2026-09-14 定）。
- **"命令没跑"不等于"命令通过"**：管道 / 子命令失败时，外层 `&&` 链会停在失败点，若失败段恰在最末，前面的 OK 极易被读成整体通过 —— 验收必须看到**该命令自己的结果输出**（如 jest 的 `Tests: N passed`），不看"到没报错"（坑 18）（2026-09-14 定）。
- **文档联动不是"额外动作"，是执行令的一部分**（→ §一 F）：凡改口径 / 接口 / 表结构，**同批**把上游文档、下游指针（README / CODEBUDDY.md / 代码注释里的版本号）、生成物（`gen:types`）一起改掉，**不另行请示**；只有"写新口径"才需要先拿指令（2026-09-14 定）。
- **批量改文件的验收与改代码同等严格**：`tsc / lint / test` 全绿**证明不了** md / 注释没被写坏（坑 19 实测）—— 批量文本改动必须配**"改动行数上限 + 损坏签名 grep + 试跑单文件"**这三板斧，且**首选定点工具（编辑器级替换），而非正则脚本**（2026-09-15 定）。
- **命令输出只让它走一路**（2026-09-15 实测）：`npm` / `jest` 的输出在 `stdout` 与 `stderr` **几乎逐字重复**，两头都读＝**白烧一倍上下文**（本窗口 4 次全量测试无一例外）。一律 `2>&1 | Select-Object -Last N` 并**只取一处**；能合并的校验命令**合并成一条**。★ 这属**读的纪律**，与"少读规则"是两回事 —— **该读的规则不省，重复的输出必须省**（→ `CODEBUDDY.md §11`）。
- **"清单"要先判是不是穷尽**（2026-09-15 立 · 当日通盘审计）：规格里的**括号列举 / 「如…」/「A、B、C」默认是举例**；只有写了「**完整清单**」「**一律**」，或需求 §十六 表头那种"表里没有才代表允许"才算**穷尽**。
  - **不许**由"清单外"推出「**不做**」（漏做：§7.4 敏感动作 → B/C 域"不写审计"）；
  - **也不许**推出「**可以做**」（放任：管理员"只读"举例 → 放行建档）。
  - ★ **两种误读方向相反、后果都是错**，且**都发生过**：本项目已翻车 **5 次**（+ 数据架构 A6 `perm_key`、接口 §2.6 `urgency` / `stage` 举例 →《废止口径登记表》#31 / #36 / #37）。读法铁律钉在**需求 §1.3**（读者会打开的那一页）。
- **给方案 / 选型建议，必须附「长期价值评估」**（2026-09-21 七叔定 · 坑 39）：多方案先排**长期价值序**、明确标出哪条是"图省事的技术债"（后期返工成本），**不默认推当前最快的那个**。★ 推荐即负责——把"这次改动长期值不值"讲清，不能把债甩给后期；与"待拍板四要素"配合（建议栏须含长期价值对比）。

---

## 四、引用与维护

- **被引**：`CODEBUDDY.md`（顶部"动工前必读"＋导航速查）。
- **维护**：新增同类规则直接追加；改动**必须随 git 提交**（沿革唯一落点＝ `git log`）。
