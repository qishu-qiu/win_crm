# AI 协作铁律与踩坑复盘 V1.8

> **定位（★ 规范级 · 唯一落点）**：本文是「**AI 协作元规则**」的唯一权威落点 —— 只管「**AI 该怎么干活、别重复踩什么坑**」，**不定义任何业务规则**。
>
> **★ 维护铁律（2026-09-12 七叔立）**：今后**无论来自 WorkBuddy 还是 CodeBuddy**，凡新增的同类元规则（AI 行为约束 / 踩坑教训 / 协作通例），**一律追加到本文**，不另开文档、不复制到别处。
>
> **业务口径不进来**：业务规则 / 表字段 / 接口 / 页面 / 状态一律以五份现行规格与 `.workbuddy/memory/MEMORY.md` 为准，本文只留指针。
>
> **与 README 的分界**：`README.md` §三 / §八 / §九 / §十 管「**文档治理**」（Git 约定、L0 / L1 联动、归档规则、权威落点总表）；本文管「**AI 行为与经验**」。两者重叠处**只写指针、不复述**。

---

## 一、AI 行为硬约束（★ 最高优先级 · 2026-09-12 七叔拍板）

> 本节约束 AI 自身行为，**优先级高于一切"默认推进"惯性**。七叔已多次重申"要跟我确认"，且曾因 AI 擅自推进被当场叫停。

- **A 明文执行令**：凡 **装依赖 / 写文件 / 跑命令 / 调用有副作用工具**，必须收到七叔**当轮明文执行令**（如"装 X""走 Y""写进去"）。**严禁**从"我建议 X"自行推导成"按 X 做" —— 建议与执行令之间必须有七叔的明确指令桥接。
- **B 短回复不外推**：七叔回"继续 / 采纳 / 好的 / 等"等短词，**仅对最近一个待办有效**，绝不外推到其他未决议题；多议题**逐条单独确认**，不合并。
- **C 动手前先发「准备做」**：跨界执行前先发"我准备做：___。回『执行』我才动"，未回『执行』**原地停住**。
- **D 回退机制**：七叔一喊"等一下 / 我没说 / 谁确认的 / 谁给你确认的"→ **立即回退到等待指令态、承认擅自推进、不辩解往前蹭**。
- **根因备忘**：2026-09-12 曾把"第 2 个顺手写进正文"误读为对"装哪套 skill"的整体批准，并自写"收到走 X" —— 属**范围偷换 ＋ 建议变决定**双重错误。**此错不得重演。**
- **E 提交前逐文件认领（2026-09-12 新增 · 多窗口并行）**：**禁用 `git add -A` / `git add .` / `git commit -a`**。提交前必跑 `git status --short` 逐条认领，**只显式 `git add <本轮自己动过的具体路径>`**。工作树里若有**另一个窗口正在改、尚未改完**的文件（尤其 `服务端/`），**一律不碰、不提交，原地报告**（"这两处不是我这轮改的，要一起提交吗？"），等七叔指令。
- **E 根因备忘**：2026-09-12 一次 docs 提交用 `git add -A`，把另一窗口**改到一半**的 `服务端/prisma/migrations/0001_init/migration.sql`（补 `ENGINE=InnoDB`）与 `服务端/.env.example`（新文件）一并带进 `ffd899e`。七叔未撤回，但**"别人的半成品挂在别人的提交名下"此错不得重演**。
- **F L0 联动的执行边界（2026-09-14 新增 · 七叔问"每次还要我发指令吗"）**：**执行令指向的动作若改变了口径 / 接口 / 表结构，则"同批的文档联动 ＋ 版本指针同步"是该执行令的**必要子项，不必每次另行确认**。例：动 `DataScopeType` 就要同批改《架构设计说明》§7.2 ＋ README 指针 ＋ 代码注释里的版本号；改接口入参就要同批改《接口API文档》并重跑 `gen:types`。
  - **唯一的边界（仍须单独拿令）**：**新增 / 修改业务口径本身**（哪怕一行）—— 联动的对象只能是上游**已拍板**的内容，**不许借"联动"之名把自造口径写成既成事实**。
  - **不做会怎样（风险，这才是"要不要每次问"的答案）**：① **版本指针漂移** → 下个窗口（或 AI）照旧版口径实现（坑 #1 / #6 的复现路径，本轮实测：`src/` 里 21 处仍写《架构说明》V1.1、16 处写接口 V1.12）；② **双真相源** → 上游改了、下游没跟，读者"命中哪处照哪处写"（坑 #9）；③ **沿革断档** → 改文档不提交＝没还原点，事后说不清哪版生效（README §八）。

---

## 二、踩坑复盘（本项目已发生，勿重演）

| # | 坑 | 后果 | 已立的防线 |
| --- | --- | --- | --- |
| 1 | **AI 按关键词命中**：旧口径散落正文，命中哪处就照哪处写 | 同一文档自相矛盾（`1062` vs `P2002`）、15 处正文没跟上拍板 | 正文为准 ＋「索引·非规范」标注 ＋《废止口径登记表》 |
| 2 | **索引被当规范**（速查表 / 接口总目录 / 追溯索引 / 文档地图） | 漏掉正文里的完整条件 | 索引类必须显式标「索引·非规范」 |
| 3 | **归档目录检索污染**（旧文档近乎同名） | grep 到作废口径照做 | `.ignore` 机械隔离 `归档/`；归档件加"⚠已作废"头标 |
| 4 | **负面约束只收一半** | AI 以为"表里没有＝允许"，自行补全被封路功能 | 需求 §十六 收全为**完整清单**，"表里没有"才代表允许 |
| 5 | **机械扫描只扫 `.md`** | 漏掉 `schema.prisma`、`prisma/README.md`、过程 HTML 的旧版本引用 | 扫描覆盖全部文本类型（`.md / .html / .prisma / .sql / .json`） |
| 6 | **版本沿革写进正文** | 正文漂着旧版本号，改一处漏一处 | 沿革只留 `git`，文档内删「修改记录」 |
| 7 | **派生对外文档没同步** | 给老板的摘要说出已推翻口径 | L0 联动要求**同批、同次提交** |
| 8 | **AI「缺一块就自行补全」** | 实现被封路的功能 | 动手前先查需求 §十六；"表里没有"才代表允许 |
| 9 | **同事实多处落点＝双真相源** | 改一处漏两处 | L1 单一事实源；非权威位置**只留指针** |
| 10 | **`git add -A` 扫进别人未完成的改动**（多窗口并行：工作树 ≠ 本轮改动范围） | 另一窗口改到一半的服务端文件被挂进 docs 提交 `ffd899e` | 提交前 `git status --short` 逐文件认领；**只 add 本轮明确路径**；他人未完成改动原地报告等指令 |
| 11 | **`@/` 路径别名只活在编译期**：`tsconfig.paths` ＋ jest `moduleNameMapper` 能骗过 `tsc --noEmit` 与单测，但 **`tsc` 不会重写产物里的路径** | 源码里写 `@/kernel` 的进程，本地三绿全过、CI 也过，却**到 `node dist/main.js` 才报 Cannot find module** | **src 内一律相对路径**（如 `../../kernel`），`@/` 只留给 spec；口径已写死在 `服务端/src/kernel/index.ts` 顶部注释 |
| 12 | **`nest build` 默认读 `tsconfig.json`**，不会自己排除测试文件 | `dist/` 里混进 10 个 `*.spec.js`：发行产物不卫生，测试代码被带上生产 | 新增 `服务端/tsconfig.build.json`：`exclude` 全部 `*.spec.ts` ＋ 独立 `tsBuildInfoFile`（避免与 `tsc --noEmit` 抢同一个增量文件）；验收＝dist 里 spec 数 **0** |
| 13 | **Jest 载不动 NestJS 12**（`@nestjs/common@12.0.1` 是 `"type": "module"` ESM-only） | 凡含 `@Injectable` 的用例一律 `Must use import to load ES Module` —— 守卫 / 拦截器 / 服务**全都写不了测试** | `test` 脚本改用 `node --experimental-vm-modules node_modules/jest/bin/jest.js`；**运行时不受影响**（Node 24 原生 `require(ESM)`，已实测能取到 `Module` / `Injectable` / `NestFactory`） |
| 14 | **`@nestjs/testing` 没装**（M0-02 只装了 Jest，没装它） | 用例里 import 不到 → `tsc` 直接 TS2307；顺手一装就变成「未拿执行令装依赖」 | 单测要 DI 容器时用 `@nestjs/core` 的 `NestFactory.createApplicationContext`（**零新依赖**，且比测试模块更接近真实装配路径）；真要装 `@nestjs/testing` 须先拿执行令 |
| 15 | **拿 Proxy 做「库访问绊线」放进 DI 容器**（想证「没查库」） | Nest 的 `callModuleInitHook` 会对容器里**每个 provider** 读 `onModuleInit` 等生命周期钩子 → 绊线被框架自省误伤而抛，用例**假红**，跟业务有没有查库毫无关系 | 「不碰库」改用两条硬证：① 容器里**根本不放**库客户端也能装配成功（若注入 Prisma，Nest 此刻报依赖解析失败）；② 断言类上**构造依赖个数**（多挂一个依赖立刻红） |
| 16 | **测试假件比真实形状少字段** | 用例假红：守卫读元数据时 `executionContext.getHandler is not a function`，看着像守卫有 bug，实际是假件不是真形状 | 假件按**真实上下文形状**造：`getType` / `getHandler` / `getClass` / `switchToHttp` 一个不少；**假红先修假件，再怀疑被测代码** |
| 17 | **计划书判据与规格冲突**：开发计划 M0-35 写「坏 DTO → **422**」，而接口 API §2.4 **第一行**把「字段缺失 / 类型错 / 枚举非法」定为 **400 / 20001**（422＋204xx 是**业务校验**专用，如「完成预约未留跟单」） | 照计划写会把「前端传错类型」错记成业务校验失败；前端按 422 分支处理则**全线接错** | 按 `CONSTRAINTS` 首行「与规格冲突时以规格为准」取 **400 / 20001**；冲突**写进批次报告＋实现文件顶部注释**，不静默按计划做；计划书口径已按此对齐（开发计划 M0-35 / M1-14） |
| 18 | **按 bash 语法写命令，但本机 shell 是 PowerShell**（例：`npm test 2>&1 \| tail -n 25`） | `tail` 不存在 → 该段整条失败；而它挂在 `&&` 链**最末**，前面 `tsc` / `lint` 已打印 OK → 极易误读成"三绿全过"，实际 **`npm test` 根本没跑** | 取尾用 PowerShell 原生的 `... \| Select-Object -Last N`；**命令语法按当日 shell 写**（本机＝PowerShell，非 bash）；验收必须看到**该命令自己的结果输出**（如 jest 的 `Tests: N passed`），不能只看"没报错" |
| 19 | **用 PowerShell `-replace`（正则）批量改中文文档**（本次是改版本指针，模式写成 `《销售CRM接口API文档》\*{0,2}V1\.13\*{0,2}`） | **37 个文件被写坏**：`《` 被替换成 `销`（如 `《销售` → `销销售`）、`**` 被吃掉（`V1.14**（`），业务需求文档受损 **523 行** —— 而 `tsc` / `lint` / `test` **全绿**（坏的是注释与 md，测试看不见），差点直接提交 | ① 改中文文本**一律字面 `.Replace()`**（或编辑器替换），**禁用 `\*{0,2}` 这类可选/贪婪模式**；② **先在一个文件上试跑**、`git diff --numstat` 确认只有 1~3 行变化，再全量；③ 全量后必须跑**三条校验**：损坏签名 `grep`（如 `销销售` / `务务`）、`git diff --numstat` 单文件行数上限、剩余旧值 `grep`；④ 一旦怀疑写坏，**`git checkout -- <paths>` 整体回退重做**，不要在坏文件上二次修补（修补只会叠错） |

| 20 | **出参 DTO 的 `X | null` 字段没写 `type`**（例：`@ApiProperty({ description: '城市' }) city!: string | null;`） | Swagger 解析不了「联合类型」→ 该字段在 OpenAPI 里生成成**空对象 `{}`** → `openapi-typescript` 产出 `Record<string, never>` → **前端类型等于不可用**（`.id` 一写就 `TS2322`）。⚠ 后端 `tsc` / `lint` / `test` **全绿**（后端自己根本不读 OpenAPI），只在**前端 `vue-tsc` 用了该字段时**才炸 —— 若前端还没写这页，这个 bug 会一路潜伏到联调 | ① **可空字段一律写全**：`@ApiProperty({ type: String, nullable: true, description: … })`（`Boolean` / 数字同理）；② 改完出参 DTO **必须重跑 `gen:types`**，并 grep 前端生成物 `Record<string, never>` —— **除 `webhooks` / `$defs` 两处样板外应为 0**；③ 把「重跑生成的类型 diff」当作接口变更的证据留档（M0-57 同源要求） |

| 21 | **单测假对象比真实阶段"更完整"**（例：M3 激活＝事务内「插关系 → 插 owner 成员」，而插关系那次 `RETURNING` 发生在**成员写入之前**，它的 `members` 必然是空的；假对象却返回了带成员的整行） | 服务端拿**入库前**那份行去装配跨域引用 → 真库出参 `owner` 是空的；**单测全绿**（假对象"好心"补上了成员，把 bug 掩盖掉）。假对象越"完整"，越测不出真链路 | 假对象必须**复刻真实阶段**（哪一次 SELECT 发生在哪一步之后）；**改完必须真库跑一遍出参**（本次是真库探针逮到的）。延伸：`pickRefs`/装配类 bug 不看真数据出参就发现不了，单测只验证**你自己写的逻辑**，验证不了**框架的返回时机** |
| 22 | **用 `npx jest <pattern>` 跑单测**（例：`npx jest relation`） | 绕过 `npm test` 里的 `NODE_OPTIONS=--experimental-vm-modules` → 套件直接 **"Must use import to load ES Module: @nestjs/common"** 加载失败。极易误读成"**我刚写的代码坏了**"，然后去改根本没问题的源码 | 跑单测**只用 `npm test`**（或原样带上项目脚本里的环境变量）；看到 ESM 加载类报错先怀疑**调用方式**，再怀疑代码 |

| 23 | **把模糊规格「解读得比原意更严」**（本次：数据架构 C1 只说「非灰度必须**已标** `value_tier`」，实现却加码成「必须落在已定档三值内，`pending` 不算」） | 被七叔拍板纠正：「**只要标了就行**」。**过度解读与漏做同样是偏离规格**，而且它更隐蔽——单测还会把这条自造的严格规则一起钉死；实际后果是逼出一堆随手点的**假档位**（数据质量反而更差） | 规格用词模糊时，**按最直接的字面读法实现**（「已标」＝有值），把「要不要更严」当作**待确认项写进报告**，而不是先按更严的做。★ 判据：**你实现的那条"额外限制"，规格原文里有没有一句话能逐字支持？** 没有就报，不要做 |
| 24 | **上游规格自身自相矛盾**（同一份文档内 / 跨文档，本次两条：① 阶段 `stage`＝接口 §2.6 英文码 vs 需求 §8.1 ＋ 数据架构 ＋ 接口 §5.6 的数字 1~7；② `product_line.color_key`＝需求 §13.3 ＋ 接口要求返回，而数据架构 A7 表定义没有该列） | 静默挑一个当"规格"＝**替七叔做决定**，且下个窗口会照着你挑的那套继续盖楼；而两套并存时**前端筛选 / 前端展示 / 字典种子会各认一套**（→ 废止口径 #36 的教训） | ① 按**多处一致 ＋ 业务真相源（需求）＋ DB 设计一致性**取一个实现，但**必须书面登记冲突**（交接说明 §五 ＋ 报告点名）；② 若能判断是「文档漏落一列 / 漏写字段」，**补齐文档与库并升版**（本次 `color_key` 就是这么收口的：补 migration ＋ 数据架构 A7 ＋ 指针同步）；③ 若涉及**契约词汇**（如 stage 码），**只报不改**，等拍板 |

> **三条最核心**：① 先读《废止口径登记表》＋需求 §十六，再动手；② 改文档＝必须 git 提交，四份同批同次；③ 不要自行补全"缺的一块"，先查是否被封路。

---

## 三、通例（由上述坑提炼，适用于所有文档与协作）

- **"防 AI 误读"的自检，必须出现在 AI 真会读到的那一页**（表头 / 章节首行），**不能只写在章程** —— 因为 AI 读表时不会回头翻章程（2026-09-12 定）。
- **规则本体放"读者会直接打开的那份文件"里**，上级文档只留**指针与动作项**。
- **元规则（本文这类）的落点唯一**：新增同类规则一律追加进本文（见首页维护铁律）。
- **同一事实只允许一个权威落点**；非权威位置必须显式标注定位（如「索引·非规范」），否则会被 AI 当成规范。
- **"三绿"不等于"能跑"**：`tsc` / `lint` / `test` 全过只证明**编译期与测试期**成立。凡涉及**模块解析、依赖加载格式、打包产物**的改动，必须**真起一次进程**（`node dist/*.js`）才算验完 —— 坑 11 / 12 / 13 都是"三绿全过但跑不起来或产物不干净"（2026-09-14 定）。
- **否定式约束（"不许查库 / 不许越权 / 不许依赖某层"）要用「无法表达」来证，不要用「跑一遍没发生」来证**：前者是结构事实（依赖没注入 → 不可能调用），后者只覆盖一次执行、还容易被框架自身行为干扰（坑 15）（2026-09-14 定）。
- **判据与规格冲突时**：先比规格（上游优先），按规格做；**冲突本身必须写进报告**并在实现处注明依据，**不许静默按计划/顺手口径做**（坑 17）（2026-09-14 定）。
- **"命令没跑"不等于"命令通过"**：管道 / 子命令失败时，外层 `&&` 链会停在失败点，若失败段恰在最末，前面的 OK 极易被读成整体通过 —— 验收必须看到**该命令自己的结果输出**（如 jest 的 `Tests: N passed`），不看"到没报错"（坑 18）（2026-09-14 定）。
- **文档联动不是"额外动作"，是执行令的一部分**（→ §一 F）：凡改口径 / 接口 / 表结构，**同批**把上游文档、下游指针（README / CODEBUDDY.md / 代码注释里的版本号）、生成物（`gen:types`）一起改掉，**不另行请示**；只有"写新口径"才需要先拿指令（2026-09-14 定）。
- **批量改文件的验收与改代码同等严格**：`tsc / lint / test` 全绿**证明不了** md / 注释没被写坏（坑 19 实测）—— 批量文本改动必须配**"改动行数上限 + 损坏签名 grep + 试跑单文件"**这三板斧，且**首选定点工具（编辑器级替换），而非正则脚本**（2026-09-15 定）。

---

## 四、引用与维护

- **被引**：`CODEBUDDY.md`（顶部"动工前必读"＋导航速查）；建议 `.workbuddy/memory/MEMORY.md` 同步改为一行指针（由 WorkBuddy 侧维护）。
- **维护**：新增同类规则直接追加；改动**必须随 git 提交**（沿革唯一落点＝ `git log`）。
