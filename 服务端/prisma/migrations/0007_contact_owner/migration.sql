-- =============================================================================
-- 0007_contact_owner（2026-09-16）
--
-- 口径来源（★ 真相源）：《销售CRM业务需求文档》§6.1 ⑦~⑪（2026-09-16 七叔定）
--   —— 给「待关联（未挂公司）联系人」一个**明确的归属落点**：
--   · 归属＝**建档时的录入人**；
--   · 依据：「先只有手机号、后来才问到公司」是常态（实测口径约 **30%**），
--     这批人不是边角数据，必须能被筛（"我的待关联"）、被催（7 天补全）、被改（分派 / 交接）；
--   · 已挂公司的联系人不看本列（归属走 `business_relation` 的 owner 成员）；
--   · 可空、无外键（与 `created_by` 等审计列口径一致）；
--   · 归属**可改**（经理分派 / 离职交接）**不走审批、只留痕**；
--   · 未关联公司期间**不占部门、不占产品线**（部门 × 产品线是**关系**的属性，不是人的属性）。
--   同步：《销售CRM数据架构文档》B3 已加本列说明。
--
-- ★ 列位置 `AFTER phone_locked_by`（紧跟其它业务列，审计列仍在末尾）：仅为可读性，不影响功能。
-- ★ `idx_owner` 供「我的待关联」列表按归属过滤（→ 需求 §6.1 ⑪；列表收敛的剩余部分见欠账 D-28）。
--
-- ⚠ `0001_init` 是**冻结基线**（已 `resolve --applied`），改动它会与 `_prisma_migrations`
--   的校验和不一致 ⇒ 一律走**新增量**（口径见 `服务端/prisma/README.md §六` / `CODEBUDDY.md §5`）。
--
-- 性质：**新增可空列 ＋ 普通索引**，不动数据、不动既有约束（重复执行会报「列已存在」，符合增量迁移惯例）。
-- =============================================================================

ALTER TABLE `contact`
  ADD COLUMN `owner_id` BIGINT UNSIGNED NULL
    COMMENT '待关联（未挂公司）联系人的归属人（建档=录入人）；已挂公司者不看本列（归属走 relation_member 的 owner 成员）★ 2026-09-16 migration 0007',
  ADD INDEX `idx_owner` (`owner_id`);
