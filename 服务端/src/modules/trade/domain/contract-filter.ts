// =============================================================================
// E 域合同列表筛选条件（纯数据，不引 Prisma；翻译在 repository）
//
// 口径来源（★ 真相源，勿自造）：
//   · 《销售CRM接口API文档》§4.8 / §5.9：状态筛选 ＋ 关键字（合同号 / 公司名）
// =============================================================================

/** 列表筛选（service 组装、repository 翻译成 Prisma `where`） */
export interface ContractFilter {
  /** 状态精确匹配（可空） */
  status?: string;
  /** 关键字：合同号 / 公司名模糊（可空） */
  keyword?: string;
}
