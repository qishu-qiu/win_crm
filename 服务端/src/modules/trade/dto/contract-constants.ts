// =============================================================================
// E 域合同 DTO 常量（从状态码派生，避免与 domain 互相 import 形成环）
// =============================================================================
export const CONTRACT_STATUSES_DTO = ['unpaid', 'partial', 'running', 'done', 'terminated'] as const;
