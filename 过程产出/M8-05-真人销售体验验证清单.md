# M8-05 真人销售体验验证清单

> 配套《M8-05-反面用例-CLI试用.ps1》（CLI 已跑通，PASS=15 / FAIL=0 / SKIP=2 / FIND=2）。
> 本文件只列**CLI 测不了、必须真人/前端验证**的部分，以及 CLI 跑出的待确认问题。
> 系统真相源仍是各需求/接口/前端规范文档；本清单是"交销售试用前最后一次人眼验收"的 checklist。

---

## 〇、当前可用环境

- 后端：已在 `3000` 监听（当前 dist，pid 见 `服务端/backend.pid`）。无 `/api` 前缀，登录 `POST /account/login`。
- 前端：需另开。在 `前端/` 执行 `npm run dev`（5173，`/api` 代理到 3000）。
- 试用账号（密码统一 `Dev@123456`）：

  | 角色 | 账号(手机) | 工号 | 数据范围 |
  | --- | --- | --- | --- |
  | 销售 | 13800000003 | E003 王海涛 | 本人 ∪ 协同 ∪ 公海（部门2 销售一部） |
  | 部门经理 | 13800000002 | E002 刘敏 | 管辖部门2/3 |
  | 总经理 | 13800000001 | E001 陈志远 | all（含金额不脱敏、查看写日志） |
  | 管理员 | 13800000006 | E006 周静 | 只读（看板/汇总/导出），不进公海 |
  | 客服 | 13800000004 | E004 赵晓雯 | serving（合同服务期内，只读） |
  | 交付 | 13800000005 | E005 孙立国 | serving |

---

## 一、CLI 反面用例结论（已自动跑通，供对照）

| 标记 | 场景 | 结果 |
| --- | --- | --- |
| P3–P7 | 建档公司/联系人/激活关系/写跟单/建承诺（正面动线） | ✅ 全过 |
| N01/N17 | 未带 token 读/写 | ✅ 401 / 20002 |
| N02 | 错误密码 | ✅ 401 / 20002（统一包人话"手机号或密码不正确"） |
| N03 | 销售跨部门建关系(dept=3) | ✅ 403 / 20003 |
| N05 | 重复激活同三元组 | ✅ 409 / 20401 |
| N09 | 有效跟单 summary 为空 | ✅ 400 / 20001 |
| N10 | 同幂等键异入参写跟单 | ✅ 409 / 20004（D-06 撞键生效） |
| N11 | 承诺 waived 不填原因 | ✅ 422 / 20403 |
| N13 | order_by 非法列 | ✅ 400 / 20001（白名单生效） |
| N08 | 对已关联公司的联系人写跟单 | ⚠️ **200（应 403）见下"待确认问题 ①"** |
| N04 | 部门未承接线 422 | ⚠️ 当前种子无法触发（见③） |
| N07/N12 | 公海写跟单 / 管理员领公海 | ⚠️ 跳过（库内无公海关系，见②） |

> 重跑：`powershell -NoProfile -ExecutionPolicy Bypass -File 过程产出\M8-05-反面用例-CLI试用.ps1`
> （脚本结尾会自动清理产生的 `M8NEG*` 测试数据；万一本机 `mysql.exe` 经 PowerShell 调用异常，按文末"清理兜底"手动清。）

---

## 二、CLI 跑出的待确认问题（交给销售前必须拍板）

### ① 【疑似 bug】已关联公司的联系人仍可被写跟单（§5.7 的 403 未生效）
- **现象（CLI N08）**：用 `POST /contacts` 带 `company_id` 建档的联系人，再 `POST /contacts/:id/events` 返回 **200**，未按接口 §5.7 返回 `403 "请到业务关系里记"`。
- **根因（已定位）**：`服务端/src/modules/company/company.service.ts` 的 `createContact`（约 L405）**无条件** `owner_id: operatorId`，即便 `company_id` 已传入（已关联公司）。而 `engine.service.ts` 的"已挂公司 403"依赖"已关联联系人的 `owner_id` 为 null"（需求 §6.1 ⑦：归属转给业务关系）。于是"本人建的已挂公司联系人"仍带 `owner_id`，§5.7 的拦截对它不生效。
- **影响**：销售可绕过"请到业务关系里记"，在"已关联公司"的联系人上继续记跟单——破坏"联系人 ＝ 待关联线索、业务关系 ＝ 跟单主战场"的模型边界。
- **建议修法**：`createContact` 中，当 `dto.company_id` 存在时把 `owner_id` 置为 `null`（与"激活业务关系"动线的口径一致）。改动前需 `grep` 全部读取 `contact.owner_id` 的地方（已发现 `getContactOwners`、可见性判定），确认无其它假设被打破。
- **真人复现**：前端"新建联系人"时若允许直接选"关联公司"，建完后在详情页"写跟进"应被拦截；当前不会拦，肉眼可见。

### ② 【覆盖缺口】库内无公海关系 → N07/N12 未验
- 当前 `win_crm` 没有"掉海/未领取"的关系，故"未领取公海写跟单(422/20408)"与"管理员领公海(403/20003)"无法自动验。
- **构造数据**：让某销售把一条私海关系**释放到部门公海**（经理动作 / 或到期未跟进自动掉海，若已实现），再用 CLI/前端复测 N07、N12。
- **真人验证**：见第三节"公海"步骤。

### ③ 【覆盖缺口】所有产品线均承接部门2/3 → D-74 的 422 无法触发
- 种子 7 条产品线 `dept_ids` 均为 `[2,3]`，所以"销售建关系选部门未承接的产品线"永远构不出（N04 实测返回 200）。
- **处理**：要么补一条"部门受限"的产品线种子，要么接受该分支只能靠单测覆盖。不影响交销售试用（销售本就不会选到不能选的线——前端只给可选线）。

---

## 三、必须真人销售体验的部分（CLI 测不了）

> 以下全部需要浏览器 + 真人操作。建议让**真实销售**（非开发）照做，记录卡点。

### 启动
1. 后端已在 3000（当前 dist）。若不在：`cd 服务端 && npm run build && node dist/main.js`。
2. 前端：`cd 前端 && npm run dev` → 打开 `http://127.0.0.1:5173`。

### A. 销售主流程走查（账号 13800000003）
1. 登录 → 落地页是否清晰（D-14：导航/待办/快捷入口是否一眼能懂）。
2. 建档公司：填名称→撞重预检提示是否人话；强行新建是否放行（需求 §12.1 分支4）。
3. 建档联系人：只填主号→自动归一；撞号提示（409 人话）是否友好。
4. 激活业务关系：选"我的部门 + 可承接产品线"→ 成功；**故意选别的部门**应被前端拦（对应 N03 的 403，验前端是否先把不可选部门/线灰掉）。
5. 写跟单：填"有效沟通"但漏 summary → 前端是否拦截（对应 N09）；正常提交后时间线是否展示。
6. 建承诺：选"豁免"但不填原因 → 前端应拦（对应 N11）。

### B. 数据范围与脱敏（对照 §8）
7. 销售视角列表/卡片：手机号应为 `phone_masked`（掩码）；详情页给全号（D-6/D-8）。
8. 经理（13800000002）登录：能否看到管辖部门2/3 的关系；能否看到销售看不到的。
9. 总经理（13800000001）：金额是否**不脱敏**（报表/看板/汇总出口不脱敏，§8）。
10. owner 上锁的联系人：非 owner 应见 `phone_locked`（§8 联系人锁定）。

### C. 公海（需先有公海数据，见②）
11. 销售在"部门公海"领取一条 → 变成私海（对应 CLI 未验的领取成功路径）。
12. 未领取就写跟单 → 应 422（N07）。
13. 管理员（13800000006）尝试领公海 → 应被拦（N12，只读角色）。

### D. 列表/看板体验（D-12/D-13/D-10）
14. 关系列表分页、筛选、排序是否流畅；`order_by` 是否只给白名单列（N13 已证后端拦非法列）。
15. 看板/汇总：金额不脱敏（§8）；数据是否实时。

### E. 异常与提示
16. 会话过期（清 token）→ 前端是否跳登录、提示人话（对应 N01/N02 的 401/20002 人话包）。
17. 表单校验错误是否就近提示、文案是否非技术（对应 N09/N11/N13 的 20001/20403）。

---

## 四、清理兜底（如 CLI 自动清理未生效）
本机 `mysql.exe` 经 PowerShell `&`/管道调用会解析失败（phpStudy 构建怪癖）。若发现库里残留 `M8NEG*` 数据，二选一：
- 在 phpStudy 的 MySQL 控制台执行：
  ```sql
  DELETE FROM business_relation WHERE company_id IN (SELECT id FROM company WHERE full_name LIKE 'M8NEG%');
  DELETE FROM contact WHERE name LIKE 'M8NEG%';
  DELETE FROM company WHERE full_name LIKE 'M8NEG%';
  ```
- 或把清理 SQL 写到无中文路径（如 `D:\_cleanup.sql`）后：`cmd /c "D:\ITtool\phpstudy_pro\Extensions\MySQL8.0.12\bin\mysql.exe -h127.0.0.1 -uroot -p123456 win_crm < D:\_cleanup.sql"`。

---

## 五、验收结论模板（销售试用回填）
| 项 | 结论 | 备注 |
| --- | --- | --- |
| CLI 反面用例 | 通过（除 N08 待修） | |
| ① 已挂公司联系人 403 | ☐ 已修 / ☐ 已知晓暂不修 | |
| 公海两条（N07/N12） | ☐ 已构造并验证 / ☐ 跳过 | |
| 前端主流程 | ☐ 顺畅 / ☐ 卡点：____ | |
| 脱敏/数据范围 | ☐ 符合 / ☐ 偏差：____ | |
| 看板/汇总不脱敏 | ☐ 符合 / ☐ 偏差：____ | |
