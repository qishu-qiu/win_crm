# M1 交接说明（2026-09-14 窗口 · 下个窗口必读）

> **本文件是「下个窗口开工前要交代的事」的唯一落点。**
> 读完本文件 ＋ `技术决策/AI协作铁律与踩坑复盘.md`（**V1.7**，AI 行为铁律）＋ `过程产出/开发计划-V1.md`（M1~M3 判据原文）即可开工，不必重啃本窗口对话。
> ⚠ 本文件不复述业务口径（口径以五份现行规格为准），只记**状态、证据、缺口、踩坑**。

---

## 一、当前状态一句话

**M1（A 域：登录/刷新/me ＋ 四个组织只读接口 ＋ 登录页）已全部落地并提交**（`edf46a2`）；登录＝手机号/账号名双通道、数据范围＝四档、`/org/employees` 已按 G7 收敛、**成功响应一律 200**（接口文档已升 **V1.15**）。
**M2（B 域建档/查重 ＋ 前端建档页）已闭环**：M2-01~M2-17 全绿（`07bbb0e`）。
**M3（C 域：业务关系 / 私海 / 公海）已闭环 —— 后端 ＋ 前端列表页**：M3-01~M3-14 全绿（**34 suites · 472 tests**）＋ **真库实测**（激活 / 撞单 **409 / 20401** / 越权 403 / 别部门各自一条 / 私海列表三种范围 / 公海按部门收敛且 `owner=null` / 详情含成员 / 非灰度未标价值 **422 / 20403** / 第二 owner **409** / @求助跨部门 403 ＋ 同部门默认 7 天 / 掉海释放活跃位后同键可再激活 / 列表页经 vite 代理与 curl 一致）。
**下一步＝M4（D 域最小切片：跟单事件 / 承诺 / 工作台）**；M2 / M3 遗留的小口子见 §五（列表分页、`GET /companies/:id` 未做、`product_line.color_key` 无数据源、关系详情未返回的跨域字段等）。

## 二、M1 收尾批次做了什么（含实测证据）

### 2.1 三绿
- `npx tsc --noEmit` **0 error** ｜ `npm run lint` **0 error** ｜ `npm test` → **25 suites · 290 tests 全绿**
- `npm run build`（后端）**dist 内 `*.spec.js` = 0**；前端 `npm run build`（`vue-tsc` ＋ vite）**0 error**

### 2.2 真库真账号现场（后端 3000 / 前端 5173 都真起过）

| 探针 | 实测结果 |
| --- | --- |
| `POST /account/login`（手机号 `13800000003`） | `code=0`，`user.id="3"`、`username="wanghaitao"`、`role=sale`、`dept=2/销售一部` |
| `POST /account/login`（**账号名** `wanghaitao`） | `code=0` ✅（M1-03 判据的「账号名各验一次」） |
| 账号名**大写** `WANGHAITAO` | `code=0`（库 `utf8mb4_unicode_ci` 负责大小写，代码不做 `toLowerCase`） |
| 账号名不存在 / 密码错 | **401 / 20002**「手机号或密码不正确」（审计里 `channel` / `reason` 可分辨） |
| 旧入参 `{phone,...}` | **400 / 20001**「参数错误：account: …」 |
| `GET /account/me` | 出参含 `username`；`permissions` 三键（`contact_phone` / `relation_timeline` / `contract_amount`） |
| `GET /org/employees`（G7 收敛） | gm / admin → **6**；刘敏（`dept` 管辖 2,3）→ **2**；王海涛（`self` 部门 2）→ **2**；赵晓雯（`serving` 部门 4）→ **1**；孙立国（`serving` 部门 5）→ **1** |
| 经**前端 vite 代理**（`http://[::1]:5173/api/...`） | 登录 `code=0`、`me` `code=0`、密码错 **401** —— 前端到后端整条链路通 |
| 审计 | `operation_log` 实见 `account.login.fail` ＋ `detail.channel=username` ＋ `reason=username_not_found` |

### 2.3 本轮修掉的 5 处缺陷 / 欠账（**都是「文档改了、代码没跟」**）

1. **Prisma Client 是旧产物**：`schema.prisma` 已加 `employee.username` / `contact.phone_locked_*`，但 `src/generated/prisma` 还是旧的 → **一写 `select:{username:true}` 就编译报错**。已 `prisma generate`。
   - ⚠ **教训**：`src/generated/` 被 `.gitignore` 忽略，**`git status` 看不出它过期**；改 schema 后必须显式重跑（`npm run build` 会带跑 `prisma:generate`）。
2. **登录双通道未实现**（登记表 #32）：DTO 仍是 `phone` ＋ 严格手机号正则、仓储只有 `findEmployeeByPhone` → 账号名整条通道不可用。已改 `{account,password}` ＋ 新增 `domain/login-account.ts` 判别。
3. **`UserVO` 缺 `username`**（接口 §5.2 早已要求）→ 已补（空值给 `null`）。
4. **数据范围缺第 4 档 `serving`**（登记表 #35）→ `DataScopeType` 加档、`jwt-claims` 白名单同步（`satisfies` 钉成编译期事实）、《架构设计说明》§7.2 补齐并升 **V1.3**。
5. **`GET /org/employees` 未按 G7 收敛**（接口 §4.2）→ 新增 `domain/employee-visibility.ts`（`all` 不过滤 / `dept` 管辖部门 / `self`·`serving` 同部门；**拿不到上下文 401，绝不兜底全量**）。

### 2.3-bis 2026-09-15 补批（七叔指令：停服清理 ＋ 登录成功 200 ＋ 欠账补齐）

1. **停服清理**：后端 3000 / 前端 5173 已停；`.tmpboot3000.log/.err`、`.tmpvite.log/.err` 已删；工作树干净。
2. **误点回退修复**：`login.dto.ts` 曾于 2026-09-14 23:34:21 被外部回退成 HEAD 旧版（`{phone}`）——已按新口径重写并复验（`tsc 0 / lint 0 / 290 tests` ＋ 真机复测）。**全库标记体检未发现第二处残留**。
3. **成功响应状态码定 200**：`login` / `refresh` 显式 `@HttpCode(200)`（不写即 Nest 对 POST 的默认 201）；`org.controller.spec.ts` 加断言钉住；接口 §2.3 写明，接口文档 **V1.13 → V1.14**，全库 **35 处**版本指针同批同步（含 `src/main.ts` 的 `API_DOC_VERSION`）。**语义不同的 4 处历史引用未动**（README 的前端文档历史条目、自查报告快照、数据架构 §四/§五 的「（V1.13）」归属标注）。

### 2.4 前端（M1-18）
- 新增 `views/LoginView.vue`（**一个输入框＝手机号或账号名** ＋ 密码，不做选通道下拉；错误内联红字）、`views/WorkbenchView.vue`（首屏空壳，**不填假数据**，标题取 `home.ts` 的角色→首屏名）、`api/auth.ts`（类型全部取自 OpenAPI 生成物）。
- **修 `api/request.ts` 3 处猜错**：`/auth/refresh` → **`/account/refresh`**、`{refreshToken}` → **`{refresh_token}`**、`{accessToken}` → **`{access_token}`**。
- `index.html` 标题、`style.css` 换成最小全局样式（**删掉 Vite 脚手架的 hero / counter / 超大标题**，与设计规范 §3.2 冲突）；删除 `HelloWorld.vue` 与 `src/assets/` 模板资源。
- **未引 vue-router / pinia**（引依赖要单独拍板）：视图切换在 `App.vue` 用「有无登录人」一条状态完成。

## 三、种子数据（真库 `win_crm`）

**文件**：`服务端/prisma/seed/001_dev_seed.sql`（**幂等**，先清后插；只清 A 域 7 张表，**绝不碰 `operation_log`**；自带 Q1~Q4 自检）。

| 工号 | 姓名 | 手机 / 账号名 | 角色 | 主部门 | 数据范围 |
| --- | --- | --- | --- | --- | --- |
| E001 | 陈志远 | 13800000001 / chenzhiyuan | gm | 总公司(1) | `all` |
| E002 | 刘敏 | 13800000002 / liumin | dept_manager（管辖 2,3） | 销售一部(2) | `dept` |
| E003 | 王海涛 | 13800000003 / wanghaitao | **sale + delivery** | 销售一部(2) | `self`（多角色取更宽） |
| E004 | 赵晓雯 | 13800000004 / zhaoxiaowen | service | 客服部(4) | `serving` |
| E005 | 孙立国 | 13800000005 / sunliguo | delivery | 交付部(5) | `serving` |
| E006 | 周静 | 13800000006 / zhoujing | admin | 总公司(1) | `all` |

密码统一 `Dev@123456`。**字典 `dict_item` 仍是 0 条**（→ §五 #4，M2 前置）。

## 四、必须等七叔拍板 / 已知规格缺口（下个窗口开工前先看）

| # | 问题 | 现状 |
| --- | --- | --- |
| ~~1~~ | ~~`/account/refresh` 出参是否带 `user`~~ | ✅ **2026-09-14 定：不带**（要用户信息就调 `/account/me`） |
| ~~2~~ | ~~§4.1 与 §5.2 的 `UserVO` 冲突~~ | ✅ 已按接口 §5.2 实现（`dept` 对象 ＋ `username`） |
| ~~3~~ | ~~`permission_matrix.level` 无矩阵~~ | ✅ 已由种子升级为正式口径（`contact_phone` / `relation_timeline` / `contract_amount`），但**服务端仍无消费方**（M5 落地） |
| ~~5~~ | ~~登录成功的 HTTP 状态码~~ | ✅ **2026-09-15 定：成功一律 200**（接口 §2.3 已写明，接口文档升 **V1.15**）；实现＝`@HttpCode(200)` ＋ 单测钉住 |
| 4 | ~~字典未填~~ | ✅ **2026-09-15 已落库**：`prisma/seed/002_dict_seed.sql`（幂等）→ 真库实查 **18 个字典 / 101 项 / 全 `builtin=1`**，`urgency` ＝数据架构那套；跑第二遍无重复（items 101 ／ distinct 101）。⚠ **两道口子待确认**见 §五.11 与 §五.12 |
| 6 | **多角色数据范围合成** | ✅ **已写入规格（2026-09-15）**：需求 §4.2 新增「一人多角色」行（宽窄序 `all > dept > self > serving`）；架构 §7.2 同步 |
| 10 | ~~公司名标准化词表~~ | ✅ **2026-09-15 解决**：七叔授权由 AI 起草 → 词表落在 **`服务端/src/modules/company/domain/company-name-words.ts`**（**业务可编辑的数据，不是逻辑**；来源＝规格举例 ＋ 中文企业命名通行习惯，**非国标**）。改词表只需改该文件 + 跑 `company-name.spec.ts` |
| 11 | **`workflow_stage` 的码：`1`~`7` 还是英文码？** | ⚠ **两套并存待裁**：数据架构 §十三 写「1 初步建联 … 7 已流失」（且 `business_relation.stage` 是**数值列**）↔ 接口 §2.6 的 `stage` 举例是**英文码**（`first_contact / need_confirm / demo / objection / closing / cooperated / churned`）。字典暂按 **`1`~`7`** 落（结构证据更强），裁定后同步接口与字典 |
| 12 | **AI 起草的 6 个字典码** | ⚠ 规格只给中文、未给码：`company_identity_tag` / `company_policy_tag` / `decision_chain` / `contact_trait` / `relation_risk_label` / `review_win_reason` —— 现由 AI 按语义起草（全站要英文码），**业务确认后可改**；改前先 grep 引用（见 `prisma/README.md` 字典章节） |
| 13 | **列表未分页** | `GET /companies` / `GET /contacts` 现为「按 id 倒序取前 100」（M2 判据只要求列表可用）。统一分页 `page / page_size`（接口 §2.7）随 **M6 列表页**做 |
| 14 | **`GET /companies/:id`（公司详情）未做** | 接口 §5.4 有该端点（`/companies/:id` G/U），M2 只交付了列表 / 建档 / 查重 / 联系人；建档页「用现有公司」因此只带 `id ＋ 全称` 往下走（**不假装有字段**）。M6 建档页 / 公司抽屉要用时再补 |
| 15 | **跨域派生字段**（`relation_count` / `old_customer`） | 接口 §5.4 列表项里有，但分别属 C / E 域 —— **跨域禁止查对方的表**（架构 §5.2），等 M3/M4 的 service / 事件提供；**现在不填假值**。★ M3 起 `relation_count` **已可由 C 域提供**（`RelationService` 加一个按公司计数的方法即可），M6 公司列表页接入时补 |
| 16 | **`product_line.color_key` 无数据源** | 规格（§5.3 / §5.6 / §4.1）出参要求 `color_key`＝「7 线固定配色」，但 **`product_line` 表没有该列**、种子里没有、`/org/product-lines` 也没给 ⇒ **M3 出参 `product_line` 只给 `{id,name}`**（不编色值）。待七叔定：① 加列 ② 走字典（`dict_item`）③ 前端常量 |
| 17 | **计划 M3-10 与规格冲突：`PATCH` vs `PUT`** | 开发计划 M3-10 写 `PATCH /relations/:id`，接口 §5.6 写 **`PUT /relations/:id`** → 按铁律 A（冲突时以规格为准）**实现 PUT**。计划书本行已就地标注 |
| 18 | **写权限口径（本项目技术口径）** | 规格**没有**「谁能写业务关系」的表：M3 由三处已定口径合成 —— 可写＝`sale` / `dept_manager` / `gm`；**管理员只读**、**交付·客服只读**（→ §2.2 两处）。叠加角色（`admin` ＋ `sale`）**仍可写**（走销售那份权利，与 `resolveDataScope`「多角色取更宽」同思路）。待确认 |
| 19 | **「非灰度必标开发价值」的「必标」怎么算** | 数据架构 C1 只说「必须**已标** `value_tier`」。M3 取「落在 `high/low/medium` 之内」——`pending` 的语义是**尚未定档**，只判「列非空」的话销售点一下就绕过闸门（＝等于没有闸门）。规格未逐字写清，**待拍板** |
| 20 | **`ask_help` 跨部门的错误码** | 需求 §4.3 / 废止口径 #10 要求「@求助限同部门，跨部门走正式协同审批」，但**规格没给这条规则的错误码**。M3 取 **403 / 20003**（离「数据权限越界」最近的一档）＋ 人话「@求助仅限同部门，跨部门请走正式协同审批」 |
| 21 | **交付·客服的私海列表比规格更窄** | 规格＝「仅**服务中**（＝在合同服务期内）的客户」；合同表属 E 域、M3 未接入 ⇒ M3 先收敛到「我参与的关系（owner ∪ 有效协同）」（**比规格窄，不会越权**）。M4/M5 接上合同后补「服务期」条件 |
| 22 | **关系出参未返回的字段 ＋ 两处横切欠账** | 详情/列表**故意不返回**：`drop_in_x_days`（掉海规则 M7）/ `overdue`（承诺 D 域 M4）/ `amount` `amount_masked` `old_customer` `is_weekly`（合同 E 域）/ `round_no` `prev_round`（`sea_record` 未落库）/ `competitors` `labels` `stage_logs` `try_count_30d`（C3/C4/D，后续里程碑）—— **一律不填假值**。另：关系列表**未分页**（M6 统一）；`Idempotency-Key`（§2.5）横切能力**仍未实现**（M2 起同一现状） |
| 7 | **「同部门」的解释** | `self` / `serving` 的员工列表收敛＝「我所属 ∪ 我管辖」的部门交集（依据接口 §4.2「销售只能看同部门」）；**待规格细化** |
| 8 | **M1-18 端到端真请求 409 未验**（M0 遗留） | 需造真唯一冲突（重复建档）→ 挂 M2 一起验 |
| 9 | **前端未引 vue-router** | 方案 A 最小页够用；§五 的 29 个路由开始时须拍板引依赖 |

## 五、环境 / 操作踩坑（下个窗口别重踩）

1. **端口**：本窗口用 **后端 3000 / 前端 5173**（`npm run dev`）。⚠ **vite 只监听 `::1`（IPv6）**：用 `http://127.0.0.1:5173` 会 **HTTP 000**，必须用 `http://localhost:5173` 或 `http://[::1]:5173`。
2. **`gen:types` 依赖后端在线**：脚本写死 `http://localhost:3000/docs-json` → 先起后端再跑，否则生成的 `types.ts` 会缺接口。
3. **入参 DTO 必须带 `@ApiProperty`**：否则 OpenAPI 里该 DTO 会退化成**空对象**（实测 `LoginDto: Record<string, never>`），前端类型等于不可用 → 会被迫手写对接（违反 M0-57）。
4. **给 curl 送 JSON body 不要用 PowerShell 管道**；⚠ **且 `Invoke-RestMethod -Body '<含中文的 json 串>'` 同样会吞中文**（实测 2026-09-15：建档 `full_name` 里 13 个汉字被写成 13 个 `?`，连带 `name_core` 生成空串，看着像 B 域有 bug，其实是我探针的锅）。**唯一可靠姿势**：用 `[System.IO.File]::WriteAllText($path, $json, (New-Object System.Text.UTF8Encoding($false)))` 写 **UTF-8 无 BOM** 文件 → `Invoke-RestMethod -InFile <文件> -ContentType 'application/json; charset=utf-8'`（或 `curl.exe --data-binary "@<文件>"`）。
5. **`mysql -e "…"` 里的内层双引号会被 PowerShell 吞掉**（实测 `LIKE "x%"` 变成 `LIKE x%` → 语法错）→ 把 SQL 写进 **ASCII 临时文件**再 `-e "source <path>"`；`source` **打不开中文路径**。
6. **`operation_log` 是分区表**（覆盖 `p202609~p202712` ＋ **`pmax(MAXVALUE)` 兜底**）：⚠ **更正上轮说法** —— **有 `pmax` ⇒ 不会插失败**；真实风险是 **2028-01 起新行全落单一 `pmax`、老分区无法 `DROP PARTITION` 清理** → 须**每月固定续期**（口径＋部署清单见《数据架构文档》§十二「分区滚动（运维）」）。
7. 真库 `updated_at` 是 `datetime NOT NULL` **且无默认值** → 裸 SQL 插入必须显式给值（Prisma 的 `@updatedAt` 只在 ORM 内生效）。
8. `employee_role.role_code` / `permission_matrix.role_code` 是**外键指向 `role.code`** → 角色必须先插。

## 六、下个窗口建议的开工顺序

1. 先读：铁律（V1.7）→ 本文件 → **§七 欠账台账** → 开发计划 M4 判据 → 五份规格相关章节。
2. 先拿两个答案（都在 §五）：**`workflow_stage` 用数字码还是英文码**（#11）、**AI 起草的 6 个字典码是否照用**（#12）。
3. 再开 **M2（B 域建档/查重）**：建 `modules/company/` 四层 → 公司名标准化/相似度纯函数 → 联系人手机号唯一（`phone_active` 生成列）→ 顺手验 **M0 遗留的 409（P2002）端到端**。
4. 每完成一个里程碑：三绿 ＋ **真起进程** ＋ 逐文件 `add` 提交（**严禁 `git add -A`**）。

## 七、欠账台账（2026-09-15 立 · 「逐项：状态 / 阻塞 / 何时做」）

> 用途：**把"欠账"与"未开工的里程碑"分开**，避免下个窗口误把 M5 的活当成本轮漏做。
> 判据：**规格已生效而代码缺** ⇒ 欠账；**规格要求的功能整体未开工** ⇒ 里程碑（M2~M8），不算欠账。

| # | 项 | 状态 | 阻塞于什么 | 何时做 |
| --- | --- | --- | --- | --- |
| 1 | 成功响应 200 | ✅ **已闭环**（代码＋单测＋接口 §2.3，接口升 V1.15） | — | — |
| 2 | 接口版本指针全库漂移 | ✅ **已闭环**（35 处同批同步） | — | — |
| 3 | `login.dto.ts` 被外部回退 | ✅ **已闭环**（改回 ＋ 真机复验） | — | — |
| 4 | ~~字典 18 个未落库~~ | ✅ **已闭环（2026-09-15）**：`002_dict_seed.sql` 落真库，18 字典 / 101 项，幂等已验证；`urgency` 按数据架构 | — | — |
| 5 | 规格缺口：多角色取更宽 / refresh 出参不带 user | ⏸ **已实现、未入规格** | 一句话「写」 | 随时 |
| 6 | **数据范围注入（统一机制）** | ⛔ 未开工 | **B/C 域不存在** ⇒ 没有列表查询可注入；`serving`("服务中") 还要读 E 域合同服务期 | **M5**（计划已排） |
| 7 | **脱敏**（详情全可见 / 列表 `phone_masked` / 联系人锁 / 跨部门跟单与金额） | ⛔ 未开工 | 没有真实业务 DTO 可依托（先写必错，且错向＝把经理该看的真金额打码） | **M5** |
| 8 | 管理员「只读 ＋ 每次查看留痕 ＋ 不解除金额脱敏」 | ⛔ 未开工 | 管理员**同时要能写**组织/字典/规则 ⇒ 不能一刀切拦写；需 M5 的权限出口体系 | **M5** |
| 9 | `permission_matrix` 服务端消费方 | ⛔ 未开工 | 同上（先在 `/account/me` 出参供前端用） | **M5** |
| 10 | ~~409（P2002）端到端未验（M0 遗留）~~ | ✅ **已闭环（2026-09-15）**：真库实调重复手机号 → **HTTP 409 ＋ `{"code":20004,"message":"该手机号已存在"}`**（无 DB 原话）；撞信用代码 → 409「请使用已有档案」 | — | — |
| 11 | 分区滚动（运维） | 🟡 **低风险**（`pmax` 兜底不会插失败） | 需每月续期机制（脚本/job） | **M7**（Worker）起，2028-01 前必须有 |
| 12 | 前端 bundle 1.5 MB（gzip 466 KB） | 🟡 已知 | 页面少、内部系统可接受（先测量后优化） | **M6** 按需引入/代码分割 |
| 13 | 前端未引 vue-router / pinia | 🟡 已知 | 引依赖需单独执行令 | §五 29 路由开工时 |
| 14 | 跨域领域事件只有 `kernel/event-bus` 空壳 | 🟡 已知 | 无发布/订阅方（M4 起才真用） | **M4** |

## 八、工作树

**本轮（2026-09-15 补批）改动已提交**；工作树应干净。还原点：`git log --oneline`（M1 收尾批次 `edf46a2`）。
