# 交接说明（M1~M4 已闭环 · 下个窗口必读）

> **状态 / 缺口 / 待拍板的唯一落点**。读法：本文件 →《技术决策/AI协作铁律与踩坑复盘》V1.9 →《过程产出/开发计划-V1》。
> 业务口径一律以五份现行规格为准（需求 V1.26 / 数据架构 V1.31 / 接口 V1.17 / 前端 V1.16 / 设计规范 V1.0）；**版本沿革查 git**。
> ⚠ **起服**：`服务端/` 内 `npm run start:web`，真进程是 `node dist/main` —— 只杀命令行含 `nest` 的进程**杀不干净**（旧代码占着 3000 会让你白验一轮）。

## 一、进度

| 里程碑 | 状态 | 判据（真库实证） |
| --- | --- | --- |
| M1 A 域 / M2 B 域 / M3 C 域 | ✅ `edf46a2` / `07bbb0e` / — | 登录双通道；建档查重；激活 · 撞单 409 · 越权 403 · 私海三范围 · 公海收敛 |
| **M4 D 域 M4-01~M4-17** | ✅ 本窗口 | 三绿 **39 suites · 557 tests**；真库：建档事件（C 发 → D 落 `system`，键 `relation_created:<id>`）· 写跟单倒序 · 同内容 409 · **快速标记后 `last_event_at` 未变** · 承诺建/列/兑现 · `today-agenda` 直查（造 1 条 → 返 1 条，含跨域公司名）；`/docs`=200；`gen:types` 空对象仅剩 `webhooks` / `$defs` |

**下一步 ＝ M5**（数据范围注入 / 脱敏 / 管理员只读留痕 / `permission_matrix` 消费方）。

**M4-17 页面的实情（不谎报）**：关系页时间线抽屉已能写跟单并刷新；**工作台仍看不到新跟单** —— 动线是 05:00 组装的产物（M7），本批只做「直查」。页面级点通**未做**（本机无浏览器自动化工具，未擅自安装）。

## 二、★ 待拍板（谁 / 干什么的 / 建议 / 影响范围）

| # | 谁 | 干什么的 | 建议 | 影响范围 |
| --- | --- | --- | --- | --- |
| A | 7 条产品线（`seed/001` 注释） | 建档与关系里的「产品线」；现只有 3 条占位线 | 按已给清单（财税 / 法务 / 网站 / GEO / 短视频 / 招聘 / 房产）落地；**每条还需一个 `color_key`**（规格只说「可配颜色」，没给值） | 改 seed；已建关系不受影响；前端色点跟着变 |
| B | 承诺「豁免」原因（`commitment` 表） | 需求 §10.1 要求「豁免必填原因」，但**表里没有存原因的列** | ① 加 `waive_reason` 列（新增量 migration ＋ 数据架构 D1 升版）；② 暂不做豁免 | ① 动库 ＋ 升版；② 该功能后延 |
| C | 代码注释里约 50 处 `V1.16` 指针 | 接口已升 V1.17，注释没跟上 → 下个窗口照旧版实现 | 开**专项窗口**统一扫（混有「前端 V1.16」，**禁批量正则** → 铁律坑 19） | 纯注释，不改行为 |

## 三、欠账（非阻塞）

1. **承诺改的入参形状规格未给**（只有 `U /relations/:id/commitments` 一行）→ 本批按 `{id,status?,due_at?}` 实现；`waived` 不开放。
2. 接口 §4.6 写 `type`，§5.7 出参与数据架构 D1 都写 `party` → 本批用 **`party`**（两处一致 ＋ DB 列名）；契约词汇**只报不改**。
3. 动线条目 `relation` **可为 `null`**（D4 允许 `relation_id` 空）而 §5.7 写成非空 → 已按可空实现并登记。
4. 列表全未分页（关系 / 事件 / 承诺 / 动线）；`GET /companies/:id`、`/org/product-lines`、部门色值列 → **M6**。
5. 数据范围注入 / 脱敏 / 管理员留痕 / `permission_matrix` 消费方 → **M5**；交付 · 客服私海口径比规格窄（未接合同服务期）。
6. `Idempotency-Key` 横切未做（跟单的**内容幂等**已生效，是另一回事）；快速标记批量、动线处理动作 → 后续里程碑。
7. 前端未引 vue-router / pinia（方案 A 够用）；bundle 1.53 MB → M6 再优化。

## 四、开工动作

1. 读铁律 V1.9 → 本文件 → 开发计划 M5 判据 → 相关规格章节。
2. 基线 **39 suites · 557 tests**（勿照抄旧快照里的 38 / 536）。
3. 三绿（`tsc --noEmit` / `lint` / `test`）＋ **真起进程**＋ 逐文件 `git add`（**禁 `git add -A`**）。
4. 本轮另动了 **C 域**：`relation.service.ts`（激活后 publish `RelationCreated`、新增 `getRelationRefs` 出口）、`relation.repository.ts`（加 `findRelationRefsByIds`）、`relation.service.spec.ts`（补假总线）—— 均为加出口 / 加一处发布，未改既有逻辑。
5. 总线装配：`kernel/events/event-bus.module.ts`（`@Global`）；注入 `EventBus` **必须值导入** —— 写 `import type` 会让 Nest 元数据变成 `Function`，起服即「依赖解析失败」。
