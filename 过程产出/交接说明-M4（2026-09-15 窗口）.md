# 交接说明（M1~M4 已闭环 · 下个窗口必读）

> **状态 / 缺口 / 待拍板唯一落点**。读法：本文件 →《AI协作铁律与踩坑复盘》V1.9 →《开发计划-V1》M5 判据；业务口径以五份现行规格为准（需求 V1.27 / 数据架构 V1.32 / 接口 V1.18 / 前端 V1.16 / 设计规范 V1.0），**沿革查 git**。
> ⚠ **起服**：`服务端/` 内 `npm run start:web`，真进程 `node dist/main` —— 只杀含 `nest` 的进程**杀不干净**（旧代码占着 3000 会白验一轮）。

## 一、进度

| 里程碑 | 状态 | 判据（真库实证） |
| --- | --- | --- |
| M1~M3（A/B/C 域） | ✅ `edf46a2` / `07bbb0e` | 登录双通道 · 建档查重 · 激活 · 撞单 409 · 越权 403 · 私海三范围 · 公海收敛 |
| **M4 D 域 M4-01~17** | ✅ 本窗口 | 三绿 **39 suites · 557 tests**；真库跑通：建档事件（C 发 → D 落 `system`）· 写跟单倒序 · 同内容 409 · 快速标记不动 `last_event_at` · 承诺建/列/兑现 · `today-agenda` 直查（含跨域公司名）；`/docs`=200；`gen:types` 空对象仅剩两处样板 |

**2026-09-15 口径**：**7 条正式产品线**（财税 / 法务 / 网站 / GEO / 短视频 / 招聘 / 房产）已落 `seed/001` ＋ 存量 dev 库（实测出参 `color_key=blue`）；`code` / `color_key` AI 起草（**可改**），`dept_ids` / `service_cycle_days` 仍是 DEV 占位；6 个 AI 起草字典码**照用**，`prisma/README.md §五` 三项待裁项**全部裁定**。**下一步 ＝ M5**（数据范围注入 / 脱敏 / 管理员只读留痕 / `permission_matrix` 消费方）。

**M4-17 页面级已点通（真浏览器 · 本机 Edge）**：登录 → 工作台（真渲染 1 条动线，跨域公司名正确）→ 业务关系（产品线已显新线）→ 时间线抽屉 → 页面写跟单 → 立刻出现在时间线最上方。
⚠ **工作台不因写跟单而新增条目** —— 动线是 05:00 组装任务产物（属 M7），本批只做直查，库里无当日行即空数组。

## 二、★ 待拍板（谁 / 干什么的 / 建议 / 影响范围）

| # | 谁 | 干什么的 | 建议 | 影响范围 |
| --- | --- | --- | --- | --- |
| ~~A~~ | 承诺「豁免」原因（`commitment` 表） | **✅ 已决 · 已落地（2026-09-15）**：七叔拍板分界——`cancelled` 取消＝**录错了 / 不成立**（**不需原因**）；`waived` 豁免＝**确有其事但做不成**（客户变卦 / 特殊原因，**必填原因**）。已加 `waive_reason` 列（migration `0005`，真库已应用并核对）＋ 数据架构 D1 升 **V1.32** ＋ 需求 §10.1 升 **V1.27** ＋ 接口 §4.6 同步 | **剩余**：「豁免」这个**动作**（前端按钮 / 接口放开 `waived` 写入）**仍未开放，待指令** | 动库（已做）＋ 两份规格升版（已做）＋ 版本指针全量同步（已做） |
| B | 代码注释里约 50 处 `V1.16` 指针 | 接口已升 V1.17，注释没跟上 → 下个窗口照旧版实现 | 开**专项窗口**统一扫（混有「前端 V1.16」，**禁批量正则** → 铁律坑 19） | 纯注释，不改行为 |

## 三、欠账（非阻塞）

1. **承诺改的入参形状规格未给**（只有 `U /relations/:id/commitments` 一行）→ 本批按 `{id,status?,due_at?}` 实现。⚠ **`waive_reason` 列已就位**（`0005`），但 **`waived` 动作与出参字段仍未开放** —— 开放时同批补：`UpdateCommitmentDto.waive_reason`（豁免必填 422）＋ `CommitmentVoDto.waive_reason` ＋ 接口 §5.7 承诺形状 ＋ 重跑 `gen:types` ＋ `CLOSABLE_STATUSES` 加 `waived`。
2. 接口 §4.6 写 `type`，§5.7 ＋ 数据架构 D1 写 `party` → 本批用 **`party`**；契约词汇**只报不改**。
3. 动线条目 `relation` **可空**（D4 允许 `relation_id` 空）而 §5.7 写非空 → 按可空实现并登记。
4. 列表全未分页（关系 / 事件 / 承诺 / 动线）；`GET /companies/:id`、`/org/product-lines`、部门色值列 → **M6**。
5. 数据范围注入 / 脱敏 / 管理员留痕 / `permission_matrix` 消费方 → **M5**；交付 · 客服私海口径比规格窄（未接合同服务期）。
6. `Idempotency-Key` 横切未做（跟单**内容幂等**已生效）；快速标记批量、动线处理动作 → 后续里程碑。
7. 前端未引 vue-router / pinia（方案 A 够用）；bundle 1.53 MB → M6 优化。

## 四、开工动作

1. 按头部读法重读铁律 V1.9 ＋ 开发计划 M5 判据；基线 ＝ 上表 **39 suites · 557 tests**（勿照抄旧快照的 38 / 536）。
2. 三绿（`tsc --noEmit` / `lint` / `test`）＋ **真起进程** ＋ 逐文件 `git add`（**禁 `git add -A`**）。
3. 本轮另动 **C 域**：`relation.service.ts`（激活后 publish `RelationCreated`、加 `getRelationRefs` 出口）、`relation.repository.ts`（加 `findRelationRefsByIds`）、对应 spec（补假总线）—— 只加出口 / 加一处发布，未改既有逻辑。
4. 总线装配在 `kernel/events/event-bus.module.ts`（`@Global`）；注入 `EventBus` **必须值导入** —— 写 `import type` 会让元数据退化成 `Function`，起服即「依赖解析失败」（→ 铁律坑 31）。
