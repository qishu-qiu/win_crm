# 交接说明（M1~M5 ✅ · **M6 进行中** · 下个窗口必读）

> **状态 / 缺口 / 待拍板唯一落点**。读法：本文件 →《AI协作铁律与踩坑复盘》**V1.10** →《开发计划-V1》M6 判据；口径＝五份现行规格（需求 **V1.28** / 数据架构 **V1.34** / 接口 V1.18 / 前端 V1.16 / 设计规范 V1.0）＋《架构设计说明》**V1.5**，沿革查 git。
> ⚠ **起服**：后端 `服务端/` 内 `npm run start:web`（真进程 `node dist/main` —— **按 `dist/main` 特征杀**）；前端 `前端/` 内 `npm run dev`（**5173**，`/api` 代理到 3000）。
> ⚠ **基线**：后端 `tsc 0 / lint 0` ＝ **44 suites · 618 tests**；前端 `npm run build` 绿（主包 1.28 MB）。
> ★ **省钱要点（本窗口复盘）**：多做**成套执行**、少来回确认；**换窗更勤**（做完一批即换）；宽检索（规格审计）**交子代理并限死返回长度**。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M4 | ✅ |
| M5（① ② ③ ＋ 收尾 A / B） | ✅ `35b7fd0` `5317e78` `f0c34a3` `81bc170` `d98752b` |
| **M6-05 路由收口 ＋ 登录页接路由** | ✅ `42aeb5b` |
| M6 其余（06 工作台 / 07 列表＋分页 / 08 详情 / 09 录入 / 10 角色矩阵 / 11 主题 / 12 提交） | ⬜ **下一步** |

### ★ 已定的硬口径（**勿再按旧注释做**）

- **审计**：所有增删改由**统一切面**写（`@Audit('模块.动词')`）；查看类走 `recordStandalone`（best-effort、不阻断读）；**401 / 403** 由全局异常过滤器写 `authz.denied`。
- **可写角色**：唯一判定 ＝ `kernel/data-scope/write-role.ts`（可写 `sale`/`dept_manager`/`gm`；`admin`/交付/客服**只读**；**多角色取"能写"**）。⚠ **不走它**：台账编辑（§7.7）、组织/字典维护、§7.9 兜底转交。
- **数据范围 / 脱敏**：**判定在 kernel**，**落地 / 渲染在各域**；拦截器**不查库、不改写出参**（架构 §7.2 / §7.3 已按实现校正）。
- **前端（M6-05）**：已引 **vue-router**（**pinia 仍未引** —— 唯一跨页状态在 `src/session.ts`）。分工：**进不进得来**＝路由守卫（读 token）；**会话是否有效**＝`session.ts` 调 `GET /account/me`；**401** ＝ `api/request.ts` 派 `UNAUTHORIZED_EVENT`，由 App 跳 `/login`。★ **请求层不 import router**。

### 本窗口（M6-05）落点与**待验**

- 路由表只收**已建成的 4 页**（`/login`、`/`、`/entry`、`/relations`）＋ 未知路径回工作台；页面懒加载 → 各页独立 chunk（主包 1.53 MB → 1.28 MB）。
- ⚠ **待七叔验**（我无账号密码）：真账号登录动线 —— ① 登录**直进工作台** ② 顶栏三导航切换 ③ **未登录**敲 `/relations` 被弹回 `/login` ④ 退出登录。
- 前端后续可做：AntD **按需引入**（主包大头）、`Idempotency-Key`（→ 接口 §2.5）。

### 「把举例当穷尽」审计（**已 5 次翻车**，4 次已登记）

- 已修：§7.4 敏感动作｜需求 §4.2 管理员"只读"举例（曾放行建档）｜`relation` 注释「仅 pending → 422」｜前端 §99。**根治**：需求 **§1.3 读法铁律** ＋ 铁律 §三 ＋ `CODEBUDDY.md §7`。同类历史：《废止口径登记表》**#31 / #36 / #37**。

## 二、待拍板

**当前无待拍板项。** 新议题请在本表登记。

## 三、欠账（非阻塞）

1. `serving` 只做形状（E 域未建）⇒ 暂按 `self` 收敛。
2. 报表 / 看板 / 汇总出口未建 ⇒ M5-06 只落开关 ＋ 单测。
3. `GET /contacts/:id` 未建 ⇒ 详情全号 / `phone_locked_by` / 24h `unlocked_until` 随它落地。
4. 上锁 / 解锁 / `phone_unlock` 审批未建 ⇒ `sea.phone.view`、`phone.unlock.*`、强制解锁留痕**随接口补**（已列进 A10）。
5. 分页 / `GET /companies/:id` / `org/product-lines` → **M6**；`Idempotency-Key` 未做。
6. `shared.module.spec.ts` 起真服务后 worker 未优雅退出（用例全绿）。
7. src 内 **38 处**规格版本号引用未清理（本窗口只把新增的 4 处改成不绑版本号）。

## 四、下个窗口开工动作

1. 重读《AI协作铁律与踩坑复盘》**V1.10** ＋ 本文件。
2. 主线＝**M6-06 工作台补全**（数据与 `/today-agenda` 一致）→ **07** 列表＋分页 → **08** 详情 → **09** 录入（写跟单 / 快速标记）→ **10** 角色矩阵（不可见页**不下发路由**）→ **11** 主题（白天 / 夜间）→ **12** 提交。
3. 每片收尾：动了后端就跑三绿（＋真起 `node dist/main`）；只动前端就跑 `npm run build`；逐文件 `git add`（**禁 `-A`**）。
