# 交接说明（M1~M4 ✅ · **M5 第①批 ✅** · 下个窗口必读）

> **状态 / 缺口 / 待拍板唯一落点**。读法：本文件 →《AI协作铁律与踩坑复盘》V1.9 →《开发计划-V1》M5 判据；业务口径＝五份现行规格（需求 V1.27 / 数据架构 V1.32 / 接口 V1.18 / 前端 V1.16 / 设计规范 V1.0），**沿革查 git**。
> ⚠ **起服**：`服务端/` 内 `npm run start:web`；真进程是 `node dist/main` —— **按 `dist/main` 特征杀**，只杀含 `nest` 的**杀不干净**（旧代码占着 3000 会白验一轮）。
> ⚠ **基线勿照抄旧快照**：`tsc 0 / lint 0 / test` ＝ **40 suites · 570 tests**（旧说明写的 557 已过时）。

## 一、进度

| 里程碑 | 状态 | 判据（真库实证） |
| --- | --- | --- |
| M1~M4 | ✅ | M4 出口 ＝ 三绿 39/563；建档事件 · 写跟单倒序 · 同内容 409 · 承诺三态 · `today-agenda` |
| **M5 第①批（M5-01/02/03/08 范围注入收口）** | ✅ **`35b7fd0`** | 见下「本批实证」 |
| M5 第②批（脱敏 04/05/06/09）· 第③批（审计 ＋ 管理员只读留痕 07） | ⬜ **下一步** | — |

### ★ M5 落点已定稿（**与旧注释不同，勿照旧说法盖楼**）
「把范围注入到查询」**不在拦截器** —— 拦截器挂在响应式管道上，**改不了 service 内的 Prisma 查询**。
真落点两处：① **唯一判定** `kernel/data-scope/data-scope-target.ts`（四档 → 范围描述，纯函数）；② **各域 repository** 拼自己的 where。
⇒ `scope.type` 判定**只许出现在 ①**。机械自检：`modules/**` 内 `rg "\.type === '(all|dept|self|serving)'"` 应 **0 命中**。

### 本批实证
sale(03 王海涛) 私海 `{5,3,1}` vs gm(01 陈志远) `{5,4,3,1}` ⇒ **relation 4 销售看不到**；销售访问 relation 4 的 **详情 / 事件 / 成员 / 承诺 → 全部 403 · 20003**，自己的 5 → 200，公海 → 200；真起 `node dist/main` → `/health` `db:up`；`gen:types` **无 diff**。

## 二、★ 待拍板（谁 / 干什么的 / 建议 / 影响范围）

| # | 谁 | 干什么的 | 建议 | 影响范围 |
| --- | --- | --- | --- | --- |
| A | **协同人能否「改关系属性」** | 关系详情页那几项：紧迫档 / 价值档位 / 下一步提示 / 竞品（接口 `PUT /relations/:id`）。现状＝**只有主责销售（owner）能改**，被拉进来的**协同人改了 → 403**（但他**能写跟单**、能看全文）。规格 C2 说协同与 owner 同级 ⇒ 存在偏差 | **待七叔定**：① 放开给协同人（改 1 处判断 ＋ 补测）；② 维持 owner 唯一（把注释写死「协同只写跟单、不写关系属性」） | 放开＝动写权限面，要给协同补单测与越权用例；维持＝零代码改动，只清注释 |
| B | 架构说明 §7.2 措辞 | 文档写「**数据范围拦截器**把范围注入查询」，与定稿实现不符 | ✅ **已采建议**：**留到 M5 收尾一起改**（改它要升 V1.4 ＋ 全量指针同步，M5 还有 3 处同类，一次做省一轮） | 仅文档准确性；代码注释已写「旧说法作废」 |
| C | `GET /contacts` 是否按范围收敛 | ✅ **已定（2026-09-15 七叔）**：**联系人＝资料库，全公司共享**（联系人是公司档案的隶属关系，从公司档案带出来也能看）；**要防的不是「谁能看」，而是「批量搞走联系方式名单」** ⇒ 故**列表一律 `phone_masked`**、落锁时非 owner 给 `phone_locked` | 维持不过滤 | — |

## 三、欠账（非阻塞）
1. `serving`（交付 / 客服 ＝ 仅「服务中」客户）**只做形状、不接真数据** —— **E 域（合同）尚未建**，无「服务期」可查；现按 `self` 收敛（更窄、不越权）。
2. **报表 / 看板 / 汇总接口尚未建** ⇒ M5-06「出口不脱敏」本批只能做豁免开关（`DESENSITIZE_EXEMPT_KEY` 已就位）＋ 单测，无法 curl 举证真报表。
3. 管理员「只读 ＋ 每次查看写 `operation_log`」→ M5 第③批。
4. 列表全未分页（关系 / 事件 / 承诺 / 动线）、`GET /companies/:id`、`/org/product-lines`、前端未引 vue-router / pinia、bundle 1.53 MB → **M6**；`Idempotency-Key` 横切未做（跟单**内容幂等**已生效）。
5. **协同写权**（见 §二 A）未排期。

## 四、下个窗口开工动作（第②批 · 脱敏）
1. 重读铁律 V1.9 ＋ 本文件；基线 ＝ **40 suites · 570 tests**。
2. 落点照第①批同思路：脱敏规则＝**`kernel/` 纯函数**（一处收口）；拦截器只管「出口豁免标记 ＋ 越权兜底」，**不逐字段改写出参**（「记录属于谁」只有业务查询时知道）。
3. 先动 B 域 contacts 出口：`phone_locked` 从常量 `false` 改为真判定（读 `contact.phone_locked_at / by` ＋ 判查看者是否该关系 owner）；`extra_phones` **一并锁**。
4. 收尾同第①批：三绿 ＋ **真起进程** ＋ `gen:types` 无 diff ＋ 逐文件 `git add`（**禁 `-A`**）。
