# 交接说明（M1~M5 ✅ · **M6 进行中（07 ✅）** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（**缺口不在本节** → `过程产出/欠账登记表.md`）。读法：本文件 →《AI协作铁律与踩坑复盘》**V1.10** →《开发计划-V1》M6 判据；口径＝需求 **V1.29** / 数据架构 **V1.34** / 接口 **V1.21** / 前端 V1.16 / 设计规范 V1.0 ＋《架构设计说明》**V1.6**。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（真进程 `node dist/main`，**按 `dist/main` 杀**）；前端 `前端/` 内 `npm run dev`（**5173**，`/api`→3000）。基线：后端 `tsc 0 / lint 0` ＝ **44 suites · 621 tests**；前端 build 绿（主包 1.28 MB）。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5（①②③＋收尾 A/B） | ✅ `35b7fd0` `5317e78` `f0c34a3` `81bc170` `d98752b` |
| M6-05 路由收口＋登录接路由 | ✅ `42aeb5b` |
| M6-06 工作台补全 | ✅ `b978965` |
| **M6-07 业务关系列表补全（含分页）** | ✅ 2026-09-16（`6362fd6`）；**筛选栏**（视图 ＋ 紧迫档）已接 `73605a3` |
| **M6-08 关系详情页（详情 ＋ 成员）** | ✅ 2026-09-16 |
| M6 其余（09 录入 / 10 角色矩阵 / 11 主题 / 12 提交） | ⬜ **下一步 ＝ M6-09** |

**硬口径（勿按旧注释做）**：① 审计＝增删改走切面 `@Audit('模块.动词')`、查看类 `recordStandalone`、401/403 由全局过滤器写 `authz.denied`；② 可写角色唯一判定＝`kernel/data-scope/write-role.ts`（`sale`/`dept_manager`/`gm` 可写，多角色取"能写"；**不走它**＝台账编辑 §7.7 / 组织字典 / §7.9 兜底转交）；③ 数据范围**判定在 kernel、落地在各域**，拦截器不查库、不改写出参；④ 前端有 `vue-router`（**pinia 仍未引**，跨页状态在 `src/session.ts`），请求层**不 import router**（401 派事件给 App）；⑤ 代码注释**不绑文档版本号**；⑥ **分页**＝入参 `page`/`page_size`、出参 `{list,total,page,page_size}`，归一化**只在 `kernel/common/pagination.ts` 一处**，域内别自己夹紧、前端别切片（→ 接口 §2.3 / §2.7）。

**待七叔验**（我无账号密码）：真账号 → 业务关系列表**翻页 / 改每页条数 / 切页签回第 1 页** → 时间线。
**2026-09-16**：M6-07 落，只动 C 域（仓储/服务/DTO/控制器/单测）＋ 前端 2 文件 ＋ 接口文档升 V1.21；44 suites · **621** tests 全绿、`tsc 0 / lint 0`，前端 build 绿（1.28 MB），`gen:types` 重跑后 `Record<string, never>` ＝ 0。

## 二、待拍板

**P-01 ✅ 已拍板并落地（2026-09-16）**：业务关系列表的筛选 **由后端开参数** —— `view`（`all` / `following` / `cooperated` / `churned`）＋ `urgency`（紧迫档多选），接口升 **V1.21**，各档判定唯一落点＝`domain/relation-list-filter.ts`。
- ⚠ **「逾期未跟进」这一档没做**：它要判「逾期」，而逾期唯一来源是承诺 / 预约（D 域），字段未落地（→ 欠账 **D-10**、**D-09 剩项**）—— 不编假的逾期定义。
- ⚠ **前端筛选栏也还没接**（→ 欠账 **D-19**）：参数目前**开着没人调**。

**P-02（2026-09-16 新增）前端目录与跨页复用要不要立规矩** → 见《欠账登记表》**D-18** ＋ 调研件 `过程产出/前端目录与组件复用-调研.md`（大厂做法 ＋ 建议 ＋ 影响）。**等你拍**。

**其余无**（2026-09-16 已把《废止口径登记表》37 条核验：30 ✅ / 6 残留 / 0 缺落点，残留已全收口）。

## 三、欠账

> **★ 唯一落点已迁出** → **`过程产出/欠账登记表.md`**（缺口 / 为什么现在做不了 / 归属哪期补 / 出处，四列；**只增不删**）。本节只留指针（本文件 ≤5 KB，装不下会长大的台账）；**新缺口当场写进那张表**。

## 四、下个窗口开工动作

1. 重读《AI协作铁律与踩坑复盘》**V1.10** ＋ 本文件 ＋ `过程产出/欠账登记表.md`。
2. 主线＝**M6-09 录入页补全**（写跟单 / 快速标记）→ 10 角色矩阵 → 11 主题 → 12 提交；欠账 **D-22**（详情页 8 Tab）随各域补、**D-13 剩项**（联系人 / 员工可点）随 M9 的 B / A 域补。
3. 每片收尾：动后端跑三绿（＋真起 `node dist/main`）；只动前端跑 `npm run build`；逐文件 `git add`（**禁 `-A`**）。
4. ⚠ M6-07 已提交 `6362fd6`；开工前先 `git status --short` 确认工作树（可能有文档回填未提交）。
