# 交接说明（**D-37 ＋ D-36⑥ ＋ D-41① 已闭环** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》（每轮必读）→《开发计划-V1》判据；坑按需查案例库。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**，`/api`→3000）。基线：后端 `tsc 0 / lint 0` ＝ 45 suites ＋ **672 用例**（⚠ 本机内存紧：默认并发会 **Jest worker OOM** → 用 `npm test -- --maxWorkers=2`）；前端 `lint / build` 双绿。
> ⚠ 本轮**已改库**：migration **`0009`**（`employee` 加 `theme` / `nav_open`）已 `deploy` 到本机真库（**9 份全登记**）。

## 〇、★ 只留指针

**公海（无主）＝ 可读不可写，唯一例外＝改「开发价值」**：需求 §6.3 / §十六 N12 · 接口 §2.4 `20408` / §5.6 · 前端 §5 第 9/10 条 · 架构 §7.2 · 废止表 **#38**；实现 → **D-32**。

**个人偏好（新落）**：主题 ＋ 侧栏展开**存账号**（`employee.theme` / `nav_open`，migration `0009`）；`PUT /account/preferences`（**部分更新**、**只能改自己**、出参＝`UserVO`），读出口＝`GET /account/me`。口径 → 数据架构 A2 · 接口 §4.14.9 / §5.2 · 设计规范 §八.1 · 需求 §13.4；实现 → **D-37 / D-36⑥ / D-41①**；⚠ `notif` 未做 → **D-42**。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5 ｜ M6-05~08 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b`… |
| **M6 闭环（09~13）** | ✅ 2026-09-16~18：录入页 / 公海 **D-32** / 角色矩阵 / 主题 / 筛选入 URL（D-19·21·39·40） |
| **导航与个人偏好** | ✅ 2026-09-18：侧边栏 `54b0169`；**偏好存账号 ＋ 顶栏头像菜单** `7fd4758` |

**硬口径**（审计 / 分层 / 数据范围 / 脱敏**见常驻的 `CODEBUDDY.md`，不重复**）：① 可写角色＝`kernel/data-scope/write-role.ts`；② 分页归一化只在 `kernel/common/pagination.ts`；③ 前端有 `vue-router`（**pinia 未引**）、请求层不 import router；④ 注释不绑版本号；⑤ 快速标记＝不写幂等键 / 不回写 `last_event_at` / `action_type` 固定 `phone` / `contact_ids` 限归属人；⑥ 待关联：归属＝`contact.owner_id`、不占部门·产品线、可改只留痕、工作量按写单人；⑦ dark 色值以设计规范 §八.7 为准；⑧ 菜单结构与可见性唯一落点＝`前端/src/access.ts`；⑨ **偏好**：**账号为权威**、本机只兜**首屏**、侧栏展开**空数组不回灌**。

**待七叔验（UI 只能人点）**：① 录入三步 ② 联系人档案筛选 ③ 列表勾选批量标记 ④ 翻页 / 时间线（→ D-14）⑤ 公海页签「时间线」（→ D-32）⑥ 主题切换 ＋ 刷新不闪 ⑦ 筛选写 URL（D-21 / D-39）⑧ 侧边栏（各角色项 / 展开收起 / 数量小标 / 换页自动展开 / 刷新保持）⑨ **本轮**：**头像菜单**、**主题与折叠"换设备"仍是那套**。

**证据**：`git log --oneline -6` ＋ 欠账表各行。要点：后端三绿 / 前端双绿 ＋ dev 真转译；**真库真链路**：`me` 读 `theme=null` → `PUT {theme:dark,nav_open:[relation,customer]}` → 回读一致；**部分更新** / **空 body** 200 且库不动 / 非法 `theme` → **400**；`operation_log` 3 条留痕。

## 二、待拍板

**P-01~P-07 ✅ 全部落地**（沿革查 git）。**当前 1 项**：

**拍-D42：`notif`（通知偏好）现在定结构吗？**
- **谁**：接口 §5.2（`PUT /account/preferences` 的 `notif`）＋ 需求 §8；前端**页 28**。
- **干什么的**：规格只写 `notif?:{...}`，**括号里是什么从来没给**（需求 §8 也无字段清单）⇒ 本轮只落 `theme` / `nav_open`，**`notif` 传了也不落库**。
- **建议**：**先不定**。开关粒度取决于"实际有哪几种通知"，那批（提醒 / 目标 / 审批）还没做 ⇒ 现在拍等于**凭空造分类**，M7 一开工就推翻。
- **影响范围**：不定 ＝ 页 28 只有主题、头像菜单不摆「通知偏好」（**现状，不是缺陷**）；定 ＝ 接口 §5.2 ＋ 需求 §8 同批加结构 ＋ 又一个增量 migration。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。本轮收口 **D-37 / D-36⑥ / D-41①**；新增 **D-42**；**D-41② 消息铃铛仍欠**。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表。
2. 主线候选＝**D-29**（关联公司动线，接口已补、实现未做；**先核** `C`(L3) 不可反向依赖 `D`(L4) ＋ EventBus 是否已进 DI）· **F 域公海领取**（D-33，M7 起点）· **D-42**（若拍了 `notif`）。
3. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起 `node dist/main`；**单测加 `-- --maxWorkers=2`**；逐文件 `git add`（**禁 `-A`**）。
4. ⚠ 开工前 `git status --short` 确认工作树。
