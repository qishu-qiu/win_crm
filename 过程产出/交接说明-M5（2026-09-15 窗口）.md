# 交接说明（**M1~M5 全部完成** ✅ · 下一步 M6 · 下个窗口必读）

> **状态 / 缺口 / 待拍板唯一落点**。读法：本文件 →《AI协作铁律与踩坑复盘》**V1.10** →《开发计划-V1》M6 判据；口径＝五份现行规格（需求 **V1.28** / 数据架构 **V1.34** / 接口 V1.18 / 前端 V1.16 / 设计规范 V1.0）＋《架构设计说明》**V1.5**，沿革查 git。
> ⚠ **起服**：`服务端/` 内 `npm run start:web`；真进程是 `node dist/main` —— **按 `dist/main` 特征杀**。
> ⚠ **基线**：`tsc 0 / lint 0 / test` ＝ **44 suites · 618 tests**。

## 一、进度（M5 已闭环）

| 里程碑 | 状态 |
| --- | --- |
| M1~M4 | ✅ |
| M5 第①批（01/02/03/08） | ✅ `35b7fd0` |
| M5 第②批（04/05/06/09 脱敏） | ✅ `5317e78` |
| M5 第③批（07 审计 ＋ 查看留痕） | ✅ `f0c34a3` |
| M5 收尾 A（只读建档门 ＋ 越权留痕 ＋ 举例穷尽审计） | ✅ `81bc170` |
| **M5 收尾 B（§7.2 / §7.3 措辞校正 ＋ 竞品名册定版）** | ✅ **本轮** |

### ★ 三条已定的硬口径（**勿再按旧注释做**）

- **审计**：① 所有增删改由**统一切面**写（`@Audit('模块.动词')`）；② 查看类走 `recordStandalone`（best-effort、不阻断读）；③ **401 / 403** 由**全局异常过滤器**写 `authz.denied`（切面看不到守卫/服务抛的错）。
- **可写角色**：唯一判定点 ＝ `kernel/data-scope/write-role.ts`（可写＝ `sale`/`dept_manager`/`gm`；`admin`/交付/客服**只读**；**多角色取"能写"**）。B 域建档已上这道门（**403 ＋ `company.read_only`**）。⚠ **不走它**：台账编辑（需求 §7.7 归交付/客服）、组织/字典维护、§7.9 兜底发起转交。
- **数据范围与脱敏（本轮校正）**：**判定在 kernel**，**落地 / 渲染在各域**；拦截器**不查库、不改写出参**。

### 本批落点（收尾 B）

- 架构 **§7.2** 改写：「数据范围**拦截器**把'我能看到谁'**注入查询条件**」**作废** → 「判定一处收口（kernel）＋ 落地各域自己拼」；**§7.3** 改写：「返回前**统一处理**」**作废** → 「判定 / 渲染 / 豁免**三处一条链**，拦截器只打标记」。
- 数据架构 **C7 竞品名册**：`created_by / updated_by` **只允许 `dept_manager` 维护**（**2026-09-15 定版**；原先写 manager/**admin** 是错的——管理员对业务数据一律只读）。
- 版本：架构 V1.4 → **V1.5**、数据架构 V1.33 → **V1.34**；指针已同步（README / 项目介绍 / 各规格互引 / prisma·前端 README）。
- ⚠ **仍欠**：**src 内 38 处历史注释仍写「《架构设计说明》V1.3」**（本轮只把**本批新增的 4 处**改成不绑版本号）—— 既有漂移，未全量清理。

### ★「把举例当穷尽」审计（**已 5 次翻车**，4 次已登记）

- **已修**：§7.4 敏感动作｜需求 §4.2 管理员"只读"举例（→ 曾**放行建档**）｜`relation` 注释「仅 pending → 422」（与拍板**相反**）｜前端 §99（澄清＝仅快捷入口）。
- **根治**：需求 **§1.3 读法铁律** ＋ 铁律 §三 ＋ `CODEBUDDY.md §7`；同类历史见《废止口径登记表》**#31 / #36 / #37**。

### 实证（真进程 ＋ 真库）

- 44 suites · 618 tests；`tsc 0 / lint 0`；`/health` `db:up`。
- 第③批：`event.view` 落库、`@AuditSkip` 与销售查看**增量 0**；收尾 A：管理员 / 交付建档 **403**、匿名 401 落 `authz.denied`、销售 **200 不受影响**；探针数据已删。
- `gen:types`：仅描述注释变化（类型未动）。

## 二、待拍板

**当前无待拍板项**（D / E / F / G 与竞品名册均已定）。新议题请在本表登记。

## 三、欠账（非阻塞）

1. `serving` 只做形状（E 域未建）⇒ 暂按 `self` 收敛。
2. 报表 / 看板 / 汇总出口未建 ⇒ M5-06 只落开关 ＋ 单测。
3. `GET /contacts/:id` 未建 ⇒ 详情全号 / `phone_locked_by` / 24h `unlocked_until` 随它落地。
4. 上锁 / 解锁 / `phone_unlock` 审批未建 ⇒ `sea.phone.view`、`phone.unlock.*`、强制解锁留痕**随接口补**（已列进 A10）。
5. 分页 / `GET /companies/:id` / `org/product-lines` / 前端 router·pinia / bundle → **M6**；`Idempotency-Key` 未做。
6. `shared.module.spec.ts` 起真服务后 worker 未优雅退出（用例全绿）—— 测试资源泄漏。

## 四、下个窗口开工动作

1. 重读《AI协作铁律与踩坑复盘》**V1.10** ＋ 本文件。
2. 主线＝**M6 接前端**（→ 开发计划 M6-05 ~ M6-12：路由收口 ＋ 登录页打磨 ＋ 工作台 / 关系列表 / 详情 / 录入页补全 ＋ 角色矩阵落地）。
3. 收尾三件：三绿 ＋ **真起 `node dist/main`** ＋ `gen:types` 核对 diff ＋ 逐文件 `git add`（**禁 `-A`**）。
