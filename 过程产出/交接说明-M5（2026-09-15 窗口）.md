# 交接说明（M1~M5 ✅ · **M6 进行中（07 / 08 ✅ · 09 后端两片 ✅）** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（**缺口不在本节** → `过程产出/欠账登记表.md`）。读法：本文件 →《**AI执行清单**》（每轮必读）→《开发计划-V1》M6 判据；坑**按需查**《AI协作铁律与踩坑复盘》（案例库）；口径＝需求 V1.30 / 数据架构 V1.36 / 接口 V1.23 / 前端 V1.16 / 设计规范 V1.0 ＋《架构设计说明》V1.6。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（真进程 `node dist/main`，**按 `dist/main` 杀**）；前端 `前端/` 内 `npm run dev`（**5173**，`/api`→3000）。基线：后端 `tsc 0 / lint 0` ＝ **45 suites · 642 tests**；前端 build 绿。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5（①②③＋收尾 A/B） | ✅ `35b7fd0` `5317e78` `f0c34a3` `81bc170` `d98752b` |
| M6-05 路由收口＋登录接路由 | ✅ `42aeb5b` |
| M6-06 工作台补全 | ✅ `b978965` |
| M6-07 列表补全（分页 / 筛选栏） | ✅ 2026-09-16（`6362fd6` / `73605a3`） |
| M6-08 关系详情页（详情 ＋ 成员） | ✅ 2026-09-16（`31452ae`） |
| **M6-09 录入页补全（写跟单 / 快速标记）** | ⏳ **后端三片已落**：① `GET /org/product-lines` ＋ `POST /events/quick-mark`（`40d5aba`）；② **待关联归属**（`contact.owner_id` ＋ `GET /contacts` 可见范围 / `only_unlinked`，`3198a49`）；③ **快速标记开放 `contact_ids`（归属人限定）＋ `owner_flag` 改「在位才占位」**（`0008`）。**前端未动** |
| M6 其余（10 角色矩阵 / 11 主题 / 12 提交） | ⬜ 下一步 ＝ **M6-09 前端** |

**硬口径（勿按旧注释做）**：① 审计＝增删改写走切面 `@Audit('模块.动词')`、查看走 `recordStandalone`、401/403 由过滤器写 `authz.denied`；② 可写角色判定＝`kernel/data-scope/write-role.ts`（例外见 §7.7 / §7.9）；③ 数据范围**判定在 kernel、落地在各域**；④ 前端有 `vue-router`（**pinia 仍未引**）、请求层**不 import router**；⑤ 注释**不绑文档版本号**；⑥ **分页**归一化只在 `kernel/common/pagination.ts`；⑦ **快速标记**＝不写幂等键、不回写 `last_event_at`、`action_type` 固定 `phone`，`contact_ids` 限**归属人**（→ §6.1 ⑦⑨）；⑧ **待关联**：归属＝`contact.owner_id`、未挂公司不占部门 / 产品线、归属可改（只留痕）、工作量按**写单人**算（→ §6.1 ⑦~⑪）。

**待七叔验**：真账号走列表翻页 / 时间线 / 联系人列表 UI（→ D-14）。

**2026-09-16（本轮）**：B / D 域 ＋ **两条增量迁移**（`0007` 待关联归属 / `0008` owner_flag 在位才占位，均已上真库）＋ 规格升版（需求 V1.30 / 数据架构 V1.36 / 接口 V1.23）＋ 全库版本指针联动。`45 suites · 645 tests` 绿、`tsc/lint 0`。**真库三验**：① 待关联线索只归属人可见（gm 为 all 档可见全部）；② 快速标记：自己的 OK、别人的（含 gm 标别人的）**403**；③ 撤销 owner 后可再落新 owner。

## 二、待拍板

**P-01 / P-02 / P-04 ✅ 已落地**：列表筛选参数（`view` / `urgency`）；前端目录复用规则（架构 §4.4 ＋ ESLint）；**待关联走「乙」**＝`contact.owner_id` 一列，不动 `relation_member`。

**P-03 / P-05 ✅ 已落地**（七叔拍板）：① `quick-mark` 开放 `contact_ids` —— 「待关联」线索**只有当前归属人能标**（别人 403）；② `owner_flag` 改**「只有在位者占位」**（`0008`）。→ D-23 / D-26 已闭环。

**P-06（待办）D-29 端点形状**：草案已给出，**待过目 → 写进接口 §5.6 → 再实现**。

## 三、欠账

> **唯一落点** → `过程产出/欠账登记表.md`（**只增不删**）。本轮：**D-23 / D-26 已闭环**；新增 **D-27**（待关联归属，口径已落地）· **D-28**（`GET /contacts` **公司维度收敛待补**：`company`(L2) 不可反向依赖 `relation`(L3)）· **D-29**（「补挂公司 → 激活关系 → 孤儿跟单挂过去」的**跨域编排 ＋ 端点规格未定义**，草案待过目）。

## 四、下个窗口开工动作

1. 读《**AI执行清单**》＋ 本文件 ＋ 欠账登记表；具体坑按需查案例库。
2. 主线＝**M6-09 前端** ⇒ **照 `过程产出/M6-09 施工单.md` 做**（该件写明了：3 步怎么做、改哪些文件、哪几件事**不做**、验收与收尾纪律；D-29 接口已落接口文档 V1.23、其**实现**另起一片）。
3. 收尾：动后端跑三绿（＋真起 `node dist/main`）；只动前端跑 `lint / build`；逐文件 `git add`（**禁 `-A`**）。
4. ⚠ 开工前 `git status --short` 确认工作树。
