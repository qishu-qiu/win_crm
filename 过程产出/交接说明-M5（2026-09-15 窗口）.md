# 交接说明（**D-29 关联公司动线（后端）已闭环** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》→《开发计划-V1》；坑按需查案例库。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**）。基线：后端三绿 ＝ 45 suites ＋ **687 用例**（内存紧 → `npm test -- --maxWorkers=2`）；前端 `lint / build` 双绿。本轮**未改库**（`0009` 仍最新）。

## 〇、★ 只留指针

**关联公司并激活业务关系（新落 · 后端）**：`POST /contacts/:id/activate-relation` —— **编排落 D 域**（唯一能同时俯视 B / C 的层）、同步调 service、**不新增领域事件**、三写**不共享事务**、顺序＝**前置校验 → C 建关系 → B 写就职 → D 搬跟单**（**先 C 后 B**：C 抛错时一行没落，反序会把线索卡死）、**只改 `relation_id`**（⇒ 可重入）。口径 → 接口 §5.6；实现 → **D-29**（✅）；⚠ 前端未接线 → **D-46**。

**公海（无主）＝ 可读不可写，唯一例外＝改「开发价值」**：需求 §6.3 / §十六 N12 · 接口 §2.4 `20408` / §5.6 · 前端 §5 第 9/10 条 · 架构 §7.2 · 废止表 **#38**；实现 → **D-32**。

**个人偏好**：主题 ＋ 侧栏展开**存账号**（`employee.theme` / `nav_open`，migration `0009`）；`PUT /account/preferences`（**部分更新**、**只能改自己**），读出口＝`GET /account/me`。口径 → 数据架构 A2 · 接口 §4.14.9 / §5.2 · 设计规范 §八.1 · 需求 §13.4；实现 → **D-37 / D-36⑥ / D-41①**；⚠ `notif` 未做 → **D-42**。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5 ｜ M6-05~08 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b`… |
| **M6（09~14）** | ✅ 2026-09-16~18：录入页 / 公海 **D-32** / 角色矩阵 / 主题 / 侧边栏 ＋ 偏好存账号 / 筛选入 URL（D-19·21·39·40）/ **M6-14 关联动线（后端）`eb4e7aa` → D-29** |

**硬口径**（审计 / 分层 / 数据范围 / 脱敏**见常驻的 `CODEBUDDY.md`，不重复**）：① 可写角色＝`kernel/data-scope/write-role.ts`；② 分页归一化只在 `kernel/common/pagination.ts`；③ 前端有 `vue-router`（**pinia 未引**）、请求层不 import router；④ 注释不绑版本号；⑤ 快速标记＝不写幂等键 / 不回写 `last_event_at` / `action_type` 固定 `phone` / `contact_ids` 限归属人；⑥ 待关联：归属＝`contact.owner_id`、不占部门·产品线、可改只留痕、工作量按写单人；⑦ dark 色值以设计规范 §八.7 为准；⑧ 菜单结构与可见性唯一落点＝`前端/src/access.ts`；⑨ **偏好**：**账号为权威**、本机只兜**首屏**、侧栏展开**空数组不回灌**；⑩ **表归谁谁写**：就职关系只走 B 域出口、跟单只由 D 域改；跨域出参**形状**继承别域 DTO（`ActivateRelationResultDto extends RelationVoDto`）。

**待七叔验（UI 只能人点）**：① 录入三步 / 联系人筛选 / 勾选批量标记（M6-09·13 三片）② 翻页 · 时间线（**D-14**）③ 公海页签「时间线」（**D-32**）④ 主题切换 ＋ 刷新不闪 ⑤ 筛选写 URL（**D-21/39**）⑥ 侧边栏（各角色项 / 展开收起 / 数量小标 / 换页展开 / 刷新保持）⑦ 头像菜单 ＋ 主题·折叠「换设备」。**本轮无 UI 变化**（前端只加封装、页面未接 → **D-46**），无需人验。

**证据**：后端三绿（45 suites / **687** 用例）＋ **真起 `node dist/main`** ＋ **真库真链路**（`wanghaitao`：待关联联系人 → 2 条孤儿跟单 → 调端点 → 关系建出 / `linked_events=2` / 就职行有中文 `position` / 跟单已挂新关系 → **重调 409**；三元组重复 **409 且零落库**；越权 · 只读 **403**；id 不存在 · 缺参 **400**；`operation_log` 有痕）—— **逐步明细见 `欠账登记表` D-29 行**。前端双绿 ＋ `gen:types` 纯新增 123 行。

## 二、待拍板

**P-01~P-07 ✅ 全部落地 / 已拍板**（沿革查 git）。**当前无待拍项**；本轮新登记 **D-45**（`POST /contacts/:id/events` 未实现）、**D-46**（关联动线前端未接线）。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。本轮收口 **D-29**；新增 **D-45 / D-46**；**D-41② 消息铃铛仍欠**。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表。
2. 主线候选＝**D-46**（前端接关联动线：后端已通、有真出口可接）· **D-45**（`POST /contacts/:id/events`，与 D-46 同域、可同批）· **F 域公海领取**（D-33，M7 起点）。
3. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起 `node dist/main`；**单测加 `-- --maxWorkers=2`**；逐文件 `git add`（**禁 `-A`**）。
4. ⚠ 开工前 `git status --short` 确认工作树。
