# 交接说明（**D-03 联系人详情（后端）已落地** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》→《开发计划-V1》。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**）。基线：后端三绿 ＝ 46 suites ＋ **701 用例**（内存紧 → `npm test -- --maxWorkers=2`）；前端 `lint / build` 双绿。本轮**未改库**（`0009` 仍最新）。
> ⚠ **并行窗口**：工作树里有**另一个窗口在改前端**（`views/*` 8 个 ＋ 新增 `ContactDetailView.vue`），**本窗未碰前端**。

## 〇、★ 只留指针

**联系人详情（新落 · 后端）**：`GET /contacts/:id` —— **详情给全号**（§2.8）；唯一例外＝**被上锁且查看者非落锁人** ⇒ `phone` 与 `extra_phones` **两键都不出现**（锁跟人）＋ `phone_locked_by` 给姓名；含**就职历史** ＋ **谈判特质**（`label` 走 **A 域 `DictService` 出口**）。可见性同列表（待关联只给归属人 403；`all` 档全看）。口径 → 接口 §5.5 / §2.8；实现 → **D-03**（⏳）；剩 `unlocked_until` → **D-04**、**详情页（页 15）** → **D-47**。

**关联公司并激活业务关系（后端）**：`POST /contacts/:id/activate-relation` —— 口径 → 接口 §5.6；实现 → **D-29**（✅，完整理由见该行）。⚠ 前端未接线 → **D-46**。

**公海（无主）＝ 可读不可写，唯一例外＝改「开发价值」**：需求 §6.3 / §十六 N12 · 接口 §2.4 `20408` / §5.6 · 前端 §5 第 9/10 条 · 架构 §7.2 · 废止表 **#38**；实现 → **D-32**。

**个人偏好**：主题 ＋ 侧栏展开**存账号**（`0009`）；`PUT /account/preferences`（**部分更新**、**只能改自己**）；读出口＝`GET /account/me`。口径 → 数据架构 A2 · 接口 §4.14.9 / §5.2；实现 → **D-37**；`notif` → **D-42**。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5 ｜ M6-05~08 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b`… |
| **M6（09~15）** | ✅ 2026-09-16~18：录入页 / 公海 **D-32** / 主题 / 侧边栏＋偏好存账号 / 筛选入 URL / **M6-14 关联动线 ＋ M6-15 联系人详情（均后端）** |

**硬口径**（审计 / 分层 / 数据范围 / 脱敏**见常驻 `CODEBUDDY.md`，不重复**）：① 可写角色＝`kernel/data-scope/write-role.ts`；② 分页归一化只在 `kernel/common/pagination.ts`；③ 前端有 `vue-router`（**pinia 未引**）、请求层不 import router；④ 注释不绑版本号；⑤ 快速标记＝不写幂等键 / 不回写 `last_event_at` / `action_type=phone` / `contact_ids` 限归属人；⑥ 待关联：归属＝`contact.owner_id`、不占部门·产品线、可改只留痕、工作量按写单人；⑦ dark 色值＝设计规范 §八.7；⑧ 菜单与可见性唯一落点＝`前端/src/access.ts`；⑨ 偏好：**账号为权威**、本机只兜首屏、侧栏空数组不回灌；⑩ **表归谁谁写**（就职→B 出口 / 跟单→D / 字典文案→A 出口）；跨域出参**形状**继承别域 DTO；⑪ **锁跟人**：上锁时主号 ＋ 备用号一并隐藏，**锁不挡自己**；⑫ **出口按能力分家**：A 域 `OrgService`（人 / 部门）与 `DictService`（**字典**）各出各的；`kernel/common/` 只放**纯形状**函数，**不认业务字段**。

**待七叔验（UI 只能人点）**：翻页 · 时间线（**D-14**）/ 公海页签「时间线」（**D-32**）/ 主题不闪 / 筛选写 URL（**D-21/39**）/ 侧边栏。**本轮无 UI 变化**，无需人验。

**证据**：后端三绿（46 suites / **701** 用例）＋ **真起 `node dist/main`** ＋ **真库真链路**（全号 · 锁两分支 · 特质 `label` 走 `DictService` · `nav_open` 走 kernel 版 `parseStringList` · 403/200/400；数据已还原）—— **明细见 `欠账登记表` D-03 行**。前端双绿。

## 二、待拍板

**P-01~P-07 ✅ 已拍**（沿革查 git）。**当前无待拍项**；新登记 **D-47**（详情页未建）、**D-48**（§5.5 还有一批 B 域端点未实现）。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。本轮 **D-03 ⏳ 部分已补**（剩 → D-04）＋ 字典出口搬到 `DictService`（D-03 行已改指针）；**D-46 定位修正**；新增 **D-47 / D-48**；**D-41② 仍欠**。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表。
2. 主线候选＝**D-47**（详情页 ＋「关联公司」入口；⚠ 先看**并行窗口**是否已做掉）· **D-45** · **F 域公海领取**（D-33，M7 起点）。
3. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起 `node dist/main`；**单测加 `-- --maxWorkers=2`**；逐文件 `git add`（**禁 `-A`**）。
4. ⚠ 开工前 `git status --short` 确认工作树 —— **别人的文件不碰不提交**。
