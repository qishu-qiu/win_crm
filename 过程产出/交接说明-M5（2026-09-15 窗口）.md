# 交接说明（M1~M5 ✅ · **M6 进行中（05~09 ✅）** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口不在本节 → `过程产出/欠账登记表.md`）。读法：本文件 →《AI执行清单》（每轮必读）→《开发计划-V1》M6 判据；坑按需查案例库；口径＝需求 **V1.31** / 数据架构 V1.36 / 接口 **V1.24** / 前端 **V1.17** / 设计规范 V1.0 ＋《架构设计说明》**V1.7**。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（真进程 `node dist/main`，按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**，`/api`→3000）。基线：后端 `tsc 0 / lint 0` ＝ 45 suites（未重跑）；前端 `lint / build` 双绿（2026-09-16 实测）。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5 ｜ M6-05~08 | ✅ `35b7fd0` `5317e78` `f0c34a3` `81bc170` `d98752b` ｜ `42aeb5b` `b978965` `6362fd6` `73605a3` `31452ae` |
| **M6-09 录入页补全** | ✅ 2026-09-16：后端 `40d5aba` `3198a49` `e69ccce`（接口 `ff83ec8`）＋ 前端三片 `cf57e4a` `22a3e7c` `82f8c80` |
| **公海口径（2026-09-18 拍板）** | ✅ **只进了文档**（需求 V1.31 / 接口 V1.24 / 前端 V1.17 / 架构 V1.7 ＋ 废止表 #38）；**实现未做 → D-32** |
| M6-10~12（角色矩阵 / 主题 / 提交） | ⬜ 主线 ＝ **M6-10 角色矩阵**；⚠ **D-32 可插队**（口径已定、代码未动） |

**硬口径（勿按旧注释做）**：① 审计＝增删改写走 `@Audit('模块.动词')`、查看走 `recordStandalone`、401/403 由过滤器写 `authz.denied`；② 可写角色判定＝`kernel/data-scope/write-role.ts`；③ 数据范围**判定在 kernel、落地在各域**；④ 前端有 `vue-router`（**pinia 未引**）、请求层**不 import router**；⑤ 注释**不绑文档版本号**；⑥ 分页归一化只在 `kernel/common/pagination.ts`；⑦ **快速标记**＝不写幂等键、不回写 `last_event_at`、`action_type` 固定 `phone`、`contact_ids` 限**归属人**；⑧ **待关联**：归属＝`contact.owner_id`、不占部门 / 产品线、可改（只留痕）、工作量按**写单人**算；⑨ **公海 ＝ 可读不可写**（2026-09-18）：读＝**所属部门内**可翻阅历史跟进（跟单 ＋ 承诺 ＋ 阶段留痕；公司档案只给**计数**不给正文），别部门看不到；写＝一律 **422 / `20408`**，**唯一例外＝改开发价值 `value_tier`**；⚠ **代码尚未实现** → D-32。

**待七叔验（UI 必须人在浏览器点）**：① 录入页三步（含跳过公司）；② 联系人档案筛选；③ 列表勾选批量标记；④ 翻页 / 时间线（→ D-14）。

**证据**：**09-16**（M6-09 前端）vite 起 5173（三个改动页面模块 200、`/api/health` 代理 200）；真库（销售 王海涛）建公司→建联系人→激活关系（出参逐字段核对）、撞单 409 人话 / 越权 403 / 重号 409、跳过公司建「待关联」**只归属人可见**、`quick-mark` 连点两次各 `{marked:1}`。**09-17/18**（公海）销售读**别部门**公海：详情 403 / 时间线 403 / 列表空；**经理（管辖内）**对公海关系 `quick-mark {marked:1}`、写跟单落库（`owner_snapshot:null`、`branch:"sub"`）、改属性 200 ⇒ 即 **D-31**。

## 二、待拍板

**P-01~P-06 ✅ 已落地**：列表筛选参数｜前端目录复用规则｜待关联走「乙」｜`quick-mark` 限归属人｜`owner_flag` 在位才占位｜D-29 端点形状（**剩实现**）。

**P-07 ✅ 已拍板（2026-09-18）＝公海「可读不可写」**：读＝所属部门内可翻阅历史跟进全文（别部门看不到；公司档案只给计数不给正文）；写＝一律拒 **422 / `20408`**，**唯一例外＝改开发价值**（要跟单**先领取到私海**）。**实现未做 → D-32**。

## 三、欠账

> 唯一落点 → `过程产出/欠账登记表.md`（只增不删）。本轮新增 **D-32**（公海口径已进文档、实现未做：读门补「∪ **本部门**公海」＋ 写门判无主 ＋ 公司档案 `event_count_30d` ＋ 前端**留读封写**）；**D-31 已由该口径收口**（归属改指 D-32）。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件 ＋ 欠账登记表；坑按需查案例库。
2. 主线＝**M6-10 角色矩阵**（→ 前端文档 §4.2：可见性由 `role ＋ permission_matrix` 决定、**不可见页面不下发路由**；⚠ 现行导航是 4 项简化版，矩阵落地时一并收口）。
3. 可插队：**D-32**（公海口径实现，文档已写、照做即可）／**D-29**（关联公司动线，先核 `C`(L3) 不可反向依赖 `D`(L4)）。
4. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起一次 `node dist/main`；逐文件 `git add`（**禁 `-A`**）。
5. ⚠ 开工前 `git status --short` 确认工作树。
