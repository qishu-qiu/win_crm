# 交接说明（M1~M5 ✅ · **下一步 M6** · 下个窗口必读）

> **状态 / 缺口 / 待拍板唯一落点**。读法：本文件 →《AI协作铁律与踩坑复盘》V1.9 →《开发计划-V1》M6 判据；口径＝五份现行规格（需求 **V1.28** / 数据架构 **V1.33** / 接口 V1.18 / 前端 V1.16 / 设计规范 V1.0），沿革查 git。
> ⚠ **起服**：`服务端/` 内 `npm run start:web`；真进程是 `node dist/main` —— **按 `dist/main` 特征杀**。
> ⚠ **基线**：`tsc 0 / lint 0 / test` ＝ **44 suites · 609 tests**。
> ★ 2026-09-15 审计口径**已扩写**：**所有增删改都留痕**（等保）→ 需求 §4.2 ★ ＋ 架构 §7.4。

## 一、进度

| 里程碑 | 状态 | 判据 |
| --- | --- | --- |
| M1~M4 | ✅ | 三绿 39/563 |
| M5 第①批（01/02/03/08） | ✅ `35b7fd0` | sale 私海 `{5,3,1}` vs gm `{5,4,3,1}`；越权 403·20003 |
| M5 第②批（04/05/06/09 脱敏） | ✅ `5317e78` | 协同人写权维持 owner 唯一；联系方式默认全可见 ＋ 可上锁 |
| **M5 第③批（07 审计 ＋ 管理员查看留痕）** | ✅ **本轮** | 见下 |

### ★ 本批落点（**勿照旧注释盖楼**）

- **审计＝一处收口**：**所有增删改**由**统一切面**（`kernel/audit/audit.decorator.ts` 的 `@Audit('模块.动词','对象类型')` ＋ `shared/interceptors/audit-log.interceptor.ts`）自动写 `operation_log`；语义是读的写方法标 `@AuditSkip()`；**只认 POST/PUT/PATCH/DELETE、只在成功时写**。
- **动作名**：各域 `*_AUDIT_ACTIONS` 常量（**只增不改**），清单同步在**数据架构 A10**。
- **两条写入路径**：`record()`＝业务事务内（敏感动作，带 `before/after`，随业务回滚）；`recordStandalone()`＝**独立写入 best-effort**（查看类；写失败只记日志、**不阻断读**）。
- **查看留痕**：D 域 `listEvents` 里「**持 `admin` 角色**」→ 写 `event.view`（读路径无业务事务）。
- **机械自检**：`shared/audit-coverage.spec.ts` 扫「所有写端点必须显式 `@Audit`／`@AuditSkip`」，**漏标即红**。
- ⚠ **旧注释已纠正**：B / C 域文件头原写「不在 §7.4 清单 ⇒ 不写审计」＝**把举例当穷尽**（坑 23 同类），已改为「由统一切面留痕」。
- ⚠ **src 内规格版本号的历史引用（V1.27 / V1.3 等）本批未清理**（避免大范围中文替换，→ 坑 19）；**新增注释不绑版本号**。

### 本批实证（真进程 `node dist/main` ＋ 真库）

- 44 suites · 609 tests 全过；`tsc 0 / lint 0`；`/health` `db:up`（切面注入依赖后**真起过**）。
- **真库举证**：`POST /companies`（销售）→ 落 `company.create`（`operator_id` / `target_id` / `ip` / `ua` / `req_id` 齐）；`search-dup`（`@AuditSkip`）**增量 0**；管理员 `GET /relations/5/events` → **`event.view` 落库**；销售本人看 → **增量 0**。探针公司**已逻辑删**。
- `gen:types`：前端 `types.ts` **无 diff**（契约未漂移）。

## 二、★ 待拍板

| # | 谁 | 状态与落点 |
| --- | --- | --- |
| D / E | 锁消失落点；金额脱敏 | ✅ 已定（D 待 F 域掉公海落地；E 已落 `desensitize.ts`） |
| B | 架构 §7.2 / §7.3 措辞 | ⏳ 留到 M5 收尾一次改 |
| **F** | **管理员能不能建档**（`POST /companies`、`/contacts`） | ⏳ **待拍板**：现任何登录人（含管理员）都能建；建议按需求 §4.2 ★「一律只读」→ **拒**（403；`admin`＋`sale` 叠加仍可写，同 C 域口径）；规格举例未点"建档" → **只报不改** |
| **G** | 多角色叠加时的**查看留痕** | ⚠ **本批按字面从严**：持 `admin` 即留痕（`admin`＋`sale` 也记）—— 规格未定义，**从严不越权**，等确认 |

## 三、欠账（非阻塞）

1. `serving` 只做形状（E 域未建），暂按 `self` 收敛。
2. 报表 / 看板 / 汇总出口未建 ⇒ M5-06 只落开关 ＋ 单测。
3. `GET /contacts/:id` 未建 ⇒ 详情全号 / `phone_locked_by` / 24h `unlocked_until` 随它落地。
4. 上锁 / 解锁 / `phone_unlock` 审批未建 ⇒ `sea.phone.view`、`phone.unlock.*`、强制解锁留痕**随接口补**（点名项已列进 A10）。
5. 分页 / `GET /companies/:id` / `org/product-lines` / 前端 router·pinia / bundle → **M6**；`Idempotency-Key` 未做。
6. **越权尝试（401 / 403）是否也留痕**：等保常见要求，本批**只记成功的增删改** —— 待拍板。
7. `shared.module.spec.ts` 起真服务后 worker 未优雅退出（用例全绿）—— 测试资源泄漏。

## 四、下个窗口开工动作

1. 重读《AI协作铁律与踩坑复盘》V1.9 ＋ 本文件。
2. 主线＝**M6 接前端**（→ 开发计划 M6-05 ~ M6-12：路由收口 ＋ 登录页打磨 ＋ 工作台 / 关系列表 / 详情 / 录入页补全 ＋ 角色矩阵）。
3. 收尾三件：三绿 ＋ **真起 `node dist/main`** ＋ `gen:types` 核对 diff ＋ 逐文件 `git add`（**禁 `-A`**）。
