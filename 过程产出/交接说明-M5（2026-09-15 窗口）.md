# 交接说明（M1~M4 ✅ · **M5 第①②批 ✅** · 下个窗口必读）

> **状态 / 缺口 / 待拍板唯一落点**。读法：本文件 →《AI协作铁律与踩坑复盘》V1.9 →《开发计划-V1》M5 判据；口径＝五份现行规格（需求 V1.27 / 数据架构 V1.32 / 接口 V1.18 / 前端 V1.16 / 设计规范 V1.0），沿革查 git。
> ⚠ **起服**：`服务端/` 内 `npm run start:web`；真进程是 `node dist/main` —— **按 `dist/main` 特征杀**。
> ⚠ **基线**：`tsc 0 / lint 0 / test` ＝ **42 suites · 588 tests**（旧的 40/570 已过时）。

## 一、进度

| 里程碑 | 状态 | 判据 |
| --- | --- | --- |
| M1~M4 | ✅ | 三绿 39/563（M4 出口判据见 M4 提交） |
| M5 第①批（01/02/03/08） | ✅ `35b7fd0` | sale(03) 私海 `{5,3,1}` vs gm(01) `{5,4,3,1}`；越权 → 403·20003 |
| M5 口径（协同人写权） | ✅ `9ca55d6` | **维持 owner 唯一**：协同人只写跟单 |
| **M5 第②批（04/05/06/09 脱敏）** | ✅ **本轮完成 · 待提交** | 见下 |
| M5 第③批（审计 ＋ 管理员只读留痕 07） | ⬜ **下一步** | — |

### ★ 本批落点（**勿照旧注释盖楼**）

- 脱敏判定＝**kernel 纯函数** `kernel/common/desensitize.ts`（**按部门 ＋ 总经理例外**，2026-09-15 七叔口径）；报表豁免＝`@DesensitizeExempt()`（`kernel/context/desensitize-exempt.decorator.ts`，与 `@Public()` 同居 kernel —— 域 controller 要写它）。
- **拦截器不逐字段改写出参**：只做「读豁免标记 ＋ 打标到请求」；渲染在**各域 service 组装出参时**。
- 联系人「锁」＝**B 域私有**纯函数 `modules/company/domain/contact-lock.ts`；锁列查询时带出（`CONTACT_SELECT`）。
- 自检：`scope.type === '...'` 在 `modules/` ⇒ **0 命中**（M5-03）。

### 本批实证（真进程 `node dist/main` ＋ 真库）

- 42 suites · 588 tests 全过；`tsc 0 / lint 0`；`/health` `db:up`。
- `/docs-json` 的 `phone_locked`＝新口径 ⇒ 跑的是**新产物**；`gen:types` **仅 1 行描述注释**变化（类型未变），`Record<string, never>` 仅 2 处样板。
- **M5-04 举证**（临时上锁 contact 7、`by=3`，验完**已还原**）：落锁人王海涛(3) → `false`；非落锁人陈志远(gm) → `true` 且仍 `139****1111`（**列表不因上锁改形态**）。

## 二、★ 待拍板 / 已定

| # | 谁 | 状态与落点 |
| --- | --- | --- |
| A / C | 协同人写权；`/contacts` 收敛口径 | ✅ **均已定（2026-09-15）**：协同写权**归 owner 唯一**（只写跟单）；联系人＝**资料库共享**、列表一律打码（防批量捞号） |
| B | 架构说明 §7.2 / §7.3 **措辞** | ⏳ **留到 M5 收尾**一次改（文档写「拦截器注入范围」「出口统一处理」，与实现不符；要升版 ＋ 指针同步） |
| **D** | **锁「自动消失」的落点** | ✅ **已定**：在「解除业务关系」时清锁（**不在读时判**）。触发＝①离职/停用 ②转岗 ③关系掉公海（§4.3 二 ★）。⚠ **待落地**：三处分属 A / C / **F 域（掉公海，M7）**均未建；届时 **B 域出「按联系人清锁」出口**，「落锁人是否仍持有该联系人任一活跃关系」的判断放 **C / F 侧**。**本批读法不变**（查看者≠落锁人），偏严不越权 |
| **E** | **金额脱敏口径** | ✅ **已定：「本部门可查，其他部门不可查；总经理可查全部」** ⇒ 已落 `desensitize.ts`（按部门 ＋ `isGeneralManager`；**管理员不是例外**）。⚠ **待确认一格**：§4.3 四 首行「归属本人 / 协同人 ＝ 真金额」中的**跨部门协同人**按部门会被打码（七叔未点协同人，**只报不改** → 坑 24） |

## 三、欠账（非阻塞）

1. `serving` 只做形状（E 域未建），暂按 `self` 收敛。
2. 报表 / 看板 / 汇总出口未建 ⇒ M5-06 只落开关 ＋ 单测。
3. 管理员只读 ＋ 留痕 → 第③批。
4. **`GET /contacts/:id` 未建** ⇒「详情给全号」「上锁不给 `phone` ＋ `extra_phones` 一并隐藏」「`phone_locked_by`」「24h `unlocked_until`」随它落地。
5. 上锁 / 解锁接口与 `phone_unlock` 审批（G 域）未建。
6. 列表分页 / `GET /companies/:id` / `org/product-lines` / 前端 router·pinia / bundle 1.53 MB → **M6**；`Idempotency-Key` 未做。
7. **测试资源泄漏**：`shared.module.spec.ts` 起真 HTTP 服务后 worker 未优雅退出（`npm test` 末尾 `failed to exit gracefully`）；用例全绿。（原记 MEMORY，该文件 2026-09-15 废弃删除，本条迁此）

## 四、下个窗口开工动作（第③批 · 审计 ＋ 管理员只读留痕）

1. 重读铁律 V1.9 ＋ 本文件；基线 **42 suites · 588 tests**。
2. 敏感动作在**业务事务内**写 `operation_log`（随业务回滚）；管理员「只读 ＋ 查看留痕 ＋ 不解除金额脱敏」。
3. 收尾：三绿 ＋ **真起 `node dist/main`** ＋ `gen:types` 核对 diff ＋ 逐文件 `git add`（**禁 `-A`**）。
