# 交接说明（M1~M5 ✅ · **M6 进行中（05~09 ✅）** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口不在本节 → `过程产出/欠账登记表.md`）。读法：本文件 →《AI执行清单》（每轮必读）→《开发计划-V1》M6 判据；坑按需查案例库；口径＝需求 **V1.32** / 数据架构 V1.36 / 接口 **V1.25** / 前端 **V1.18** / 设计规范 V1.0 ＋《架构设计说明》**V1.8** / 废止表 **V1.13**。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（真进程 `node dist/main`，按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**，`/api`→3000）。基线：后端 `tsc 0 / lint 0` ＝ 45 suites（未重跑）；前端 `lint / build` 双绿。

## 〇、★ 本窗口（2026-09-17/18）拍板约定 —— **下个窗口先看这节**

**公海（无主关系）＝「可读不可写」，四条**：
1. **读**：跟单全文 ＋ 承诺 ＋ 阶段留痕**可翻阅**（看不到历史就判断不出值不值得捞）；范围＝**该关系所属部门**：**销售＝本部门**（**别部门 → 403**）、经理＝管辖部门、总经理 / 管理员＝全部；**交付 / 客服不进公海**。
2. **写只开一个口**：`PUT /relations/:id` **只传 `value_tier`** —— **销售也能标本部门公海的开发价值**（开发价值＝**部门共同维护**；判定＝**在我读范围内 ＋ 可写角色**，**⚠ 不看 owner** —— 无主关系没有 owner 可看；其余字段一起传也算写）。
3. **其余写动作一律拒** → **422 / `20408`**：写跟单 · 快速标记 · 建改承诺 · 换阶段 · 加删成员；**写跟单只能在私海**（先领取到私海）。
4. **公司档案只给「基本资料 ＋ 跟单计数（近 30 天 N 次）」、不给正文**（**经理视角同样只给计数**）；正文只在**关系层**看。

**代码未实现 → D-32**（照四条做）；落点：需求 §6.3 / §十六 N12 · 接口 §2.4 / §5.6 · 前端 §5 第 9/10 条 · 架构 §7.2 · 废止表 **#38**。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5 ｜ M6-05~08 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b` `b978965` `6362fd6` `73605a3` `31452ae` |
| **M6-09 录入页** | ✅ 2026-09-16：后端 `40d5aba` `3198a49` `e69ccce` ＋ 前端 `cf57e4a` `22a3e7c` `82f8c80` |
| **公海口径** | ✅ 2026-09-18 拍板并进文档；**实现未做 → D-32** |
| M6-10~12 | ⬜ 主线 ＝ **M6-10 角色矩阵**；⚠ **D-32 可插队** |

**硬口径**：① 审计＝增删改写 `@Audit('模块.动词')`、查看 `recordStandalone`、401/403 过滤器写 `authz.denied`；② 可写角色＝`kernel/data-scope/write-role.ts`；③ 数据范围**判定在 kernel、落地在各域**；④ 前端有 `vue-router`（**pinia 未引**）、请求层**不 import router**；⑤ 注释**不绑版本号**；⑥ 分页归一化只在 `kernel/common/pagination.ts`；⑦ **快速标记**＝不写幂等键、不回写 `last_event_at`、`action_type` 固定 `phone`、`contact_ids` 限**归属人**；⑧ **待关联**：归属＝`contact.owner_id`、不占部门 / 产品线、可改（只留痕）、工作量按**写单人**算。

**待七叔验（UI 只能人点）**：① 录入三步 ② 联系人档案筛选 ③ 列表勾选批量标记 ④ 翻页 / 时间线（→ D-14）。

**证据**：**09-16** vite 起 5173（改动页面模块 200、`/api/health` 200）＋ 真库录入动线（撞单 409 人话 / 越权 403 / 重号 409 /「待关联」只归属人可见 / `quick-mark` 各 `{marked:1}`）；**09-17/18** 公海实测见 **D-31**（销售读别部门公海 403，经理管辖内可写）→ 已被 §〇 收口。

## 二、待拍板

**P-01~P-07 ✅ 全部落地 / 已拍板**（末项＝**公海「可读不可写」＋ 开发价值部门共同维护** → §〇）。**当前无待拍项**（沿革查 git）。

## 三、欠账

> 唯一落点 → `过程产出/欠账登记表.md`（只增不删）。**D-32**（公海口径：读门补「∪ **本部门**公海」＋ 写门判无主且放行 `value_tier`（**销售也在内、不看 owner**）＋ 公司档案 `event_count_30d` ＋ 前端**留读封写**）；**D-31 已由该口径收口**。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表；坑按需查案例库。
2. 主线＝**M6-10 角色矩阵**（→ 前端文档 §4.2：可见性由 `role ＋ permission_matrix` 决定、**不可见页面不下发路由**；⚠ 现行导航 4 项是简化版，落地时一并收口）。
3. 可插队：**D-32**（公海口径实现，四条已写清）／**D-29**（关联公司动线，先核 `C`(L3) 不可反向依赖 `D`(L4)）。
4. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起一次 `node dist/main`；逐文件 `git add`（**禁 `-A`**）。
5. ⚠ 开工前 `git status --short` 确认工作树。
