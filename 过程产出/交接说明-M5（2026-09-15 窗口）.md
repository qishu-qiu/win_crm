# 交接说明（M1~M5 ✅ · **M6 进行中（05~09 ✅）** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口不在本节 → `过程产出/欠账登记表.md`）。读法：本文件 →《AI执行清单》（每轮必读）→《开发计划-V1》M6 判据；坑按需查案例库；口径＝需求 V1.30 / 数据架构 V1.36 / 接口 V1.23 / 前端 V1.16 / 设计规范 V1.0 ＋《架构设计说明》V1.6。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（真进程 `node dist/main`，按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**，`/api`→3000）。基线：后端 `tsc 0 / lint 0` ＝ 45 suites（本轮未动后端，未重跑）；前端 `lint / build` 双绿（本轮实测）。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5 | ✅ `35b7fd0` `5317e78` `f0c34a3` `81bc170` `d98752b` |
| M6-05 ~ M6-08 | ✅ `42aeb5b`（路由收口）· `b978965`（工作台）· `6362fd6` `73605a3`（列表分页筛选）· `31452ae`（关系详情） |
| **M6-09 录入页补全** | ✅ **2026-09-16 落地**：后端 `40d5aba` `3198a49` `e69ccce`（接口 `ff83ec8`）＋ **前端三片** `cf57e4a`（录入页 3 步）· `22a3e7c`（联系人档案）· `82f8c80`（批量快速标记） |
| M6-10~12（角色矩阵 / 主题 / 提交） | ⬜ **下一步 ＝ M6-10 角色矩阵**；《M6-09 施工单》已完成（不必再照它做），其「关联公司动线」＝ D-29 另起一片 |

**硬口径（勿按旧注释做）**：① 审计＝增删改写走 `@Audit('模块.动词')`、查看走 `recordStandalone`、401/403 由过滤器写 `authz.denied`；② 可写角色判定＝`kernel/data-scope/write-role.ts`；③ 数据范围**判定在 kernel、落地在各域**；④ 前端有 `vue-router`（**pinia 未引**）、请求层**不 import router**；⑤ 注释**不绑文档版本号**；⑥ 分页归一化只在 `kernel/common/pagination.ts`；⑦ **快速标记**＝不写幂等键、不回写 `last_event_at`、`action_type` 固定 `phone`、`contact_ids` 限**归属人**；⑧ **待关联**：归属＝`contact.owner_id`、不占部门 / 产品线、可改（只留痕）、工作量按**写单人**算；⑨ **公海不做动作**：前端已只给私海勾选框，服务端**尚未拦** → D-31。

**待七叔验（UI 必须人在浏览器点）**：① 录入页三步（含跳过公司）；② 联系人档案筛选；③ 列表勾选批量标记；④ 翻页 / 时间线（→ D-14）。

**本轮（2026-09-16）**：M6-09 前端三片 ＋ 新增 `api/org.ts` / `src/contact.ts` / `views/ContactListView.vue`。真进程验：vite 起 5173（三个改动页面模块均 200、`/api/health` 代理 200）。真库验（销售 王海涛）：建公司→建联系人→激活关系（出参逐字段核对）、撞单 **409 人话**、部门越权 **403**、重号 **409**、跳过公司建「待关联」**只归属人可见**、`quick-mark` 连点两次各 `{marked:1}`。

## 二、待拍板

**P-01~P-05 ✅**：列表筛选参数｜前端目录复用规则（架构 §4.4＋ESLint）｜待关联走「乙」｜`quick-mark` 开 `contact_ids`（限归属人）｜`owner_flag` 改「在位才占位」。

**P-06（待办）D-29**：端点形状已写进接口 §5.6，**剩实现**（跨 B/C/D 三域，先核层级与 EventBus）。

**P-07（新）D-31 公海可写改哪层**：**谁**＝C 域服务端判定；**干什么的**＝现只判「读得到＋可写角色」，**没拦无 owner 的公海关系**；**建议**＝① C 域加「公海不可写」判定，或 ② 等 M7「领取到私海」一起收口（前者动已上线行为）；**影响**＝不改则需求 §6.3「公海内不做任何动作」形同虚设。

## 三、欠账

> 唯一落点 → `过程产出/欠账登记表.md`（只增不删）。本轮新增 **D-30**（撞库分支 3 的「对方部门名＋日期」**无出口** ⇒ 做不出；⚠《M6-09 施工单》原列"本片做"，**以欠账表为准**）· **D-31**（公海关系仍可写）。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件 ＋ 欠账登记表；坑按需查案例库。
2. 主线＝**M6-10 角色矩阵**（→ 前端文档 §4.2：可见性由 `role ＋ permission_matrix` 决定、**不可见页面不下发路由**；⚠ 现行导航是 4 项简化版，矩阵落地时一并收口）。
3. 可插队：**D-29 关联公司动线实现**（接口已定义、实现未做；`C`(L3) 不可反向依赖 `D`(L4)，先核层级）。
4. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起一次 `node dist/main`；逐文件 `git add`（**禁 `-A`**）。
5. ⚠ 开工前 `git status --short` 确认工作树。
