# 交接说明（M1~M5 ✅ · **M6 进行中** · 下个窗口必读）

> **状态 / 缺口 / 待拍板唯一落点**。读法：本文件 →《AI协作铁律与踩坑复盘》**V1.10** →《开发计划-V1》M6 判据；口径＝需求 **V1.29** / 数据架构 **V1.34** / 接口 **V1.19** / 前端 V1.16 / 设计规范 V1.0 ＋《架构设计说明》**V1.5**。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（真进程 `node dist/main`，**按 `dist/main` 杀**）；前端 `前端/` 内 `npm run dev`（**5173**，`/api`→3000）。基线：后端 `tsc 0 / lint 0` ＝ **44 suites · 618 tests**；前端 build 绿（主包 1.28 MB）。

## 一、进度与硬口径

| 里程碑 | 状态 |
| --- | --- |
| M1~M5（①②③＋收尾 A/B） | ✅ `35b7fd0` `5317e78` `f0c34a3` `81bc170` `d98752b` |
| **M6-05 路由收口＋登录接路由** | ✅ `42aeb5b` |
| **M6-06 工作台补全**（今日概览 3 数 ＋ 今日安排 ＋ 同类提醒合并） | ✅ `b978965` |
| M6 其余（07 列表＋分页 / 08 详情 / 09 录入 / 10 角色矩阵 / 11 主题 / 12 提交） | ⬜ **下一步 ＝ M6-07** |

**硬口径（勿按旧注释做）**：① **审计**＝增删改走统一切面 `@Audit('模块.动词')`、查看类 `recordStandalone`（不阻断读）、**401/403** 由全局过滤器写 `authz.denied`；② **可写角色**唯一判定＝`kernel/data-scope/write-role.ts`（`sale`/`dept_manager`/`gm` 可写，`admin`/交付/客服**只读**，多角色取"能写"；**不走它**＝台账编辑 §7.7 / 组织字典维护 / §7.9 兜底转交）；③ **数据范围判定在 kernel、落地在各域**，拦截器**不查库、不改写出参**；④ **前端**已引 vue-router（**pinia 仍未引**，跨页状态在 `src/session.ts`），守卫管"进不进得来"、`GET /account/me` 管会话有效、**401** 由 `api/request.ts` 派事件令 App 跳 `/login`，★ 请求层不 import router；⑤ **代码注释不绑文档版本号**（写「《文档名》§X」，不写 `V1.xx`）。

**本窗口/待验**：路由表只收**已建成 4 页**（`/login`、`/`、`/entry`、`/relations`）＋未知路径回工作台，懒加载后主包 1.53 → 1.28 MB；⚠ **待七叔验**（我无账号密码）：真账号登录 ① 直进工作台 ② 顶栏三导航 ③ 未登录敲 `/relations` 弹回 `/login` ④ 退出。后续可做：AntD 按需引入、`Idempotency-Key`。
**2026-09-16 补记**：**M6-06 工作台补全已落**（`b978965`，只动 `前端/`；后端**实测** 44 suites · 618 tests 全绿）；⚠ 本轮**未重测前端 build / 主包体积**。

## 二、待拍板

**当前无待拍板项。** 2026-09-16 已把《废止口径登记表》**37 条逐项核验**（30 ✅ / 6 残留 / 0 缺落点），6 处残留**已全部收口**（README ×2 · 需求 ×1 · 构想图 ×1 · `0001_init` 列 COMMENT ×1；明细查 `git show`）。新议题请在本表登记。

## 三、欠账（非阻塞）

1. `serving` 只做形状（E 域未建）⇒ 暂按 `self` 收敛。
2. 报表 / 看板 / 汇总出口未建 ⇒ M5-06 只落开关＋单测。
3. `GET /contacts/:id` 未建 ⇒ 详情全号 / `phone_locked_by` / 24h `unlocked_until` 随它落地。
4. 上锁 / 解锁 / `phone_unlock` 审批未建 ⇒ `sea.phone.view`、`phone.unlock.*`、强制解锁留痕**随接口补**（已列 A10）。
5. 分页 / `GET /companies/:id` / `org/product-lines` → **M6**；幂等头未做。
6. `shared.module.spec.ts` 起真服务后 worker 未优雅退出。
7. ✅ **已清（2026-09-16）**：① src 内规格版本号引用**全去除** ＝ 93 文件 / 139 行（服务端 125 行·81 文件 ＋ 前端 14 行；原记"38 处"偏窄），`main.ts` OpenAPI 版本改 `v1`；② 登记表 37 条核验的 6 处残留全收口 —— 其中 `0001_init` 列 `COMMENT` 走**新增量 `0006_perm_key_comment`**（冻结基线不能改；已 `deploy` 并回读 `information_schema` 核对）。

## 四、下个窗口开工动作

1. 重读《AI协作铁律与踩坑复盘》**V1.10** ＋ 本文件。
2. 主线＝**M6-07 业务关系列表页补全（含分页）** → **08** 详情 → **09** 录入 → **10** 角色矩阵（不可见页**不下发路由**）→ **11** 主题 → **12** 提交。
3. 每片收尾：动后端跑三绿（＋真起 `node dist/main`）；只动前端跑 `npm run build`；逐文件 `git add`（**禁 `-A`**）。
