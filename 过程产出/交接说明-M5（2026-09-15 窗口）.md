# 交接说明（M1~M5 ✅ · **M6 进行中（07 / 08 ✅ · 09 后端片 ✅）** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（**缺口不在本节** → `过程产出/欠账登记表.md`）。读法：本文件 →《**AI执行清单**》（每轮必读）→《开发计划-V1》M6 判据；坑**按需查**《AI协作铁律与踩坑复盘》（案例库）；口径＝需求 V1.29 / 数据架构 V1.34 / 接口 V1.21 / 前端 V1.16 / 设计规范 V1.0 ＋《架构设计说明》V1.6。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（真进程 `node dist/main`，**按 `dist/main` 杀**）；前端 `前端/` 内 `npm run dev`（**5173**，`/api`→3000）。基线：后端 `tsc 0 / lint 0` ＝ **45 suites · 640 tests**；前端 build 绿（1.28 MB）。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5（①②③＋收尾 A/B） | ✅ `35b7fd0` `5317e78` `f0c34a3` `81bc170` `d98752b` |
| M6-05 路由收口＋登录接路由 | ✅ `42aeb5b` |
| M6-06 工作台补全 | ✅ `b978965` |
| M6-07 列表补全（分页 / 筛选栏） | ✅ 2026-09-16（`6362fd6` / `73605a3`） |
| M6-08 关系详情页（详情 ＋ 成员） | ✅ 2026-09-16（`31452ae`） |
| **M6-09 录入页补全（写跟单 / 快速标记）** | ⏳ 2026-09-16 **后端片已落**：`GET /org/product-lines`（解 D-05 一半）＋ `POST /events/quick-mark`（批量标记，**只开放 `relation_ids`** → D-23）；**前端未动** |
| M6 其余（10 角色矩阵 / 11 主题 / 12 提交） | ⬜ 下一步 ＝ **M6-09 前端** |

**硬口径（勿按旧注释做）**：① 审计＝增删改走切面 `@Audit('模块.动词')`、查看类 `recordStandalone`、401/403 由全局过滤器写 `authz.denied`；② 可写角色唯一判定＝`kernel/data-scope/write-role.ts`（`sale`/`dept_manager`/`gm`，多角色取"能写"；例外见 §7.7 / §7.9）；③ 数据范围**判定在 kernel、落地在各域**，拦截器不查库、不改写出参；④ 前端有 `vue-router`（**pinia 仍未引**，跨页状态在 `src/session.ts`），请求层**不 import router**；⑤ 代码注释**不绑文档版本号**；⑥ **分页**＝`page`/`page_size` → `{list,total,page,page_size}`，归一化**只在 `kernel/common/pagination.ts`**（→ §2.3 / §2.7）；⑦ **快速标记**＝**不写幂等键**（每次点击都该是一条独立记录）、**不回写 `last_event_at`**、`action_type` 固定 `phone`。

**2026-09-16（本轮）**：只动 A / D 域后端 ＋ `gen:types` 重跑；`45 suites · 640 tests` 绿、`tsc/lint 0`、前端 `lint/build` 绿。**真库验过**（gm `chenzhiyuan`）：新路径在 `/docs-json` 可见；`quick-mark` 连标**三次**都 `{marked:1}`、时间线 `no_answer` 计数 3→4（**不写幂等键＝每次点击都落一条**）、`last_event_at` **未变**；非法 `outcome` / `contact_ids` 均 **400**。**待七叔验**：真账号走列表翻页 / 时间线 UI（→ D-14）。

## 二、待拍板

**P-01 ✅ 已落地**（09-16）：列表筛选参数 `view` ＋ `urgency`（接口 V1.21，判定落点 `domain/relation-list-filter.ts`）；「逾期未跟进」档仍缺（等 `overdue`，→ D-09 / D-10），URL query 同步仍欠（D-21）。
**P-02 ✅ 已落地**（09-16）：前端目录 / 复用规则进《架构说明》**§4.4**（V1.6）＋ ESLint 机械卡点（D-18 / D-20）。

**P-03（新增 · 等你拍）「待关联公司」联系人的快速标记要不要开放**：
1. **谁** —— `POST /events/quick-mark`（"勾选多个客户 → 点一下：未联系 / 未接 / 说两句"）。
2. **干什么的** —— 它只认**业务关系 id**；"还没问到公司、只建了联系人"的客户（需求 §6.1 模型 B）**没有关系 ⇒ 标不了**。
3. **建议** —— 我建议**谁建的归谁**（建档人可标）；**不建议**按「同部门可见」放开（联系人没有 owner 概念，放开＝全部门可写别人的线头）。
4. **影响范围** —— 做：B 域开「联系人可写吗」出口 ＋ D 域放开 `contact_ids` ＋ 前端加入口；不做：只能"先建关系再标记"。

## 三、欠账

> **唯一落点** → `过程产出/欠账登记表.md`（**只增不删**）。本轮：**D-05 部分已补**（公司详情仍欠）、新增 **D-23**（`contact_ids` 未开放）、**D-24**（`quick-mark` 出参规格未给）。

## 四、下个窗口开工动作

1. 读《**AI执行清单**》（每轮必读）＋ 本文件 ＋ 欠账登记表；具体坑按需查案例库。
2. 主线＝**M6-09 前端**：录入页 3 步（联系人 → 公司 → 确认）＋ 第 2 步可跳 ＋ 激活业务关系（产品线走 `GET /org/product-lines`）＋ 撞库 4 分支（**分支 1 / 2 依赖 F / G 域未建 ⇒ 登记欠账，不摆假入口**）；再接**批量快速标记**入口（列表页勾选）。
3. 收尾：动后端跑三绿（＋真起 `node dist/main`）；只动前端跑 `lint / build`；逐文件 `git add`（**禁 `-A`**）。
4. ⚠ 开工前 `git status --short` 确认工作树。
