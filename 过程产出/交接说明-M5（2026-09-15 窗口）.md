# 交接说明（M1~M5 ✅ · **下一步 M6** · 下个窗口必读）

> **状态 / 缺口 / 待拍板唯一落点**。读法：本文件 →《AI协作铁律与踩坑复盘》V1.10 →《开发计划-V1》M6 判据；口径＝五份现行规格（需求 **V1.28** / 数据架构 **V1.33** / 接口 V1.18 / 前端 V1.16 / 设计规范 V1.0），沿革查 git。
> ⚠ **起服**：`服务端/` 内 `npm run start:web`；真进程是 `node dist/main` —— **按 `dist/main` 特征杀**。
> ⚠ **基线**：`tsc 0 / lint 0 / test` ＝ **44 suites · 618 tests**。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M4 | ✅ |
| M5 第①批（01/02/03/08） | ✅ `35b7fd0` |
| M5 第②批（04/05/06/09 脱敏） | ✅ `5317e78` |
| M5 第③批（07 审计 ＋ 查看留痕） | ✅ `f0c34a3` |
| **M5 收尾 A（只读建档门 ＋ 越权留痕 ＋ 举例穷尽审计）** | ✅ **本轮** |
| M5 收尾 B（架构 §7.2 / §7.3 措辞） | ⬜ **下一步**（纯文档） |

### ★ 两条已定的硬口径（**勿再按旧注释做**）

- **审计**：① **所有增删改**由统一切面写（域 controller 标 `@Audit('模块.动词')` ＋ `shared/interceptors/audit-log.interceptor.ts`，只在成功时写）；② **查看类**（管理员）走 `AuditService.recordStandalone()`（独立写入、best-effort、**不阻断读**）；③ **401 / 403 越权尝试**由**全局异常过滤器**写 `authz.denied` —— ⚠ 切面**看不到**守卫/服务抛的错，只有过滤器能覆盖所有失败出口；只记 401/403，不记 400/422。
- **可写角色**：唯一判定点 ＝ `kernel/data-scope/write-role.ts`（可写＝ `sale` / `dept_manager` / `gm`；`admin` / 交付 / 客服**只读**；**多角色取"能写"** —— `admin`＋`sale` 仍可写）。B 域建档已上这道门（**403 ＋ `company.read_only`**）。
  ⚠ **不在该判定范围**：**台账编辑**（需求 §7.7 明文归交付/客服，属"交付物"）、组织/字典维护、管理员点名例外（§7.9 兜底发起离职转交）—— 套了会把正当写权掐掉。

### ★ 2026-09-15「把举例当穷尽」通盘审计（**已 5 次翻车**，4 次已登记）

- **本次修**：架构 §7.4 敏感动作（B / C 域文件头原写"不写审计"）｜需求 §4.2 管理员"只读"举例（→ 曾放行**建档**）｜`relation` 接口描述残留"仅 `pending` → 422"（与拍板「**已标即算**」相反）｜前端文档 §99「交付/客服只给工单动作」（易读成漏掉台账编辑，已澄清＝仅快捷入口）。
- **已登记（同类）**：《废止口径登记表》**#31**（数据架构 A6 `perm_key` 举例被当既定）· **#36**（接口 §2.6 `urgency` 码）· **#37**（`stage` 英文码）。
- **根治**：读法铁律钉在**需求 §1.3**（括号列举**默认是举例**；只有写明「完整清单」「一律」或 §十六 表头那种"表里没有才代表允许"才算**穷尽**；**不许**由"清单外"推出"不做"、**也不许**推出"可以做"）＋ 铁律 §三 ＋ `CODEBUDDY.md §7` 各一条。
- **已知未修（低危 · 已注记）**：`domain-event.spec.ts` 的"一个不多一个不少"（架构 §5.3 标题是"**首批**"，已补"增补须先改规格"的注释）。

### 实证（真进程 `node dist/main` ＋ 真库）

- 44 suites · 618 tests 全过；`tsc 0 / lint 0`；`/health` `db:up`。
- 第③批：`company.create` / `event.view` 落库；`@AuditSkip` 与销售查看**增量 0**；探针数据**已逻辑删**。
- 本轮：管理员建档 → **403（`company.read_only`）**；`gen:types` 前端 `types.ts` **无 diff**。

## 二、★ 待拍板

| # | 谁 | 状态 |
| --- | --- | --- |
| B | 架构 §7.2 / §7.3 **措辞**（写"拦截器注入范围""出口统一处理"，与实现不符） | ⏳ **下一步一次改**（文档，无代码） |
| — | 数据架构 A6 竞品名册「`created_by/updated_by` **只允许 manager / admin** 维护」，而需求 §11.1 只说「**经理**维护」 | ⏳ 只报不改（是否给 admin 口子，等你一句话） |

## 三、欠账（非阻塞）

1. `serving` 只做形状（E 域未建）⇒ 暂按 `self` 收敛。
2. 报表 / 看板 / 汇总出口未建 ⇒ M5-06 只落开关 ＋ 单测。
3. `GET /contacts/:id` 未建 ⇒ 详情全号 / `phone_locked_by` / 24h `unlocked_until` 随它落地。
4. 上锁 / 解锁 / `phone_unlock` 审批未建 ⇒ `sea.phone.view`、`phone.unlock.*`、强制解锁留痕**随接口补**（已列进 A10）。
5. 分页 / `GET /companies/:id` / `org/product-lines` / 前端 router·pinia / bundle → **M6**；`Idempotency-Key` 未做。
6. `shared.module.spec.ts` 起真服务后 worker 未优雅退出（用例全绿）—— 测试资源泄漏。

## 四、下个窗口开工动作

1. 重读《AI协作铁律与踩坑复盘》**V1.10** ＋ 本文件。
2. **先收 M5 尾巴**：架构 §7.2 / §7.3 措辞 ＋ 指针同步（纯文档，见 §二 B）。
3. 主线＝**M6 接前端**（→ 开发计划 M6-05 ~ M6-12：路由收口 ＋ 登录页打磨 ＋ 工作台 / 关系列表 / 详情 / 录入页补全 ＋ 角色矩阵）。
4. 收尾三件：三绿 ＋ **真起 `node dist/main`** ＋ `gen:types` 核对 diff ＋ 逐文件 `git add`（**禁 `-A`**）。
