# 交接说明（**M6-16 联系人详情页 ＋「关联公司」动线（前端）已落地** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》→《开发计划-V1》。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**）。基线：后端三绿 ＝ 45 suites ＋ **696 用例**（内存紧 → `npm test -- --maxWorkers=2`）；前端 `lint / build` 双绿。本轮**未改库**（`0009` 仍最新）。

## 〇、★ 只留指针

**联系人详情页（新落 · 页 15）**：`/contacts/:id` —— 详情**给全号**；上锁态（**锁跟人**：按 `'phone' in detail` 判，主号与备用号一起没）＋ 落锁人；就职 / 跳槽历史；谈判特质（`label` 走 **A 域字典出口**）；「⚠ 尚未关联公司」横幅 ＋**「关联公司」入口**（撞库 → 激活 → 跳关系详情）。口径 → 接口 §5.5 / §2.8；实现 → **D-47 / D-46 / D-13**（均 ✅）。⛔ 不摆假入口：申请解锁（**D-04**）、公司 / 员工可点（D-13 剩一半）、特质编辑（D-48）、悬浮卡（**D-50** 新增）。

**跨页复用件（新落 · 架构 §4.4）**：`components/CompanyDupPicker.vue`（撞库选公司；**取数由页面注入**，件不连 `api/`）、`components/RelationTargetPicker.vue`（部门 × 产品线）—— 录入页第 2 / 3 步改为复用。展示口径 → `src/company.ts` / `src/org.ts`；日期格式化 → `src/format.ts`（**唯一落点**）。

**公海（无主）可读不可写，唯一例外＝改「开发价值」**：→ D-32。**个人偏好**（主题 ＋ 侧栏存账号）→ D-37；`notif` → D-42。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5 ｜ M6-05~08 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b`… |
| **M6（09~16）** | ✅ 09-16~18：录入页 / 公海 D-32 / 角色矩阵 / 主题 / 侧边栏＋偏好存账号 / 筛选入 URL / M6-14·15（后端）/ **M6-16 详情页 ＋ 关联动线（前端）`94d3fe4`** |

**硬口径**（审计 / 分层 / 数据范围 / 脱敏见常驻 `CODEBUDDY.md`，不重复）：① 可写角色＝`kernel/data-scope/write-role.ts`；② 分页归一化只在 `kernel/common/pagination.ts`；③ 注释不绑版本号；④ 菜单与页面可见性唯一落点＝`前端/src/access.ts`；⑤ 偏好**账号为权威**（本机只兜首屏）；⑥ 表归谁谁写（就职→B / 跟单→D / 字典文案→A）；⑦ **锁跟人**：主号 ＋ 备用号一并隐藏，**锁不挡自己**；⑧ **提交不靠 `<a-form @finish>`**（无 `:model` / `name` 时**不触发** → 坑 33），一律 `@click` ＋ `@press-enter`；⑨ 跨页复用件一律进 `components/`。

**本轮已自动验（浏览器真机走查）**：详情页全流程 —— 查重 → 用这家 → **409 人话且零落库** → 换三元组成功 → 跳 `/relations/9`；录入页三步 ＋ 手机号命中分支；关系详情 / 时间线日期渲染；走查写入的 **4 行已从 dev 库删净**（复核 `rels=8`、`contact12 links=0`）。
**待七叔验（只剩"眼睛看"的部分）**：新页视觉（上锁态文案 / 特质与标签排版 / 就职历史表）；其余旧项 → **D-14**。

**证据**：前端 `lint / build` 双绿（新页与两个件都已分包）；真库前后对照（`rels` 8 → 9 → **8**）；明细见 `欠账登记表` D-46 / D-47 行。

## 二、待拍板

**P-01~P-07 ✅ 已拍**（沿革查 git）。**当前无待拍项**。本轮新登记 **D-49**（`extra_phones[].type` 两套值域 ＋ `gender` 无值域）、**D-50**（悬浮卡未建）。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。本轮：**D-47 / D-46 ✅**、D-13 联系人那一半 ✅、D-03 的「详情页」收口、**D-43 澄清为「量法不一致」**（按 §11 ④ 的规定量法 `git cat-file -s` ＝ 3062 ≤ 3072，**无需压缩**）；新增 **D-49 / D-50**；**D-04**（解锁审批）/ **D-41②**（消息铃铛）仍欠。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表。
2. 主线候选＝**D-45**（`POST /contacts/:id/events`，待关联记跟单）· **D-33**（F 域公海领取，M7 起点）· **D-43**（37 字节压缩）· **D-44**（页 28 个人资料 / 改密，**要先拍板**）。
3. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起 `node dist/main`；单测加 `-- --maxWorkers=2`；**逐文件 `git add`（禁 `-A`）**。
4. ⚠ 开工前 `git status --short` 确认工作树；**本轮已 2 次提交 ＋ 全文读过 ≥2 个大文件 ⇒ 建议换窗**（换窗前：更新本文件 ＋ 提交 ＋ 树干净）。
