# 交接说明（M1~M5 ✅ · **M6 已闭环（05~12 ✅ ＋ D-32 插队 ✅）** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》（每轮必读）→《开发计划-V1》剩余判据；坑按需查案例库；口径＝五份现行规格 ＋ 架构 V1.8 / 废止表 V1.13。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**，`/api`→3000）。基线：后端 `tsc 0 / lint 0` ＝ 45 suites（未重跑）；前端 `lint / build` 双绿。

## 〇、★ 公海口径（2026-09-18 拍板 · **已实现**）—— 只留指针

**公海（无主）＝ 可读不可写，唯一例外＝改「开发价值」**。全文：需求 §6.3 / §十六 N12 · 接口 §2.4 `20408` / §5.6 · 前端 §5 第 9/10 条 · 架构 §7.2 · 废止表 **#38**；实现与实测 → **D-32**（③ 随 **D-05**）。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5 ｜ M6-05~08 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b`…`31452ae` |
| **M6-09 录入页** | ✅ 2026-09-16 `40d5aba` `3198a49` `e69ccce` ＋ 前端 `cf57e4a` `22a3e7c` `82f8c80` |
| **公海口径（D-32）· M6-10 角色矩阵** | ✅ 2026-09-18 `2a94843` · `e1fdc7d` ＋ `adc5d94`（首屏越权漏拦）· `48657af`（D-34 方案 A） |
| **M6-11 主题** | ✅ 2026-09-18 `c94ec7a`：`theme.ts` ＋ `tokens.css` dark 块 ＋ `index.html` 内联预置 ＋ 页 28 ＋ 顶栏入口；**D-38 回填** `b30e1b7`（设计规范 §八.7 dark 色值表） |
| **M6-12 · D-21** | ✅ 2026-09-18（同批提交）：业务关系页筛选 ↔ URL query 双向同步（进页还原 / `replace` 写回 / 默认值不入 URL / 脏值回落并归一）；当场发现同类缺口 → 新登记 **D-39**。**M6 闭环**；可插队 **D-36** · **D-29** |

**硬口径**：① 审计＝增删改写 `@Audit('模块.动词')`、查看 `recordStandalone`、401/403 过滤器写 `authz.denied`；② 可写角色＝`kernel/data-scope/write-role.ts`；③ 数据范围**判定在 kernel、落地在各域**；④ 前端有 `vue-router`（**pinia 未引**）、请求层**不 import router**；⑤ 注释**不绑版本号**；⑥ 分页归一化只在 `kernel/common/pagination.ts`；⑦ **快速标记**＝不写幂等键、不回写 `last_event_at`、`action_type` 固定 `phone`、`contact_ids` 限**归属人**；⑧ **待关联**：归属＝`contact.owner_id`、不占部门 / 产品线、可改（只留痕）、工作量按**写单人**算；⑨ **主题**：唯一落点＝`前端/src/theme.ts`（`setTheme`）＋ `tokens.css` 的 dark 块 ＋ `index.html` 内联预置；AntD（含静态 `message`）由 `ConfigProvider` 接**同一份** token；**dark 色值以设计规范 §八.7 为准**，改它必须同批改 `tokens.css`。

**待七叔验（UI 只能人点）**：① 录入三步 ② 联系人档案筛选 ③ 列表勾选批量标记 ④ 翻页 / 时间线（→ D-14）⑤ 公海页签「时间线」：应能翻跟单 / 承诺，只出现「开发价值」一个可改项（→ D-32）⑥ **主题**：顶栏「外观」→ 切夜间**立即生效**（含表格 / 抽屉 / 弹层），**刷新不闪**且保持 ⑦ **筛选写 URL**（D-21）：选「跟进中」＋「周重点」→ 地址栏现 `?view=following&urgency=weekly`，**刷新 / 复制链接新开标签**结果一致；手改 `?view=overdue` 回落「我的全部」且地址栏被纠正；改筛选后「后退」回上一页。（M6-10 角色导航已走查）

**证据**：**09-16** 真库录入动线（撞单 409 人话 / 越权 403 / 重号 409 /「待关联」只归属人可见 / `quick-mark {marked:1}`）。**09-18**：D-32 真起 `dist/main` ＋ 4 真账号（读 200、四写 **422|20408**、`value_tier` 200、被拒写**零落库**）；M6-10 六账号核对 `role` ＋ 浏览器走查（修「首屏越权漏拦」）；M6-11 前端双绿 ＋ dev 真转译（`crm_theme` 两侧同键）；M6-12 / D-21 前端 lint / build 双绿（**URL 行为待人验**，见 ⑦）。

## 二、待拍板

**P-01~P-07 ✅ 全部落地 / 已拍板**。**当前无待拍项**（沿革查 git）。

## 三、欠账

> 唯一落点 → `过程产出/欠账登记表.md`（只增不删）。**D-21 已补**；**D-31 / D-32（剩 ③ 随 D-05）/ D-38 均已收口**；新增 **D-37**（主题存账号欠服务端）· **D-39**（联系人页筛选未写 URL）· **D-40**（执行清单超 3 KB，已压回）。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表；坑按需查案例库。
2. M6 已闭环；下窗口主线候选＝**D-36**（顶栏收口，只能部分：改标签 / 顺序，缺页不摆假入口）· **D-39**（联系人页筛选写 URL，照 D-21 同款写法）· **D-29**（关联公司动线，动手前先核 `C`(L3) 不可反向依赖 `D`(L4)）。
3. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起一次 `node dist/main`；逐文件 `git add`（**禁 `-A`**）。
