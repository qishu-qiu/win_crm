# 交接说明（M1~M5 ✅ · **M6 进行中（05~10 ✅ ＋ D-32 插队 ✅）** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》（每轮必读）→《开发计划-V1》M6 判据；坑按需查案例库；口径＝五份现行规格（需求 V1.32 / 接口 V1.25 / 前端 V1.18…）＋ 架构 V1.8 / 废止表 V1.13。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**，`/api`→3000）。基线：后端 `tsc 0 / lint 0` ＝ 45 suites（未重跑）；前端 `lint / build` 双绿。

## 〇、★ 公海口径（2026-09-18 拍板 · **已实现**）—— 只留指针

**公海（无主）＝ 可读不可写，唯一例外＝改「开发价值」**。全文：**需求 §6.3 / §十六 N12** · **接口 §2.4 `20408` / §5.6** · **前端 §5 第 9/10 条** · 架构 §7.2 · 废止表 **#38**；实现与实测 → **D-32**（③ 随 **D-05**）。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5 ｜ M6-05~08 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b` `b978965` `6362fd6` `73605a3` `31452ae` |
| **M6-09 录入页** | ✅ 2026-09-16：后端 `40d5aba` `3198a49` `e69ccce` ＋ 前端 `cf57e4a` `22a3e7c` `82f8c80` |
| **公海口径（D-32）** | ✅ 2026-09-18 口径进文档 ＋ 实现落地（插队，证据见下） |
| **M6-10 角色矩阵** | ✅ 2026-09-18 `e1fdc7d`：新建 `前端/src/access.ts`（矩阵唯一落点）＋ 守卫 / 顶栏 / 登录落点按角色收敛 |
| M6-11~12 | ⬜ 主线 ＝ **M6-11 主题（白天 / 夜间）**（**下一步就做它**） |

**硬口径**：① 审计＝增删改写 `@Audit('模块.动词')`、查看 `recordStandalone`、401/403 过滤器写 `authz.denied`；② 可写角色＝`kernel/data-scope/write-role.ts`；③ 数据范围**判定在 kernel、落地在各域**；④ 前端有 `vue-router`（**pinia 未引**）、请求层**不 import router**；⑤ 注释**不绑版本号**；⑥ 分页归一化只在 `kernel/common/pagination.ts`；⑦ **快速标记**＝不写幂等键、不回写 `last_event_at`、`action_type` 固定 `phone`、`contact_ids` 限**归属人**；⑧ **待关联**：归属＝`contact.owner_id`、不占部门 / 产品线、可改（只留痕）、工作量按**写单人**算。

**待七叔验（UI 只能人点）**：① 录入三步 ② 联系人档案筛选 ③ 列表勾选批量标记 ④ 翻页 / 时间线（→ D-14）⑤ **公海页签「时间线」**：应能翻跟单 / 承诺（销售也翻得到本部门公海），只出现「开发价值」一个可改项（→ D-32）。（**M6-10 角色导航已走查**）

**证据**：**09-16** vite 5173 ＋ 真库录入动线（撞单 409 人话 / 越权 403 / 重号 409 /「待关联」只归属人可见 / `quick-mark` `{marked:1}`）。

**★ 09-18 D-32**（真起 `dist/main` ＋ 4 真账号，测完已还原；关系 2＝dept3 公海）：经理读 3 出口 200、四写 **422 / `20408`**（修复前会落库 → D-31）、只传 `value_tier` 200；销售读**本部门**公海 200（修复前 403）、标 `value_tier` 200、其余写 422、别部门 403；交付 / 客服 403；总经理读 200 / 写 422；被拒的写**零落库**（`summary='probe'`＝0）。

**★ 09-18 M6-10**：6 账号真登录核对 `role`（`wanghaitao` 挂 `sale+delivery` 取 `sale`）；**浏览器实走**：6 角色登录后顶栏项与落点**全对**（交付 / 客服无「建档」；管理员只剩「业务关系 / 联系人」、落点 `/relations`），交付手输 `/entry`、管理员手输 `/`、`/entry` 均被拦回首个可见页，销售手输 `/entry`、`/contacts` 不误拦。**走查揪出真 bug 已修**：首屏完整加载时校正跑在 `router.currentRoute` 仍是初始占位 ⇒ 落空（交付能停在录入页）；修法＝`bootstrap()` 加 `await router.isReady()`。

## 二、待拍板

**P-01~P-07 ✅ 全部落地 / 已拍板**（末项＝**公海「可读不可写」＋ 开发价值部门共同维护** → §〇）。**当前无待拍项**（沿革查 git）。

## 三、欠账

> 唯一落点 → `过程产出/欠账登记表.md`（只增不删）。**D-32 已补**（剩 ③ 随 **D-05**）；**新增 D-33 / D-34 / D-35 / D-36**（领取到私海属 F 域；一人多角色可见性合成；管理员留痕只在 D 域；顶栏未按 §4.1 收口）；**D-31 已收口**。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表；坑按需查案例库。
2. 主线＝**M6-11 主题（白天 / 夜间）**（→ 设计规范 §二 token；切换生效且无闪烁）。
3. 可插队：**D-29**（关联公司动线 `POST /contacts/:id/activate-relation`，先核 `C`(L3) 不可反向依赖 `D`(L4)）。
4. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起一次 `node dist/main`；逐文件 `git add`（**禁 `-A`**）。
5. ⚠ 开工前 `git status --short` 确认工作树。
