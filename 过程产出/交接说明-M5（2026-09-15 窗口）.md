# 交接说明（M1~M5 ✅ · **M6 进行中（05~11 ✅ ＋ D-32 插队 ✅）** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》（每轮必读）→《开发计划-V1》剩余判据；坑按需查案例库；口径＝五份现行规格 ＋ 架构 V1.8 / 废止表 V1.13。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**，`/api`→3000）。基线：后端 `tsc 0 / lint 0` ＝ 45 suites（未重跑）；前端 `lint / build` 双绿（2026-09-18 复核）。

## 〇、★ 公海口径（2026-09-18 拍板 · **已实现**）—— 只留指针

**公海（无主）＝ 可读不可写，唯一例外＝改「开发价值」**。全文：需求 §6.3 / §十六 N12 · 接口 §2.4 `20408` / §5.6 · 前端 §5 第 9/10 条 · 架构 §7.2 · 废止表 **#38**；实现与实测 → **D-32**（③ 随 **D-05**）。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5 ｜ M6-05~08 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b` `b978965` `6362fd6` `73605a3` `31452ae` |
| **M6-09 录入页** | ✅ 2026-09-16：后端 `40d5aba` `3198a49` `e69ccce` ＋ 前端 `cf57e4a` `22a3e7c` `82f8c80` |
| **公海口径（D-32）· M6-10 角色矩阵** | ✅ 2026-09-18 `2a94843` · `e1fdc7d` ＋ `adc5d94`（首屏越权漏拦修复）· `48657af`（D-34 方案 A） |
| **M6-11 主题（白天 / 夜间）** | ✅ 2026-09-18 `c94ec7a`：`theme.ts` ＋ `tokens.css` dark 块 ＋ `index.html` 内联预置 ＋ 页 28「外观设置」＋ 顶栏「外观」入口；**D-38 回填** `b30e1b7`（设计规范 **V1.1** §八.7 dark 色值表，前端文档 V1.20 同批） |
| M6-12 | ⬜ 下一步 ＝ **M6 提交**；M6 收尾可插队 **D-21**（筛选写 URL）· **D-36**（顶栏收口） |

**硬口径**：① 审计＝增删改写 `@Audit('模块.动词')`、查看 `recordStandalone`、401/403 过滤器写 `authz.denied`；② 可写角色＝`kernel/data-scope/write-role.ts`；③ 数据范围**判定在 kernel、落地在各域**；④ 前端有 `vue-router`（**pinia 未引**）、请求层**不 import router**；⑤ 注释**不绑版本号**；⑥ 分页归一化只在 `kernel/common/pagination.ts`；⑦ **快速标记**＝不写幂等键、不回写 `last_event_at`、`action_type` 固定 `phone`、`contact_ids` 限**归属人**；⑧ **待关联**：归属＝`contact.owner_id`、不占部门 / 产品线、可改（只留痕）、工作量按**写单人**算；⑨ **主题**：唯一落点＝`前端/src/theme.ts`（`setTheme`）＋ `tokens.css` 的 dark 块 ＋ `index.html` 内联预置；AntD（含静态 `message`）由 `ConfigProvider` 接**同一份** token，**不新增色值**；**dark 色值以设计规范 §八.7 为准**，改它必须同批改 `tokens.css`。

**待七叔验（UI 只能人点）**：① 录入三步 ② 联系人档案筛选 ③ 列表勾选批量标记 ④ 翻页 / 时间线（→ D-14）⑤ 公海页签「时间线」：应能翻跟单 / 承诺，只出现「开发价值」一个可改项（→ D-32）⑥ **主题**：顶栏「外观」→ 切夜间**立即生效**（含表格 / 抽屉 / 提示弹层），**刷新不闪**且保持；再切回白天。（M6-10 角色导航已走查）

**证据**：**09-16** vite 5173 ＋ 真库录入动线（撞单 409 人话 / 越权 403 / 重号 409 /「待关联」只归属人可见 / `quick-mark` `{marked:1}`）。**09-18 D-32**：真起 `dist/main` ＋ 4 真账号实测（经理读 3 出口 200、四写 **422 / `20408`**、只传 `value_tier` 200；销售读本部门公海 200、写 422、别部门 403；交付 / 客服 403；被拒的写**零落库**）。**09-18 M6-10**：6 账号真登录核对 `role` ＋ 浏览器实走顶栏与越权落点（揪出并修复「首屏完整加载时越权漏拦」）。**09-18 M6-11**：前端双绿 ＋ dev 5173 **真转译**新模块、内联预置已进产物（两侧键名同为 `crm_theme`）；**浏览器观感待走查**（见 ⑥）。

## 二、待拍板

**P-01~P-07 ✅ 全部落地 / 已拍板**。**当前无待拍项**（沿革查 git）。

## 三、欠账

> 唯一落点 → `过程产出/欠账登记表.md`（只增不删）。**D-31 已收口**、**D-32 已补**（剩 ③ 随 D-05）、**D-38 已补**（`b30e1b7` 设计规范 V1.1 §八.7）；M6-11 新增 **D-37**（主题「存账号」＝`PUT /account/preferences` 未实现 ＋ 数据架构无落点 ⇒ 现只**本机**持久化）。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表；坑按需查案例库。
2. 主线＝**M6-12 提交**（M6 闭环）；可插队 **D-21** / **D-36** / **D-29**（关联公司动线，先核 `C`(L3) 不可反向依赖 `D`(L4)）。
3. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起一次 `node dist/main`；逐文件 `git add`（**禁 `-A`**）。
4. ⚠ 开工前 `git status --short` 确认工作树。
