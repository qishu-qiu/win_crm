# 交接说明（**M6-17「待关联」记跟单（后端）已落地** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》→《开发计划-V1》。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**）。基线：后端三绿 ＝ 46 suites ＋ **711 用例**（内存紧 → `npm test -- --maxWorkers=2`）；前端 `lint / build` 双绿。本轮**未改库**（`0009` 仍最新）。

## 〇、★ 只留指针

**「待关联」记跟单（新落 · D-45）**：`POST /contacts/:id/events` —— 无关系形态（`relation_id` / `owner_snapshot` 双 `null`）；**只有该联系人当前归属人**能记（别人 / 已挂公司 → 403），有效沟通必写一句话结果；不碰 C 域出口、不回写 `last_event_at`。口径 → 接口 §5.7 第 4 行（判定待回填 → **D-51**）；实现 → `engine.service.ts#recordContactEvent`。⛔ 前端**无入口**（页 15 尚无"记跟单"，不摆假入口）。

**联系人详情页（页 15）**：`/contacts/:id` —— 详情给全号；上锁态（**锁跟人**）＋ 落锁人；就职 / 跳槽历史；特质（`label` 走 A 域字典出口）；「⚠ 尚未关联公司」横幅 ＋「关联公司」入口（撞库 → 激活 → 跳关系详情）→ D-47 / D-46 / D-13。⛔ 不摆假入口：申请解锁（**D-04**）、公司 / 员工可点（D-13 剩一半）、特质编辑（D-48）、悬浮卡（**D-50**）。

**跨页复用件（架构 §4.4）**：`components/CompanyDupPicker.vue` / `RelationTargetPicker.vue`；展示口径 `src/company.ts` / `src/org.ts`；日期格式化 `src/format.ts`（唯一落点）。

**公海（无主）可读不可写，唯一例外＝改「开发价值」** → D-32。**个人偏好**（主题 ＋ 侧栏存账号）→ D-37；`notif` → D-42。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5 ｜ M6-05~08 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b`… |
| **M6（09~17）** | ✅ 09-16~18：录入页 / 公海 D-32 / 角色矩阵 / 主题 / 侧边栏＋偏好存账号 / 筛选入 URL / M6-14·15（后端）/ **M6-16 页 15 ＋ 关联动线（前端）`94d3fe4`** / **M6-17「待关联」记跟单 `63eed5a`** |

**硬口径**（审计 / 分层 / 数据范围 / 脱敏见常驻 `CODEBUDDY.md`，不重复）：① 可写角色＝`kernel/data-scope/write-role.ts`；② 分页归一化只在 `kernel/common/pagination.ts`；③ 注释不绑版本号；④ 菜单与页面可见性唯一落点＝`前端/src/access.ts`；⑤ 偏好**账号为权威**（本机只兜首屏）；⑥ 表归谁谁写（就职→B / 跟单→D / 字典文案→A）；⑦ **锁跟人**：主号 ＋ 备用号一并隐藏，**锁不挡自己**；⑧ **提交不靠 `<a-form @finish>`**（无 `:model` / `name` 时不触发 → 坑 33），一律 `@click` ＋ `@press-enter`；⑨ 跨页复用件一律进 `components/`；⑩ **「待关联」线索的写权限＝当前归属人**（`contact.owner_id`，→ D-45 / D-51）。

**本轮已自动验（真库真机）**：M6-17 真起 `dist/main` 走查 —— 正常落库（出参 `owner_snapshot:null` / `branch:"sub"`）/ 重复 **409** / 缺 summary **422** / 非归属人 **403** / 已挂公司 **403**；**走查行已按 SQL 删净**。
**待七叔验（只剩"眼睛看"）**：页 15 视觉（上锁态文案 / 特质与标签排版 / 就职历史表）；其余旧项 → **D-14**。

**证据**：后端三绿（46 suites / **711 用例**）；前端 `types.ts` 经 `gen:types` 重生成（+46 行、无 `Record<string, never>`）；明细见 `欠账登记表` D-45 行。

## 二、待拍板

**P-01~P-07 ✅ 已拍**（沿革查 git）。**当前无待拍项**。新登记 **D-51**（待关联记跟单的判定口径待回填接口 §5.7）。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。本轮：**D-45 ✅**；新增 **D-51**（判定口径回填）；**D-04**（解锁审批）/ **D-33**（公海领取）/ **D-41②**（消息铃铛）/ **D-48** / **D-50** 仍欠。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表。
2. 主线候选＝**D-51**（回填接口 §5.7 判定句，最小）· **D-33**（F 域公海领取，M7 起点）· **D-28**（`GET /contacts` 数据范围收敛，B＋C 域）· **D-44**（页 28 个人资料 / 改密，**要先拍板**）。
3. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起 `node dist/main`；单测加 `-- --maxWorkers=2`；**逐文件 `git add`（禁 `-A`）**。
4. ⚠ 开工前 `git status --short` 确认工作树；**本轮已 2 次提交 ＋ 全文读过 ≥2 个大文件 ⇒ 建议换窗**（换窗前：更新本文件 ＋ 提交 ＋ 树干净）。
