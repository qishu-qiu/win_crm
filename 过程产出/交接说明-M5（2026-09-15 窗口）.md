# 交接说明（**M6 已闭环（05~12 ✅）** ＋ **D-36 前半：顶栏 → 侧边栏 ✅** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》（每轮必读）→《开发计划-V1》剩余判据；坑按需查案例库；口径＝五份现行规格 ＋ 架构 V1.8 / 废止表 V1.13。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**，`/api`→3000）。基线：后端 `tsc 0 / lint 0` ＝ 45 suites（未重跑）；前端 `lint / build` 双绿。

## 〇、★ 公海口径（2026-09-18 拍板 · **已实现**）—— 只留指针

**公海（无主）＝ 可读不可写，唯一例外＝改「开发价值」**。全文：需求 §6.3 / §十六 N12 · 接口 §2.4 `20408` / §5.6 · 前端 §5 第 9/10 条 · 架构 §7.2 · 废止表 **#38**；实现与实测 → **D-32**（③ 随 **D-05**）。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M5 ｜ M6-05~08 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b`…`31452ae` |
| **M6-09 录入页** | ✅ 2026-09-16 `40d5aba` `3198a49` `e69ccce` ＋ 前端 `cf57e4a` `22a3e7c` `82f8c80` |
| **公海口径（D-32）· M6-10 角色矩阵** | ✅ 2026-09-18 `2a94843` · `e1fdc7d` ＋ `adc5d94`（首屏越权漏拦）· `48657af`（D-34 方案 A） |
| **M6-11 主题** | ✅ 2026-09-18 `c94ec7a`（＋ **D-38 回填** `b30e1b7`） |
| **M6-12 · D-21 / D-39** | ✅ 2026-09-18 `9d477f7` / `0eeae9f`（列表页、联系人页筛选 ↔ URL query 双向同步）。**M6 闭环** |
| **D-36 前半（顶栏 → 侧边栏）** | ✅ 2026-09-18 `54b0169`：形态 ＋ §4.1 顺序用词 ＋ 分组折叠 ＋ 项数徽标 ＋ 名实不符收口；**仍欠** ⑤⑥⑦（→ D-36 行 / D-37 / **D-41**） |

**硬口径**：① 审计＝增删改写 `@Audit('模块.动词')`、查看 `recordStandalone`、401/403 过滤器写 `authz.denied`；② 可写角色＝`kernel/data-scope/write-role.ts`；③ 数据范围**判定在 kernel、落地在各域**；④ 前端有 `vue-router`（**pinia 未引**）、请求层**不 import router**；⑤ 注释**不绑版本号**；⑥ 分页归一化只在 `kernel/common/pagination.ts`；⑦ **快速标记**＝不写幂等键、不回写 `last_event_at`、`action_type` 固定 `phone`、`contact_ids` 限**归属人**；⑧ **待关联**：归属＝`contact.owner_id`、不占部门 / 产品线、可改（只留痕）、工作量按**写单人**算；⑨ **主题**：落点＝`theme.ts` ＋ `tokens.css` dark 块 ＋ `index.html` 内联预置，**dark 色值以设计规范 §八.7 为准**；⑩ **导航**：菜单结构与可见性的**唯一落点**＝`前端/src/access.ts`（`NAV_GROUPS` / `navGroupsOf`）—— **建一页在那里加一行**，外壳只渲染、不写角色判断；折叠状态存本机 `crm_nav_open`（**按账号 → D-37**）。

**待七叔验（UI 只能人点）**：① 录入三步 ② 联系人档案筛选 ③ 列表勾选批量标记 ④ 翻页 / 时间线（→ D-14）⑤ 公海页签「时间线」（→ D-32）⑥ 主题切换 ＋ 刷新不闪 ⑦ 筛选写 URL（D-21 / D-39）⑧ **侧边栏**：各角色菜单项与 §4.2 对得上；「客户档案」可展开 / 收起、**收起时组名后有数量小标**；换页自动展开当前组；**刷新后展开状态保持**；关系详情页时「业务关系」仍高亮。

**证据**：详录见 `git log --oneline -12` 与欠账表各行。要点：09-18 真起 `dist/main` ＋ 4 真账号实测（公海读 200、四写 **422|20408**、`value_tier` 200、**被拒的写零落库**）；M6-10 六账号核对 ＋ 浏览器走查；前端 `lint / build` 双绿；**侧边栏**另核 dev 真转译（`App.vue` / `access.ts` 200 且含新特征）。

## 二、待拍板

**P-01~P-07 ✅ 全部落地 / 已拍板**。**当前无待拍项**（沿革查 git）。

## 三、欠账

> 唯一落点 → `过程产出/欠账登记表.md`（只增不删）。**D-36 前半收口**；**新增 D-41**（顶部头像菜单 ＋ 消息铃铛 —— 随 D-37 一起做）。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表；坑按需查案例库。
2. M6 已闭环、D-36 前半已收；主线候选＝**D-37 ＋ D-41**（账号侧偏好 `PUT /account/preferences` ＋ 头像菜单 / 铃铛：要服务端 ＋ 数据架构落点，**动手前先拍**）· **D-29**（关联公司动线，先核 `C`(L3) 不可反向依赖 `D`(L4)）。
3. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起一次 `node dist/main`；逐文件 `git add`（**禁 `-A`**）。
4. ⚠ 开工前 `git status --short` 确认工作树。
