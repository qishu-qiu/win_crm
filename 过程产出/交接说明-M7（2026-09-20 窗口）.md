# 交接说明（**M7-03/04/05 已落（掉海预警）** ｜ F-01 片 2 已人验 ⇒ 片 3 解锁 · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》→《开发计划-V1》。
> ⚠ 起服 / 端口 / 常用命令 **见 `CODEBUDDY.md` §9**（Worker **恒 1 实例、不监听端口**）。基线：后端 **51 suites ＋ 765 用例**；前端双绿。**本轮未改库**（`0009` 最新）。

## 〇、★ 只留指针

**掉海预警（M7-03/04/05 · `2b5abfd`）**：F 域 `domain/sea-warning.ts`（三档阈值 ← 需求 §6.3；L4→L1 解析含 7 天缓冲；触发①＝`last_event_at` 否则建档 ＋N 天）＋ `SeaService.scanSeaWarning`（**只告警**：业务数据零写，落库仅 `job_run_log`）＋ `jobs/sea-warning.job.ts`（每小时 ＋ 启动即跑，`rows_affected` ＝ **命中数**）＋ C 域只读出口 `listSeaWarningCandidates`（**系统专用、不收敛范围**）。⛔ **只覆盖「跟进频次」一条触发**（其余 ＋ `stay_days` 冲突 → **D-57 / P-10**）；三档**出口动作**（进动线 / 推人 / 真掉）全未接 → **D-56**。★ **Worker 装配修正**：补 kernel 的 `EventBusModule` ＋ `AuditModule`、`SeaModule` 导出 `SeaService` —— **三绿全绿，真起进程连炸三轮**（坑 35）。
**Worker 骨架（M7-01＋02）**：`worker.module.ts` ＋ `src/jobs/`：`ScheduledJob` → `JobScheduler`（固定间隔 / 重叠跳过 / 关服清定时器）→ `JobRunner`（**唯一写 `job_run_log`**）。⛔ **不许挂进 `app.module.ts`**（ESLint 硬卡）。
**F-01 公海领取（D-33）**：`POST /sea/company/:id/claim`（`:id` ＝ **公司 id**），req `{dept_id, product_line_id}`；权限＝可写角色 ＋ 读范围（**管理员 · 交付 · 客服 403**）；**409 `sea.claim_conflict`** / 400 / 403；编排＝F 域（C 域条件 UPDATE → F 回填 `sea_record` → `RelationClaimed` → D 域转 open 承诺 owner）。✅ 片 2 `7f24d8f` **已人验**（`b33c49b`，走查明细 → 台账 **D-33**）⇒ **片 3 解锁**；⛔ A 域通知未接（→ D-42）。
**旧指针**：`POST /contacts/:id/events`（D-45）· 页 15（全号 / 上锁态 / 就职历史 / 特质 / 「关联公司」走撞库 → D-47 · D-46 · D-13；⛔ 不摆 解锁 D-04 · 特质编辑 D-48 · 悬浮卡 D-50）。
**跨页复用件（架构 §4.4）**：`components/CompanyDupPicker.vue` / `RelationTargetPicker.vue`。**公海可读不可写，唯一例外＝改「开发价值」** → D-32；**偏好** → D-37；`notif` → D-42。ⓘ **编号归口**：公海领取 ＝ `F-01`（旧称 M7-01）；`M7-01`~`M7-06` ＝ Worker。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M6 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b`…`63eed5a` |
| **M7-01＋02** | ✅ 09-20 `f8346d8`（Worker 骨架 ＋ `jobs/` 约定 ＋ 留痕 ＋ ESLint 卡点） |
| **M7-03/04/05** | ✅ 09-20 `2b5abfd`（掉海预警；真机实测见 §〇） |
| **F-01 片 1 / 片 2** | ✅ 片 1 `88839c4` ＋ `5c59d9f` ｜ ✅ 片 2 `7f24d8f` ＋ **人验 `b33c49b`**（浏览器走查，明细 → 台账 D-33） |
| F-01 片 3 | ⏳ **已解锁**（下一片）：接口 §5 / §4.5 回填 |

**硬口径**（分层 / 范围 / 脱敏 / 审计 / 提交约定 **见 `CODEBUDDY.md`**）：① **F / D 同层禁依赖** ⇒ 走领域事件；② **`jobs/**` 只被 Worker 加载**；③ **系统任务读口不收敛，HTTP 出口才收敛**。
**已自动验**：+25 用例（三档边界各 ≥2 / 规则解析 / 零写）；**真起 Worker**（DI 通、两任务各跑一次）；真库三断言（`sea_record` 仍 0 ＋ 关系逐列未动 ＋ `job_run_log` 落行 rows=6/4/0）；Web 真起（3000 被占＝环境态）。
**待七叔验（眼睛看）**：① 片 2 领取后**详情页观感**；② 页 15 视觉（上锁文案 / 特质排版 / 就职历史表）。
**证据**：后端 **51 suites / 765 用例**；本窗口 9 提交（本轮 `2b5abfd`）。

## 二、待拍板

**P-01~P-08 ✅ 已拍**（沿革查 git）；**P-09 ＋ P-10 待拍** —— 回复末尾按《AI执行清单》#21「四要素」列出。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。本轮新 **D-56** / **D-57**；仍欠 **D-04** / **D-41②** / **D-44** / **D-48** / **D-50** / **D-51** / **D-53** / **D-54** / **D-55**。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表。
2. 主线候选：**F-01 片 3**（**已解锁**：接口 §5 / §4.5 回填）· **M7-06（收口）** · **D-51 / D-44 回填**（D-44 先拍板）；掉海预警后续（D-56 / D-57）**先等 P-09 / P-10**。
3. 收尾：后端三绿 ＋ **真起 `node dist/main` 和 `node dist/worker`**；前端 `lint / build`；单测 `-- --maxWorkers=2`；**逐文件 `git add`（禁 `-A`）**。
4. ⚠ 开工前 `git status --short`；**本窗口已 9 次提交 ⇒ 建议换窗**（换窗前：更新本文件 ＋ 提交 ＋ 树干净）。
