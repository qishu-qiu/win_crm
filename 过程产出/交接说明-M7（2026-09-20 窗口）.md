# 交接说明（**F-01 片 1 ✅（后端）＋ 片 2（前端领取入口）代码已落 · 待人点验** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》→《开发计划-V1》。
> ⚠ 起服：后端 `服务端/`→`npm run start:web`（按 `dist/main` 杀）；前端 `前端/`→`npm run dev`（**5173**）。基线：后端三绿 ＝ **47 suites ＋ 728 用例**；前端双绿。本轮**未改库**（`0009` 仍最新）。

## 〇、★ 只留指针

**公海「领取到私海」（D-33）**：`POST /sea/company/:id/claim`（`:id` ＝ **公司 id**），req `{dept_id, product_line_id}`；权限＝可写角色 ＋ 读范围（**管理员 · 交付 · 客服 403**）；错误 **409 `sea.claim_conflict`** / **400**（定位不到 / 参数指错）/ 403。编排 ＝ F 域 `modules/sea/`（认领本体走 C 域出口 → 回填 `sea_record` → 发 `RelationClaimed` → D 域转 open 承诺 owner）—— 细节 → 台账 **D-33**。出参 ＝ 关系列表项 ＋ `claimed_at`（无入海历史则 `null`）。✅ 前端入口（**片 2 `7f24d8f`**：公海抽屉「领取到我的私海」＋ 确认 → 跳 `/relations/:id`；⏳ **待人点验**）；⛔ A 域「通知前 owner」未接（→ D-42）。
**旧指针（细节查台账）**：`POST /contacts/:id/events`（D-45，前端无入口）· 页 15 `/contacts/:id`（全号 / 上锁态 / 就职历史 / 特质 / 「关联公司」走撞库 → D-47 · D-46 · D-13；⛔ 不摆 解锁 D-04 · 特质编辑 D-48 · 悬浮卡 D-50）。
**跨页复用件（架构 §4.4）**：`components/CompanyDupPicker.vue` / `RelationTargetPicker.vue`；口径 `src/company.ts` / `src/org.ts`；日期 `src/format.ts`。
**公海可读不可写，唯一例外＝改「开发价值」** → D-32。**偏好** → D-37；`notif` → D-42。ⓘ **编号归口**：公海领取 ＝ `F-01`（旧称 M7-01）；`M7-01`~`M7-06` ＝ Worker（→ 开发计划 M7 段）。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M6 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b`…`63eed5a`（含页 15 `94d3fe4`） |
| **F-01 片 1** | ✅ 09-20：`88839c4`（C 域出口）＋ `5c59d9f`（F 域端点 / 编排 / D 域承诺级联 / 出参 / A10 ＋ `gen:types`） |
| **F-01 片 2** | ⏳ **代码已落 `7f24d8f`**（公海抽屉「领取到我的私海」＋ 二次确认 ＋ 成功跳详情；失败关抽屉重拉列表；前端 lint / build 双绿）—— **待人点验** |
| F-01 片 3 | ⏳ 接口 §5 回填 ＋ §4.5 补两句话（**片 2 人验后一次性回填**） |

**硬口径**（审计 / 分层 / 数据范围 / 脱敏 / 可写角色 / 分页 / 可见性 **见 `CODEBUDDY.md`**，不重复）：① 表归谁谁写（认领本体→C、`sea_record`→F）；② **锁跟人**（备用号一并隐藏、不挡自己）；③ 提交一律 `@click` ＋ `@press-enter`；④ 跨页复用件进 `components/`；⑤ 「待关联」写权限＝当前归属人；⑥ **F / D 同层禁依赖** ⇒ 承诺级联走领域事件；⑦ 领取后**阶段回 1**、承诺只动 `open`。

**本轮已自动验**：片 1 负例 4 类 ＋ **3 轮 8 并发**（每轮 1 个 200、其余 **409|20004**）＋ 级联三断言；片 2 只动前端 ⇒ 双绿（**UI 必须人点**）。
**待七叔验（眼睛看）**：① **片 2 领取动线** —— `/relations` → **公海页签** → 某行「时间线」抽屉 → 「领取到我的私海」→ 确认 → 应跳 `/relations/:id`；被抢应弹人话并刷新列表；② 页 15 视觉（上锁文案 / 特质排版 / 就职历史表）。
**证据**：后端 47 suites / **728 用例**（+9）；`gen:types` +110 行；片 2 改动 → 台账 **D-33**。

## 二、待拍板

**P-01~P-07 ✅ 已拍**（沿革查 git）；**无待拍项**。本轮两条已拍：**D-52 ✅ `9771199`**（A10 补齐 4 条动作名）· **D-53 ✅ 走治本**（掉海片保证「同一三元组至多一条可领」；不做列表过滤）。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。本轮：**D-33 片 1 ✅ ＋ 片 2 代码已落（待人点验；片 3 仍欠）** · **D-52 ✅** · **D-53 已拍**（治本归掉海片）；仍欠 **D-04** / **D-41②** / **D-48** / **D-50** / **D-51**。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表。
2. **先请七叔点验片 2**（见 §一「待七叔验」①）—— 验过即做 **片 3**（接口 §5 回填 ＋ §4.5 补「谁能领 / 定位规则」两句话）；其它候选＝**D-51**（`POST /contacts/:id/events` 的判定口径回填 §5.7）· **D-44**（页 28 个人资料 / 改密，**先拍板**）。
3. 收尾：前端跑 `lint / build`；后端三绿 ＋ 真起 `node dist/main`；单测 `-- --maxWorkers=2`；**逐文件 `git add`（禁 `-A`）**。
4. ⚠ 开工前 `git status --short`；**本窗口已 4 次提交 ＋ 读过 ≥2 个大文件 ⇒ 建议换窗**（换窗前：更新本文件 ＋ 提交 ＋ 树干净）。
