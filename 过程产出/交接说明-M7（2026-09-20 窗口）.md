# 交接说明（M7 · 下个窗口必读）

> 状态 / 证据 / 待拍板唯一落点（缺口 → `欠账登记表.md`）；读法：本文件 →《AI执行清单》→《开发计划-V1》。
> 起服 / 端口 / 命令见 `CODEBUDDY.md §9`（Worker 恒 1 实例）。基线：后端 54 suites（`shared.module.spec` 跑不起来 → **D-15**，其余 **823 用例全绿**）；库：migration 至 **`0012`**，`sea_rule` / `sea_record` 空。

## 〇、★ 只留指针

- **掉海（M7-03/04/05 ＋ M9-F 真掉）**：F 域 `scanSeaWarning`（分档 ＋ **`overdue` 档真掉**）。**真掉＝C 域出口 `dropPrivateSeaRelation` → F 域写 `sea_record`（`reason=follow_timeout`）**；**系统动作留痕＝任务级一条 `sea.drop`**（`operator_id=0`，→ 架构 §7.4 ⑤）。⛔ 未发 `RelationReleased`（→ **D-68①**）／触发②③（→ **D-57②**）／三档告警出口（→ **D-56**）。★ Worker 装配见案例库**坑 35**。
- **同三元组至多一条关系（**D-53 · 本批闭环**）**：`uk_active_rel` 生成列＝**未删未并**即占位（**公海也占位**，migration `0012`）⇒ "新一轮" ＝ **领取同一条**；同三元组已有公海行时再激活 → 409 **分档**引导（私海→转交/协同；公海→直接领取）。口径落点＝需求 §6.3 / 数据架构 C1 §10.2-5。
- **Worker 骨架（M7-01＋02）**：`ScheduledJob → JobScheduler → JobRunner`（唯一写 `job_run_log`）；⛔ 不许挂进 `app.module.ts`（ESLint 硬卡）。
- **F-01 公海领取（D-33）**：形状落点＝接口 **§4.5 ＋ §5.16**；**409 `sea.claim_conflict`**。✅ 闭环（已人验）；⛔ 通知未接（→ D-42）。
- **跨域装配**：公司详情 ＋ 联系人列表 / 详情**都在聚合层** `company-aggregate`（→ D-61 / D-28 / D-67）。
- **前端**：页 15（记跟单 / 关联公司走撞库）；复用件 `CompanyDupPicker.vue` / `RelationTargetPicker.vue`；⛔ 不摆 D-04 / D-48 / D-50 假入口。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M7 ＋ F-01 全片 | ✅ 沿革查 `git log` |
| 09-20 批 | ✅ D-06 / D-07 / D-08 / D-35 / D-45 |
| **D-61 / D-28 / D-67** | ✅ 桥③（＋ `event_count_30d`）／联系人收敛／经理可见线索；**仍欠** `sign_date`·`amount`（E 域未建） |
| **D-65 / D-66** | ✅ 09-21 `8c5fc33`（`source` 改 `auto` ＋ `0011` 订正历史行） |
| **M9-F 真掉 ＋ D-68② ＋ D-53（本批）** | ✅ 09-21：**真掉**（＋ `sea_record`）／**任务级审计 `sea.drop`**（`operation_log` **+1 行**、非逐条）／**D-53 结构层收口**（migration `0012`：生成列改「未删未并」＋ 存量清理 1 例 ＋ 409 分档）。真库实测：掉 1 条（关系回公海 ＋ owner 撤 ＋ 幂等）；**同三元组再建被 `uk_active_rel` 拦**（私海 / 公海两态都验） |

**硬口径**：F / D 同层禁依赖（走领域事件）；`jobs/**` 只被 Worker 加载；**系统任务读口不收敛、HTTP 出口才收敛**。
**证据**：各项真库实测 → 台账（D-53 / D-68 / D-28 / D-61 / D-67 / D-15）＋ `git log`。
**待七叔验（眼睛看）**：① 关系详情页领取后观感；② 页 15 视觉。⚠ **本批 HTTP 层没走**（无登录凭据）：激活 409 的**分档人话**由单测钉、**DB 行为**已真库验。

## 二、待拍板

09-21 已拍并落地：六条（`event_count_30d` 窗口三要素 / 系统事件不计入 / 前端依赖姿势 / D-28 落点 / D-67 经理＝管辖部门内）＋ **D-65** ／ **D-66** ／ **D-68②**（系统任务审计＝任务级一条）／ **D-53**（方案 A：结构层收口）。**仍待拍：无**（下一批由 §四 主线产生）。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。**待拍**：D-04 / D-41② / D-44 / D-48 / D-50；**待设施**：D-56①② / D-57② / D-68①；**基建**：D-15 / D-62；**等设施**：`sign_date`·`amount`、D-01、D-02。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（先看 §〇）＋ 欠账登记表。
2. 主线：**① M9-F 掉海收尾**（D-56① 动线 / ② 通知 / **D-57② 另两条触发 —— 锚点是规格缺口，需先拍**）· **② D-49 回填 / D-48** · **③ D-44 页 28**（先拍板）· **④ 基建**：D-62 / D-15。⚠ `sign_date`·`amount` 做不了。
3. 收尾：后端三绿 ＋ 真起 `dist/main` / `dist/worker`（脏先 `npm run clean` → D-58）；前端 `lint / build`；单测 `-- --maxWorkers=2`；逐文件 `git add`（禁 `-A`）。
4. ⚠ 开工前 `git status --short` ＋ `git log --oneline -5`（本工作区**有并行窗口**）；暂存后别插长动作（案例库坑 38）。
