# 交接说明（**M7 窗口 · 含 F-01 / D-45 / D-06 / D-07 / D-08 闭环** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》→《开发计划-V1》。
> ⚠ 起服 / 端口 / 命令 **见 `CODEBUDDY.md` §9**（Worker **恒 1 实例**）。基线：后端 **51 suites ＋ 765 用例**（本轮 B 域 +1 分页用例）；**本轮未改库**（`0009`）。

## 〇、★ 只留指针

**掉海预警（M7-03/04/05 · `2b5abfd`）**：F 域 `domain/sea-warning.ts`（阈值 ← 需求 §6.3；L4→L1 解析含 7 天缓冲；触发①＝`last_event_at` 否则建档＋N 天）＋ `SeaService.scanSeaWarning`（**只告警**：业务数据零写，落库仅 `job_run_log`）＋ `jobs/sea-warning.job.ts`（每小时＋启动即跑，`rows_affected`＝**命中数**）＋ C 域只读出口 `listSeaWarningCandidates`。⛔ **只覆盖「跟进频次」一条触发**（另两条 → **D-57②**，`stay_days` 已按 P-10 回填）；三档**出口动作**（进动线 / 推人 / 真掉）全未接 → **D-56**。★ **Worker 装配**：见案例库**坑 35**（三绿全绿、**真起才炸**）。
**Worker 骨架（M7-01＋02）**：`worker.module.ts` ＋ `src/jobs/`：`ScheduledJob` → `JobScheduler` → `JobRunner`（**唯一写 `job_run_log`**）。现**只跑 1 个任务**（掉海预警；`heartbeat` 已删 `a1d477a`）。⛔ **不许挂进 `app.module.ts`**（ESLint 硬卡）。
**F-01 公海领取（D-33）**：形状唯一落点 ＝ 接口 **§4.5 ＋ §5.16**（片 3 新增）；权限＝可写角色 ＋ 读范围（**管理员 · 交付 · 客服 403**）；**409 `sea.claim_conflict`**；编排＝F 域（C 域条件 UPDATE → F 回填 `sea_record` → `RelationClaimed` → D 域转 open 承诺 owner）。✅ 片 1 `88839c4`/`5c59d9f` ｜ 片 2 `7f24d8f` **已人验** `b33c49b` ｜ 片 3 ✅（回填 ＋ 归档）；⛔ A 域通知未接（→ D-42）。
**旧指针**：`POST /contacts/:id/events`（D-45）· 页 15（全号 / 上锁态 / 就职历史 / 特质 / 「关联公司」走撞库 → D-47 · D-46 · D-13；⛔ 不摆 D-04 / D-48 / D-50）。
**跨页复用件（架构 §4.4）**：`components/CompanyDupPicker.vue` / `RelationTargetPicker.vue`。**公海可读不可写，唯一例外＝改「开发价值」** → D-32；**偏好** → D-37；`notif` → D-42。ⓘ 编号：公海领取＝`F-01`；`M7-01~06`＝Worker。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M6 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b`…`63eed5a` |
| **M7-01＋02** | ✅ 09-20 `f8346d8`（Worker 骨架 ＋ `jobs/` 约定 ＋ ESLint 卡点） |
| **M7-03/04/05** | ✅ 09-20 `2b5abfd`（掉海预警；实测见 §〇） |
| **F-01（片 1~3）** | ✅ **全片闭环**：片 1 `88839c4`＋`5c59d9f` ｜ 片 2 `7f24d8f` ＋ **人验 `b33c49b`** ｜ 片 3 接口 §5.16 ＋ §4.5 ＋ 归档 |
| **D-51 / D-16 / D-24 回填** | ✅ 09-20 **并行窗口** `11605a1`／`a5203bc`（接口 §5.7 三处缺口） |
| **D-45 前端接线** | ✅ 09-20 `0a57f9d`（页 15「待关联」区加「记一条跟单」入口） |
| **D-06 幂等头** | ✅ 09-20 `1bd916e`（横切第 5 件 ＋ 第 47 表 `idempotency_key`；真库实测修正＝等落库再放响应） |
| **D-07 列表排序** | ✅ 09-20 `6d0bd8b`（`GET /relations` 接 `order_by`/`desc`/`keyword`；接口升 **V1.33**） |
| **D-08 分页铺 B 域** | ✅ 09-20 `3b46698`（三列表接 `kernel/listing` 公共件；接口升 **V1.32**） |
| **D-59 登记** | 09-20 `5b349e9` 新登记（原 `POST /contacts/:id` 改联系人端点不存在 → 并入 **D-05**） |
| **D-35 管理员读留痕** | ✅ 09-20 本批首件：三个读出口一起收 `admin` 留痕（`company.view`/`contact.view`/`relation.view`），best-effort 不冒泡，列表不收 |
| **D-05 公司详情端点（B 域内部）** | 🟡 09-20 已建 `GET /companies/:id`（基本档案＋`completeness`＋`profile_tags`＋`contacts[]` 卡片掩码）；跨域字段 `relations_summary`/`event_count_30d` **待 D-61 落点** |

**硬口径**（分层 / 范围 / 脱敏 / 审计 / 提交 **见 `CODEBUDDY.md`**）：**F / D 同层禁依赖** ⇒ 走领域事件；**`jobs/**` 只被 Worker 加载**；**系统任务读口不收敛、HTTP 出口才收敛**。
**已自动验**：+25 用例（三档边界 / 规则解析 / 零写）；**真起 Worker**（DI 通、任务落行）；真库三断言（`sea_record` 仍 0 ＋ 关系未动 ＋ `job_run_log` rows=6/4/0）；Web 真起（3000 被占）；**D-07/D-08 后端三绿 ＋ 真库实测通过**（三列表分页 ＋ 排序；前端双绿）。
**待七叔验（眼睛看）**：① 详情页领取后观感；② 页 15 视觉（上锁文案 / 特质 / 就职历史）。
**证据**：后端 **51 suites / 765 用例**（本轮 B 域 +1 分页用例）；提交见 `git log`。

## 二、待拍板

**P-01~P-10 ✅ 全已拍**（P-09/P-10 于 09-20 落地 `a1d477a`；沿革查 git）；**本轮新增待拍＝ D-05 / D-28 / D-35 口径**（见 §四，勿由实现反推契约）。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。**D-33 主链已通**（余 ③通知 / ④其余海域端点 / ⑤D-53）；**回填类**：**D-49**（D-16 / D-24 / D-51 已由并行窗口补）；**本轮闭环**＝**D-45 / D-06 / D-07 / D-08**（台账已标 ✅）。
> **本批（09-20 续）状态**：**D-35 ✅ 已收口**（三读出口 `admin` 留痕）；**D-05 端点已建**，但其 `relations_summary`/`event_count_30d` 与 **D-28** 收敛都依赖 C 域 `business_relation`，而 **B(L2) 禁止依赖 C(L3)**（架构 §3，company.module 明写只依赖 kernel＋A），B 调不到 C 的出口 ⇒ **三者统一卡在跨域落点（→ 新登记 D-61），须先拍板再实现**。本轮 D-05 先建 B 域内部部分（跨域字段暂缺、不编假值），D-28 维持现状。
> **待拍板 / 待设施类**（其余）：**D-04** / **D-41②** / **D-44** / **D-48** / **D-50** / **D-54** / **D-56** / **D-57②**

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表。
2. 主线候选（**先拍板 D-61 跨域落点，再收 D-05 跨域字段 ＋ D-28**）：**D-61**（B 禁依赖 C，三种跨域读取的落点选 A/B/C/D 之一 → 回填架构 §3 ＋ 接口 §5.4）· 落点定后：**D-05** 补 `relations_summary`/`event_count_30d`（计数口径已拍板 Q1：自然日 30 天、`action_event.event_at`、只计登录人可见关系）· **D-28** `GET /contacts` 收敛（已挂公司联系人按 D-61 出口判可见；待关联仍只给归属人）。｜ 其余：**M7-06（收口）** · **D-49 回填** · **M9-F 掉海判定**（**D-53** / **D-56** / **D-57②**）· **D-44 页 28**（要先拍板）· **D-62**（test ESM 债务）。
3. 收尾：后端三绿 ＋ **真起 `node dist/main` / `dist/worker`**（产物脏先 `npm run clean`，弹一次确认 → 台账 **D-58**）；前端 `lint / build`；单测 `-- --maxWorkers=2`；**逐文件 `git add`（禁 `-A`）**。
4. ⚠ 开工前 `git status --short` ＋ **`git log --oneline -5`**（★ **本工作区有并行窗口**：先对齐再动手、别重复做）；**本窗口已 13+ 次提交 ⇒ 已换窗**（交接已更新于此）；★ **暂存后别插长动作** → 案例库坑 38。
