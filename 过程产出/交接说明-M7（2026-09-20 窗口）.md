# 交接说明（M7 · 下个窗口必读）

> 状态 / 证据 / 待拍板唯一落点（缺口 → `欠账登记表.md`）；读法：本文件 →《AI执行清单》→《开发计划-V1》。
> 起服 / 端口 / 命令见 `CODEBUDDY.md §9`（Worker 恒 1 实例）。基线：**854 用例全绿**（54 suites；`shared.module.spec` 跑不起来 → **D-15**）；库：migration 至 **`0012`**，`sea_rule` / `sea_record` 空。

## 〇、★ 只留指针

- **掉海（M7-03/04/05 ＋ M9-F 真掉）**：F 域 `scanSeaWarning`＝分档 ＋ **`overdue` 档真掉**（走 C 域出口 → 写 `sea_record`）；系统动作留痕＝**任务级一条** `sea.drop`（→ 架构 §7.4 ⑤）。⛔ `RelationReleased` 未发（→ **D-68①**）／触发②③（→ **D-57②**）／三档告警出口（→ **D-56**）。★ Worker 装配见案例库**坑 35**。
- **M9-F 规则配置（本批）**：`GET/PUT /sea/rules` —— 7 天缓冲（插新版本行 ＋ 旧行停用）／`confirmed=false` **只回预告、零写库**／`affected_customers` 与扫描**同一套** `resolveSeaRuleFor`；★ **生效即重算** ＝ 锚点取 `max(锚点, effective_from)`。落点：**§5.16**、审计 `sea.rule_update`。⛔ 页 25 子页未建（→ **D-70**）／写权限＝合成口径（→ **D-69**）。
- **同三元组至多一条关系（D-53）**：`uk_active_rel`＝**未删未并即占位**（**公海也占位**，`0012`）⇒「新一轮」＝**领取同一条**；撞键 409 **分档**（分档话术见 §5.6）。口径＝需求 §6.3 / C1 §10.2-5。
- **Worker 骨架（M7-01＋02）**：`ScheduledJob → JobScheduler → JobRunner`（唯一写 `job_run_log`）；⛔ 不许挂进 `app.module.ts`（ESLint 硬卡）。
- **F-01 公海领取（D-33）**：形状＝接口 §4.5 ＋ §5.16；409 `sea.claim_conflict`。✅ 已人验；⛔ 通知未接（→ D-42）。
- **跨域装配**：公司详情 ＋ 联系人列表 / 详情**都在聚合层** `company-aggregate`（→ D-61 / D-28 / D-67）。
- **前端**：页 15；复用件 `CompanyDupPicker.vue` / `RelationTargetPicker.vue`；⛔ 不摆 D-04 / D-48 / D-50 假入口。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M7 ＋ F-01 ／ 09-20 批 ／ **D-65·D-66** | ✅ 沿革查 `git log`（D-06/07/08/35/45；`source` 改 `auto` ＋ `0011` ＝ `8c5fc33`） |
| **D-61 / D-28 / D-67** | ✅ 桥③（＋ `event_count_30d`）／联系人收敛／经理可见线索；**仍欠** `sign_date`·`amount`（E 域未建） |
| **M9-F（09-21 ＋ 本批）** | ✅ 真掉（＋ `sea_record`）／**任务级**审计／`0012` 结构层收口 ＋ 409 分档 ／ **规则配置** `GET/PUT /sea/rules`（7 天缓冲 ＋ 预告 ＋ 生效重算，§5.16 已回填；真库**真账号**实测 → 台账 **D-69 / D-70**） |

**硬口径**：F / D 同层禁依赖（走领域事件）；`jobs/**` 只被 Worker 加载；**系统任务读口不收敛、HTTP 出口才收敛**。
**待七叔验**：① 关系详情页领取后观感；② 页 15 视觉。★ **HTTP 层可走了**（seed 6 个 dev 账号 / 密码 `Dev@123456`；本批已走通）；**D-53 批的 409 分档仍只由单测钉**。

## 二、待拍板

09-21 已拍并落地：六条（`event_count_30d` 三要素 / 系统事件不计入 / 前端依赖姿势 / D-28 落点 / D-67）＋ **D-65**／**D-66**／**D-68②**／**D-53** ＋ **M9-F ①②**（规则配置 ＋ 预告口径＝「真正命中这条规则的**在途私海**客户数」）。
**待确认一条**（不拦推进）：§6.3 / F1 原句写「**所有**在途关系从生效日重新起算」，实现取「**受该规则约束的**」那批 —— ①「改小幅度」只在影响面随范围变时才成立；② 字面全局在 F1 里**无处落**（`effective_from` 逐行）。要改就先补承载列。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。**待拍**：D-04 / D-41② / D-44 / D-48 / D-50；**待设施**：D-56①② / D-57② / D-68①；**待返工**：D-17 / **D-69**（规则配置写权限＝合成口径，待需求 §7 权限表）；**前端欠**：**D-70**（页 25 公海规则子页）；**基建**：D-15 / D-62；**等设施**：`sign_date`·`amount`、D-01、D-02。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（先看 §〇）＋ 欠账登记表。
2. 主线：**① M9-F 掉海收尾**（规则配置 ✅；剩 **D-56① 动线**＝要 `daily_agenda` 组装任务 ／ **② 通知**＝要 A 域 `notification` ／ **D-57②** ＝ 锚点是规格缺口**需先拍**）· **② 页 25 公海规则子页**（→ D-70）· **③ D-49 回填 / D-48** · **④ D-44 页 28**（先拍板）· **⑤ 基建** D-62 / D-15。⚠ `sign_date`·`amount` 做不了。
3. 收尾：后端三绿 ＋ 真起 `dist/main` / `dist/worker`（脏先 `npm run clean` → D-58）；前端 `lint / build`；单测 `-- --maxWorkers=2`；逐文件 `git add`（禁 `-A`）。
4. ⚠ 开工前 `git status --short` ＋ `git log --oneline -5`（本工作区**有并行窗口**）；暂存后别插长动作（案例库坑 38）。
