# 交接说明（M7 · 下个窗口必读）

> 状态 / 证据 / 待拍板唯一落点（缺口 → `欠账登记表.md`）；读法：本文件 →《AI执行清单》→《开发计划-V1》。
> 起服 / 端口 / 命令见 `CODEBUDDY.md §9`（Worker 恒 1 实例）。基线：后端 54 suites（`shared.module.spec` 跑不起来 → **D-15**，其余 **823 用例全绿**）；库：migration 至 **`0011`**（`0011` 为 D-65 的数据订正），`sea_rule` / `sea_record` 均空。

## 〇、★ 只留指针

- **掉海（M7-03/04/05 `2b5abfd` ＋ 本批 M9-F 真掉）**：F 域 `sea-warning.ts` ＋ `scanSeaWarning`（分档 ＋ **`overdue` 档真掉**）＋ 每小时跑。**真掉＝C 域出口 `dropPrivateSeaRelation`（条件 UPDATE ＋ 撤 owner）→ F 域写 `sea_record`（`reason=follow_timeout`）**；系统动作无 actor（未发事件 → **D-68①**）。⛔ 触发②③（→ **D-57②**）；三档告警出口（→ **D-56**）；**「同三元组至多一条可领」未落**（→ **D-53**，形态待拍）。★ Worker 装配见案例库**坑 35**（三绿全绿、真起才炸）。
- **Worker 骨架（M7-01＋02）**：`ScheduledJob → JobScheduler → JobRunner`（唯一写 `job_run_log`）；⛔ 不许挂进 `app.module.ts`（ESLint 硬卡）。
- **F-01 公海领取（D-33）**：形状落点＝接口 **§4.5 ＋ §5.16**；**409 `sea.claim_conflict`**；编排在 F 域。✅ 全片闭环（片 2 已人验）；⛔ 通知未接（→ D-42）。
- **跨域装配**：公司详情 ＋ 联系人列表 / 详情**都在聚合层** `company-aggregate`（→ D-61 / D-28 / D-67）。
- **前端**：页 15（记跟单 / 关联公司走撞库）；复用件 `CompanyDupPicker.vue` / `RelationTargetPicker.vue`；⛔ 不摆 D-04 / D-48 / D-50 假入口。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M7 ＋ F-01 全片 | ✅ 沿革查 `git log` |
| 09-20 批 | ✅ D-06 / D-07 / D-08 / D-35 / D-45 ＋ 三处回填（见台账） |
| **D-61 / D-28 / D-67** | ✅ 桥③（＋ `event_count_30d`）／联系人收敛／经理可见线索；**仍欠** `sign_date`·`amount`（E 域未建） |
| **D-65 / D-66 闭环** | ✅ 09-21 `8c5fc33`（系统事件 `source` 改 `auto` ＋ 增量 `0011` 订正历史行）／ 本文压缩 **10667 → 3438 B** |
| **M9-F 真掉海（本批）** | ✅ 09-21：`scanSeaWarning` 的 `overdue` 档**真掉**（C 域出口 ＋ 写 `sea_record`）。真库实测：掉 1 条 → 关系回公海 ＋ owner 撤销（`revoked_by=null`）＋ `sea_record` 落行；**幂等**（二次跑候选 7→6、真掉 0）；`dept_id` / `stage_id` 未动 |

**硬口径**：F / D 同层禁依赖（走领域事件）；`jobs/**` 只被 Worker 加载；**系统任务读口不收敛、HTTP 出口才收敛**。
**证据**：各项真库实测数字 → 台账（D-28 / D-61 / D-67 / D-15）＋ `git log`。
**待七叔验（眼睛看）**：① 关系详情页领取后观感；② 页 15 视觉（上锁文案 / 特质 / 就职历史）。

## 二、待拍板

09-21 六条已拍并落地（`event_count_30d` 窗口三要素 / 系统事件不计入 / 前端依赖姿势 / D-28 落点 / D-67 经理＝管辖部门内），另 **D-65**（改码 `auto`）/ **D-66**（本文压缩）已闭环。**本批产生 2 条待拍**：**① D-53 落地形态** —— 同一「公司×部门×产品线」反复掉海会**并存两条公海行**、点第二条必 409（真库已复现；"旧行让位"用哪个机制 / 要不要加生成列唯一键兜并发）；**② D-68② 系统任务审计留痕口径** —— 掉海（定时任务）改了 2 张表却没写 `operation_log`，而数据架构 §164 是"两者都写"。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。**待拍**：D-53（形态）/ D-68② / D-04 / D-41② / D-44 / D-48 / D-50；**待设施**：D-56①② / D-57② / D-68①；**基建**：D-15 / D-62；**等设施**：`sign_date`·`amount`、D-01、D-02。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（先看 §〇）＋ 欠账登记表。
2. 主线：**① M9-F 掉海收尾**（**D-53 形态先拍** → 同批补「不变量」判据；再 D-56① 动线 / ② 通知）· **② D-49 回填 / D-48** · **③ D-44 页 28**（先拍板）· **④ 基建**：D-62 / D-15。⚠ `sign_date`·`amount` 做不了。
3. 收尾：后端三绿 ＋ 真起 `dist/main` / `dist/worker`（脏先 `npm run clean` → D-58）；前端 `lint / build`；单测 `-- --maxWorkers=2`；逐文件 `git add`（禁 `-A`）。
4. ⚠ 开工前 `git status --short` ＋ `git log --oneline -5`（本工作区**有并行窗口**）；暂存后别插长动作（案例库坑 38）。
