# 交接说明（**M7-01 片 1：F 域领取公海 ＋ 承诺级联（后端）已落地** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》→《开发计划-V1》。
> ⚠ 起服：后端 `服务端/` 内 `npm run start:web`（按 `dist/main` 杀）；前端 `前端/` 内 `npm run dev`（**5173**）。基线：后端三绿 ＝ **47 suites ＋ 728 用例**（`npm test -- --maxWorkers=2`）；前端 `lint / build` 双绿。本轮**未改库**（`0009` 仍最新）。

## 〇、★ 只留指针

**公海「领取到私海」（M7-01 片 1 · D-33）**：`POST /sea/company/:id/claim`（`:id` ＝ **公司 id**），req `{dept_id, product_line_id}`。权限＝可写角色 ＋ 读范围（销售本部门 / 经理管辖 / 总经理全部；管理员 · 交付 · 客服 → 403）。错误：**409 `sea.claim_conflict`**（并发被抢）· 400（定位不到 / `dept_id` 不存在）· 403（越部门 / 只读）。编排 ＝ **F 域** `modules/sea/`：认领本体走 C 域出口（条件 UPDATE ＋ owner 成员 ＋ 阶段回 1 ＋ 留痕，一个 C 域事务）→ 回填 `sea_record.claimed_by/at`（**无历史行不造行**、出参 `claimed_at: null`）→ 发 `RelationClaimed`；**D 域**订阅后**本域事务**落 `system` 事件（`owner_snapshot` ＝ 新 owner）＋ **open 承诺 `owner_id` 转新 owner**。出参 ＝ C 域列表项 ＋ `claimed_at`。⛔ 前端无入口（片 2）；⛔ A 域「通知前 owner」未接（→ D-42）。
**旧指针（M6 已落，细节查台账）**：`POST /contacts/:id/events` 「待关联」记跟单（D-45；只有当前归属人能记、前端无入口）· 页 15 `/contacts/:id`（全号 / 上锁态 ＋ 落锁人 / 就职历史 / 特质 / 「关联公司」走撞库激活 → D-47 · D-46 · D-13；⛔ 不摆解锁 D-04 · 特质编辑 D-48 · 悬浮卡 D-50）。
**跨页复用件（架构 §4.4）**：`components/CompanyDupPicker.vue` / `RelationTargetPicker.vue`；口径 `src/company.ts` / `src/org.ts`；日期 `src/format.ts`。
**公海可读不可写，唯一例外＝改「开发价值」** → D-32。**偏好** → D-37；`notif` → D-42。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M6 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b`…`63eed5a`（含页 15 `94d3fe4`） |
| **M7-01 片 1** | ✅ 09-20：`88839c4`（C 域出口）＋ `5c59d9f`（F 域端点 / 编排 / D 域承诺级联 / 出参 / A10 ＋ `gen:types`） |
| M7-01 片 2 / 3 | ⏳ 片 2 ＝ 前端公海抽屉加「领取到我的私海」→ 跳 `/relations/:id`；片 3 ＝ 接口 §5 回填（**已拍板：片 2 真库验过后一次性回填**） |

**硬口径**（审计 / 分层 / 数据范围 / 脱敏 / 可写角色 / 分页归一化 / 页面可见性 / 注释不绑版本号 **见常驻 `CODEBUDDY.md`**，不重复）：① 表归谁谁写（认领本体→C、`sea_record`→F）；② **锁跟人**（主号 ＋ 备用号一并隐藏，锁不挡自己）；③ 提交不靠 `<a-form @finish>`，一律 `@click` ＋ `@press-enter`；④ 跨页复用件进 `components/`；⑤ 「待关联」线索写权限＝当前归属人；⑥ **F / D 同层禁依赖** ⇒ 承诺级联只能走领域事件；⑦ 领取后**阶段回 1**、承诺「随关系走」只动 `open`。

**本轮已自动验（真库，探针已删）**：负例 4 类 ＋ **3 轮同进程 8 并发**（每轮恰好 1 个 200、其余 **409|20004**）＋ 级联三断言（走查行已按 SQL 删净）。
**待七叔验（眼睛看）**：页 15 视觉（上锁文案 / 特质排版 / 就职历史表）。
**证据**：后端 47 suites / **728 用例**（+9）；`gen:types` +110 行；明细 → 台账 **D-33** 行。

## 二、待拍板

**P-01~P-07 ✅ 已拍**（沿革查 git）；**无待拍项**。本轮两条已拍：**D-52 ✅ `9771199`**（A10 补齐 **4 条**动作名 —— 机械枚举比原记多出 `account.preferences.update`）· **D-53 ✅ 走治本**（随 M7 掉海片保证「同一三元组至多一条可领」；**不做**列表过滤）。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。本轮：**D-33 片 1 ✅**（片 2/3 仍欠）· **D-52 ✅** · **D-53 已拍**（治本归掉海片）；仍欠 **D-04** / **D-41②** / **D-48** / **D-50** / **D-51**。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表。
2. 主线候选＝**M7-01 片 2**（入口在 `前端/src/views/RelationListView.vue` 公海页签抽屉；**不许前端自己判"是不是公海"**，以服务端返回为准）· **D-51** · **D-44**（**先拍板**）。
3. 收尾：只动前端跑 `lint / build`；动后端跑三绿 ＋ 真起 `node dist/main`；单测 `-- --maxWorkers=2`；**逐文件 `git add`（禁 `-A`）**。
4. ⚠ 开工前 `git status --short`；**本轮已 2 次提交 ＋ 读过 ≥2 个大文件 ⇒ 建议换窗**（换窗前：更新本文件 ＋ 提交 ＋ 树干净）。
