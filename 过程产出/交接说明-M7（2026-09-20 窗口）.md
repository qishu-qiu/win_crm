# 交接说明（**M7-01＋M7-02 已落（Worker 骨架）｜ F-01 片 2 待人点验** · 下个窗口必读）

> **状态 / 证据 / 待拍板唯一落点**（缺口 → `欠账登记表.md`）。读法：本文件 →《AI执行清单》→《开发计划-V1》。
> ⚠ 起服：Web `npm run start:web`（按 `dist/main` 杀）；**Worker** `npm run start:worker`（**恒 1 实例**）；前端 `npm run dev`（**5173**）。基线：后端 **49 suites ＋ 740 用例**；前端双绿。本轮**未改库**（`0009` 最新）。

## 〇、★ 只留指针

**Worker 骨架（M7-01＋M7-02）**：`worker.module.ts`（只装 Prisma ＋ Jobs；不挂 Shared / Context）＋ `src/jobs/`：`ScheduledJob` 约定 → `JobScheduler`（固定间隔、**重叠跳过**、关服清定时器）→ `JobRunner`（**唯一写 `job_run_log`**，running → success|failed；**任务失败不抛**、留痕失败照抛）。现只有 `heartbeat`（自检，去留 → D-55）。★ **不许挂进 `app.module.ts`**（ESLint 硬卡）。⛔ M7-03/04/05 未做。
**F-01 公海领取（D-33）**：`POST /sea/company/:id/claim`（`:id` ＝ **公司 id**），req `{dept_id, product_line_id}`；权限＝可写角色 ＋ 读范围（**管理员 · 交付 · 客服 403**）；错误 **409 `sea.claim_conflict`** / 400 / 403。编排 ＝ F 域（C 域条件 UPDATE → F 回填 `sea_record` → `RelationClaimed` → D 域转 open 承诺 owner）—— 细节 → 台账 **D-33**。出参 ＝ 关系列表项 ＋ `claimed_at`。✅ 前端入口（片 2 `7f24d8f`）；⛔ A 域通知未接（→ D-42）。
**旧指针**：`POST /contacts/:id/events`（D-45）· 页 15（全号 / 上锁态 / 就职历史 / 特质 / 「关联公司」走撞库 → D-47 · D-46 · D-13；⛔ 不摆 解锁 D-04 · 特质编辑 D-48 · 悬浮卡 D-50）。
**跨页复用件（架构 §4.4）**：`components/CompanyDupPicker.vue` / `RelationTargetPicker.vue`；口径 / 日期：`src/*.ts`（`company` / `org` / `format`）。
**公海可读不可写，唯一例外＝改「开发价值」** → D-32。**偏好** → D-37；`notif` → D-42。ⓘ **编号归口**：公海领取 ＝ `F-01`（旧称 M7-01）；`M7-01`~`M7-06` ＝ Worker。

## 一、进度

| 里程碑 | 状态 |
| --- | --- |
| M1~M6 | ✅ `35b7fd0`…`d98752b` ｜ `42aeb5b`…`63eed5a` |
| **M7-01＋02** | ✅ 09-20 `f8346d8`：Worker 骨架 ＋ `jobs/` 约定 ＋ `job_run_log` 留痕 ＋ ESLint 卡点（真起：常驻 / 0 端口 / 表有行） |
| **F-01 片 1** | ✅ 09-20 `88839c4` ＋ `5c59d9f`（C 域出口 / F 域端点 / D 域级联 / 出参 / A10） |
| **F-01 片 2** | ✅ 已验 09-20（`7f24d8f`：**浏览器走查**通过，详见台账 D-33）—— 片 3 解锁 |
| F-01 片 3 | ⏳ 接口 §5 / §4.5 回填（**片 2 人验后一次性**）· 下一片 ＝ M7-03/04/05 掉海预警 |

**硬口径**（审计 / 分层 / 数据范围 / 脱敏 / 可写角色 / 分页 **见 `CODEBUDDY.md`**）：① 表归谁谁写（认领本体→C、`sea_record`→F）；② **锁跟人**；③ 提交一律 `@click` ＋ `@press-enter`；④ 跨页复用件进 `components/`；⑤ 「待关联」写权限＝当前归属人；⑥ **F / D 同层禁依赖** ⇒ 走领域事件；⑦ 领取后**阶段回 1**；⑧ **`jobs/**` 只被 Worker 加载**。

**已自动验**：F-01 片 1 负例 4 类 ＋ 3 轮 8 并发 ＋ 级联三断言；**M7-01/02 真起 Worker**（常驻 / **0 端口** / `job_run_log` 有行）；**F-01 片 2 浏览器走查**：造本部门公海 → 抽屉见「领取到私海」＋「开发价值」（**无**写跟单 / 建承诺）→ 确认 → **跳 `/relations/8`**、转「私海」、主责＝本人；库侧 owner **复活** / `sea_record` **不造行** / 审计 ＋ D 事件各 1 条（明细 → 台账 **D-33**）。
**待七叔验（眼睛看）**：片 2 详情页观感 ＋ 页 15 视觉。
**证据**：后端 **49 suites / 740 用例**（+12，M7-02 两个 spec）；本窗口 7 提交（本轮：`47f865e` 编号归口 · `f8346d8` Worker 骨架 · 本提交）。

## 二、待拍板

**P-01~P-08 ✅ 已拍**（沿革查 git）；**无待拍项**。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。本轮：**D-53 归属澄清** · 新 **D-54**（`run_at` 时区）· **D-55**（`heartbeat` 去留）；仍欠 **D-04** / **D-41②** / **D-48** / **D-50** / **D-51**。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（**先看 §〇**）＋ 欠账登记表。
2. 主线候选＝ **M7-03/04/05**（掉海预警：`domain/sea-warning.ts` 三档纯函数 ＋ 每小时 job，**只告警不真掉**）· **F-01 片 3**（**已解锁**：接口 §5 / §4.5 回填）· **D-51** · **D-44**（先拍板）。
3. 收尾：前端 `lint / build`；后端三绿 ＋ 真起 `node dist/main` **和** `node dist/worker`；单测 `-- --maxWorkers=2`；**逐文件 `git add`（禁 `-A`）**。
4. ⚠ 开工前 `git status --short`；**本窗口已 7 次提交 ＋ 读过 ≥2 个大文件 ⇒ 建议换窗**（换窗前：更新本文件 ＋ 提交 ＋ 树干净）。
