# 交接说明（下个窗口必读）

> 状态 / 证据 / 待拍板唯一落点（缺口 → `欠账登记表.md`）；读法：本文件 →《AI执行清单》→《开发计划-V1》。
> 起服 / 端口 / 命令见 `CODEBUDDY.md §9`。基线：**854 用例全绿**（`shared.module.spec` 跑不起来 → **D-15**）；migration 至 **`0012`**。

## 〇、★ 只留指针

- **掉海（M9-F ＋ 按天改）**：F 域 `scanSeaWarning` —— **每日一次**、**按自然日（Asia/Shanghai）**：`≤3天`进动线 / `前1天`推销售 / `当天`标红推经理 / **`到期日已过` ⇒ 次日真掉**（写 `sea_record`）；留痕＝任务级 `sea.drop`（→ §7.4 ⑤）。★ 单位＝**天**（→ 废止 **#41**）。⛔ `RelationReleased`（→ **D-68①**）／触发②③（→ **D-57②**）／三档出口（→ **D-56**）。装配见**坑 35**。
- **规则配置**：`GET/PUT /sea/rules`（7 天缓冲＝插新版本行＋旧行停用；`confirmed=false` 只回预告、**零写库**；**生效即重算**＝锚点 `max(锚点, effective_from)`）。落点 §5.16、审计 `sea.rule_update`。⛔ 页 25 子页（→ **D-70**）／写权限合成口径（→ **D-69**）。
- **同三元组至多一条关系（D-53）**：`uk_active_rel`＝**未删未并即占位（公海也占位，`0012`）**⇒「新一轮」＝**领取同一条**；撞键 409 **分档**。口径＝需求 §6.3 / C1 §10.2-5。
- **Worker 骨架**：`ScheduledJob → JobScheduler → JobRunner`（唯一写 `job_run_log`）；⛔ 不许挂进 `app.module.ts`（ESLint 硬卡）。
- **F-01 公海领取（D-33）**：形状＝接口 §4.5 ＋ §5.16；409 `sea.claim_conflict`。⛔ 通知未接（→ D-42）。
- **跨域装配 / 前端**：公司详情 ＋ 联系人列表·详情在聚合层 `company-aggregate`（→ D-61 / D-28 / D-67）；复用件 `CompanyDupPicker` / `RelationTargetPicker`；⛔ 不摆 D-04 / D-48 / D-50 假入口；★ 页面 / 页签可见性落点＝`前端/src/access.ts`（抄 §4.2 矩阵，**不可见即不下发**）。

## 一、进度

**已闭环**（沿革一律查 `git log`）：**M1~M7** ／ **M6 收口（05~12 ✅，`4b764cf`＝矩阵收口 ＋ 主题人验）** ／ **F-01** ／ **M9-F**（余 → D-56①②·57②·68①·70）／ **D-28·D-53·D-61·D-65·D-66·D-67·D-71**（D-61 欠 `sign_date`·`amount`，等 E 域）。

**硬口径**：F / D 同层禁依赖（走事件）；`jobs/**` 只被 Worker 加载；**系统任务读口不收敛、HTTP 出口才收敛**。
**待七叔验**：09-22 已验 ① M6-10 走查（六账号侧栏 / 「公海」页签）② M6-11 主题（无闪烁）；其余人眼项（领取后观感 / 页 15 视觉）并入 **M8-01**。★ **HTTP 层可走了**（seed 6 账号 / `Dev@123456`）。

## 二、待拍板

09-21 已拍并落地：六条（`event_count_30d` 三要素 / 系统事件不计入 / 前端依赖姿势 / D-28 落点 / D-67）＋ D-65／D-66／D-68②／D-53 ＋ M9-F ①②（规则配置；预告口径＝命中该规则的**在途私海**客户数）＋ 掉海按天（三档／任务每日／次日掉落）。
**待确认一条**：§6.3 / F1 原句「**所有**在途关系从生效日重算」，实现取「**受该规则约束的**」那批 —— 字面全局在 F1 里**无处落**（`effective_from` 逐行）⇒ 要改先补承载列。
**09-22 已定**：§4.2「系统 / 部门公海」**管理员 ➖ 按矩阵原文**（`4b764cf`）—— 接口 §4.4 给的是**数据层读权**、矩阵答的是**菜单层呈不呈现**，两层不冲突；要开管理员入口**先改矩阵**再改码。
**09-22 待拍（流程）**：是否允许「阶段已闭环 ＋ 已确认验收 ⇒ 直接提交」，省一轮往返；⚠《AI执行清单》**3059 / 3072 B**，加字前须先压别处。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。**本轮无新增**（M6-10·11 是**回归规格**，不是缺口）。开工先翻该表**主表二**（未补 / 补了部分）。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（先看 §〇）＋ 台账。
2. 主线（★ 2026-09-22 · 计划 **V1.1**）：**M6 收口 ✅ → M8 验收 → 再进 M9**。**下一步＝M8**：**M8-01** 真账号走完整动线（登录 → 建档 → 建关系 → 写跟单 → 工作台；★ 证据须含**人眼点过的**截图）→ **M8-02** 勾 §七 退出标准 ＋ T2 四项 → **M8-03** 打 tag（⚠ 仓库 tag **为空**）→ **M8-05** 交销售试用、收反馈。**M9 首域＝E 域（合同）**（解锁 D-01/D-02/D-10/D-61；序 `E→A→G→B/C/D→report`）；F 域剩余随时可续、不占主线。**其余批次**：D-49 回填 / D-48 · D-44 页 28（先拍板）· 基建 D-62 / D-15。
3. 收尾：后端三绿 ＋ 真起 `dist/main` / `dist/worker`（→ D-58）；前端 `lint / build`；单测 `--maxWorkers=2`；逐文件 `git add`（禁 `-A`）。
4. ⚠ 开工前 `git status --short` ＋ `git log --oneline -5`（本工作区**有并行窗口**）；暂存后别插长动作（案例库坑 38）。
