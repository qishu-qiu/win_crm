# 交接说明（下个窗口必读）

> 状态 / 证据 / 待拍板唯一落点（缺口 → `欠账登记表.md`）；读法：本文件 →《AI执行清单》→《开发计划-V1》。
> 起服 / 端口 / 命令见 `CODEBUDDY.md §9`。基线：**854 用例全绿**（`shared.module.spec` 跑不起来 → **D-15**）；migration 至 **`0012`**。

## 〇、★ 只留指针

- **掉海（M9-F ＋ 按天改）**：F 域 `scanSeaWarning` —— **每日一次**、**按自然日（Asia/Shanghai）**：`≤3天`进动线 / `前1天`推销售 / `当天`标红推经理 / **`到期日已过` ⇒ 次日真掉**；留痕＝任务级 `sea.drop`（→ §7.4 ⑤）。★ 单位＝**天**（→ 废止 **#41**）。⛔ `RelationReleased`（→ **D-68①**）／触发②③（→ **D-57②**）／三档出口（→ **D-56**）。装配见**坑 35**。
- **规则配置**：`GET/PUT /sea/rules`（7 天缓冲＝插新版本行＋旧行停用；`confirmed=false` 只回预告、**零写库**；**生效即重算**＝锚点 `max(锚点, effective_from)`）。落点 §5.16、审计 `sea.rule_update`。⛔ 页 25 子页（→ **D-70**）／写权限合成口径（→ **D-69**）。
- **可建范围（D-73）**：新建关系可选哪个部门 ＝ `checkActivateScope`（销售＝**我所属部门**（含兼职）／经理＝管辖部门／总经理＝任意；只读角色不受理），随 `me.activatable_dept_ids` 下发（前端只收敛、不加规则）；**拍板 09-22：销售只能建档自己部门的关系，不放宽**。口径＝架构 §7.2 ＋ 接口 §5.2。★ **产品线同理（D-74，09-22 拍板「该部门承接的都能选」）**：判据＝ `product_line.dept_ids`，**服务端 422/`20409` ＋ 前端下拉同源**。
- **同三元组至多一条关系（D-53）**：`uk_active_rel`＝**未删未并即占位（公海也占位，`0012`）**⇒「新一轮」＝**领取同一条**；撞键 409 **分档**。口径＝需求 §6.3 / C1 §10.2-5。
- **Worker 骨架**：`ScheduledJob → JobScheduler → JobRunner`（唯一写 `job_run_log`）；⛔ 不许挂进 `app.module.ts`（ESLint 硬卡）。
- **F-01 公海领取（D-33）**：形状＝接口 §4.5 ＋ §5.16；409 `sea.claim_conflict`。⛔ 通知未接（→ D-42）。
- **跨域装配 / 前端**：公司详情 ＋ 联系人列表·详情在聚合层 `company-aggregate`（→ D-61 / D-28 / D-67）；复用件 `CompanyDupPicker` / `RelationTargetPicker`；⛔ 不摆 D-04 / D-48 / D-50 假入口。

## 一、进度

**已闭环**（沿革一律查 `git log`）：**M1~M7** ／ **M6 收口（`4b764cf`）** ／ **F-01** ／ **M9-F**（余 → D-56①②·57②·68①·70）／ **D-28·D-53·D-61·D-65·D-66·D-67·D-71·D-73①·D-74**（D-61 欠 `sign_date`·`amount`，等 E 域；D-73 欠 ②＝用户自理）。

**硬口径**：F / D 同层禁依赖（走事件）；`jobs/**` 只被 Worker 加载；**系统任务读口不收敛、HTTP 出口才收敛**；可见性落点＝`前端/src/access.ts`（**不可见即不下发**）。
**M8-01/02/03 ✅**（tag `v0.1-mvp-关系激活验收`@`840c1df`；CLI 动线+T2+越权403；人眼已确认——git log）。⛔ M8-05 试用反馈待收。
**人眼项**：M6-10 / M6-11 已验；余下并入 M8-01。★ **HTTP 层可走了**（seed 6 账号 / `Dev@123456`）。

## 二、待拍板

**待确认一条**：§6.3 / F1 原句「**所有**在途关系从生效日重算」，实现取「**受该规则约束的**」那批 —— 字面全局在 F1 里**无处落**（`effective_from` 逐行）⇒ 要改先补承载列。
**待拍板一条**：`me.product_line_ids` 已**无消费方**（产品线下拉改按"部门承接"）—— **删字段**还是**留着**？
**已定**（细则见台账 / `git log`）：09-21 六条 ＋ D-53/65/66/68②/M9-F ①②/掉海按天；**09-22 三条**＝① D-73「只能建档自己部门」**不放宽** ＋ **D-74「产品线＝该部门承接」（前后端同源）** ② 系统·部门公海管理员 ➖＝矩阵原文 ③ 流程＝「闭环＋已验收 ⇒ 直提」（#8 例外）。

## 三、欠账

> 唯一落点 → `欠账登记表.md`（只增不删）。**本轮收口 D-73（② 用户自理）＋ D-74**。开工先翻**主表二**。

## 四、下个窗口开工动作

1. 读《AI执行清单》＋ 本文件（先看 §〇）＋ 台账。
2. 主线（计划 **V1.1**）：**M6 收口 ✅ → M8 验收 → 再进 M9**。**下一步＝M8 收尾**：M8-01/02/03 ✅ → **M8-05** 交销售试用（收反馈闭环 M8）。**M9 首域＝E 域（合同）**（解锁 D-01/D-02/D-10/D-61；序 `E→A→G→B/C/D→report`）；F 域剩余可续、不占主线。**其余**：D-49 回填 / D-48 · D-44 页 28（先拍板）· 基建 D-62 / D-15。
3. 收尾：后端三绿 ＋ 真起 `dist/main` / `dist/worker`（→ D-58）；前端 `lint / build`；单测 `--maxWorkers=2`；逐文件 `git add`（禁 `-A`）。
4. ⚠ 开工前 `git status --short` ＋ `git log --oneline -5`（本工作区**有并行窗口**）；暂存后别插长动作（案例库坑 38）。
