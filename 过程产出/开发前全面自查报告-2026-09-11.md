# 销售 CRM · 开发前全面自查报告（2026-09-11）

> 触发：七叔「开发前再检查所有需求文档和规范是否全部对齐，用真人肉眼操作体验模拟，找出还没设计好的」。
> 范围：需求 V1.14 / 数据架构 V1.17 / 接口 API V1.4 / 设计规范 V1.0 / 前端页面与交互 V1.6 + schema.prisma + migration。
> **★ 处置状态（2026-09-11 更新）**：①**§一.1.1 的 15 处矛盾已全部改正**（需求→V1.13、数据架构 V1.15→V1.17、接口 V1.2→V1.4，schema.prisma + migration 同步删 `gray_release_days` / `gray_release_at`）；②**§一.1.2 的 3 项澄清已按建议落地**（B 组）；③**第 6 条「高级搜索」按"最新＝只放动作型"一并删除**；④**P0-①「角色 × 页面可见性矩阵」已完成**（需求 V1.14 §4.1 主入口 / §4.2 数据范围 / §7.7 台账权限 ＋ 前端 V1.6 §四.2 矩阵）；⑤**P0 其余四项（撞库分支 / 签约校验 / 历史轮次 / 领海归组）尚未开工**（待七叔排期）。
> 结论一句话：功能骨架已齐、无大悬空；但有 15 处"正文没跟上决策"的自相矛盾（**已改正**），以及一批"体验层没设计到"的断层（**待补**）。

---

## 一、五文档对齐核查

### 1.1 🔴 硬伤：正文与已拍板决策矛盾（15 处，必须改）

> 根因：09-10 三批拍板改了规则，但**只在 §16 关键决策记录里记了，正文的旧表述没同步**。

| # | 位置 | 过期内容 | 与谁矛盾 |
|---|---|---|---|
| 1 | 需求 §12 表 row3 | 跨部门同产品线「**谁先签约谁得**」 | §5.1 / §16 #33·#46：**各签各的、互不干扰**（提示不拦截） |
| 2 | 需求 §三 术语词典「竞争并行」 | 「各跟各的…**谁先签约谁得**」 | 同上 |
| 3 | 需求 §12 末条 | 「**介绍费**：跨部门转介绍成交，介绍人拿 **10% 提成**」 | §7.6 / §14.2 / §16 #49：**介绍费不做**，改"合同业绩分配" |
| 4 | 需求 §6.3 | 「**灰度释放**（灰度 N/M 天规则）同口径…」 | §8.2：灰度寿命规则**只管提醒，不再列为释放候选** |
| 5 | 数据架构 A8 `gray_release_days` 说明 | 「再 M 天未动 → **列为公海释放候选**」 | 同上（应删该字段或改说明） |
| 6 | 数据架构 C1 `gray_release_at` 字段 | 「灰度寿命**释放候选**时间戳」 | 同上（应删字段） |
| 7 | 数据架构 §十二 定时任务「灰度寿命」行 | 「再超 release_days → **置 gray_release_at**」 | 同上 |
| 8 | 数据架构 F2 `sea_record.reason` 枚举 | 含 `gray_release` | 同上（应删该枚举值） |
| 9 | 接口 §5.3 部门规则出参 | 含 `gray_release_days` | 同上（应删；接口 §4.2 已是对的，此处漏改） |
| 10 | 数据架构 E3 工单 type | `aftersale/opportunity` | 接口 §2.6 / §5.10：`after_sale`/`opportunity`（**枚举名不一致**） |
| 11 | 需求 §一 文档体系表（2 行） | 接口/前端文档「**待建（V1 后置）**」 | 均已建成（接口 V1.2 / 前端 V1.4） |
| 12 | 需求 §一 前言 | 「接口怎么定/页面长什么样 → 见…（**待建**）」 | 同上 |
| 13 | 需求 §13 开头 | 「页面级细节见《前端页面与交互文档》（**待建**）」 | 同上 |
| 14 | 数据架构 §一 「下游」行 | 「接口文档（**待建**）、前端文档（**待建**）」 | 同上 |
| 15 | 需求 §13.4 快捷入口候选清单 | 含「高级搜索、**报表分析**」 | 同段写「**只放动作型**；数据看板类不进」——**报表分析是看板类，自相矛盾** |

> 另：需求 §12 整节（撞单与竞争 SOP）**只改了 §5.1，§12 没同步**；建议把 §12 作为"正文旧区"整体扫一遍。

### 1.2 🟡 需要澄清（非硬伤，但会卡开发）

| # | 事项 | 现状 | 需明确 |
|---|---|---|---|
| 16 | **业务关系详情 Tab 集合** | 需求 §7.4 列 8 个 Tab（含**商机**）；前端 V1.4 给了 8 个但含**复盘**、去掉了商机 | 二者不一致；且**「商机」到底是工单的一种（§6.4 商机工单）还是独立实体**，文档有歧义 |
| 17 | **`contact_trait_max` 调小后** | 需求说"存量保留、仅新写入生效" | 接口 `PUT /traits` 超限返回 422——**存量已超限的人再编辑任一特质会不会被误拦？** 校验口径需写明（只对"新增"拦，不对"编辑已有"拦） |
| 18 | **"跨业务线"金额脱敏 vs 目标/报表** | 脱敏按**部门**判断（§4.3） | 经理看**跨部门**汇总报表时，金额是否也脱敏？报表口径需明确（现文档只定义了列表/详情） |

### 1.3 ✅ 已对齐良好（可放心开发）

- **表结构**：45 表 ↔ 数据架构 ↔ schema.prisma ↔ migration 一致。
- **报表**：需求 §7.10 九张 ↔ 接口 9 个报表端点齐。
- **目标 / 复盘 / 竞品 / 文件 / 手机号变更**：需求 → 接口端点 → 前端页面三段齐全。
- **脱敏三处**（跨线金额 / 非归属手机 / 公海未领不返全文）：需求 §4.3 ↔ 接口 §2.8 ↔ 前端 §六 一致。
- **主题**：设计规范 §八 ↔ 前端 §十二 ↔ 接口 `/account/preferences` 一致（仅明暗两套）。

---

## 二、真人操作模拟（10 条动线，逐步走）

> 走法：假装我是"某销售/某经理"，按真实顺序点，看**每一步是否顺畅、下一步有没有着落**。

### 动线 1：销售早上开工 → 写第一条跟单 ✅ 基本顺
工作台 → 今日概览 → 点"写跟进" → 弹窗（一句话 + 承诺三快选 + 卡点 + 竞品折叠）。
- ⚠ **卡点**：工作台「今日概览 4 数」**没定义是哪 4 个**（前端 §六只写"4 数"）。
- ⚠ **卡点**：时间轴的「**补单**」是术语黑话，没定义（补写跟单？补录？）。

### 动线 2：电销连续拨打（一天 50 通，大半没接）⚠️ 设计不足
点"快速标记 → 未接电话" → 落库、不重置倒计时 ✅。
- 🔴 **断层**：**"批量"入口没有落点**。工作台是逐条动线、没有可勾选的多选列表；真正要批量的是"公海/关系列表"——但**列表页没设计"勾选 + 批量标记"的交互**（前端 §五 只写了"行操作"）。
- 🔴 **断层**：**没有"电销模式"**。连续打下一个客户 → 打不通 → 标记 → 下一个，这个**流水动作链没有设计**，而这是电销日常主场景。

### 动线 3：经理周一早上看盘 ⚠️ 一屏放不下
看板要装：4 KPI + 今日待跟进 + 状态环形图 + 活跃预警 3 卡 + **目标进度条** + **周重点僵尸榜** + **公海决策待办** + 部门对比 + 销售 TOP。
- 🔴 **断层**：**≥9 块内容**，与《设计规范》§七「一屏一主、减少视觉噪音」**直接冲突**。经理"第一眼该看什么"没有排序。
- 🔴 **断层**：**经理的动线没设计**——周一开周会要什么？月底复盘要什么？现在只有"一个大看板"。

### 动线 4：录入新客户（只拿到手机号）⚠️ 与 3 步流程打架
需求 §6.1 明说「**录入时允许只有手机号，公司名可暂不明确**」。
- 🔴 **矛盾**：前端录入页是**3 步（联系人→公司→确认）**，若"公司可空"，**第 2 步要能跳过**——但页面没设计"跳过公司/暂不明确"的路径与校验。

### 动线 5：撞库命中（4 种分支）⚠️ 只设计了一半
手机号查重命中后，需求 §6.3 有 **4 种分支**：公海可直领 / 本部门已激活 / 跨部门可并行激活 / 挂到现有公司。
- 🟡 **断层**：前端只写了"手机号查重提示"和"公司匹配确认弹窗"，**这 4 分支各自的按钮与文案没逐一设计**，销售遇到会不知道点哪个。

### 动线 6：签约（建合同）⚠️ 校验来源不清
关系详情 → 点"创建合同" → 需求说**按"本条业务线自己的必填清单"卡**。
- 🔴 **断层**：**"本条业务线必填清单"从哪来、谁配置、缺什么提示什么、一键去哪儿补**，页面全没设计。文档只说"完善度百分比只展示不卡"，但**真正卡签约的那份清单没有可视化的补全引导**。
- 🟡 **断层**：**业绩分配 UI 缺细节**（怎么选人、合计 100% 怎么校验、默认 signer 100% 怎么显示）。

### 动线 7：判死一单 → 别人重新领取 ⚠️ 接手人一脸懵
判死 → 死因点选 → 掉回公海（阶段保留）→ 别的销售领取。
- 🔴 **断层**：**接手人打开只看到"阶段 N"，没有任何"这是重新领取的关系 / 上一轮谈到哪 / 上次为什么判死"的提示**。需求说 `relation_stage_log` 留了上一轮，但**界面没设计这个"历史轮次"的展示**——接手人会重踩坑。

### 动线 8：领取公海后的跟单视图 ⚠️ 主线是空的
领取 → 继承全文跟单。但跟单"以 owner 分界"（主跟单人=新 owner）→ **新 owner 自己没填过，所有历史都成了"树杈"**。
- 🔴 **断层**：**新 owner 一打开，"我的主线"是空白，"树杈"一大堆**——视觉上很别扭，且分不清哪条是"上一个人跟的"。需要一个"前主人跟的"归组/标识设计。

### 动线 9：看自己的目标进度 ⚠️ 销售侧缺入口
需求 §7.11：目标由经理填，**销售只看自己**。
- 🟡 **断层**：**销售在哪看自己的月目标进度？** 数据看板/工作台都没明确"个人目标条"的落点（目标页 26 是经理/老板视角）。

### 动线 10：交付/客服 / 管理员登录 ⚠️ 角色视角缺失
需求 §4.1 有 5 个角色（销售/交付客服/部门经理/总经理/管理员），但 29 页几乎是**"销售 + 经理"视角**。
- 🔴 **断层**：**没有「角色 × 页面可见性矩阵」**。交付/客服登录后看到什么菜单、能操作什么？管理员呢？总经理呢？全都未定义——**这是最大的一个设计缺口**。

---

## 三、「还没设计好」清单（按优先级）

### P0 · 开工前必须定（否则代码写不下去）
1. ✅ **角色 × 页面/操作 可见性矩阵**（5 角色 × 29 页 + 关键按钮）——**已补（P0-①）：前端 V1.6 §四.2 矩阵 + 4 条落地规则**。
2. ✅ **撞库 4 分支 + 录入可跳过公司** 的交互与校验——**已补（P0-②）：需求 §12.1 + §6.1 模型 B + 前端 V1.7 页 7**。
3. **签约校验清单**来源与补全引导（影响成交主流程）。
4. **判死/流失后重新领取的"历史轮次"展示** + **公海领取后跟单"前主人"归组**（影响接手体验，需求说留了数据但没设计视图）。
5. **15 处文档矛盾**（§一.1.1）——尤其"介绍费/谁先签约谁得/灰度释放"，**改完再开工，否则开发照旧文档就白改**。

### P1 · 强烈建议开工前补
6. **数据看板的层级与经理动线**（一屏一主怎么落地、周一/月底各看什么）。
7. **电销批量/连续拨打** 的交互（列表勾选批量标记）。
8. **销售侧个人目标进度**落点。
9. **通知中心 vs 今日动线** 的分工与去重（双通道重复风险）。

### P2 · 可开发中迭代
10. 移动端范围是否够（外勤看客户资料/签约要 PC → 真实场景断点）。
11. 「补单」等术语统一；工作台"今日概览 4 数"明确。
12. 经理跨部门汇总报表的金额脱敏口径。

---

## 四、建议

1. **先清 15 处矛盾**（本报告 §一.1.1）——这是纯校正，我可以一次性改完、逐条留痕。
2. **再补 P0 五项设计**（角色矩阵 / 撞库分支 / 签约校验 / 历史轮次 / 领海归组）——这五项要么进前端文档，要么单独出《交互补充说明》。
3. P1 可并入前端文档 V1.5 一起做。
4. 全部落定后，再按 数据架构 §十五.4 顺序开写模块。

> **总评**：文档体系已经很扎实（45 表 / ~80 端点 / 29 页 / 设计系统齐全），**功能层面无大悬空**；真正欠的是**"角色视角"和"几个关键时刻的体验细节"**——这些往往是最影响"好不好用"的地方，值得在动手前补上。
