# 销售 CRM · 开发计划

> **当前修订**：**V1.1**（2026-09-22，七叔拍板）—— ① **M8 验收提前**（紧跟 M6 收口，先让销售试用）；② **M9 按「阻塞图」重排**（`E → A → G → B/C/D → report → 前端`）；③ 新增 **§五·补〈插入批次登记表〉**（计划外工作的唯一归口）；④ 清掉 M7-03 残留作废口径（"小时级 / 24h / 6h"）；⑤ §三 基线回填至 **2026-09-21**（旧数 621/618 作废）。版本沿革见 git。
>
> **版本口径**：文件名保持 `开发计划-V1.md`（其中的 `V1`＝"**产品 V1 的计划**"，不是修订号）；**本文件自身的修订号只写在上面的状态行** —— 标题不写、互引不写（→《AI执行清单》#13 / 坑 37）。⚠ 代码注释与文档里既有的「《开发计划-V1》M\*-**」指针**一律继续有效、不必改**。
>
> **⚠ 定位声明（本文件是"过程产出·执行计划"，不是规范）**
> - 本文件＝**★ 索引·非规范**：只做「任务拆解 + 排序 + 验收口径」，**不定义任何业务规则**。
> - 规则以五份现行规格为准；进度沿革**唯一落点＝ git**（`git log` / `git show`）。
> - 与规格文档冲突时，**一律以规格为准**，并回改本文件。
>
> **★ 开工前铁律**：先读《废止口径登记表》→ 再读需求 §十六《否决与后置清单》（负面约束**完整**清单）→ 再动手。**"表里没有"才代表"允许"。**
>
> **本版＝唯一现行开发计划**。被取代的两份旧计划（里程碑地图版 / 原子任务版）已于 2026-09-12 拍板**物理删除、不入归档**，决策留痕见 §十 P1。

---

## 〇、本版如何产出（按《生产级工程生命周期技能》）

### 0.1 Define → Plan 两阶段的产物与退出标准

| 阶段 | 产物 | 退出标准 | 达成 |
| --- | --- | --- | --- |
| **Define** | §一（依据锁版）＋ §二（目标 / 范围 / DoD）＋ `过程产出/CONSTRAINTS.md`（质量门槛固化） | ① 规格已定稿且锁定版本；② 质量门槛**以文件形式固化** | ① 五份规格现行版已锁定 ② `CONSTRAINTS.md` 已存在（**沿用，不重造**） |
| **Plan** | §四（里程碑地图）＋ §五（原子任务 M0–M8）＋ §七（退出标准）＋ §九（反合理化表） | 任务列表存在；每条任务 **2–5 分钟**可完成且**有可粘贴的验收证据** | §五 任务已原子化；§六 M9 按域**渐进下钻**（避免一次塞满上下文） |

### 0.2 六阶段在本项目的落法（Build / Verify / Review / Ship 门禁）

| 阶段 | 本项目动作 | 门禁（不达标不进下一步） |
| --- | --- | --- |
| **Build** | **垂直薄切片**：一条动线逐段打通（架构 §九 步 0→8）；先写用例再改逻辑（红→绿） | 每步＝**一次可提交的还原点**；T0/T1 全绿（CONSTRAINTS §一） |
| **Verify** | 真库 / 真账号 / 真实运行时数据验证（**不许只跑单测**）；调试走五步：复现 → 二分定位 → 假设验证 → 最小修复 → 回归证据 | T2-1 真库 migration 核对、T2-2 动线端到端 |
| **Review** | **五轴评审**，变更控制在 ~100 行：① 业务口径正确性 ② 可读性 ③ **模块边界（架构 §5.2/§5.4）** ④ 安全（越权 / 脱敏 / 注入 / 密钥）⑤ 性能（**先测量再优化**） | 越界依赖、跨域大事务、报表被脱敏 —— 任一出现即打回（CONSTRAINTS §二） |
| **Ship** | 单线 `main` + 原子提交（提交即还原点）；文档改动**同批同次提交**（README §八 L0） | 未提交的文档改动＝沿革断档，视为未完成 |

> **反合理化**：本技能的核心武器是「借口 vs 反驳」表，见 §九。出现表内话术，**直接判未完成**。

---

## 一、计划依据（锁定版本）

> 版本以**各文档头部权威声明块**为准；本表只记"本计划用到哪部分"。版本沿革查 git。

| 依据 | 文档 | 本计划用到哪部分 |
| --- | --- | --- |
| 业务规则 | 《销售CRM业务需求文档》 | §4 角色权限 / §5~§6 模型与生命周期 / §7 功能模块 / §8 意向体系 / §10 行动引擎 / §11 复盘 / §14 范围 / **§16 负面约束** |
| 表结构 | 《销售CRM数据架构文档》 | §三~§九 七域 46 表 / §10.1 索引清单 / §10.2 并发兜底 / §十二 定时任务 / §15.4 开发顺序 / §15.5 Prisma 落库口径 |
| 接口 | 《销售CRM接口API文档》 | §二 通用约定 / §三 总目录（索引·非规范）/ §四 关键契约 / §五 数据结构 |
| 页面 | 《销售CRM前端页面与交互文档》 | §四.2 角色矩阵 / §五 29 页 / §六 单页 |
| 设计 | 《销售CRM设计规范》 | Design Tokens（Ant Design 5 基准）/ 组件规格 / 主题 |
| 代码架构 | 《销售CRM架构设计说明》 | §四 目录 / §五 边界 / §六 进程 / §七 横切 / **§九 8 步竖切顺序** |
| 已废止口径 | 《废止口径登记表》 | 全文（**条数以该表实数为准**） |
| 质量门槛 | `过程产出/CONSTRAINTS.md` | T0/T1/T2/T3 四层 ＋ 10 条硬约束 ＋ DoD |

---

## 二、目标与范围

### 2.1 V1 目标（＝需求 §14.1）

打通「**登录 → 建公司 / 联系人 → 建业务关系 → 写跟单 → 工作台看到它**」这条动线（穿透 A/B/C/D 四域）→ 让真实销售**能开始试用**；随后按域补齐 V1 全部功能。

### 2.2 明确不做（**防走偏清单**，动手前先记牢）

**范围级（需求 §14.2）**：客户出口 / 报销审批 / 介绍费·提成分账 / 客户地图·附近客户·顺路推荐 / 自动处境引擎 / 竞品价格表·功能对照档案库 / 自动地址解析 / 拜访 GPS 校准 / 外部工商数据源对接。

**实现级（需求 §十六，★ 规范级 · 完整清单）**：如**承诺不做独立录入页**、**阶段绝不自动改**、**客户等级不可手填**、**掉公海只掉一步**、**@求助限同部门**、**跟单分界＝owner 不是登录人**、**快速标记不重置掉海倒计时**、**无任何自动删除 / 自动流转**、**报表出口不脱敏**……

> **本段不复制规则本体**，只作任务计划时的"封路提醒"；**实现边界一律以 §14.2 与 §十六 为准**。

### 2.3 完成定义（DoD，每个里程碑都适用）

1. `npm run lint` 通过（**模块边界硬卡**，故意跨域 import 必须报错）；
2. 涉及的业务铁律有**单测**（`domain/` 纯函数用假数据）且 `npm test` 绿；
3. 接口在 `/docs`（OpenAPI）可见、响应符合接口 §二 统一包与 §五 数据结构；
4. 一次**可提交的还原点**（README §三：直接 `main`，提交信息写"改了什么 / 为什么 / 联动哪些文档"）；
5. 本次变更若触动业务规则 → **先改需求文档，同批联动接口 / 前端 / 数据架构，同一次提交完成**（README §八 L0 铁律）。

---

## 三、当前基线

> **进度快照不在本文件复述** —— 唯一落点＝《过程产出/**交接说明**》。本节只留「开工环境」与「不得照抄的旧数」。
> ★ **V1.1（2026-09-22）回填**：上一版此处停在 M6-07（2026-09-16），已滞后 **20+ 次提交**；下表照实回填到 2026-09-21。

- **已闭环**：**M0 地基 / M1 A 域 / M2 B 域 / M3 C 域 / M4 D 域 / M5 横切层 / M7 Worker 骨架**；**M6 前端收口 ✅ 闭环**（05 ✅ `42aeb5b` · 06 ✅ `b978965` · 07 ✅ `6362fd6` · 08 ✅ `31452ae` · 09 ✅ `6169492` · **10 ✅ `4b764cf`** · 11 ✅ `c94ec7a` · **12 ✅ 本批提交**）。
- **插入批次已完工**：**F-01 公海「领取到私海」** ✅（施工单已归档）；**M9-F 掉海 ⏳ 大部分 ✅**（真掉 ＋ `sea_record` / 规则配置 `GET|PUT /sea/rules` / **掉海改按天** / migration `0012`）。
- **★ 下一批（V1.1 口径）**：**M8 验收** —— M8-01 真账号走完整动线 → M8-02 勾 §七 退出标准 ＋ T2 四项 → M8-03 打 tag（⚠ 仓库 tag **为空**）→ M8-05 交销售试用。
- **环境**：本机 phpStudy（**不走 Docker**）**MySQL 8.0.12** ＋ **Redis 3.0.504**（⚠ 代码里**尚未接入** Redis 客户端）；真库 `win_crm`；migration 已应用到 **`0012`**（`0001_init` 基线 ＋ 增量 `0002`~`0012`）。⚠ 8.0.12 < 8.0.16 → `CHECK` 静默失效（→ `服务端/prisma/README.md` §四.1）。
- **测试基线**：后端 **854 用例 / 54 suites**（2026-09-21；⚠ `shared.module.spec` 在本工作区跑不起来 ＝ **1 failed to run**，用例本身 0 断言失败 → **D-15**）。⚠ **作废旧数**：本节上一版写的 `44 suites · 621`，以及更早的 `618 / 25·290 / 23·254 / 18·160` **一律不认**。
- **前端**：`前端/` 已建 **15 页**（登录 / 工作台 / 建档 / 业务关系列表 · 详情 / 联系人详情（页 15）…）＋ 路由收口（M6-05）；**`pinia` 仍未引**（跨页状态在 `src/session.ts`）；**引依赖要单独拍板**。

---

## 四、里程碑总览

> 步 0~8＝**竖切一条动线**（架构说明 §九），每步一个还原点；M9 起＝按域横切补齐（数据架构 §15.4）。
>
> ★ **V1.1 两处顺序调整**（2026-09-22 拍板）：**① M8 验收提前** —— 紧跟 M6 收口，**不再等 M9**（理由：M8 出口＝"可以让销售开始试用"，而**真实试用是"需求细节调整"的唯一可靠输入源**，不先上用户则后续调整全靠推演）；**② M9 首域改 E 域（合同）** —— 它是当前**最大阻塞源**，`D-01`（`serving` 档）/ `D-02`（报表出口）/ `D-10`（`amount`·`amount_masked`）/ `D-61` 剩余（`sign_date`·`amount`）**四处同时卡在它身上**；而已开工的 F 域**不阻塞任何域**，其剩余项走 §五·补，不占主线。完整排序见 §六。

| 里程碑 | 范围 | 交付标志（能看见什么） | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| **M0 地基** | 环境 + kernel + Prisma 接入 + OpenAPI + ESLint 边界 + 双进程入口 | `start:web` 起来、`/docs` 有骨架、跨域 import 被 lint 拦下、真库 migration 跑通 | — | ✅ 闭环 |
| **M1 A 域最小切片** | 登录 / 当前用户 / 组织只读 ＋ 登录页（M1-18） | 真账号登录，拿到「我是谁、管哪些部门」；**浏览器里能登进去** | M0 | ✅ 闭环（`edf46a2`） |
| **M2 B 域最小切片** | 公司查重建档 + 联系人建档 ＋ 建档页（M2-17） | 建 1 公司 + 1 联系人；重复手机号被拦（409）；**页面上能建** | M1 | ✅ 闭环（`07bbb0e`） |
| **M3 C 域最小切片** | 建业务关系 + 私海 / 公海列表 ＋ 关系列表页（M3-14） | 能建关系，列表能看到它；**页面上能看到** | M2 | ✅ 闭环 |
| **M4 D 域最小切片** | 写跟单 + 关系时间线 + 工作台今日概览 ＋ 工作台 / 时间线页（M4-17） | 写跟单后，时间线与首页都看得到；**页面上能点通** | M3 | ✅ 闭环（`4375d66` `4bd73fb` `18ed8f9`） |
| **M5 横切层** | 数据范围注入 + 脱敏 + 审计 | 销售 A 登进去看不到销售 B 私海 | M4 | ✅ 闭环（`35b7fd0` `5317e78` `f0c34a3` `81bc170` `d98752b`） |
| **M6 前端收口** | 页面**补全**（29 页分批）＋ 视觉打磨 ＋ 角色矩阵 | 整条动线在浏览器里**好用、好看**（页面已在前序里程碑逐步点通，M6 ＝ 补全 + 打磨） | M5 | ✅ **闭环**（05 ✅ `42aeb5b` · 06 ✅ `b978965` · 07 ✅ `6362fd6` · 08 ✅ `31452ae` · 09 ✅ `6169492` · **10 ✅ `4b764cf`** · 11 ✅ `c94ec7a` · **12 ✅ 本批提交**） |
| **M7 Worker 骨架** | 独立入口 + 首个定时任务（掉海预警） | Worker 单独跑起来，日志见扫描结果 | **M0**（可并行） | ✅ **闭环** |
| **M8 验收** | 真账号走查 | **可以让销售开始试用** | **M6-12**（M7 ✅ 已不阻塞） | ⬜ **★ V1.1 提前：紧跟 M6 之后** |
| **M9 按域补齐** | **`E → A → G → B/C/D → report → 前端`**（V1.1 按阻塞图重排；F 域已提前开工 → §五·补） | V1 全功能就位 | M8 | ⏳ **部分开工**（F 域大部分 ✅；其余各域状态见 §六） |

---

## 五、阶段任务明细（原子任务）

> **读法**：每条任务目标 **2–5 分钟**完成；「依赖」写前置任务编号；「验收方式」＝**可粘贴的证据**（CONSTRAINTS §三）。
> **约定**：服务端路径相对 `服务端/`；前端路径相对 `前端/`；`Mx-yy` 为全局唯一编号，可作提交信息前缀与 tag 依据。
> **✅ 已完成里程碑的任务定义已压缩为清单**（验收证据属过程留痕，一律查 git；**编号保留**，供跨里程碑引用）。

### M0 · 打地基（✅ 闭环）

**M0.1 环境与骨架**：`package.json`（`start:web` / `start:worker` / `start:prod:*` / `lint` / `test` / `gen:types`）｜`tsconfig.json`（`strict` ＋ `@/*`）｜`nest-cli.json`｜`.gitignore`｜`.env.example`｜`docker-compose.yml`（**本机不跑**，留服务器 / CI 用）｜最小 `main.ts` / `worker.ts`。

**M0.2 真库 migration 核对（最贵，里程碑级必跑 ＝ CONSTRAINTS T2-1）**：起真库 ＋ 真缓存（本机 phpStudy，**不走 Docker**）｜建空库 `win_crm`（utf8mb4）｜执行 baseline ｜核对 3 个生成列 `GENERATED ... STORED`｜补 3 个唯一索引 `uk_active_rel` / `uk_owner` / `uk_phone_active`｜核对**恰 3 张**分区表 ＋ `action_event` / `daily_agenda` **未分区**｜核对 `action_event.uk_idem` 回退为全局唯一｜核对视图 `v_contract_performance`｜核对 `approval` CHECK（**受 8.0.12 限制，改应用层兜底**）｜反查 drift｜`migrate resolve --applied` 登记基线。

**M0.3 kernel**：`errors/app-error`｜`errors/prisma-error.mapper`（**P2002 → 409/422**，约束名取 `meta.driverAdapterError.cause.constraint.index`）｜`common/pagination`｜`common/bigint`｜`context/request-context`（`AsyncLocalStorage`）｜`context/context.module`｜`events/domain-event`（10 个约定事件名）｜`events/event-bus`｜`audit/audit.service`（**只接受事务上下文**）｜`kernel/index`。

**M0.4 shared**：`guards/jwt-auth.guard`｜`interceptors/response.interceptor`（统一包）｜`filters/all-exceptions.filter`｜`pipes/validation.pipe`（全局 class-validator）｜`interceptors/data-scope.interceptor`（**占位**）｜`interceptors/desensitize.interceptor`（**占位**）｜`shared.module` ＋ 全局注册。

**M0.5 ESLint 模块边界（硬卡，不靠自觉）**：基础配置 ＋ 8 条负向规则 —— 第 4 层互禁（M0-40）｜`company` 禁第 4 层（M0-41）｜`org` 禁业务域（M0-42）｜`domain/**` 禁框架 / Prisma（M0-43）｜`*.repository.ts` 仅本域（M0-44）｜`kernel/**` 禁 `modules/*`（M0-44b）｜业务域只许引 `kernel`、禁 `shared/**`（M0-44c）｜`kernel/**` 禁 `shared/**`（M0-44d）｜**T1-3 负向自检逐条跑并留档**（M0-45）。

**M0.6 Prisma 接入 + OpenAPI + 冒烟**：`prisma.service` / `prisma.module`（`@Global`）｜Swagger `/docs` 骨架｜`app.module` 组装｜`GET /health`（真查库）｜跑冒烟 5 条（M0-51）｜提交（M0-52）。

**M0.7 前端接入（★ 2026-09-13 按方案 A 前置，原 M6-01~04）**：前端骨架 `前端/`（Vue 3 + TS + Vite + **Ant Design Vue 4.2.6**）｜落地《设计规范》token｜axios 封装（统一包 + 401 自动刷新）｜`npm run gen:types` → `前端/src/api/types.ts`（**禁止手写**；每里程碑末重跑，diff ＝ 接口变更证据）。

> **双轨验收铁律**（M0.7 起）：curl / 单测 ＝ **质量门（不可省）**；页面可点通 ＝ **体感门**。**页面不得替代单测。**
> ⚠ **Ant Design Vue 版本口径**：库里 `dist-tags.latest` ＝ **`4.2.6`**（**不存在"Ant Design Vue 5"**）；「**Ant Design 5**」指**设计基准 token**（《设计规范》§二），**不是库版本号**，两者不可混写。

**M0-53 / M0-58（已完成）**：增量 migration `0002` 上真库｜**Prisma 6.19.3 → 7.10.0 升级**（→ 口径见 `服务端/prisma/README.md` §二）。
**M0 出口**：见 §七。**未过 T2-1（真库 migration）不得进入 M1。**

### M1 · A 域最小切片（✅ 闭环，提交 `edf46a2`）

M1-01 建 `modules/org/` 四层 + `org.module`｜M1-02 对照数据架构 §四 逐字段确认 6 张表｜M1-03 `org.repository` 按**手机号 或 `username`** 查员工（**双通道**）｜M1-04 查角色码｜M1-05 查管辖部门｜M1-06 `domain/data-scope.ts` **四档**（含 `serving`）＋ 多角色取更宽｜M1-07 `domain/password.ts`｜M1-08 `login`｜M1-09 `refresh`｜M1-10 `me`｜M1-11 `POST /account/login`｜M1-12 `POST /account/refresh` ＋ `GET /account/me`｜M1-13 四个组织只读接口，**`/org/employees` 服务端按数据范围收敛（G7）**｜M1-14 登录 DTO｜M1-15 审计（**事务内**）｜M1-16 T0/T1 ＋ `/docs` 自检｜M1-17 提交｜**M1-18 登录页（方案 A 最小页）**。

> **A 域自检**：`src/modules/org` 内的 import **不含其他业务域**。
> **⚠ 域内单测若被边界规则拦下**（想 import 别的域造场景，或想引 `shared`）→ **停下报告，不许自行改 `eslint.config.mjs` 绕过**：护栏被绕一次就形同虚设（架构 §5.4「靠工具，不靠自觉」）。跨域场景按计划放 `test/`（路径不含域名 → 天然不被拦），或按当时真实需求拍板后再动配置。

### M2 · B 域最小切片（✅ 闭环，提交 `07bbb0e`）

M2-01 建 `modules/company/` 四层 + `company.module`｜M2-02 `company.repository` 插入公司（`credit_code` 撞 `uk_credit_code` → P2002）｜M2-03 公司名查重（**第一段走 `name_core` 前缀**、含 `deleted_at IS NULL` ＋ 跳过墓碑 `merged_into`）｜M2-04 插入联系人（`phone_active` 生成列生效）｜M2-05 `domain/company-name.ts` 标准化纯函数（词表＝`company-name-words.ts`，**业务可编辑的数据**：地域词**只在开头剥**、行业 / 类型词按长度降序任意位置剥、**剥空回退基础归一值**）｜M2-06 `domain/company-name-similarity.ts` **两段式第二段**（完全相同 → `same`；编辑距离 ≤1 且长度差 ≤1 且 core ≥2 字 → `high_sim`；**码值取接口 §5.4 `match_type`**，不另造中文码）｜M2-07 `domain/contact-phone.ts` 归一纯函数（去空格 / `+86` / `-` / 全角数字 / 圆括号 / `0086`；**去国家码只在「剩 13 位且以 86 开头」时做**）｜M2-08 `createCompany`｜M2-09 `searchDup`｜M2-10 `createContact`（**本域事务**）｜M2-11~14 四个 controller｜M2-15 软删后旧号可复用（真库集成）｜M2-16 提交｜**M2-17 建档页（方案 A 最小页）**。

### M3 · C 域最小切片（✅ 闭环）

M3-01 建 `modules/relation/` 四层 + `relation.module`｜M3-02 插入 `business_relation`（**不写生成列** `active_key`，由 DB 生成；预检用等价条件）｜M3-03 插入 `relation_member`（撞 `uk_member` / `uk_owner` → P2002 → 人话）｜M3-04 `domain/relation-active-key.ts`（**格式与 DB 生成列 `CONCAT_WS('-',…)` 逐字一致**，单测钉死）｜M3-05 `domain/relation-owner.ts`（一关系一 owner ＋ 有效协同 ＋ @求助同部门）｜M3-06 `createRelation`（**本域事务：关系 ＋ owner 成员**；预检 ＋ P2002 兜底**同一句人话**）｜M3-07 私海列表（`self` / `dept` / `all`）｜M3-08 公海列表（`company_sea` 且未并；**部门公海＝本部门的关系集合**）｜M3-09 `GET|POST /relations`｜M3-10 `GET /relations/:id` ＋ **`PUT /relations/:id`**（⚠ 计划原写 `PATCH`，**§5.6 规格是 `PUT`** → 以规格为准）｜M3-11 `GET|POST /relations/:id/members`（owner 一关系一人；`ask_help` 跨部门 403；默认 7 天）｜M3-12 **并发同键 → 一成功一 409**（以**单测**覆盖；真库只验「预检命中 409」）｜M3-13 提交｜**M3-14 关系列表页（方案 A 最小页，只读）**。

> **跨域约定**：D 域**禁止查 C 域的表**；用 `RelationCreated` 事件（架构 §5.3）。

---

### M4 · D 域最小切片（✅ 闭环，架构 §九 步 4）

M4-01 建 `modules/engine/` 四层 + `engine.module`｜M4-02 `action-event.repository`（写 `action_event`，**幂等 `uk_idem`**）｜M4-03 `commitment.repository`（建 / 查）｜M4-04 `domain/event-effective.ts`（**有效沟通**判定；**快速标记不算**）｜M4-05 `domain/last-event.ts`（仅有效沟通才回写 `last_event_at`）｜M4-06 `domain/event-idempotency.ts`（幂等键纯函数）｜M4-07 `engine.service.recordEvent`（**本域事务**：写事件 ＋ 条件回写）｜M4-08 关系时间线查询（按时间倒序）｜M4-09 承诺 建 / 列 / 改（三型 me / them / verdict 基础）｜M4-10 今日概览（简版，直查 `daily_agenda`）｜M4-11 订阅 `RelationCreated` → 落首条建档事件｜M4-12~14 三个 controller：`GET|POST /relations/:id/events`、`GET|POST /relations/:id/commitments`、`GET /today-agenda`｜M4-15 测试铁律 #3（快速标记**不重置**掉海倒计时）｜M4-16 提交｜**M4-17 工作台今日概览 ＋ 关系时间线最小页**（页面级已用**真浏览器**点通）。

> **已裁定（原「M4 开工前必须先裁」项）**：`workflow_stage` 取**数字 `1~7`**（`4375d66`）—— 该项已闭，不再待裁。
> **跨域约定**：D 域**禁止查 C 域的表**；用 `RelationCreated` 事件（架构 §5.3）。

### M5 · 横切层（✅ 闭环，架构 §九 步 5 / §七）

**M5-01** 填充 `data-scope.interceptor`（**判定已收口到 kernel、各域只落地**）—— 销售＝`owner` ∪ 未过期协同 ∪ 公海；**交付 / 客服＝仅"服务中"（合同服务期内）客户，只读、不进公海**；经理＝管辖部门；总经理 / 管理员＝不过滤（管理员另加只读 ＋ 每次查看留痕）⚠ **A 域 `/org/employees` 的 G7 收敛已在 M1-13 先行落地**，M5 只做"注入到所有列表查询"的统一机制。
M5-02 `domain/data-scope-filter.ts`（**四档** scope → where 片段）｜M5-03 硬约束 2（`repository` **不得手写** `where owner_id = …`）｜M5-04 填充 `desensitize.interceptor`（**详情全号 / 列表·卡片 `phone_masked` / owner 上锁时给 `phone_locked`**）｜M5-05 `domain/desensitize.ts`（按**部门**判跨业务线）｜M5-06 出口白名单（报表 / 看板 / 汇总**不脱敏**）｜M5-07 审计切面接敏感动作（＋ 管理员查看留痕）｜M5-08 越权统一 → 403｜M5-09 测试铁律 #11｜M5-10 提交。

> **⚠ 落地现状（防误读 · 明细见《交接说明》§三「欠账」）**：**报表 / 看板 / 汇总出口尚未建** ⇒ M5-06 **只落开关 ＋ 单测**；`serving` 档**只做形状**（E 域合同未建）⇒ 暂按 `self` 收敛；`GET /contacts/:id`、上锁 / 解锁 / `phone_unlock` 审批**未建** ⇒ 其脱敏与留痕随接口补。

### M6 · 接前端（⏳ 进行中，架构 §九 步 6）

| 编号 | 任务 | 验收方式 | 依赖 |
| --- | --- | --- | --- |
| M6-01~04 | ✅ **已前置 → M0-54 ~ M0-57**（2026-09-13 方案 A） | 见 M0.7 | — |
| M6-05 ✅ | **已完成**（`42aeb5b`）路由收口 + **登录页打磨**（最小版已于 M1-18 落地） | 真账号登录进工作台，视觉符合设计规范 | M0-56 |
| M6-06 ✅ | **已完成**（`b978965`）工作台页**补全**（最小版已于 M4-17 落地） | 页面数据与 `/today-agenda` 一致 | M0-57 |
| M6-07 ✅ | **已完成**（`6362fd6`）业务关系列表页**补全**（最小版已于 M3-14 落地）＋**分页**（后端出参改 §2.3 分页形态、前端接分页器；接口文档升 V1.20） | 与 `/relations` 一致 | M0-57 |
| M6-08 ✅ | **已完成**（`31452ae`，2026-09-16）关系详情页（详情 + 成员）：新增 `/relations/:id` 路由＋详情页（信息卡/徽标/成员表，三态齐全），列表页公司名可点进详情（解 D-13 一半）。⚠ 规格 §5 的 8 Tab（合同/工单/关系网/复盘…）**未建**（数据分属其它域、服务端未返回 → 欠账 D-22） | 与 M4 接口一致 | M0-57 |
| M6-09 ✅ | **已补 2026-09-22 `6169492`**：录入页结果态补「写一条跟单」（新建 `components/EventComposer.vue` 复用件，`relation`/`contact` 两态；快速标记在列表页，片 3 早已落地，本片只补"写跟单"）。⚠ 列表页 / 联系人详情页两处内联同款表单待迁 → 欠账 **D-72** | 提交后列表与工作台可见 | M6-08 |
| M6-10 ✅ | **已完成 2026-09-22 `4b764cf`**（★ 代码 09-18 就落在 `e1fdc7d`，本批补的是**那一格缺口 ＋ 走查**）：矩阵新增 `sea` 档位（§4.2「系统 / 部门公海」行的落点，＝关系列表页的「公海」页签），**交付 · 客服 / 管理员不下发该页签** —— 原「页签照摆 + 403 内联提示」正是 §4.2 落地规则 4 禁的"显示了再禁用"。**走查**：6 真账号 × 4 写端点全 403 `20003`（落库前拦）＋ 真模块跑出 6 角色导航/档位 ＋ 人眼过（侧栏 / 公海页签）。⚠ 管理员 ➖ 按矩阵原文（接口 §4.4 的数据层读权不变） | 前端 §四.2 逐行走查（T2-3）✅ | M6-05 |
| M6-11 ✅ | **已完成 2026-09-22**（代码 `c94ec7a`）：主题（白天 / 夜间）切换生效且无闪烁 —— **人真点过一次**（判据 → D-41①）；存账号侧由 D-37 ✅ / D-38 ✅ 承担 | 切换生效且无闪烁 ✅ | M0-55 |
| M6-12 ✅ | **M6 提交**：收口提交 ＝ `4b764cf`（M6-10 代码）＋ 本批文档提交 | `git log -1` 可见 ✅ | M6-11 |

> ★ **V1.1 注（M6-11 已收口 2026-09-22）**：主题 / 侧栏折叠**存账号**由 **D-37 ✅**（`7fd4758`，migration `0009`）＋ **D-38 ✅**（`b30e1b7`，设计规范 §八.7 dark 色值表）承担；本批补的是"切换生效无闪烁"的**人眼验收**（⚠ 判据：**人真点过一次**才算 —— 教训见 D-41①）。
> ★ **M6 出口 → 立即 M8（V1.1）**：M6-12 提交后**不停顿**，直接进 **M8-01 真账号走查**；M8 不再排在 M9 之后（见 §四注 / §五 M8 段）。

### M7 · Worker 骨架（✅ 闭环）

> **前置**：仅需 M0 完成，**可与 M1–M6 并行推进**。
>
> ⚠ **编号归口（2026-09-20）**：F 域「领取到私海」这批曾用 `M7-01` 编号（历史文档与旧注释）—— 它**不是**本段的 M7-01。自本日起它改称 **`F-01`**（施工单 ＝ `归档/过程产出/F-01 施工单.md`，**2026-09-20 已完工归档**）；本段 `M7-01`~`M7-06` 专指 **Worker 骨架 / 掉海预警**。

| 编号 | 任务 | 验收方式 | 依赖 | 状态 |
| --- | --- | --- | --- | --- |
| M7-01 | `worker.module.ts` 组装 + 独立入口（**不监听端口**） | Worker 单独跑起来，无端口占用 | M0-52 | ✅ |
| M7-02 | `jobs/` 约定 + `job_run_log` 写入（每次跑留痕） | 跑一次后表中有记录 | M7-01 | ✅（`D-55` 已删空转的 `heartbeat.job`） |
| M7-03 | 第一个 job：**掉海预警** —— ★ **V1.1 口径**：**每日一次**、**按自然日（Asia/Shanghai）判档**（`≤3天`进动线 / 到期前 `1天` 推销售 / 到期当天标红推经理 / **到期日已过 ⇒ 次日真掉**） | 日志可见扫描结果与命中数 | M7-02 | ✅ |
| M7-04 | `domain/sea-warning.ts`：三档阈值判定纯函数 | 单测：三档边界各 ≥2 例 | M7-01 | ✅ |
| M7-05 | 测试：**只告警、不真掉**（不写 `sea_record`、不清 owner） | 单测 / 集成断言无写操作 | M7-03 | ✅（见下行注） |
| M7-06 | M7 提交 | `git log -1` 可见 | M7-05 | ✅ |

> ★ **V1.1 注（三处已作废 / 已升级的口径，防照旧文实现）**：
> ① M7-03 原写「**小时级**，`≤3天 / 24h / 6h`」—— **已作废**（`f7fece8` 改**按天**，→《废止口径登记表》**#41**）；现口径＝上表。
> ② M7-05 的「**只告警、不真掉**」是 **M7 阶段**的口径；**M9-F 已实现"真掉"**（`overdue` 档写 `sea_record` ＋ 撤 owner，→ §五·补 M9-F 行）。**M7-05 不因此改为"已作废"** —— 它描述的是"该里程碑内不许越权写库"，仍是有效护栏。
> ③ 掉海扫描的**任务级留痕**已落（`sea.drop`，→ 架构 §7.4 ⑤ / D-68②）。

> **纪律**：Worker **全局只允许 1 实例**（架构 §六，硬约束 6）。

### M8 · 验收（★ V1.1 提前：紧跟 M6 之后，架构 §九 步 8）

> ★ **为什么提前**：M8 的出口是「**可以让销售开始试用**」—— 它是需求 §14.1 的 V1 目标，也是**"需求细节调整"的唯一可靠输入源**。不先让真实销售用起来，后续所有细节调整都只能是推演出来的（这正是当前"调整很多"的根因之一）。
> **前置**：**M6-12 提交**（M7 ✅ 已闭环，不再阻塞；原写的 `M7-06` 依赖已满足）。

| 编号 | 任务 | 验收方式 | 依赖 |
| --- | --- | --- | --- |
| M8-01 ⏳ | 用**真账号**走完整动线（登录 → 建档 → 建关系 → 写跟单 → 工作台）。**★ 2026-09-22 接口侧已走通**（销售 E003 王海涛）：登录 ✅ → 建档公司 **12** → 建档联系人 **22**（挂 12）→ 建关系 **13**（`stage=1`、owner＝王海涛）→ 写跟单 **39**（`phone` / `advanced`）→ 时间线 **2 条**（跟单 ＋ 系统「建档：激活业务关系」）。⚠ 工作台 `/today-agenda` ＝ `[]` **属合理空态**：承诺 2 条均已闭环、预约 0、节奏规则 0、**掉海进动线未接（→ D-56①）** | 逐步留证据（截图 / curl）；⛔ **剩余＝人眼截图** | M6-12 |
| M8-02 | 勾选 §七 全部退出标准 + CONSTRAINTS T2 四项 | 逐条打钩 | M8-01 |
| M8-03 | 打 tag（如 `v0.1-mvp-竖切动线`） | `git tag` 可见（⚠ 当前仓库 **tag 为空**） | M8-02 |
| M8-04 | M8 提交 | `git log -1` 可见 | M8-03 |
| M8-05 | **交销售试用** ＋ **收集反馈清单**（哪些细节要改 / 缺什么） | 反馈清单入库，转为 §五·补 或 §六 任务 | M8-04 |

> **出口判据**：**可以让销售开始试用。**（M8-05 是 V1.1 新增 —— 把"能试用"接成"真在用"。）
> ⚠ **UI 交互必须由人看**（AI 无浏览器会话，→ D-14）；M8-01 的证据里**必须含人眼点过的截图**。
> ★ **2026-09-22 追加（工具坑·实测两次）**：HTTP 证据发**中文 body 一律改用 UTF-8 字节体** —— PowerShell 5.1 的 `Invoke-RestMethod` 会把中文写成 **`?`**（本轮探针公司名落库成 `M8??????(??)`，已删；库里另有前窗口留下的 **`company.id=9`**，名字全是 `?`，属同类残留）。写法：`-Body ([Text.Encoding]::UTF8.GetBytes($json))`，或直接走 `node fetch`。

---

## 五·补、插入批次登记（★ V1.1 新增 · 计划外工作的唯一归口）

> **为什么有这节**：需求细节调整产生的工作，若不在计划里登记，就只能落进《欠账登记表》—— 台账会退化成"**事实上的第二计划**"（只累加、无排序、无归属），现在它已涨到 **50 KB**（自身体例目标 21 KB）。
> **纪律**（四条）：
> ① **开工前**先在此登记（原缺口 / 触发来源 / 归属 / 状态），否则**不算计划内工作**；
> ② 「归属」必须写清**属 §六 哪个域的展开**，或注明「新批次」；
> ③ 编号一律 `X-nn`（`X` ＝该域字母），**绝不复用 `M*` 编号** —— 2026-09-20 的 `M7-01` 编号冲突（F-01 占了 Worker 的号）就是前车之鉴；
> ④ 完工后**只改状态**（`✅ 已完工 <commit>`），**不删行**（同《欠账登记表》"只增不删"）。
>
> ⚠ **本节自 V1.1 起启用**；此前未登记的插入批次沿革查 `git log`（本节不追溯补录，避免造假沿革）。

| 批次 | 原缺口 / 触发来源 | 归属 | 状态 |
| --- | --- | --- | --- |
| **F-01** 公海「领取到私海」 | `D-33`（422 人话指向它却没有出口） | §六 **F 域**（提前开工） | ✅ 已完工（施工单 `归档/过程产出/F-01 施工单.md`；编号归口 `47f865e`） |
| **M9-F** 掉海（真掉 / 规则配置 / 按天） | `D-56③` · `D-57①` · `D-68②` · `D-53` · 《废止口径登记表》**#41** | §六 **F 域** | ⏳ **大部分 ✅**（真掉 `dc293dd` ／ 规则配置 `e340ce4` ／ 按天 `f7fece8` ／ 任务级审计 `2ca994a` ／ `0012` `8967daf`）；**欠** `D-56①②` · `D-57②` · `D-68①` |
| **桥③ 聚合层编排** | `D-61`（B(L2) 禁止依赖 C(L3)，跨域字段无落点） | §六 **B / C / D 域**（架构 §5.2 编排层） | ⏳ 已落 `company-aggregate`（`relations_summary` `dce0d9f` ／ `event_count_30d` `615d2d1` ／ 联系人收敛 `7a5cbde` ／ `D-10` `drop_in_x_days` `eca4d1a`）；**欠** `sign_date`·`amount`（E 域） |
| **横切：幂等键** | `D-06`（接口 §2.5 只写了半句） | §六 **横切 / jobs**（非业务域） | ✅ 已完工（`1bd916e`，第 47 张表 `idempotency_key`，migration `0010`）；⚠ 剩 `D-60` 清理任务未建 |
| **口径收口三件** | `D-28` ／ `D-35` ／ `D-67`（可见性 / 管理员留痕 / 经理可见线索） | §六 **B / C / A 域** | ✅ 已完工（`7a5cbde` ／ `e632625` ／ `282417f`，规格已同批回填） |
| **口径订正** | `D-65`（`source` 越出值域）／`D-53`（同三元组"新一轮"） | §六 **D 域** ／ **C 域** | ✅ 已完工（`8c5fc33` ＋ migration `0011` ／ `8967daf` ＋ `0012`） |

---

## 六、M9 · 按域补齐（**任务骨架**，开域时再原子化）

> 依假设 A5：**渐进式披露** —— 竖切通过后，每域开工前把该域骨架按 §五 同格式下钻为 2–5 分钟任务。
> ★ **V1.1 排序改按「阻塞图」，不按域字母序**（原序「依数据架构 §15.4：A/B/C → D → E/G → F」）。新序：**`E → A → G → B/C/D → report → 前端`**（`jobs` 随其消费方并行）。
> ★ **为什么 E 域打头**：**它是当前最大阻塞源**。四处缺口同时卡在 E 域（合同未建）：`D-01`（`serving` 档只做形状）／`D-02`（报表 / 看板 / 汇总出口无数据源）／`D-10`（`amount`·`amount_masked` 恒空）／`D-61` 剩余（`sign_date`）。而**已开工的 F 域不阻塞任何域**（其剩余项走 §五·补）。
> ★ **为什么 A / G 排第二三**：**A 域**的 `notification` ＋ **正式权限表**是 G 域审批与 D-04 解锁的前置；**G 域**（审批五型）是 **D-04**（手机号上锁 / 解锁 / `phone_unlock` 审批）的硬前置，而 D-04 又牵着 M5 的脱敏出口。

| 域 | 执行序（V1.1） | 任务骨架（每条＝一个可下钻的任务簇） | 状态 |
| --- | --- | --- | --- |
| **A 域** | **2** | 部门树 CRUD｜部门经理多对多｜员工 CRUD + 角色分配｜**离职批量转交**｜产品线｜`dept_rule` 部门规则｜字典 `dict_type`/`dict_item`｜**通知 `notification`（★ 优先）**｜**权限矩阵维护 / 正式权限表（★ 优先 —— 回填后即可撤销 `D-17`·`D-69` 两条"技术口径替代规格"）**｜`system_config`｜操作日志查询 `/operation-logs`｜`target` 目标层（**放最后**） | ⬜ |
| **B 域** | **4** | 公司详情 / 编辑｜公司档案标签｜**信用代码撞重·墓碑合并**（单事务 6 步）｜联系人详情 / 特质（≤ 上限）/ 就职历史｜**手机号变更申请**（审批 → 冻结 24h）｜联系人墓碑合并（经理）｜`file_asset` 上传 + 带鉴权下载｜`D-48` 批次｜`D-59` `PUT /contacts/:id`（⚠ **先定"谁能改别人的联系人"**） | ⬜ |
| **C 域** | **4** | 关系详情（8 Tab，`D-22`）｜**阶段推进**（建议态 + 一键确认 + 限频 ≥3 天 / 跨里程碑 + 留痕）｜成员（owner / collaborator / ask_help）｜阶段留痕｜关系标签｜转交审批｜批量转交｜竞品态快照｜`appointment`｜`visit_log`（**不产生业务事件**）｜竞品名册 | ⬜ |
| **D 域** | **4** | 承诺三型 + 豁免留痕 + them 催 3 次出口｜**快速标记**（三选一、可批量、**不重置倒计时**）｜`cadence_rule`｜**今日动线组装 + 处理反馈**（done / snoozed / ignored，防逃逸）｜出口复盘三型 `review`（win 自动建档 / loss 死因必填 / churn 统一出口）｜赢单弹药库 + 防守清单｜`stat_daily` 汇总｜**跟单轮次归组 + 前主人归组** | ⬜ |
| **E 域** | **1（★ 首域）** | 合同 CRUD｜**签约校验清单 `sign_checklist`**（预校验标红 + 提交硬卡 422 + 缺项锚点）｜**合同业绩分配 `contract_split`**（默认 signer 100%）｜`payment_record` + **客户等级自动重算**｜**疑似重复合同**（只检测不合并）｜`workorder` 双向流转 + `workorder_log`｜`ledger` + `field_template`｜续约预警 | 🟡 **B1+B2 ✅**（合同 CRUD 全链路：`POST/GET/PUT /contracts` ＋ 撞 `uk_contract_no`→409 ＋ `@Audit('contract.*')` ＋ domain 单测 18 项绿）｜**B3-B9 ⬜** **★ 首域已解锁 `D-01`(serving 形状)·`D-02`(报表源雏形)·`D-10`(amount 口径)·`D-61`(sign_date 口径)** |
| **G 域** | **3** | 审批**五型一单**（转交 / 协同 / 手机号变更 / **联系方式解锁** / **关系重分配 `relation_reassign` ＝ 离职批量转交，经理发起**）｜**审批人规则**（默认直属上级；跨部门转交双签；上级缺失上溯；⚠ **联系方式解锁例外＝落锁人本人**）｜解锁限时可见（**24h**）｜DB CHECK 兜底｜派发 `ApprovalApproved` / `ApprovalRejected` | ⬜ **★ 是 `D-04` 的硬前置** |
| **F 域** | **已提前开工**（走 §五·补，**不占主线**） | 公海规则 L1–L4 + **7 天缓冲**（倒计时重算、变更前提示"将影响 X 个客户"）｜**掉海判定纯函数**｜`sea_record` 入海历史｜**抢公海原子认领**（条件 UPDATE，影响 1 行才算抢到，否则 409）｜公海停留超期**经理决策待办**（**无自动删除 / 流转**）｜老客户徽标（派生）｜历史轮次查询 | ⏳ **大部分 ✅**（→ §五·补）；**欠** `D-56①②` · `D-57②` · `D-68①` · `D-33④` · `D-70` |
| **report** | **5** | `v_contract_performance` 口径｜9 张报表｜目标三层 + 时间进度对比｜**报表出口不脱敏**｜复杂查询走 `$queryRaw + Prisma.sql` | ⬜ **★ 依赖 E 域（`D-02`）** |
| **jobs** | **随消费方并行** | 12 项定时任务（数据架构 §十二）：组装今日动线（05:00，**结转不重写**）/ 动线清理 / 日级行为汇总 / 承诺提醒 / 节奏提醒 / **掉海预警（★ 每日一次，已改"按天"）** / 公海停留超期 / 灰度寿命（只提醒）/ 协同到期 / 判死教训缓写 / win 复盘超时 / 事件冗余回写｜`D-60` 幂等过期行清理 | ⏳ 部分 ✅（掉海已落） |
| **前端** | **6** | 铺开至 29 页（**先 7 高频页**：工作台 / 看板 / 业务关系列表 / 详情 / 预约 / 录入 / 公海）｜落实 §四.2 角色矩阵｜主题｜移动端工作台（`D-44①`）｜页 25 系统设置（`D-70`） | ⏳ **15/29** |

---

## 七、各里程碑退出标准（未达标不进下一步）

| 里程碑 | 退出标准（同时满足） |
| --- | --- |
| **M0** ✅ | `start:web` 起｜`/docs` 可见｜**故意跨域 import 被 lint 拦下**｜**真库 migration 全通过**（T2-1）｜人为唯一冲突返回 409 且提示为人话 |
| **M1** ✅ | 真账号登录｜`/account/me` 正确组装 角色 + 管辖部门 + 数据范围｜A 域**不依赖任何业务域**｜T0/T1 全绿｜**★ 浏览器里能用真账号登录（M1-18）** |
| **M2** ✅ | 建成 1 公司 + 1 联系人｜重复手机号 → **409**｜软删后旧号可复用｜**★ 页面上能建档（M2-17）** |
| **M3** ✅ | 能建关系｜私海列表 + 公海列表均可见｜并发同键 → 一条 409｜**★ 关系列表页可点通（M3-14）** |
| **M4** ✅ | 写一条跟单后，**关系时间线**与**工作台首页**都能看到｜快速标记不重置倒计时｜**★ 工作台 / 时间线页可点通（M4-17）** |
| **M5** ✅ | 销售 A 看不到销售 B 私海｜越权 → 403｜报表出口**不脱敏**（⚠ **出口未建 ⇒ 该条只落开关 ＋ 单测**）｜审计随业务事务回滚 |
| **M6** ✅（05~12 ✅） | 浏览器里**点通整条动线**｜不可见页不下发路由｜`gen:types` 无 diff｜（★ 方案 A：页面**已在前序里程碑逐步点通**，M6 ＝ 补全 + 打磨） |
| **M7** ✅ | Worker 单独跑起来且**不占端口**｜日志见掉海扫描结果｜全局 1 实例｜（★ V1.1：「只告警不真掉」**只约束 M7 阶段**；真掉已由 M9-F 实现） |
| **M8** | 真账号走查通过｜T2 四项全过｜打 tag｜**可以让销售开始试用**｜**★ V1.1：紧跟 M6-12（不等 M9）＋ 新增 M8-05「试用反馈清单」** |
| **M9（每域）** | 该域骨架已下钻为原子任务｜T0/T1 全绿｜该域铁律有单测｜T2 通过｜**★ V1.1：按 `E → A → G → B/C/D → report → 前端` 顺序** |

---

## 八、测试铁律清单（`domain/` 纯函数单测，CI 必跑）

> 对照需求 **§十五 速查** 与 **§十六（★ 规范级负面约束）** 逐条打钩。以下为**优先必须**覆盖的红线：

| # | 铁律（写单测） | 依据 | 落在哪条任务 |
| --- | --- | --- | --- |
| 1 | 同 公司×部门×产品线 仅一条活跃关系；激活竞态 → 409 | §15-1 | M3-12 |
| 2 | 手机号 / 信用代码唯一；软删释放旧号 | §15-2 | M2-10、M2-15 |
| 3 | **有效沟通才重置 `last_event_at`；快速标记不算** | §15-28 / §16 | M4-15 |
| 4 | 停滞判定＝N 天内阶段未前进（**不看 outcome**） | §15-24 / §16 | M9-F（掉海判定） |
| 5 | 掉海只掉一步、`dept_id` 不变、无更深池 | §16 | M9-F |
| 6 | 一关系仅一 owner（`owner_flag`） | §10.2-1 | M3-03、M3-05 |
| 7 | 抢公海原子认领（影响行 = 1） | §10.2-3 | M9-F |
| 8 | 阶段推进＝建议态 + 限频 + 留痕，**绝不自动改** | §16 | M9-C |
| 9 | 开发价值非灰度**必标**（422），灰度可留待定 | §15-30 | M9-D |
| 10 | 签约校验按**清单**卡，不按完善度百分比 | §16 | M9-E |
| 11 | 脱敏按**部门**判跨业务线；报表不脱敏 | §15-39 / §4.3 | M5-09 |
| 12 | 审批人＝直属上级；跨部门双签；上级缺失上溯 | §15-37 | M9-G |
| 13 | 轮次号＝已掉海次数 + 1（**派生不落表**） | §16 | M9-F |
| 14 | 合同业绩默认 signer 100%，split 合计 100% | §15-35 | M9-E |
| 15 | 灰度不豁免掉海；灰度只提醒不定性 | §15-31 / §16 | M9-F、M7-05 |
| 16 | 规则变更 7 天缓冲 + 倒计时重算 | §15-32 | M9-F |

---

## 九、反合理化表（Agent 偷懒防线，逐条预判）

> AI 想偷懒时的借口 → 立刻反驳。出现下列话术，**直接判未完成**。

| 借口（AI 会说的） | 反驳 |
| --- | --- |
| 「文档太长，先写代码再说」 | 规格是真相源。先读《废止口径登记表》+ 需求 §十六，是**动手前提**，不是可选项。 |
| 「这个字段文档没写，我顺手加上」 | §十六＝"**表里没有才代表允许**"。文档没写 ≠ 可自由发挥；先查登记表，再问。 |
| 「migration 先跳过真库，代码先跑起来」 | 建库失败**阻塞一切**。M0 第一件事就是真库核对（T2-1）。 |
| 「测试我稍后补」 | CONSTRAINTS T0-3：**没有测试的提交不算完成**。现在补。 |
| 「这个改动太小不用测」 | 掉海 / 撞单 / 脱敏这类铁律的破坏，往往就发生在"看起来无害"的小改里。 |
| 「跨域直接查对方的表更快」 | 违反硬约束 1 / 5。用 exports service 或领域事件（架构 §5.3）。 |
| 「报表也统一脱敏，省事」 | 硬约束 3：**报表 / 看板 / 汇总不脱敏**，经理和老板要看真实金额。 |
| 「顺手重构一下」 | 顺手重构＝**没人审查的变更**，单独提一条任务。 |
| 「这个边界情况不可能发生」 | 并发激活 / 重复手机号 / 抢公海正是"不可能"却**必测**的项。 |
| 「上线前再统一补安全」 | 安全是**每个阶段的门禁**，不是最后一道工序（CONSTRAINTS T3-2）。 |
| 「应该好了」 | **不是证据**。要测试输出 / 构建产物 / 运行时数据。 |

---

## 十、待拍板项（**未拍板前不得动**）

| # | 待拍板 | 现状 |
| --- | --- | --- |
| **P1** | 旧两份计划如何处置 | ✅ **已处置（2026-09-12 拍板）**＝**物理删除、不入 `归档/`**。留痕三点：① 该拍板与 README §十「归档＝移动，绝不删除」**不一致**，属**单次例外**裁定；② 两份**从未入 git**，删除后**沿革不可恢复**（§0.3 的纠错摘要**不等于原计划全文**）；③ 删除为例外，后续废弃文档仍按 §十 走归档 |
| **P2** | README §一 目录口径残留 | ✅ **已处置**：已统一为 `前端/src/api/types.ts`（同批修 `CODEBUDDY.md` 同句）。**刻意不改** `README` 中「旧代码已移入 `归档/旧代码/`」的历史事实记录 |
| **P3** | 本计划是否纳入 git | ✅ **已消解**：本文件**已在 git 跟踪**，沿革不再断档 |
| **P4** | 假设 A1–A5 是否认可 | ⚠ 我按文档推定、**尚未经口头确认**；若不符，在**第 1 次提交前**纠正（改这里，不改规格） |

**假设清单 A1–A5**：

| # | 假设 | 依据 | 若不成立的成本 |
| --- | --- | --- | --- |
| A1 | Worker 不在 M0–M6 出现，M7 才起独立入口（可并行） | 架构 §九 步 7 | 低 |
| A2 | 公海 L1–L4 可配置规则，M5 前用最简硬编码，配置化留到 M9-F 域 | M9 才配规则 | 低 |
| A3 | ~~前端 M6 才接入~~ → **2026-09-13 选定「方案 A · 页面随身走」**：前端骨架 / token / axios / `gen:types` **前置到 M0**；**M1 起每步配 1 个"能点通的最小页"**；M6 收口为「补全 + 打磨」。**双轨验收**：curl / 单测＝质量门（**不可省**），页面可点通＝体感门（**页面不得替代单测**） | 架构 §九 步 6（原假设已作废）＋ 2026-09-13 拍板 | 低 —— 增 **2~3 天**；换来「接口口径不合**当场暴露**」 |
| A4 | **真库 migration 核对是 M0 第一件事，不许推迟** | 风险最高、阻塞一切 | **高（阻塞全部）** |
| A5 | M9 各域只展开到"任务骨架"，**开该域时再原子化** | 渐进式披露（避免一次性塞满上下文） | 中 |

---

## 十一、风险与对策

| 风险 | 影响 | 对策 |
| --- | --- | --- |
| **文档口径漂移 / AI 读串** | 按过期口径写代码 | 动手前先读《废止口径登记表》+ 需求 §十六；改动走 L0 联动（同批升级、同次提交） |
| **字段细节漏项 / 自造字段** | 前后端字段对不上 | 字段**以《数据架构》§10.1 ＋《接口》§五 为唯一依据**；**`归档/` 内容不得作为实现依据** |
| **交付 / 客服、管理员视角缺设计** | 部分页面无法验收 | 以 前端 §四.2 角色矩阵为唯一口径；不可见页面不下发路由 |
| **边界靠自觉** | 越界依赖不可逆 | ESLint `no-restricted-imports` 硬卡，故意跨域 import 必须报错（架构 §5.4） |
| **跨域大事务** | 将来拆服务要重写 | 跨域只走 service / 事件；改多表只在本域 `$transaction` |
| **Worker 多实例** | 定时任务重复执行 | Worker 全局只跑 1 个（架构 §六） |
| **"缺一块就自行补全"**（AI 通病） | 实现被封路的功能 | 负向任务先对照需求 §十六；**表里没有才代表允许** |
| **Prisma 表达不了的段落**（生成列 / 分区 / CHECK / 视图） | 建库失败、阻塞一切 | **做法已固化为前置条件**：首次即在真实 MySQL 上逐条执行并核对，不得推后；表结构变更一律**增量 migration**，**严禁重生成 / 改动 `0001_init`** |
| **计划状态层滞后**（⚠ **已实际发生**：2026-09-16 → 09-22 落后 20+ 次提交） | 新窗口 / 协作者看不出「现在在哪、下一步做什么」；**台账被迫退化为"第二计划"**（只累加、无排序，已涨到 50 KB） | ★ **V1.1 对策**：① 新增 **§五·补 插入批次登记**（计划外工作的唯一归口，见该节四条纪律）；② §三 / §四 / §六状态列 **完工即回填，滞后不得超一个里程碑**（→ §十二）；③ 待拍板项按《AI执行清单》#21 **四要素**写，不留"看不见的队列" |

---

## 十二、进度与提交约定

- **单线 `main`**，不切 feat / dev 分支（README §三）。
- **一个原子任务 = 一次语义完整的改动**；一个里程碑 = 一次或多次提交，**提交即还原点**。
- 提交信息写清「**改了什么 / 为什么 / 联动哪几份文档**」，里程碑可打 tag（如 `v0.1-mvp-竖切动线`）。
- **文档改动必须提交**，否则版本沿革断档（沿革唯一落点 ＝ git）。
- **本文件的地位**：它是**唯一现行开发计划**（旧两份已于 2026-09-12 拍板删除，见 §十 P1）。
- ★ **V1.1 状态同步义务**：**§三 基线 / §四 状态列 / §五·补 插入批次 / §六 状态列，完工即回填**（滞后不得超过一个里程碑）；计划外工作**开工前**必须先登记进 §五·补。⚠ 未回填的计划＝失效的计划（2026-09-16 → 09-22 落后 20+ 次提交，即为反例）。
