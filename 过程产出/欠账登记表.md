# 欠账登记表（★ 唯一落点 · 只增不删）

> **定位（★ 规范级）**：凡「**本轮做不了 / 只做了一半 / 按规格本该有但上游未就绪**」的缺口一律登记在此，目的是**避免「当时知道、后来忘了」**。本表**只管「欠什么、为什么欠、归哪期补」**，**不定义任何业务口径**（口径以五份现行规格为准）。
> **维护规则**：(1) **只增不删** —— 补完不删行，把状态改成 `✅ 已补 YYYY-MM-DD <commit>`（删了就看不出「曾经欠过」）；(2) **谁发现谁登记**（动工中当场加行，别攒到收尾）；(3) **出处必填**（写清在哪份规格 / 哪段代码注释能对上）；(4) **归属必填**（归哪个里程碑 / 哪个域，只写"以后补"＝没登记）；(5) **不得借本表写新口径**（只能说"规格里有、现在没做"，**不许**把没做过的写成"要做"）。
> **体例（2026-09-20 压缩 · 57 KB → 21 KB）**：**已闭环条目（✅）只留 `commit ＋ 补在哪`**，过程实录 / 实测证据不再粘贴（查 `git show <commit>`；依据 `CODEBUDDY.md §11 ⑤`「**可省的是证据；规则正文与四项校验不可省**」）。⚠ **编号一个不动、条目一条不删**（表外指针：D-13 / D-29 / D-33 / D-36 / D-41 等被交接说明与规格引用）。
> ⚠ **台账自身缺陷（未修，待拍板）**：**D-44 编号重复**（① 移动端 ② 页 28 个人资料/改密）—— 未拍板前不动编号（交接说明 §三「仍欠 D-44」会指错）；**D-25 空缺**（编号只增不改，留空）。
> **表格拆分（2026-09-22）**：原单表按缺口列前缀拆为「主表一 · 已补（✅）」与「主表二 · 未补 / 补了部分」两张；**行 / 编号 / 出处一律不动**，各自按 D 编号升序。主表二内 `⏳`＝部分已补、无前缀＝纯未做。

## 主表一 · 已补（✅ 已闭环）

> 仅含主表内标记 `✅` 的条目；早期未进主表的留痕见文末「二」。

| # | 缺口（人话：现在少什么） | 为什么现在做不了 | 归属 | 出处 |
| --- | --- | --- | --- | --- |
| **D-06** | ✅ **已补 2026-09-20 `1bd916e`**：`Idempotency-Key` 落地（横切第 5 件 ＋ `kernel/idempotency/` ＋ 第 47 张表 `idempotency_key`，migration `0010`；接口 §2.5 回填、数据架构 §A15、废止表 #40）。★ **真库实测口径**：落库必须「**等落库完成再放响应**」（`tap` fire-and-forget 会让紧接着的重试漏过幂等）。⚠ 当时四项备选（TTL / 作用域 / 存储介质 / 无头请求）**查 `git show 1bd916e`** | §2.5 通篇只有「写操作带头 ＋ 重复同 key 返回首次结果 200」两句，**其余全无规定** ⇒ **不许由实现反推契约**（同 D-16 / D-24 / D-49） | — | 接口 §2.5 / §2.4；`shared/shared.module.ts`；`服务端/.env` |
| **D-07** | ✅ **已补 2026-09-20 `6d0bd8b`** —— `GET /relations` 接 `order_by`（白名单）/ `desc` / `keyword`（`start_at`/`end_at` 规格未定义，未接）；排序不再恒 `id desc`。接口 §2.7 回填、升 **V1.33** | 各域列表统一能力，逐域接 | M9 · C 域 | 接口 §2.7 |
| **D-08** | ✅ **已补 2026-09-20 `3b46698`** —— 分页铺到 B 域三列表（公司 / 公司联系人 / 联系人），复用 `kernel/listing`（`ListQuery` / `paginate`）；出参合 §2.3 `ListResult<T>`。接口 §2.3 / §5.4 / §5.5 / §2.7 回填、升 **V1.32** | 同一 kernel 公共件逐域接 | M9 · B 域 | 接口 §2.3 / §2.7 |
| **D-16** | ✅ **已补 2026-09-20 `11605a1`** —— 接口 §5.7 补 `PUT /relations/:id/commitments` 入参形状 | — | — | 接口 §5.7 |
| **D-17** | ✅ **已补 2026-09-20 `561319e`**（拍板：按建议）—— **合成口径已写进《架构设计说明》§7.2**「写权限：谁能写业务关系」段：可写角色 ＝ `sale` / `dept_manager` / `gm`（＝读得到的角色减掉两个只读角色），完整判据 ＝ 可写角色 ∩ ①判定的读范围，且**公海不可写**；★ 段内明标「**属本项目技术口径、非规格原文**；规格给出正式表则以规格为准并撤销本段」。**原缺口**：**「谁能写业务关系」＝合成口径，规格没有这张表**（由三处口径推出 ⇒ `sale` / `dept_manager` / `gm`；已上移 kernel `write-role.ts` 共用） | ⚠ **技术债·待返工**：属本项目的**技术口径替代了缺失的规格**（非规格原文），待需求 §7 补正式「写权限表」后撤销本段、回归规格；期间行为由 `write-role.ts` 合成规则驱动 | **待返工** → 回填需求 §7 / 架构 §7.2 | `domain/relation-scope.ts` 头注；`kernel/data-scope/write-role.ts` |
| **D-18** | ✅ **已补 2026-09-16**（P-02）—— 架构说明 §4.4 新增「前端目录与组件复用」约定 ＋ §十；router 错引已改 | — | — | 架构 §4.4 |
| **D-19** | ✅ **已补 2026-09-16** —— 列表页筛选栏（视图 4 档 ＋ 紧迫档多选，筛选一变回第 1 页） | — | — | 接口 §5.6 |
| **D-20** | ✅ **已补 2026-09-16** —— 前端装 ESLint（卡 §4.4 约定 1），并用**故意违规的临时文件实测「真会被拦」** | — | — | 架构 §4.4 |
| **D-21** | ✅ **已补 2026-09-18 `9d477f7`** —— 列表页筛选与 URL query 双向同步（默认值不写 URL、脏值归一化；范围＝**筛选**，页签 / 分页不入 URL） | — | — | 设计规范 §4.2 |
| **D-23** | ✅ **已补 2026-09-16**（P-03）—— `contact_ids` 开放：只有当前归属人能标（别人 403）；B 域新增 `getContactOwners` | — | — | 接口 §5.7 |
| **D-24** | ✅ **已补 2026-09-20 `11605a1`** —— 接口 §5.7 补 `quick-mark` 出参 `{marked}` | — | — | 接口 §5.7 |
| **D-26** | ✅ **已补 2026-09-16**（P-05）—— 生成列改「只有在位（未撤销）的 owner 才占位」，migration `0008` | — | — | 数据架构 §10.2-1 |
| **D-28** | ✅ **已补 2026-09-21 `7a5cbde`**（补需求 §6.1 ⑪ 缺的那半边）：`GET /contacts` **按数据范围收敛**（原任何登录人可见全公司联系人，与「销售名下待跟进」冲突）。**★ 落点（桥③ 同族）**：不搬端点 —— 跨域只过一个过滤条件（可见 `company_id` 集合），**聚合层算出集合 → 入参交给 B 域**（B 不 import C）；① C 域 `listVisibleCompanyIds`（`all` 档回 `null` 不收敛）；② B 域 `listContacts`/`getContact` 必填该集合入参（漏传编译期红）；③ 两读路由迁 `company-aggregate`。⚠ **拆出待拍 → D-67**（经理可见待关联线索） | 原「C 域开出口」在 B(L2) 禁依赖 C(L3) 下不成立 ⇒ 落点＝聚合层编排 | — | `company.repository.ts`；`relation.service.ts`；需求 §6.1 ⑪ / §6.2；架构 §3 |
| **D-29** | ✅ **已补 2026-09-18 `eb4e7aa`** —— `POST /contacts/:id/activate-relation`（编排落 D 域、跨域路①同步调 service、三件事不共享事务：**先 C 后 B** 防线索卡死；留痕 `contact.activate_relation`）。前端接线 → ✅ D-46 | — | — | 接口 §5.6 / §4.4 |
| **D-32** | ✅ **已补 2026-09-18 `2a94843`** —— ① 读门 `self` / `serving` 补「∪ **本部门**公海」；② 写门共用 `checkSeaUnclaimed`（无主 → **422 / `20408`**；看不到的公海照旧 403）；唯一放行 `PUT /relations/:id` 只传 `value_tier`；④ 前端公海抽屉只留读 ＋ 开发价值。真库实测通过。③ 公司档案随 D-05 | — | ③ 剩余 → **D-05** | 需求 §6.3 / §十六 N12；接口 §2.4 |
| **D-35** | ✅ **已补 2026-09-20**：管理员「只读 ＋ 留痕」**三个读出口一起收**（拍板 Q4）—— `GET /companies/:id`(`company.view`) ＋ `GET /contacts/:id`(`contact.view`) 在 B 域 service 加 `recordAdminView`；`GET /relations/:id`(`relation.view`) 在 C 域 service 加。**仅 `admin` 角色写 `operation_log`**（best-effort，不冒泡）；列表出口**不收**（避免刷一页写几十条）。动作名 `*.view` 已落 `COMPANY_AUDIT_ACTIONS`/`RELATION_AUDIT_ACTIONS`。后端三绿（tsc＋lint）通过 | — | — | 前端 §4.2；需求 §4.2 ★；`company.service.ts` / `relation.service.ts` |
| **D-36** | ✅ **已补 2026-09-18 `54b0169`（前半收口）** —— 侧边栏形态 ＋ 一级/子菜单 ＋ 顺序用词按 §4.1 ＋ 名实不符收口（菜单取名＝页面名）。**仍欠**：⑤ 菜单仍是子集（§4.1 的 15 项里 **11 项页面未建**，属**进度**）；⑦ 头像菜单 ✅、**铃铛 → D-41②** | ⑤ 属后续里程碑 | ⑤ M9 · 各域 | 前端 §4.1 ＋ §五；需求 §13.4 |
| **D-37** | ✅ **已补 2026-09-18 `7fd4758`** —— 主题 / 侧栏折叠**存账号**（数据架构 A2 加 `theme` / `nav_open`，migration `0009`；`PUT /account/preferences` 部分更新 ＋ 只能改自己；`me` 下发两列；前端由账号驱动）。⚠ 剩余 → D-42 / D-44② | — | 剩余 → **D-42** | 设计规范 §八.1；接口 §4.14.9 / §5.2 |
| **D-38** | ✅ **已补 2026-09-18 `b30e1b7`** —— 设计规范 §八.7 新增 **dark 色值表**（升 V1.1）。★ 今后改 dark 值顺序 ＝ **先改规格 §八.7**，同批改 `tokens.css` | — | — | 设计规范 §八.7 |
| **D-39** | ✅ **已补 2026-09-18 `0eeae9f`** —— 联系人页「范围」筛选接 URL（键 `only_unlinked`，写法同 D-21） | — | — | 设计规范 §4.2 |
| **D-40** | ✅ **已补 2026-09-18 `1501ef3`** —— 《AI执行清单》4340 → 3062 字节（≤3 KB ✓），编号与动作点一条不减 | — | — | `CODEBUDDY.md §11 ④` |
| **D-43** | ✅ **已澄清 2026-09-18** —— 非超限，是量法不一致：按 §11 ④ 量法 `git cat-file -s` = **3062 ≤ 3072 ✓**（当初量的是磁盘 3109，差额 47 字节 ＝ CRLF）⇒ **无需压缩** | 已澄清（量法以 §11 ④ 为准） | — | `CODEBUDDY.md §11 ④` |
| **D-45** | ✅ **已补 2026-09-20 `63eed5a`** —— `POST /contacts/:id/events` 落地（落库两列均 `null`、不碰 C 域、判当前归属人 403、有效沟通必写 summary、重复 409、`contact_id` 冲突 400、审计 `contact.event_create`）。判定口径 → ✅ D-51 | 前端接线 ✅ **已补 2026-09-20 `0a57f9d`**（页 15「待关联」区加「记一条跟单」卡片 ＋ `api/engine.ts` 新封装 `recordContactEvent`；⛔ **只在该联系人"尚未关联公司"时出现** —— 已挂公司服务端 403、摆别处＝假入口；表单与关系页抽屉**同款**；**不展示历史**：联系人层**无列表端点**，不编"已记 N 条"） | 接口 §5.7；`engine.service.ts` · `前端/src/api/engine.ts` · `ContactDetailView.vue` |
| **D-46** | ✅ **已补 2026-09-18 `94d3fe4`** —— 前端接线：入口在**联系人详情页「⚠ 尚未关联公司」横幅**（→「关联公司」→ 撞库 → 激活）；录入页只负责"先存为待关联" | — | — | 前端 §5 第 7 / 14·15 条 |
| **D-47** | ✅ **已补 2026-09-18 `94d3fe4`** —— 页 15 已建，路由 `/contacts/:id`（与 `/relations/:id` 同形） | — | — | 前端 §五 页 14/15 |
| **D-49** | ✅ **已补 2026-09-20**（① `561319e`／② `7992860`，拍板「以数据架构 B2 为准」）：① `extra_phones[].type` 值域收敛为 B2 的 `mobile/tel/wechat`——出参 DTO 注释、接口 §5.5（升 V1.29）、前端 `contact.ts`（旧码降级"历史数据兜底"）同批改，登记《废止口径登记表》#39；② `gender` 已定值域（拍板 A）：B2 落 `male/female/unknown`（`unknown`＝明确不便说，≠空值），建档 DTO 收紧为枚举（非法→400/20001），前端映射。真库实测：`gender=男→400/20001`、`male→200`、探针删净。**原缺口**：两处出参字段值域未对齐 | — | — | 数据架构 B2；接口 §5.5（升 V1.30）；`前端/src/contact.ts`；`company-response.dto.ts` |
| **D-51** | ✅ **已补 2026-09-20 `11605a1`** —— 接口 §5.7 补 `POST /contacts/:id/events` 判定口径（归属人 / 已挂公司 403 / `contact_id` 冲突 400 / 落库形态） | — | — | 接口 §5.7 |
| **D-52** | ✅ **已补 2026-09-20 `9771199`** —— 数据架构 A10 补齐 4 条审计动作。★ 教训：**清点缺口要机械枚举**（18 个审计名逐一检索） | — | — | 数据架构 A10 |
| **D-53** | ✅ **已补 2026-09-21**（方案 A·结构层收口；根因在「**激活**」侧非掉海侧）：① 需求 §6.3 补「同三元组『新一轮』＝领取同一条」；② 数据架构 C1 `active_key` 改「未删未并」（公海也占位）＋ §10.2-5；③ 接口 §4.4/§5.6/§六.1 激活撞键 **409 分档**（私海→转交·协同；公海→直接领取）；④ migration `0012` 先清存量再重建生成列＋唯一索引；⑤ C 域 `findActiveRelation` 去 `sea_status` ＋ `activeSlotConflict`（预检与 `P2002` 同源分档）。真库实测：两态同三元组再建均拦（`1062/uk_active_rel`），存量 `DUP_GROUPS=0`；HTTP 层人话由单测钉 | — | — | 需求 §6.3；数据架构 C1 / §10.2-5；接口 §4.4 / §5.6 / §六.1；`migrations/0012_active_key_include_sea` |
| **D-54** | ✅ **已补 2026-09-20 `561319e`**（拍板「UTC 存、出口换算」）：写进 `prisma/README.md §七`（直读一律 `CONVERT_TZ`／出参 ISO8601 带 Z、本地化交前端／分区边界按 UTC，写明"少 8 小时是口径不是故障"）＋ 数据架构 §十 索引表新增「时间列时区」一行。**原缺口**：`job_run_log.run_at` 时区无记载——Prisma 写无时区 `DATETIME(0)` 按 UTC 落库、mariadb 驱动直读按本地解释 ⇒ 同行为地 12:00 / SQL 直读 04:00（差 8 小时）；分区键跨月错位 8 小时；全项目 `DATETIME` 同此 | 需一处定调：全库「UTC 存、出口换算」还是「+08:00 存」 | 已拍板 2026-09-20 → 已回填 `prisma/README.md §七` ＋ 数据架构 §十 | `jobs/job-runner.service.ts`；数据架构 **A13** |
| **D-55** | ✅ **已补 2026-09-20 `a1d477a`**（P-09「删」）—— `heartbeat.job.ts` 已删；M7-02 判据改由掉海预警每小时那行自证 | — | — | 数据架构 §十二 |
| **D-58** | ✅ **已补 2026-09-20**（七叔拍「按建议走」）｜原缺口：本工作区 `npm run build`＝`prisma:generate && nest build`，`nest build` 每次先清 `outDir`，环境删除守卫按「一次删除 > 500 项」拦截 ⇒ 构建停"清 dist"、无新产物（起进程验的是旧代码，最易骗过验收） | **改法（项目内，不动全局安全网）**：① `nest-cli.json` `deleteOutDir` `true→false`（只覆盖不清目录）；② `package.json` 加 `clean` 脚本（需绝对干净时偶发跑，弹确认由人点）。⚠ 不松守卫阈值（全局安全网，影响所有项目）；`dist` 可再生产物，项目内解决代价更小。**验证**：`npm run build`→BUILD=0 ＋ `dist/main.js`/`dist/worker.js` 新产物；真起 `node dist/worker.js`→DI 全通、注册 1 任务、`JobRunner` 落行成功。**残余**：`dist` 留 52 个历史残留（51 `*.spec.js`＋`jobs/heartbeat.job.js`，旧产物、无引用方；`tsconfig.build.json` 已 exclude spec），要清就 `npm run clean` 并点确认。★ **判据改**：坑 12 的「dist spec 数 0」在 `deleteOutDir:false` 下不再等价于"产物卫生"，改「本次构建不新增 spec ＋ dist 无孤儿 .js」 | — | `服务端/nest-cli.json` · `服务端/package.json`（`build`/`clean`）；`CODEBUDDY.md` §9；`prisma/README.md` §八 |
| **D-63** | ✅ **已补 2026-09-21 `87e5a78`**（根因：前端多一份冗余依赖致 peer 冲突）：`openapi-typescript@7.13.0` peer `typescript@^5.x` 而项目 TS 6.0.3 ⇒ 普通 `npm i -D` 直接 ERESOLVE；本轮用 `--legacy-peer-deps` 强装（gen:types 跑通）。**修法（长期价值最高）**：类型生成入口唯一化到服务端（那边 TS 5.9 满足 peer）⇒ 前端删 `openapi-typescript`＋`gen:types`，普通 `npm install` 即可、不再需 flag、前端 TS 保持 6.x 不降（对比：`--legacy-peer-deps` 留坑、锁 TS5 牵动 vue-tsc 构建） | — | — | `前端/package.json`；`5e1e016`（引入）·`87e5a78`（修掉） |
| **D-64** | ✅ **已补 2026-09-21 `87e5a78`**（根因：`--legacy-peer-deps` 跳过 peer 安装，`vue-eslint-parser` 是 `eslint-plugin-vue` 的 peer，不在 `前端/node_modules/` ⇒ `npm run lint` 报 `ERR_MODULE_NOT_FOUND`、0 文件可检）｜非本轮引入（`git status` 干净、本轮只重生成 `types.ts`）；已随 D-63 修掉：删冗余依赖后普通 `npm install`（added 1/removed 26）装回 `vue-eslint-parser` ⇒ `npm run lint` exit 0、`npm run build` exit 0（前端双绿恢复） | — | — | `前端/eslint.config.mjs`；`前端/package.json`；`87e5a78` |
| **D-65** | ✅ **已补 2026-09-21 `8c5fc33`**（拍板取**改码**）：`action_event.source` 值域外码（`system`）→ 改 `auto`（D2 值域 `manual/auto/import`，建档/领取是系统自动生成，`auto` 语义正好）。**为什么不用"补 `system` 进值域"**：两同义码并存＝"同一事实两个落点"（一号坑）。**实现**：`engine.service.ts` 两处用常量 `AUTO_EVENT_SOURCE` ＋ 增量 `0011` 纯数据订正（幂等，新库 0 行）。⚠ `action_type='system'` 不动（在值域内，§5.4 计数按它排除系统事件）。**真库核对**：`source` 分组 ⇒ `auto` 5 / `manual` 19 / 无 `system`；写入路径单测断言覆盖 | 不影响当前功能（本列无过滤条件用到）；但值域是给后来者看的，留值域外码会照抄 ⇒ 已改 | 数据架构 **D2**；`engine.service.ts`；`prisma/migrations/0011_event_source_auto/`；`8c5fc33` |
| **D-66** | ✅ **已补 2026-09-21**：交接说明 10667→3438 B、执行清单压回 3059 B（均按 §11 ⑤ 只压导航/状态/证据，规则正文未动）。**原缺口**：两份状态类文档超出 §11 ④ 上限（按 `git cat-file -s` 实测：交接说明 7400→峰值 10667→3438，上限 5 KB；执行清单 3059≤3072）。⚠ 数字以提交后 `git cat-file -s` 实测为准；§11 ④「到顶＝先压老内容再加新内容」⇒ 两份都到顶，再加载持续侵蚀重放成本 | 本窗口只做"保持状态最新"，欠压缩工序；压缩动导航/状态/索引/证据（规则正文不可省，→ §11 ⑤） | **已随 D-71 同批补**（改名 ＋ 本次台账压缩） | `CODEBUDDY.md §11 ④`；台账 D-40 / D-43；本窗口实测 |
| **D-67** | ✅ **已补 2026-09-21 `282417f`**（拍板「经理＝管辖部门内、销售＝只有自己」）；原缺口：经理能否看「别人录入的待关联线索」＝口径缺口（§6.1 ⑪「自己的」、§6.1 ⑨「经理分派」要先看得见、§4.2 总经理全公司，三处指向不同 ⇒ 不许由实现自定；刻意不借 D-28 顺手改） | 已拍板并落地：经理（`dept` 档）＝可见管辖部门内员工录入的待关联线索；销售＝只有自己（同部门同事也不给）；`all` 档不套过滤。**实现**：A 域 `listEmployeeIdsOfManagedDepts` ＋ domain `managedDeptIdsOf`（不复用 `visibleEmployeeDeptIds`：后者对销售会给"同部门"＝越权；`all` 档回 `null` 不查库）；B 域两处判「归属人 ∈ 我 ∪ 管辖部门内员工」。**真库实测**：刘敏（dept_manager）待关联 5 条（王海涛录入，改动前 0）；王海涛仍 5；gm 6（含孤儿 1） | — | 需求 §6.1 ⑨⑪（已回填）/ §4.2；接口 §5.5；`org.service.ts`；`company.service.ts`；`282417f` |
| **D-71** | ✅ **已补 2026-09-22**（① 改名 ＋ ② 台账压缩已完成）。**原缺口（2026-09-22 修订计划 V1.1 时发现）**：① **交接说明是"改名滚动"的单文件** —— 标题已去阶段名，文件名仍是 `交接说明-M7（2026-09-20 窗口）.md` 而内容已到 M9-F；**改名会断带全名的引用**；② **容量实测**：交接说明 **5113 B / 上限 5120**（余 **7 B**）、台账 **50854 B**（自身体例目标 21 KB，**超 2.4 倍**，D-28 / D-53 / D-61 / D-68 等 ✅ 行单行上千字）。**① 改名（本批）**：→ **固定名 `过程产出/交接说明.md`**（`git mv` 保沿革），**4 处引用同批字面改**（`CODEBUDDY.md` §0 表 / `README` §41·§43·§218）；台账 D-66 行内的旧文件名属**历史留痕、不动**。**② 台账压缩（本批已完成）**：**51997 B → 40642 B**（压 D-06/D-28/D-53/D-61/D-68/D-58/D-65/D-67/D-49/D-64/D-63/D-66/D-54 共 13 条 ✅ 行的过程实录；规则口径 / 技术债标注 / 落点 / 出处保留，编号一条未删）。⚠ 体量仍超自身体例目标 21 KB，但剩余长行多为「待补 / 部分补」的缺口描述（规则正文，不可省）→ 到此为止。 | ① 改名牵动多处指针，须逐处字面改（禁正则）；② 压缩按 §11 ④「**先删旧段再加新内容**」走 —— ⚠ **状态类文档禁整篇重写**（坑 36） | — | `CODEBUDDY.md §11 ④`；开发计划 §十一 风险行 / §十二；本批实测 |

## 主表二 · 未补 / 补了部分

> 未标 `✅` 的条目：纯未做（无前缀）与部分已补（`⏳`）混排。

| # | 缺口（人话：现在少什么） | 为什么现在做不了 | 归属 | 出处 |
| --- | --- | --- | --- | --- |
| **D-01** | **`serving` 档只做了形状**：交付 / 客服本应「只看合同服务期内的客户」，现按 `self` 收敛 | E 域（合同）未建，「服务期」无从判定 | M9 · E 域 | 架构 §7.2 |
| **D-02** | **报表 / 看板 / 汇总出口未建**（只落了「出口豁免脱敏」开关 ＋ 单测） | 无数据源（依赖 E / F 域） | M9 · report | 交接说明 §三.2 |
| **D-03** | ⏳ **部分已补 2026-09-18 `4cff29f`**：`GET /contacts/:id` 已建（详情给全号 ＋ `phone_locked` / `phone_locked_by`，被锁且非落锁人时 `phone` 与 `extra_phones` **两键都不出现**、锁跟人；同批落就职历史 / 特质 / 备用号等；可见性与列表同源）。**仍欠**：`unlocked_until`（24h）→ D-04；详情页 → ✅ `94d3fe4` | — | 剩余 → **D-04** | 接口 §5.5 / §2.8；需求 §4.3 二 / §6.1 ⑪ |
| **D-04** | **手机号上锁 / 解锁 / `phone_unlock` 审批未建**（`sea.phone.view`、`phone.unlock.*`、强制解锁留痕同欠）。**只剩**：上锁 / 解锁两端点、`phone_unlock` 审批、详情出参 `unlocked_until` | G 域（审批五型）未建 | M9 · G 域（A10 已列） | 接口 §5.5 / §2.8 |
| **D-05** | ⏳ **部分已补 2026-09-16 ＋ 2026-09-20**：`GET /org/product-lines` 已建；**2026-09-20 已建 `GET /companies/:id` 端点**（B 域内部：基本档案 ＋ `completeness` ＋ `profile_tags`(B2 本域表) ＋ `contacts[]` 简卡(按卡片 `phone_masked`，拍板 Q2)）。⚠ **跨域字段 `relations_summary` / `event_count_30d` 本轮暂不出**（不编假值）—— 二者都依赖 C 域 `business_relation`，而 **B(L2) 禁止依赖 C(L3)**（架构 §3 层级），B 调不到 C 的出口 ⇒ **D-61 已落点：聚合层编排（桥③）** | 跨域字段归 M9 按桥③实现（D-61 已拍） | M9 · B 域 | 接口 §5.4 / §5.6；D-32③；架构 §3 |
| **D-09** | ⏳ **部分已补 2026-09-16**：`view`（4 档）＋ `urgency`（多选）已进接口 §4.4 / §5.6。⚠ **未关闭**：第 5 档「**逾期未跟进**」仍无（依赖 D-10） | 剩的「逾期」档要等 `overdue` 字段 | 剩余 → M9（随 `overdue`） | 接口 §4.4 / §5.6；前端 §5 |
| **D-10** | **C 域列表项缺 9 个字段**：`drop_in_x_days` / `overdue` / `amount` / `amount_masked` / `old_customer` / `is_weekly`；`customer_level` **恒 `null`** | 分属 F（掉海）/ D（逾期）/ E（合同回款）域。★ **2026-09-21 定（七叔拍）**：`drop_in_x_days` **改「访问时实时派生」**（不进表、不进 `daily_agenda`）—— ⚠ 落点卡在层级：**C(L3) 读不到 F(L4) 的 `sea_rule`**（架构 §3 只能高层→低层）⇒ 归**聚合层编排（桥③）**，与 D-61 同族；本批只落「按天」口径，派生字段待那片 | M7 / M9 · D/E/F | 接口 §5.6；`dto/relation-response.dto.ts` 头；需求 §6.3 |
| **D-11** | **C 域详情缺 5 组字段**：`round_no` / `prev_round` / `competitors` / `labels` / `stage_logs` / `try_count_30d` | 分属 C3 / C4 / D 域 | M9 · C / D 域 | 接口 §5.6 |
| **D-12** | **列表页视觉补全未做**（阶段彩点 / 逾期标红 / 24h 掉海⚠ / 跨线脱敏金额 / 竞争徽标 / 等级徽标 / 千分位） | 数据侧全在 D-10 欠着（**无数据不编假值**）；阶段色还需设计规范先落 `--crm-color-stage-*` | M9 ＋ 设计规范补 token | 前端 §5；设计规范 §4.3 / §4.4 |
| **D-13** | ⏳ **部分已补**：公司名可点（09-16）、**联系人那一半已收口**（`94d3fe4`）；**公司 / 员工仍无端点、无落点**（→ D-05） | 缺落点页 | 剩余 → M9（B / A 域） | README §九 不变量⑥ |
| **D-14** | **真账号走查**：**后端已验**（09-16：私海翻页 `total=4` → `ids 5,4` / `3,1`、公海 `total=1`、交付看公海 403）；**前端 UI 仍待人眼点**（分页器 / 每页条数 / 切页签回第 1 页） | AI 没有浏览器会话，**UI 交互必须由人看** | **待七叔验** | 交接说明 §一 |
| **D-15** | ⏳ **2026-09-21 实测：症状升级** —— 该 spec 在本工作区**直接跑不起来**：`NestFactory.create` 阶段 `process.exit(1)`（`ExceptionsZone` 的 `DEFAULT_TEARDOWN` 抛出；`logger:false` 故看不到根因）⇒ 全量跑出「**53 passed ＋ 1 failed to run**」、**用例 791 全绿 / 0 断言失败**。★ **已用 `git stash` 在改动前的 HEAD 上复现同一失败** ⇒ **非本轮引入**（该 spec 只 import `SharedModule` ＋ kernel，与业务域无关；停掉 3000 端口进程后亦失败）。⚠ 连带：交接说明原写的「51 suites / 765 用例全绿」**当前不可复现**。**原缺口**：起真服务后 worker 进程不优雅退出（jest force exit 警告，用例仍全绿） | 测试基建收尾项，不影响功能（用例本身无断言失败） | 收尾窗口 / 测试基建 | 交接说明 §三.6；本轮实测（`npm test -- --maxWorkers=1 shared.module` ＋ stash 对照） |
| **D-22** | **关系详情页只落「详情 ＋ 成员」**：规格 §5 的 8 Tab 均未建；时间线仍在列表页抽屉 | Tab 数据分属各域、服务端未返回（→ D-11）—— 摆上＝**假入口** | M6 后续 / M9 | 前端 §5；接口 §5.6 |
| **D-27** | ⏳ **口径已定并落地 2026-09-16** —— 待关联归属＝**建档录入人**（`contact.owner_id` 可空）；未关联期间不占部门 / 产品线；归属**可改**（不走审批、只留痕）；工作量按写单人算。剩余 → D-28 | — | — | 需求 §6.1 ⑦ |
| **D-30** | **撞库「分支 3（跨部门可并行）」的展示数据无处可取**：需求要求显示「**对方部门名 ＋ 激活 / 签约日期**」，但候选出参无部门 / 关系，也无「某公司已有哪些关系」端点 ⇒ 录入页**做不出分支 3**（本片只落分支 4 ＋ 409 路径） | 缺**服务端出口**（新增「按公司取关系概要」且按数据范围收敛）—— 前端补不了 | M9 · **B＋C 域**（可与 D-05 同批） | 需求 §12.1；`EntryView.vue` 头 ★ |
| **D-31** | **公海关系仍可写**：经理 / 总经理对管辖部门内公海关系七个写动作全放行（⚠ **销售不在此列** —— 读门 403，即"公海做不了动作"是**撞出来的、不是判出来的**） | ✅ **已拍板 2026-09-18**（公海**可读不可写**，唯一例外＝改开发价值）⇒ 实现归 **D-32**（已落地） | — | 需求 §6.3 / §十六 N12 |
| **D-33** | ⏳ **主链已通 2026-09-20**（余 ③通知 / ④其余海域端点 / ⑤D-53）—— 原欠：公海**「领取到私海」动线未建**（422 人话指向它却没有出口）。已落：C 域出口原子条件 UPDATE `88839c4`／F 域 `POST /sea/company/:id/claim` ＋ D 域承诺级联 `5c59d9f`／前端抽屉领取 ＋ **人验 `b33c49b`** `7f24d8f`／接口 §5.16 回填 `18c7aca`。**仍欠**：③ A 域通知 → D-42；④ ~~掉海扫描~~（`overdue` 档真掉 ✅ 2026-09-21，M9-F）／**`GET/PUT /sea/rules` ✅ 2026-09-21**（接口 §5.16 已回填）⇒ ④ 剩 `G /sea/company`、`G /sea/department`、`G /sea/records`、`G /sea/manager-todo`、`P /sea/manager-decision`；⑤ D-53 ✅ `0012` | ③ 通知设施未建；④ 属后续片 | M7 · F 域 | 接口 §5.16 / §4.5；数据架构 §10.2-3 |
| **D-34** | **一人多角色时页面可见性合成未定义**（`me` 只回单 `role`，与前端矩阵**不同源**） | ✅ **已拍板 2026-09-18（方案 A，⚠ 图省事延后）**：不新增接口字段；实现归 M9 · A 域（「角色」子页加**只提示不硬拦**的分配提示）。※ 种子账号无此组合，当前不暴露 | ⚠ **技术债·待返工（图省事延后）**：一人多角色页面可见性合成**未定义**，真实出现该组合时行为未定义，会暴露 bug；M9 · A 域补正式合成规则 | 需求 §4.1 / §十六 **N15**；`前端/src/access.ts` |
| **D-41** | ⏳ **①头像菜单 ✅ `53b21dd`**（★ 原「✅」**不成立**：dev 下菜单是空的 —— AntD 取槽内首节点而它是模板注释，两个入口等于不存在；`trigger` 默认 hover 致点不开。★ **教训：「已补」的判据是"人真点过一次"**）。**② 消息铃铛 ＋ `/notifications` 页仍欠**（接口 §4.13 未建）⇒ 不摆假入口 | ② 要通知接口 ＋ 未读数出口 | M9 · A 域（②随 notification） | 前端 §四.1 ＋ §五 页 27 |
| **D-42** | ⏳ **已拍板 2026-09-18：`notif` 结构「先不定」** —— 等 M7 通知落地再回填（现在拍＝**凭空造分类**）。⇒ 本轮**不实现、不建列**（**拍的结论，不是缺陷**）。原缺口：接口 §5.2 只写 `notif?:{...}`，括号里从未定义 | 拍板＝**延后**（≠ 不做）：M7 通知落地时**同批**回填接口 §5.2 ＋ 需求 §8，再补 `notif_prefs` 列 | M7 · A 域 | 接口 §5.2；需求 §8 |
| **D-44 ①** | **移动端适配一行未落**（无 `/m/*`、零 `@media` / 零断点、无 Tab Bar / `.sheet` / 「其余用 PC」兜底 ⇒ 窄屏挤 ＋ 横向溢出）。⚠ **口径已补**（P-06，代码未动）：**按角色开放**（销售 ✅ / 交付·客服 ✅ / 经理·总经理 🔒只读＋待我审批 / 管理员 ➖）；**不新增任何页面**，只用已有页的窄屏可读版 | 页 M 属 M9 前端 | M9 · 前端 | 设计规范 §6.1 / §6.3；前端 §十三 / §五 页 M |
| **D-44 ②** | **页 28「个人资料 / 修改密码」未做**（页面只有主题卡片）。⚠ 与 D-42 不是一件事，故单列 | ① 员工自助改资料**无端点**（`PUT /org/employees` 是管理员操作，还牵扯账号唯一性）；② 改密**无端点**，且要先定旧密码校验 / 强度 / 是否强制下线 ⇒ **都得先拍板** | **待拍板** → 回填接口 §三 / §4.14 ＋ 需求 §7.1 | 前端 §五 页 28；`AppearanceView.vue` |
| **D-48** | **接口 §5.5 里 B 域还有一批端点未实现**：`PUT /contacts/:id/traits`（上限只拦新增 → 422/20402）、`POST /contacts/:id/merge`、`DELETE /contacts/:id`（仅经理）、`GET /contacts/:id/employments`、`POST /contacts/phone-change/apply`、`POST /companies/:id/merge` | 均属后续批次未排期；逐条实现前**要先有对应页面 / 审批域** | M9 · B 域（随页面 / 审批域） | 接口 §5.5 / §4.14.2 |
| **D-50** | **页 15（及全站）「悬浮卡」未建**（前端 §3.1 铁律 5 / 需求 §13.1 都要求人名 → 悬浮卡；架构 §4.4 点名为 `ContactHoverCard.vue` 样板件）。现人名一律整页跳详情 | ① 消费场景（公司抽屉 / 跟单卡 / 工作台）**都还没建** ⇒ 先造＝没有调用方的封装；② 卡内**员工**信息无出口（A 域无「按 id 取员工概要」） | M9 · 前端（随公司抽屉 / 跟单卡那批） | 前端 §3.1 / §5 第 14·15 条；架构 §4.4 |
| **D-56** | **掉海预警三档「只报出来」、没有「报给谁」**：命中数只进**日志 ＋ `job_run_log`**，三档出口动作一个都没接（①进动线 ②推销售 / 标红推经理 ③到期真掉落）⇒ **今天系统里看不到任何提醒** | ⏳ **2026-09-21 部分已落**：**③「到期真掉落」✅ 已建**（M9-F：`overdue` 档真掉 ＋ 写 `sea_record`，真库实测通过）；**①进动线 / ②推销售 · 标红推经理仍未接** —— 分别要 `daily_agenda` 组装任务（M9 jobs，每日 05:00）与 `notification`（→ 另见 D-68①）。★ **2026-09-21 续（口径改「按天」）**：三档＝**≤3 天进动线 / 到期前 1 天推销售 / 到期当天标红推经理**，**到期日已过 ⇒ 次日掉落**（到期日当天仍归销售一整天）；任务频率 **每小时 → 每日**（固定间隔 24h，与「组装动线 05:00」的**整点对齐**等那条任务落地时统一 —— 本行同记）；判定**一律按自然日（Asia/Shanghai）**（→ 需求 §6.3 /《废止口径登记表》**#41**） | ①② → M9 · jobs / A 域 | 数据架构 §十二；`sea.service.ts`；`domain/sea-warning.ts` |
| **D-57** | **掉海倒计时只落了三条触发里的第一条**：需求 §6.3 三条（跟进频次 / 成单周期 / 停滞）**只实现「跟进频次」**；**另两条的锚点规格没写**（"激活"＝首轮建档还是本轮领回？"向前"含不含回退？） | ⏳ **2026-09-21**：**触发①已能真掉**（M9-F；`reason=follow_timeout`，F2 值域内）；**②③ 仍欠**（属**规格缺口**，同 D-16 / D-24 处理；开发计划 §八 铁律 4 明列为 M9-F 的活 ⇒ **不猜不替**）。**续 2026-09-21**：规则配置端点（`PUT /sea/rules`）已能**配** `deal_cycle_days` / `no_progress_max`，但**只配不生效**（掉海扫描不读这两列，等锚点口径拍板后再接）；`stay_days` 同批落库、按 F1 ★P-10 只属"公海停留超期"那一片（任务与决策端点未建 → D-33 ④）。原③ ✅ **已补 2026-09-20 `a1d477a`**（P-10：数据架构 F1 改「`stay_days` 只用于公海停留超期」） | ② → M9 · F 域 | 需求 §6.3；数据架构 F1 / §十二；`domain/sea-warning.ts` 文件头 |
| **D-59** | **`PUT /contacts/:id`（改联系人）端点根本不存在** —— 接口 §5.5 写的是「建档 / 改 req 同形」，而 B 域 controller 现 7 条路由（`GET /companies` / `POST /companies` / `POST /companies/search-dup` / `GET /contacts` / `POST /contacts` / `GET /contacts/:id` / `GET /companies/:id/contacts`）里**一个"改"都没有** ⇒ 联系人**建完之后姓名 / 电话 / 备用号 / 微信 / 邮箱 / 性别 / 生日 / 决策角色 / 标签一概改不了**（做 D-49② 真库验证时当场撞到：想用 `PUT /contacts/1` 送非法 `gender` → **404 / 20099「接口不存在」**，只能改走建档端点造探针行） | 不是做不了，是**没排进批次**（现有页面也没有"编辑联系人"入口 ⇒ 与「不提前造没有调用方的端点」一致）；实现前**要先定编辑权限**（谁能改别人的联系人？当前归属人 / 所在关系的 owner / 经理？）—— 规格未写，**不许由实现自定** | **M9 · B 域**（可随 D-48 那批） | 接口 §5.5；`服务端/src/modules/company/company.controller.ts`（7 条路由）；D-48 |
| **D-60** | **幂等键的过期行清理任务未建**（做 D-06 时当场确认）：`idempotency_key` 行按 **24h 有效**（**查询条件已忽略过期行**，幂等行为是对的），但**没有任何任务删除它们** ⇒ 行数随写请求**只增不减**（每个「登录人 × 端点 × key」一行）。⚠ 单行很小（≈200 字节），但它是**全库唯一一张没有保留策略的表** | 属"清理类定时任务"，与《数据架构》§十二 的「动线清理」同族；实现前要定**保留窗口 ＋ 批次大小**（建议：保留 48h、每小时 `DELETE ... LIMIT 5000`，**避免长事务**） | **M9 · jobs**（可随「动线清理」那批） | 数据架构 **§A15**（末行"清理任务尚未建"）· §十二；接口 §2.5；`服务端/src/kernel/idempotency/idempotency.service.ts` |
| **D-61** | **跨域落点已拍板 2026-09-21（★ 规范级，影响 D-05 跨域字段 ＋ D-28）**：`relations_summary` / `event_count_30d` / 联系人收敛三者需查 C 域，但 **B(L2) 禁止依赖 C(L3)**（架构 §3），B 调不到 C 的 service、禁查 C 的表 ⇒ 落点＝**聚合层编排（桥③）** | 已拍板落点（2026-09-21 七叔拍，桥③ 长期价值最高）：选 **C＝聚合层编排**——拼装挪到同时依赖 B＋C 的更高层 service，B/C 各自纯净、不破 §3。⚠ 排除 A（破例放宽 §3＝技术债）／B（污染 kernel）／D（不符需求 §13.2） | **桥③ 已落地 2026-09-21**：`company-aggregate`（import B＋C）＋ `GET /companies/:id` 迁聚合层 ＋ `relations_summary` 出 C 域子集；`event_count_30d` 已落（D 域 `countCompanyEvents30d` ＋ C 域 `listVisibleRelationIdsOfCompany`，B/C/D 零改动）；**仅剩 `sign_date`/`amount`**（E 域 `contract`，module 未建）；口径三要素＋「系统事件不计入」已拍板（接口 §5.4 升 V1.35） | 架构 §3 / §5.2；`company.module.ts` 头 ★；接口 §5.4；案例库坑 39 |
| **D-62** | **test 环境 ESM 债务（pre-existing，非本轮引入）**：`npx jest` 跑 service 层 spec 报 `Must use import to load ES Module: @nestjs/common/index.js`（`kernel/context/public.decorator.ts` 间接 import ESM 的 nest），**0 用例可执行**。Node 当前版本 < 24.9（jest 不支持原生 require(esm)）；或需调 `transformIgnorePatterns` 让 node_modules 的 nest 可被 ts-jest transform。**本轮 tsc＋lint 全绿，但单测三绿未达成**（环境层） | 不属本轮改动（栈不含我改的文件）；修复 = 升 Node ≥ 24.9 或调 jest `transformIgnorePatterns`（属构建/测试基建，越权不擅自改） | **收尾窗口 / 测试基建** | `服务端/jest.config.*`；`kernel/context/public.decorator.ts:25` |
| **D-68** | **掉海（系统任务）两处「出口」未接**（做 M9-F 时当场发现）：① 不广播 `RelationReleased`（架构 §5.3 清单 F/C 发→D 落事件＋A 通知＋E 回写，本批只落掉海本体，事件没发）；② 系统任务写动作不写 `operation_log`（数据架构 §164 明写两者都写，而定时任务无登录人，留痕口径从未定） | **② ✅ 已补 2026-09-21**：口径写进《架构设计说明》§7.4 ⑤「系统任务（无 HTTP 端点）留痕＝任务级一条」（`operator_id=0`、与 `job_run_log.run_at` 同源、批次明细进 `detail`、不逐条、无增删改不写、best-effort）；F 域 `recordDropAudit` ＋ `SEA_AUDIT_ACTIONS.drop='sea.drop'`，A10 同步。**真库实测**：`operation_log` +1（非逐条）、`operator_id=0`、探针删净。**① 仍欠**：三消费方（D 落事件 / A 通知 / E 回写）未接 ⇒ 发了没人听 | ① 随订阅方 / 通知那批 → M9 · A / D 域 | 架构 §5.3 / §7.4 ⑤；数据架构 §164 / A10；`kernel/events/domain-event.ts`；`sea.service.ts`（`recordDropAudit`） |
| **D-69** | **公海规则配置端点的写权限＝合成口径**（**非规格原文**）：需求 §6.3 只写了「全局 / 产品级＝**老板**、部门级 / 部门×产品级＝**部门经理**」**两档**，**未提管理员**；而《前端》§四.2 把「系统设置」整块给了管理员 ✅（含公海规则子页）、`kernel/data-scope/write-role.ts` 文件头也把「**规则维护**」点名为 `admin` 的本职 ⇒ M9-F 实现**三处取并**：`gm` / `admin` 可配任意层，`dept_manager` 可配**管辖部门内**的 L3 / L4，其余角色 403。★ 只影响 `GET` / `PUT /sea/rules` | ⚠ **技术债·待返工**：属**技术口径替代缺失规格**（同 D-17 姿势，非规格原文）；待需求 §7 补正式「权限表」后**撤销合成段、回归规格**；期间行为由 `sea/domain/sea-rule.ts` 的合成规则驱动 | **待返工** → 回填需求 §7 | 需求 §6.3；前端 §四.2 系统设置行；`kernel/data-scope/write-role.ts`；接口 §5.16；`服务端/src/modules/sea/domain/sea-rule.ts` 文件头 ⚠ |
| **D-70** | **前端「页 25 · 系统设置 → 公海规则」子页未建**：后端 `GET/PUT /sea/rules` **已就绪且真库真账号已验**，但页面上没有入口 ⇒ 规则目前**只能靠接口配**（页 25 六子页整体未建，属前端铺开进度） | 后端已就绪 ⇒ **纯前端活**；等「系统设置」那一批一起铺。⚠ 这与"不提前造没有调用方的端点"是**相反方向**的缺口（**端点已就绪、调用方未建**）—— 记在此，免得下次误以为"规则配置没做" | M9 · 前端（随页 25 系统设置那批） | 前端 §五 页 25 / §四.2 系统设置行；接口 §4.14.10 / §5.16 |
| **D-72** | **关系列表页时间线抽屉 / 联系人详情页两处「记一条跟单」内联表单未迁到 `EventComposer`**：本批新建 `前端/src/components/EventComposer.vue`（写跟单表单复用件，架构 §4.4 约定 2「第三次出现必抽件」），但**只有录入页结果态用上**，列表页 `submitEvent`（关系态）/ 详情页 `submitContactEvent`（待关联态）仍是各自堆一份同款表单（第二 / 第三处重复实现） | 本批按**垂直薄切片**只闭环 M6-09（录入页结果态补写跟单），两处既有页面是 20KB+ 文件，本批一并迁会让 diff 超可审范围；迁只需把页面内 `actionTypeOptions`/`outcomeOptions`/`eventForm`/`submit*` 替换为 `<EventComposer :action-type-options :outcome-options @submit>`，逻辑等价（→ 本批录入页已验证同形） | M9 · 前端（随 D-22 页面打磨那批，或单独批） | 架构 §4.4 约定 2；`前端/src/views/RelationListView.vue:891` ／ `前端/src/views/ContactDetailView.vue:100`；`前端/src/components/EventComposer.vue` 文件头 |
| **D-73** | **跨部录入数据消失 P0 修复待真账号走查**：根因＝前端原给「全部门」下拉，销售选别部门建关系被 `POST /relations` 的 403 丢弃，且页面把失败**粉饰成「已建档（待关联）」**（三处都查不到那条关系）。★ 已落（2026-09-22）：后端 `me`（`UserVO`）增 `activatable_dept_ids` / `product_line_ids`（与 `relation` 域 `checkActivateScope` **逐字同集**，单一真相源）；前端录入页 ＋ 联系人详情「关联公司」下拉＝该集。★ **真账号走查修正（同日）**：首版前端擅自加「承接我业务线的部门也给」→ **比服务端宽**（销售二部承接了王海涛的产品线 1/2 ⇒ 被放进下拉）⇒ 选中即 403；**已删**，下拉不再加任何额外规则。同时修「激活 403/409 后仍显示已建档（待关联）」的状态错显（`v-else-if` 补 `companyChoice === null`）。**① ✅ 已验 2026-09-22**（CLI：`me` 返回 `activatable_dept_ids=["2"]` ⇒ 王海涛只见销售一部；越权 dept=3 被 403 `20003`；人眼布局用户已确认）；**仍待**：② 清历史跨部孤儿（用户自理）。⚠ 待拍板：销售能否替「承接自己业务线的部门」建档（若要放开，须**先改 `checkActivateScope` ＋ 回填需求 §6.1 ⑧ 口径**，不许前端单方面放宽） | AI 不点 UI（→ D-14 同类）｜前端 `EntryView.vue` / `ContactDetailView.vue` / `RelationTargetPicker.vue`；后端 `org.service.ts` / `dto/org-response.dto.ts` | M8-01 真账号走查；接口 §5.6 / §5.2 |

## 二、已补（留痕，勿删）

> 格式：`✅ 已补 YYYY-MM-DD <commit>` ＋ 一句话「补在哪」。**本段不压缩**（早期未进主表的留痕）。

- ✅ **已补 2026-09-16**（M6-07 同批）：**`GET /relations` 分页** —— 入参 `page` / `page_size`、出参 `{list,total,page,page_size}`，前端列表页接分页器（原「`LIST_LIMIT=100` 截断 ＋ 裸数组」不合 §2.3）；接口升 V1.20。
