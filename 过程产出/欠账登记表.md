# 欠账登记表（★ 唯一落点 · 只增不删）

> **定位（★ 规范级 · 唯一落点）**：凡「**本轮做不了 / 只做了一半 / 按规格本该有但上游未就绪**」的缺口，
> **一律登记在这里** —— 目的是**避免「当时知道、后来忘了」**。
> 本表**只管「欠什么、为什么欠、归哪期补」**，**不定义任何业务口径**（口径以五份现行规格为准）。
>
> **维护规则**：
> 1. **只增不删**：补完了**不删行**，把「状态」改成 `✅ 已补 YYYY-MM-DD <commit>` —— 删了就看不出「曾经欠过」。
> 2. **谁发现谁登记**：任何窗口动工中发现「现在做不了、要回来补」，**当场加一行**，别攒到收尾（一攒就忘）。
> 3. **出处必填**：写清**在哪份规格 / 哪段代码注释里能对上**（否则事后没人信这条真的欠）。
> 4. **归属必填**：写清归**哪个里程碑 / 哪个域**补 —— 只写「以后补」等于没登记。
> 5. **不得借本表写新口径**：本表只能说「规格里有、现在没做」，**不许**把没做过的规则写成「要做」。

| # | 缺口（人话：现在少什么） | 为什么现在做不了 | 归属 | 出处 |
| --- | --- | --- | --- | --- |
| **D-01** | **`serving` 数据范围档只做了形状**：交付 / 客服本应「只看合同服务期内的客户」，现按 `self` 收敛 | E 域（合同）未建，「服务期」无从判定 | M9 · E 域 | 架构说明 §7.2；交接说明 §三.1 |
| **D-02** | **报表 / 看板 / 汇总出口未建**：M5-06 只落了「出口豁免脱敏」开关 ＋ 单测 | 无数据源（依赖 E / F 域） | M9 · report | 交接说明 §三.2 |
| **D-03** | **`GET /contacts/:id` 未建** ⇒ 详情「全号 / `phone_locked` / 24h `unlocked_until`」三种形态**都还没落地** | B 域详情端点属后续 | M9 · B 域 | 交接说明 §三.3 |
| **D-04** | **手机号上锁 / 解锁 / `phone_unlock` 审批未建** ⇒ `sea.phone.view`、`phone.unlock.*`、强制解锁留痕跟着一起欠 | G 域（审批五型）未建 | M9 · G 域（A10 已列） | 交接说明 §三.4 |
| **D-05** | ⏳ **部分已补 2026-09-16（M6-09 前置）**：`GET /org/product-lines`（产品线列表，含 `color_key`）已建 —— 录入页「激活业务关系」要选产品线（`POST /relations` req 含 `product_line_id`）。**`GET /companies/:id`（公司详情）仍欠** | 公司详情随 B 域补齐 | 剩余 → M9 · B 域 | 交接说明 §三.5；`org.controller.ts`（`listProductLines`） |
| **D-06** | **幂等头 `Idempotency-Key` 未做**：规格 §2.5 要求「非幂等写操作带幂等头」，当前**写了也不校验**（故先不写，免得成假契约） | 横切能力未排期 | M6 / M9 · 横切 | 接口 §2.5；交接说明 §三.5 |
| **D-07** | **`GET /relations` 只落了 `page` / `page_size`**；§2.7 通用约定里的 `order_by` / `desc` / `keyword` / `start_at` / `end_at` **均未接**（排序恒 `id desc`） | 各域列表统一能力，需逐域接 | M6 / M9 · 各域 | 接口 §2.7；`relation.repository.ts` 列表段注释 |
| **D-08** | **分页只在 C 域落地**：其它列表（公司列表等）仍返回裸数组，**不合 §2.3 分页形态** | 同一 kernel 公共件（`resolvePagination` / `buildPageResult`），逐域接 | M9 · 各域 | 接口 §2.3 / §2.7 |
| **D-09** | ⏳ **部分已补 2026-09-16**（P-01 拍板后落地）：`view`（`all` / `following` / `cooperated` / `churned`）＋ `urgency`（多选）已进接口 §4.4 / §5.6（V1.21），判定唯一落点＝`domain/relation-list-filter.ts`。⚠ **原 5 档里的「逾期未跟进」仍未提供**（依赖 D-10 的 `overdue`）⇒ 本条**未完全关闭** | 拍板已给（后端开参数）；剩「逾期」档要等 `overdue` 字段 | 剩余部分 → M9（随 `overdue`） | 前端文档 §5；接口 §4.4 / §5.6 |
| **D-10** | **C 域列表项缺 9 个字段**：`drop_in_x_days`（掉海倒计时）/ `overdue`（逾期）/ `amount` / `amount_masked` / `old_customer` / `is_weekly`；`customer_level` **恒 `null`**（E 域未接） | 分属 F 域（掉海规则 L1–L4，M7）/ D 域（逾期）/ E 域（合同回款、历史合同） | M7 · M9-D/E/F | 接口 §5.6；`dto/relation-response.dto.ts` 文件头 |
| **D-11** | **C 域详情缺 5 组字段**：`round_no` / `prev_round`（历史轮次）/ `competitors` / `labels` / `stage_logs` / `try_count_30d` | 分属 C4（竞品名册）/ C3（标签）/ D 域（阶段留痕、轮次） | M9 · C / D 域 | 同上 |
| **D-12** | **列表页视觉补全未做**：阶段彩色点、逾期标红、24h 掉公海⚠、跨线 `amount_masked`、竞争徽标、客户等级徽标、价值 ¥ 千分位 | 数据侧全在 D-10 里欠着（**无数据不编假值**）；其中「阶段色」还需设计规范 §二 先落 `--crm-color-stage-*` token | M9 ＋ 设计规范补 token | 前端文档 §5；设计规范 §4.3 / §4.4 |
| **D-13** | ⏳ **部分已补 2026-09-16（M6-08）**：列表页**公司名已可点**进关系详情（`/relations/:id`）；**联系人 / 员工的可点跳转仍欠** —— 它们的落点页（公司详情 / 员工详情）属 M9 的 B / A 域，服务端尚无对应端点（→ D-03 / D-05） | 缺落点页 | 剩余 → M9（B / A 域） | README §九 不变量⑥ |
| **D-22** | **关系详情页只落了「详情 ＋ 成员」**：规格 §5 的 8 Tab（基础 / 跟单 / 工作流 / 拜访外出 / 合同 / 工单 / 关系网 / 复盘）**均未建**；时间线也仍只在列表页抽屉里（未搬进详情页） | 8 Tab 的数据分属 C4 / C3 / D / E / G / F 域，服务端**尚未返回**相应字段（→ D-11）—— 摆上去就是**假入口**（设计规范 §3.2 第 11 条同类） | **M6 后续 / M9**（随各域） | 前端文档 §5；接口 §5.6 |
| **D-14** | **真账号走查**：**后端已验**（2026-09-16，种子 6 账号 ＋ 真服务实测：登录 gm / 私海翻页 `total=4`、第 1 页 `ids 5,4`、第 2 页 `ids 3,1` / 公海 `total=1` / **交付角色看公海 403**）；**前端 UI 仍待人在浏览器点**（点分页器翻页、改每页条数、切页签回第 1 页） | AI 没有浏览器会话，**UI 交互必须由人看** | **待七叔验**（仅剩前端 UI） | 本表 ＋ 交接说明 §一 |
| **D-15** | **`shared.module.spec.ts` 起真服务后 worker 进程不优雅退出**（jest 打印 force exit 警告，用例仍全绿） | 测试基建收尾项，不影响功能 | 收尾窗口 | 交接说明 §三.6 |
| **D-16** | **`PUT /relations/:id/commitments` 的入参形状，规格从未给**：接口 §三总览只有「`G/P/U /relations/:id/commitments`」一行，§5.7 也没写 U 的 body ⇒ 实现按「`id` 指认目标承诺 ＋ 要改的字段」**自定**跑着 | 规格缺口：**得先补接口文档**，不能由实现反过来定义契约 | 回填 · 接口文档 | `dto/engine-request.dto.ts`（`UpdateCommitmentDto` 头注）；接口 §三 / §5.7 |
| **D-17** | **C 域「谁能写业务关系」＝合成口径，规格没有这张表**：由三处已定口径推出（数据范围表只讲「看得到谁」＋ 管理员只读 ＋ 交付/客服只读）⇒ 可写集合 ＝ `sale` / `dept_manager` / `gm`；已上移 kernel `write-role.ts` 供各域共用 | 属**本项目技术口径**（非规格原文）⇒ 需**回填文档 + 确认**后才算定稿 | **待确认** → 回填 README / 架构说明 | `domain/relation-scope.ts`（`isRelationWriteRole` 头注）；`kernel/data-scope/write-role.ts` |
| **D-19** | ✅ **已补 2026-09-16**：列表页已接筛选栏 —— 视图 seg（4 档）＋ 紧迫档 chip（**多选**、可叠加），筛选一变**回第 1 页**再取数；真库实测 `view=following&urgency=gray → total=2（ids 5,4）`、`view=cooperated → 0`、非法 `view=overdue → **400**` | — | — | 接口 §5.6；`前端/src/views/RelationListView.vue` |
| **D-21** | **筛选状态没写进 URL query**：设计规范 §4.2 明写「筛选状态写入 URL query（可分享 / 刷新不丢）」，本轮筛选栏**未接 URL 同步** —— 刷新后回到默认（视图＝我的全部、无紧迫档筛选） | 本轮只落了筛选本体；URL 同步要接 `vue-router` 的 query 读写，属收尾项（不是"做不了"，是**本轮没做**） | **M6 · 收尾**（与 10 角色矩阵 / 11 主题同批） | 设计规范 §4.2；`RelationListView.vue` |
| **D-18** | **前端目录与「跨页复用」没有任何权威落点**：架构设计说明只管服务端（7 域 / 分层 / 进程 / 横切），前端「页面放 `views/`、取数放 `api/`、展示口径放 `*.ts`」是**自发约定**；`router/index.ts` 头注引的「《架构设计说明》§四 目录树」**该节并不存在**（错引）。后果：跨页复用的东西（例：各页都要的「联系人悬停卡」）**无规矩可依**，极易在每个页面各写一份 ⇒ 改一处漏多处（本项目一号坑的前端版） | 缺**一条前端目录 / 组件复用约定**（补进《架构设计说明》，或单开一节） | ✅ **已补 2026-09-16**（P-02 拍板）→ 架构说明 **§4.4**（升 V1.6）＋ §十「不做 monorepo / npm 包」＋ router 错引已改正 | 架构说明 §4.4；调研件 `过程产出/前端目录与组件复用-调研.md` |
| **D-20** | ✅ **已补 2026-09-16**（七叔拍板「装 ESLint」）：前端装上 `eslint` ＋ `typescript-eslint` ＋ `eslint-plugin-vue`；`前端/eslint.config.mjs` 用 `no-restricted-imports` 卡住 §4.4 约定 1（组件禁 import `views` / `api`、页面禁互相 import），`npm run lint` 已成脚本；★ **并用故意违规的临时文件实测「真会被拦」**（不只"跑一遍没报错"）。其余规则后续**渐进**开启 | — | — | 架构说明 §4.4；`前端/eslint.config.mjs` |
| **D-23** | ✅ **已补 2026-09-16**（七叔拍板 P-03）：`contact_ids` 开放 —— 「待关联」联系人**只有当前归属人能标**（别人 **403** `quick_mark.not_mine`）；B 域新增只读出口 `getContactOwners`（`findContactOwnersByIds`）供 D 域判「归不归我」 | — | — | 接口 §5.7；`company.service.ts`（`getContactOwners`）；`engine.service.ts`（`quickMark` ③-2） |
| **D-24** | **`POST /events/quick-mark` 的入参规格给了、出参没给**：本批按「标了几条」自定 `{marked}` 跑着 | 规格缺口（同 D-16）：**得先回填接口文档**，不能由实现反过来定义契约 | 回填 · 接口文档 | 接口 §5.7；`engine-response.dto.ts`（`QuickMarkResultDto` 头注） |
| **D-26** | ✅ **已补 2026-09-16**（七叔拍板 P-05）：生成列改为「**只有在位（未撤销）的 owner 才占位**」—— `owner_flag = IF(member_type='owner' AND revoked_at IS NULL, relation_id, NULL)`，migration **`0008_owner_flag_in_seat`**（已上真库，真库验证：撤销后可再落新 owner）；数据架构 §10.2-1 / §10.1 索引清单 / `prisma/README §六` 同批更新 | — | — | `migrations/0008_owner_flag_in_seat/`；数据架构 §10.2-1 |
| **D-27** | ⏳ **已定口径并落地 2026-09-16**（七叔拍板）：待关联联系人的**归属＝建档时的录入人**，落在新增列 `contact.owner_id`（**可空**，仅待关联的人有值）；**未关联公司期间不占部门 / 产品线**；归属**可改**（经理分派 / 离职交接）**不走审批、只留痕**；**工作量按写单人算**（归属人只决定"线索出现在谁的列表里"）。剩余见 D-28 | — | — | 需求 §6.1 ⑦（本轮新增）；`schema.prisma`（`Contact.owner_id`） |
| **D-28** | **`GET /contacts` 不按数据范围收敛**：查询条件只有「没删、没合并」，取最近 100 条 ⇒ 任何登录人都能看到**全公司**联系人（含别人录入的待关联线索的手机号）。与需求 §6.1「销售**名下**待跟进」直接冲突 | 「公司维度」收敛（我关系下公司的联系人）要复用 C 域的数据范围判定 —— **不许在 B 域重写一套**（双真相源），故须先由 C 域开一个 exports 出口（"列我可看的公司 id"） | **M6 后续 / M9 · B＋C 域** | `company.repository.ts`（`listContacts` 注释）｜需求 §6.1 / §6.2 |
| **D-29** | ⏳ **接口已补 2026-09-16（V1.23）**：`POST /contacts/:id/activate-relation`（req `{company_id,dept_id,product_line_id,position?}`、resp ＝ 关系列表项 ＋ `linked_events`、错误约定齐全）已写进**接口 §4.4 / §5.6**。**剩「实现」**：该动线跨 B（写就职）/ C（建关系）/ D（批量改挂孤儿跟单）**三域**，架构 §5.2 禁跨域大事务，且 **`C`(L3) 不可反向依赖 `D`(L4)** ⇒ 动手前**先核层级与 EventBus 现状**（EventBus 尚未接进 DI），再定走同步出口还是领域事件 | 需先核层级（见左） | M6-09 后续（施工单已列） | 接口 §4.4 / §5.6；需求 §6.1 ④ |
| **D-30** | **撞库「分支 3（跨部门可并行）」的展示数据无处可取**：需求 §12.1 要求命中时显示「**对方部门名 ＋ 激活 / 签约日期**」（只显这个、不露跟单），但**没有任何出口给这两个值** —— `POST /companies/search-dup` 的候选只有 `{id,full_name,credit_code_masked,similarity,match_type}`（无部门、无关系），也**没有**「某公司已有哪些关系」的只读端点 ⇒ 录入页撞库**做不出分支 3**（⚠ 本片因此**只落了分支 4 ＋ 服务端 409 路径**，与《M6-09 施工单》片 1 原列的"本片做"**不符**，以本表为准） | 缺的是**服务端出口**：要新增「按公司取关系概要」的能力，且必须按数据范围收敛（跨部门只给部门名＋日期、不露跟单），不是前端能补的 | M9 · **B＋C 域**（可与 D-05 公司详情同批） | 需求 §12.1（分支 3 细节）；`服务端/src/modules/company/dto/company-response.dto.ts`（`DupCandidateVoDto`）；`前端/src/views/EntryView.vue` 文件头 ★ |
| **D-31** | **公海关系仍可写（服务端未拦）**：需求 §6.3 / 前端文档 §9-10 明文「**公海内不做任何动作**」（不提供标记 / 写跟单 / 改阶段任何动作），但服务端 `relation.requireWritableRelation` 只判「**读得到 ＋ 是可写角色**」，**没有拦「无 owner 的公海关系」** ⇒ 真库上对公海关系写跟单 / 快速标记**都能落库**；前端业务关系页**公海页签的「时间线」**（M4-17）就是从这个口子进去的。⚠ 本片（M6-09 片 3）**没有**据此放宽：批量快速标记的勾选框与动作条**只在私海出现**（规格说不行就是不行），**服务端那条口子留给拍板** | 不是"做不了"，是**改哪一层未定**：① 服务端在 C 域加「公海关系不可写」判定（改的是已上线行为）；② 等 F 域「领取到私海」动线（M7）一起收口。两者都要七叔拍板 | **待拍板** → M7 · C/F 域 | 需求 §6.3；前端文档 §9/10；`服务端/src/modules/relation/relation.service.ts`（`requireWritableRelation`）；`前端/src/views/RelationListView.vue` |

---

## 二、已补（留痕，勿删）

> 格式：`✅ 已补 YYYY-MM-DD <commit>` ＋ 一句话「补在哪」。

- ✅ **已补 2026-09-16**（M6-07 同批）：**`GET /relations` 分页**（原「M3 最小列表 `LIST_LIMIT=100` 截断、出参裸数组」＝不合 §2.3 分页形态）—— 入参 `page` / `page_size`、出参 `{list,total,page,page_size}`，前端列表页接分页器；接口文档升 V1.20。
