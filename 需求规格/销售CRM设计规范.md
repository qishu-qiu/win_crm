# 销售 CRM 设计规范 V1.0

> **⚠ 开工前必读**：先读《**废止口径登记表**》（需求规格/）——已废止的旧说法不得作为实现依据（如 #12：旧《设计规范》V3.1 已作废）。
> **文档性质**：**设计系统唯一事实源**（视觉 / 组件 / 状态 / 响应式 / 信息层级 / 主题）。
> **配套文档**：《销售CRM前端页面与交互文档》**V1.15**（**管"有哪些页、怎么交互"**）、业务需求 **V1.24**、数据架构 **V1.28**、接口 API **V1.12**（均 需求规格/）。
> **分工（L1 单一事实源）**：**本文件管"长什么样"**（token / 组件 / 状态 / 主题），前端文档管"有哪些页面、页面级怎么交互"，**同一事实只在一处定义**。
> **设计基准**：以 **Ant Design 5** 官方 token 为数值基准（**不自由发挥**）；**Material 3** 提供层级/状态/密度原则；**Apple HIG** 提供栅格/字号/触控目标原则。有分歧时优先级：**Ant Design 官方约定 > 本文件 token > 个人偏好**。
> **取代关系**：本文件取代《归档/原型设计/高保真/设计规范.md》V3.1（旧原型规范：emoji 图标、深蓝侧栏 `#001529`，已于 2026-09-09 推翻，**已作废·仅存历史**）。
> **生效日期**：2026-09-11 ｜ 状态：V1.0（由前端文档 §二 / §三 / §七~§十二 抽出并立册）。
> **版本沿革**：文档内不留「修改记录」章节（→《废止口径登记表》#22）；沿革查 `git log --follow -- 需求规格/销售CRM设计规范.md`。

---

## 一、总则与铁律

1. **一切数值走本文件 §二 token**，页面/组件**禁止硬编码**颜色、间距、圆角、字号、阴影。
2. **数值不凭感觉**：颜色/控件高/圆角/字号/阴影一律取 Ant Design 5 官方默认值（§二 标注）。
3. **不引入未定义组件**：新组件先在 §四 登记规格再实现；不得随手造新视觉语言。
4. **L0 变更联动**：本文件一改，前端文档同批跟着改、同批升版。
5. **落地方式**：全部编译为 CSS 变量（`--crm-*`）挂根节点；**V1 主题＝light（白天）/ dark（夜间）两套**，切换只换变量值、**不改组件**（见 §八）。

---

## 二、Design Tokens（数值唯一事实源）

> 数值基准＝Ant Design 5 默认 token（light）；dark 映射见 §八。4 基数网格；一值一义；组件引用语义 token，不直接引用色值。

### 2.1 色彩 Token

**品牌与功能色**
| Token | 值（基准 AntD5） | 用途 |
|---|---|---|
| `--color-primary` | `#1677ff` | 主色：**只给可点主按钮/链接/选中态** |
| `--color-primary-hover` | `#4096ff` | 主按钮 hover |
| `--color-primary-active` | `#0958d9` | 主按钮按下（**点击反馈**）|
| `--color-primary-bg` | `#e6f4ff` | 选中行/选中菜单底 |
| `--color-success` | `#52c41a` | 成功/已完成 |
| `--color-warning` | `#faad14` | 警告/黄警（到期 30 天）|
| `--color-error` | `#ff4d4f` | **错误/逾期/风险**（红只给逾期风险）|
| `--color-info` | `#1677ff` | 信息 |

**中性色（背景/文字/边框）**
| Token | 值 | 用途 |
|---|---|---|
| `--color-bg-layout` | `#f5f5f5` | 页面底（布局背景）|
| `--color-bg-container` | `#ffffff` | 卡片/表格/输入框底 |
| `--color-bg-elevated` | `#ffffff` | 弹层/下拉/悬浮卡底 |
| `--color-fill-alter` | `rgba(0,0,0,0.02)` | 表格斑马/次要区块底 |
| `--color-bg-mask` | `rgba(0,0,0,0.45)` | 模态遮罩 |
| `--color-text` | `rgba(0,0,0,0.88)` | 主文字/标题 |
| `--color-text-secondary` | `rgba(0,0,0,0.65)` | 次要文字 |
| `--color-text-tertiary` | `rgba(0,0,0,0.45)` | 描述/辅助 |
| `--color-text-disabled` | `rgba(0,0,0,0.25)` | 禁用文字 |
| `--color-text-inverse` | `#ffffff` | 深底上的文字 |
| `--color-border` | `#d9d9d9` | 控件描边 |
| `--color-border-secondary` | `#f0f0f0` | 分割线/浅边（= colorSplit）|
| `--color-bg-hover` | `rgba(0,0,0,0.04)` | **行/列表项 hover 底**（唯一允许的 hover 变化）|
| `--color-bg-active` | `rgba(0,0,0,0.15)` | 文本类 active |

**业务专用色（中国习惯 + 业务编码，均需自定义）**
| Token 组 | 值 | 用途 |
|---|---|---|
| `--color-up` | `#cf1322`（红） | **涨/正向（中国习惯，红涨）** |
| `--color-down` | `#389e0d`（绿） | **跌/负向（绿跌）** |
| 紧迫档 `--urgency-week` | `#ff4d4f` 红实心 | 周重点 |
| `--urgency-month` | `#fa8c16` 橙 | 月重点 |
| `--urgency-quarter` | `#1677ff` 蓝 | 季度跟 |
| `--urgency-long` | `#52c41a` 绿 | 长期跟 |
| `--urgency-gray` | `#8c8c8c` 灰 | 灰度（默认） |
| 客户等级 S~D | `#ff4d4f`/`#fa8c16`/`#1677ff`/`#52c41a`/`#8c8c8c` | S/A/B/C/D 徽标（系统自动 ⚙） |
| 产品线 7 固定色 | 见 §四.9 实体标签 | 每条产品线一个固定色，全局不变 |

> **对比度铁律**：正文文字与底对比 ≥ 4.5:1（AA）；**禁止用颜色单独表意**（状态需「色 + 文字/图标」双通道）。

### 2.2 字体与字阶
| Token | 值 | 用途 |
|---|---|---|
| `--font-family` | `-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", "PingFang SC", "Microsoft YaHei", sans-serif` | 全局（系统字体优先，中文回退苹方/雅黑）|
| `--font-size-xs` | `12px` | 辅助/标签/表格次要 |
| `--font-size-base` | `14px` | **正文基准**（AntD5 基准）|
| `--font-size-lg` | `16px` | 小标题/卡片头 |
| `--font-size-xl` | `20px` | 区块标题 |
| `--font-size-2xl` | `24px` | 页面标题 |
| `--font-size-3xl` | `30px` | KPI 数字 |
| `--line-height` | `1.5714`（≈22px） | 正文行高 |
| `--font-weight-normal` | `400` | 常规 |
| `--font-weight-medium` | `500` | 强调 |
| `--font-weight-strong` | `600` | 标题/数字 |
| `--font-numeric` | `font-variant-numeric: tabular-nums` | **金额/统计数字等宽对齐** |

### 2.3 间距（4 基数）
| Token | 值 | 典型用途 |
|---|---|---|
| `--space-xxs` | `4px` | 图标与文字间隙 |
| `--space-xs` | `8px` | 标签间距/按钮内图标距 |
| `--space-sm` | `12px` | 表单项间距 |
| `--space-md` | `16px` | **默认：组件内边距、块间距**（AntD padding）|
| `--space-lg` | `24px` | 卡片内边距、区块间距（AntD paddingLG）|
| `--space-xl` | `32px` | 页面区块分隔 |
| `--space-2xl` | `40px` | 页面顶部留白 |
| `--space-3xl` | `48px` | 大区块留白 |

> 小卡内边距 12（bodyPaddingSM），常规卡 24（bodyPadding）。**同层级间距必须一致**。

### 2.4 圆角
| Token | 值 | 用途 |
|---|---|---|
| `--radius-xs` | `2px` | 微元素（Segmented/箭头）|
| `--radius-sm` | `4px` | 小控件/标签（AntD borderRadiusSM）|
| `--radius-base` | `6px` | **控件基准**：按钮/输入框/下拉（AntD borderRadius）|
| `--radius-lg` | `8px` | 卡片/弹窗/抽屉（AntD borderRadiusLG）|
| `--radius-pill` | `999px` | 仅限头像/胶囊标签/客户芯片 |

### 2.5 阴影与层级（描边优先，阴影克制）
| Token | 值（AntD5） | 用途 |
|---|---|---|
| `--shadow-1` | `0 1px 2px 0 rgba(0,0,0,.05), 0 1px 6px -1px rgba(0,0,0,.03), 0 2px 4px 0 rgba(0,0,0,.03)` | 卡片（三级阴影）|
| `--shadow-2` | `0 6px 16px 0 rgba(0,0,0,.08), 0 3px 6px -4px rgba(0,0,0,.12), 0 9px 28px 8px rgba(0,0,0,.05)` | 下拉/悬浮卡/模态/抽屉 |
| `--shadow-3` | `0 12px 24px rgba(0,0,0,.10)` | 最高层（慎用） |

> **硬禁**：彩色阴影、发光阴影（glow）、用 `box-shadow` 做装饰。**卡片默认「1px 描边 + 极浅阴影」，不用重阴影堆叠**。

### 2.6 描边与分割线
- `--border-width: 1px`；控件用 `--color-border`，表格/区块分割用 `--color-border-secondary`。
- **分割线优先于阴影**：区块分离先用 1px `#f0f0f0` 线，不用阴影。

### 2.7 动效（极简，禁夸张）
| Token | 值 | 用途 |
|---|---|---|
| `--motion-duration-fast` | `0.1s` | 按压/微反馈 |
| `--motion-duration-base` | `0.2s` | 常规过渡（AntD motionDurationMid）|
| `--motion-duration-slow` | `0.3s` | 抽屉/模态进出 |
| `--motion-ease` | `cubic-bezier(0.645,0.045,0.355,1)` | 标准曲线 |

> **硬禁**：bounce/spring 夸张回弹、**无限循环动画（骨架屏除外）**、发光脉冲、位移放大、视差。动效只服务「层级出现/消失」，不做装饰。

### 2.8 图标
- 统一 **Feather 线性图标**，stroke-width `1.5`，尺寸 `16 / 20 / 24`。
- **禁 emoji**（含状态、菜单、按钮、空状态插画）。

### 2.9 z-index 层级
| 层 | 值 |
|---|---|
| sticky 表头/工具栏 | `100` |
| 遮罩 mask | `999` |
| 抽屉/模态 | `1000` |
| 下拉/悬浮卡/Tooltip | `1050` |
| Message/Notification | `1010` |

### 2.10 控件尺寸
- 控件高 `--control-h: 32px` / SM `24` / LG `40`（AntD controlHeight）。
- 触控目标 ≥ `44×44`（HIG，移动端）。

---

## 三、视觉方向与反 AI 味

### 3.1 视觉方向（B1~B8，2026-09-09 推翻重做）
- **B1** emoji 全砍，统一 Feather 线性图标（语义不丢）。
- **B2** 大留白、浅色、一屏一主次。
- **B3** 主色只给可点按钮/链接/选中。
- **B4** 侧栏砍到销售日常 6 项 + 管理收进「更多」。
- **B5** 红色只给逾期/风险。
- **B6** 工作台 = 今天动作唯一入口。
- **B7** 卡片堆叠收敛。
- **B8** 高端大气、简单舒适、降学习成本。

### 3.2 ★ 反 AI 味铁律（12 条硬禁，2026-09-11 立）
1. **禁渐变**（背景/按钮/卡片/图表色块一律纯色）。
2. **禁玻璃拟态**（backdrop-filter / 半透明毛玻璃一律不用）。
3. **禁 emoji**（全站，含空状态）。
4. **禁发光 / 霓虹 / 彩色阴影**。
5. **禁夸张动画**（回弹、脉冲、无限循环、位移放大）。
6. **禁所有卡片一样大、一样重**（KPI/内容/列表按重要性分级，见 §七.2）。
7. **禁装饰性插图 / 大圆角气泡 / 悬浮漂浮元素**。
8. **禁只有展示没有交互**（每个数据块要么可点、要么可筛选、要么可下钻）。
9. **禁同屏超过 3 个语义色**（红/橙/蓝/绿按需，不铺满）。
10. **禁无意义留白与超大标题**（信息密度服务操作效率）。
11. **禁假数据撑页面**（空就显示空状态，不填占位）。
12. **禁自定义视觉语言**（一切回归 Ant Design 组件观感）。

### 3.3 中国视觉习惯（H1~H4）
- **H1** 涨红跌绿；**H2** `¥`+千分位+等宽数字；**H3** 中文文案真实感；**H4** 红优先给紧急/逾期、系统标签灰 ⚙。

---

## 四、组件规格（★ 真实业务组件，全部走 §二 token）

### 4.1 表格 Table
- 结构：表头（高 47，`--color-bg-container`，字重 600，字号 14）+ 数据行（高 54，字号 14）+ 底部工具栏。
- 行分隔用 **1px `--color-border-secondary`**，**不用斑马纹**（减少噪音）。
- 行 hover：`--color-bg-hover`（唯一背景变化）；选中行：`--color-primary-bg`。
- 列对齐：文本左对齐、**数字右对齐＋等宽数字**、状态/操作居中。
- 列宽：关键列定宽（公司名/金额/时间），弹性列自适应；**表头可固定**（长表）。
- 排序/筛选：表头图标 `--color-text-tertiary`，激活态 `--color-primary`。
- 行操作：右侧固定，默认次要按钮，**主操作只留一个**；超 3 个收进「更多」。
- 空表：显示 §五.1 空状态（不显示空表格骨架）。
- 加载：表内 loading 遮罩 或 骨架行（§五.2）。
- 分页：见 4.3。

### 4.2 筛选 / 查询栏 FilterBar
- 布局：单行 inline（label 前置）或折叠面板；标签与控件间距 `--space-xs`。
- 高频筛选项 **≤5**，其余收「高级筛选」（展开不跳页）。
- 控件高统一 `32px`；输入/下拉宽度按语义（关键词 240、状态 120、时间 240）。
- 「查询」为主按钮、「重置」为 default；**筛选变化不自动清空已选**。
- 已选条件以可删除 Tag 回显（高 22、圆角 4、字号 12）。
- **筛选状态写入 URL query**（可分享/刷新不丢）。

### 4.3 分页 Pagination
- 位置：表格右下；左侧显示「共 N 条」。
- 页码 + 每页条数（20/50/100）+ 快速跳页。
- **列表类一律分页，不用无限滚动**（便于定位与回退）。

### 4.4 状态与标签 Tag / Status
- Tag：高 22、圆角 `--radius-sm`(4)、字号 12、内边距 4/8；纯色浅底 + 深字（同色系）。
- 状态统一用「**圆点 + 文字**」（色盲友好），必要时叠加图标。
- 业务档位色严格取 §二 业务色（紧迫档/等级/产品线）；系统自动项加灰色 `⚙`。
- **不堆 emoji、不用纯色块铺满格子**。

### 4.5 按钮 Button
- 类型：`primary`（主色实心，**同屏仅一个**）/ `default`（白底描边）/ `text`（无边框，次级）/ `link`（文字链）/ `danger`（错误色）。
- 尺寸：高 32 / 24(SM) / 40(LG)；图标+文字间距 `--space-xs`(8)。
- **点击反馈**：active 用 `--color-primary-active` 或背景加深（**不做位移动效**）。
- 禁用：文字 `--color-text-disabled` + 灰底 + `cursor: not-allowed`。
- 加载：按钮内 spinner + 保持宽度；**提交类按钮防重复点击**。
- 底部操作区：右对齐，主按钮最右。

### 4.6 表单与录入 Form
- 标签：顶部对齐（长表单）或左对齐固定宽（`120px`）；必填加红色 `*`。
- 控件高 32；多列用 24 栅格，`gutter: [16,0]`。
- 校验：blur 即时 + 提交汇总；错误文案红色 12px 置于控件下；helper 文案灰色 12px。
- 错误提示定位到字段并自动滚动到首个错误。
- 步进/分步表单用 Steps（录入页 3 步）。

### 4.7 抽屉 / 弹窗 / 悬浮卡
- **抽屉 Drawer**：仅一层；宽 `480`（小）/`640`（中）；标题栏高 56，底部操作区固定；进出 0.3s。
- **模态 Modal**：宽 `520`（确认）/`720`（表单）；标题 16/600；遮罩 `--color-bg-mask`；**禁玻璃拟态**。
- **悬浮卡 PopCard**：hover + click 钉住；宽 ≤320；`--shadow-2` + `--radius-lg`(8) + padding 16；移动端 → 底部轻面板 `.sheet`。
- 三者均用 **纯色底 + 描边/阴影**，无透明模糊。

### 4.8 时间轴 Timeline
- 节点 8px 圆点；连接线 1px `--color-border-secondary`；时间文字 12px `--color-text-tertiary`。
- **主线/树杈双色**（owner 主线 / 他人树杈），带图例；逾期段用红色分隔线标注。
- 每条含「填单人 + 跟进联系人」（可点）。

### 4.9 专用业务件
| 组件 | 规格 | token |
|---|---|---|
| 人员芯片 | 头像（姓名哈希 12 色底 + 首字，圆 50%）+ 姓名；列表 mini、详情完整；未指派「＋指派」 | `--radius-pill` |
| 实体标签 | 产品线固定 7 色；部门色配置回退蓝；员工灰底 | `--radius-sm` |
| 紧迫档 chip | 5 档色，可点切换（§二业务色）| `--radius-pill` |
| 客户等级徽标 | S~D 色，系统自动 ⚙，不可手改 | `--radius-sm` |
| 目标进度条 | 回款额主 + 签约额副；时间进度对比（领先绿/持平橙/落后红）| `--radius-pill` |
| 老客户徽标 | 派生（有历史合同），灰底描边 | `--radius-sm` |
| 复盘弹窗 | win/loss/churn 三型；loss 死因必选（不点选 422）| Modal 规格 |

### 4.10 反馈组件
- **Message**：顶部居中，3s 自动消失，用于操作反馈（成功/失败）。
- **Notification**：右上，用于系统通知。
- **Tooltip**：仅解释图标/截断文本，深色底白字。
- 成功/失败/警告文案**具体化**（「已提交解锁申请」而非「操作成功」）。

---

## 五、状态设计（★ 空 / 加载 / 错误 三态必须完整）

### 5.1 空状态 Empty
- 结构：**线性图标（非 emoji）+ 一句说明 + 一个主行动按钮**。
- 三类文案区分：**无数据**（「还没有业务关系，去录入」）/ **无搜索结果**（「没有符合条件的客户，试试放宽筛选」）/ **无权限**（「此处数据不在你的可见范围」）。
- 表格空、列表空、看板空各自适配，**不留大白块**。

### 5.2 加载状态 Loading
- 首屏用 **骨架屏 Skeleton**（贴合真实布局），**不用全屏 spinner**。
- 局部（按钮/表格）用控件内 loading；表格用表内遮罩。
- **>300ms 才显示 loading**（防闪烁）；<300ms 直接出数据。
- 骨架屏用浅灰块，**不做 shimmer 以外的效果**（§二.7）。

### 5.3 错误状态 Error
- 接口失败 → **错误态块**（图标 + 说明 + 「重试」按钮），可局部替换失败区块。
- 表单错误 → 字段红提示 + 汇总；提交失败 → `Message.error` 友好文案（映射 `→接口API§2.4` 错误码）。
- 网络断开 → 顶部常驻提示条；恢复后自动重试。

### 5.4 无权限 / 边界
- 403：页面/区块显示「无权限」占位（不报错、不空白）；越权数据**列表不返回、搜索搜不到**（`→接口API§2.2`）。
- 长文本溢出：单行省略 + Tooltip；数字超长：等宽缩略 + Tooltip。

---

## 六、响应式（★ 桌面优先，移动可用）

### 6.1 断点
| 断点 | 宽度 | 策略 |
|---|---|---|
| 桌面（默认） | ≥1280 | 完整三栏/栅格 |
| 小桌面 | 992–1280 | 侧栏可收起、栅格压缩 |
| 平板 | 768–992 | 双栏、表格横向滚动 |
| 移动 | <768 | 仅工作台 H5（底部 Tab + 轻面板）|

### 6.2 栅格
- 24 栅格，`gutter 16/24`；内容最大宽 `1440` 居中，超宽留白。

### 6.3 移动可用性
- 触控目标 ≥44×44；表格转卡片列表；**慎用横向滚动**。
- 关键动作（写跟进）常驻底部或浮动，一键可达。

---

## 七、信息层级与密度（★ 主次分明、减少噪音、提高效率）

### 7.1 视觉层级四原则
1. **一屏一主**：每屏一个主任务、一个主按钮。
2. **三重区分**：字号 + 字重 + 色彩共同表达层级，不靠单一维度。
3. **同级一致**：同层级元素的间距、字号、颜色完全一致。
4. **次要降级**：辅助信息用 `--color-text-tertiary` 或 12px，不抢视线。

### 7.2 卡片不统一（禁所有卡片一样大一样重）
| 卡片类型 | 尺寸/内边距 | 重量 |
|---|---|---|
| KPI 卡 | 小、数字 30px/600 | 最轻（描边，无阴影）|
| 内容卡（区块）| 中、padding 24 | 中（描边 + `--shadow-1`）|
| 列表区/表格卡 | 大、占主区 | 主（无阴影，靠分割线）|

- **按重要性给不同内边距与视觉重量**；同一页避免 ≥4 个同等重量卡片并列。

### 7.3 降低噪音
- 低频操作收「更多」；同屏语义色 ≤3；边框优先于阴影；去掉装饰性元素。
- 数字对齐用等宽；状态用圆点+文字；时间统一 `YYYY-MM-DD HH:mm`。

---

## 八、主题与换肤（V1 只做白天 / 夜间）

> **★ V1 范围决定（2026-09-11 七叔定）：前期只做「白天模式 / 夜间模式」两套主题**；原三风格候选（A 紫蓝 / B 深藏青金 / C 青）**后置**，不进 V1。

1. **V1 主题 = 两套**：`light`（白天，默认）/ `dark`（夜间）；切换＝换 §二 token 变量值，**不改组件**（入口在前端文档页 28，存账号）。
   - 只维护 **2 张 token 表**；dark 做明确映射（背景/文字/边框/阴影/语义色）。
   - 公司只锁**默认值**（默认 light），**全员可自由切**。
2. **三风格候选后置**：A 紫蓝 / B 深藏青金 / C 青 记为 V1 后置选型；将来若启用，**必须先落成明确 token 色值表**并过 **对比度 AA + 反 AI 味 12 条**（尤其 B「藏青金」**严禁金色渐变/发光、禁整屏深底**）。
3. **夜间（dark）主题约束**：不用纯黑底（深灰 `#141414` 系）；文字/边框对比仍 ≥ AA；红/橙/绿语义色在深底上**提亮一档**；**同样禁彩色/发光阴影**。
4. **颜色变量覆盖**：主色（仅可点）、逾期红、涨红跌绿、产品线 7 固定色、部门配置色、紧迫档 5 色、等级 S~D 五色。
5. 可迁移原型 `assets/style.css` 的组件类命名，但颜色必须走新 token，旧硬编码（约 189 处）收编。
6. 改版后跑冒烟：0 处 404、风格统一、菜单 active 正确。

---

## 九、视觉验收标准

- **token 统一**：颜色/间距/圆角/字号/阴影**零硬编码**，全部走 CSS 变量（抽查 0 例外）。
- **反 AI 味 12 条**（§三.2）全过：无渐变、无玻璃拟态、无 emoji、无发光、无夸张动画。
- **卡片分级**：KPI/内容/列表卡尺寸重量不同（§七.2），无「千篇一律卡」。
- **真实组件**：表格/筛选/分页/状态/空状态均为真实业务组件（§四），非静态展示。
- **三态完整**：每个数据区都有 空/加载/错误 三态（§五）。
- **hover/点击**：hover 仅轻微背景变化；按钮有明确 active 反馈。
- **响应式**：桌面优先、≥1280 完整、移动端可用（§六）。
- **层级清晰**：一屏一主、主次分明、同屏语义色 ≤3（§七）。
- **主题**：白天 / 夜间两套生效、个人外观持久化（§八）。
- 改版冒烟 0 错。


